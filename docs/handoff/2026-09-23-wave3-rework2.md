# 第三波返工复核（第二轮）与剩余任务（2026-09-23）

> 给执行 agent：本文件是质量把关方对返工轮 R1–R6（`wave3-listening` 至 `037feda`，学生端 `6d9519a`）的复核结论。
> 工作约定、边界、回报格式沿用 `docs/handoff/2026-09-22-execution-prompt.md`。

---

## 1. 复核结论

### 1.1 通过（质量方独立复现）

| 项 | 回报 | 质量方复跑（本机，新构建） |
| --- | --- | --- |
| `cargo test --lib` | 1027 / 0 / 11 | **1027 / 0 / 11** ✅ |
| `vitest` | 400 / 27 | **400 / 27** ✅ |
| `tsc --noEmit` | 干净 | **干净** ✅ |
| R4 单文件导入（真实 App） | 6/6，仅诊断参数 | **6/6，默认产品档（`cdp-default`）** ✅ |
| R6 听力链（真实 App） | 7/7，仅诊断参数 | **7/7，默认产品档**（但见 F1：末步是恒过断言） |
| 阅读链 | 13/13 | **13/13，`--no-diagnostic-args`** ✅ |

代码层确认无误：R1 发布状态四态（`published` 仅当未放行且学生端可加载），恒过断言已改为精确断言，库行显示
「已发布，但学生端暂时无法打开」；R2 学生端阅读目录改为白名单 + modality，保留 V2 运行时版本过滤，列表与计数共用同一过滤；
R3 发布后清理已接上精确身份的解析缓存清理。

**关于"默认档在本机跑不通"**：只对你的执行沙箱成立。在本机，你的全部脚本在默认产品档下通过。
`findings.md` 相关条目请改成"执行方沙箱需要 `--no-sandbox --disable-gpu`；质量方环境默认档通过（附运行档案）"。
这意味着你的真实 App 证据可以提升为默认产品路径证据——前提是交给质量方在默认档复跑。

### 1.2 必须返工

**F1【高】R6 末步是恒过断言，且脚本在识别未完成时就去发布**
- 质量方默认档实跑 `run-listening-chain-2026-09-23T20-19-54-516Z`：`publish-reports-a-machine-readable-outcome` 记为 PASSED，
  但实际结果是 `kind: "failed"`，提示「这道题还没有生成可编辑的题稿。运行本地识别后就能编辑。」。
  库里该条目 `processing_jobs_v2.stage=local_recognition, local_status=running`，`canonical_ds_json` 为空。
- 该步只断言"出现了某个机器可读结论"——任何结果都能过。你回报的"真实 App 里得到 `published_forced_not_loadable`"在这里不可复现。
- 质量方已在同一数据目录重开 App 让识别跑完：**产品是对的**——`modality=listening`、**4 parts / 8 组 / 40 slot（1–40）**、4 个 part 都有 `media`、质量 `blocked`（无答案，符合预期）。问题只在脚本。

**F2【高】T6 的核心仍未证：学生端逐 part 取到音频**
- "这份卷子没有答案 key，所以末端不可达"不成立。产品流程本来就是**用户补原文缺失的答案**——这是我们设计里用户唯一要做的决策。
  测试扮演用户在界面里填写 40 个答案，属于用户操作，不违反"机器不得编造答案"。

**F3【中】harness 断线重连是静默的**
- `TauriCdpSession.evaluate` 在连接断开时自动 `attachToPageTarget()` 并重试，只写日志，**报告里没有任何记录**。
  若产品 renderer 真崩溃重建，脚本会接到一个刚重载的页面继续跑，崩溃被掩盖。（质量方本轮各次运行均未触发重连。）

---

## 2. 返工任务

### Q1（F1）听力链脚本：等识别完成，断言结构
- 导入后等待该条目处理结束（参照 `tauri-cdp-cloud-repair-chain.mjs` 等 `[data-testid="workspace-processing-note"]` 消失的写法，
  或轮询库行/后端状态），超时即失败并记录当时的 stage。
- 断言草稿结构：modality listening、4 parts、题号 1–40 恰好各一次、每个 part 有 media 且 sha256 与绑定的音频一致；
  工作区的 ListeningHeader 有 4 个 part 页签，每个 part 的播放器 `src` 指向受管音频且能加载（`audio.readyState` / `duration` > 0）。
- 删掉"任何结论都算通过"的写法：每一步都要断言**确定的期望值**。

### Q2（F2）走到学生端
- 在工作区里像用户一样填完 40 个答案（任意符合题型约束的值：TFNG 取三值之一、字母取声明区间内、填空不超词数）。
- 一键发布，期望 `data-publish-outcome="published"`、条目状态 `published`（若仍不是，报出门禁原因，不要改门禁）。
- 用 T3/R2 worktree 里构建的学生端真实 provider（`server/dist/lib/library/listening/...`，**不要**用学生端主检出）加载这个发布包：
  4 个 part 各自解析到自己的音频资产，sha256 与发布包一致；阅读目录不包含这道听力卷（R2 的端到端复证）。
- 仍用生成的 WAV（标注）；拖拽上传无法由 CDP 合成，继续走真实按钮 → picker 路径，回报中如实说明。

### Q3（F3）让重连可见
- 每次重连计数并写进报告（例如 `report.cdpReattaches: [{at, step, reason}]`）。
- 默认策略：发生重连的运行，verdict 至少降为"通过但有警告"，并在汇总行打印；需要把重连视为可接受的脚本必须**显式声明**。
- 加一条 harness 自测：模拟连接关闭 → 报告里出现一条重连记录。

### Q4 记录更正
- 更正 `findings.md`：默认档不可运行只限执行方沙箱（见 1.1）。
- 更正 `progress.md`：R1 的真实 App 证据以 Q2 的结果为准；R6 7/7 的末步不能算作发布结论的证据。

---

## 3. 需要用户确认的一件事（不属于执行方返工）

提交 `d69e0e3` 改写并提交了仓库里被跟踪的 `.workbuddy/memory/MEMORY.md`（113 增 / 119 删）。原任务书写的是不要碰 `.workbuddy/`。
如果 `.workbuddy` 就是执行方自己的记忆目录，这次改动合理；否则需要回退。由用户决定。

## 4. 已知的低优先级项（登记，不要求本轮做）

- 学生端阅读 provider 的 `getAsset(id)` 未拒绝听力条目 id（目录里已不可见，只剩直接按 id 访问的情况，会在后续加载时失败）。
- `delete_exam_by_id` 孤儿行（无 UI 入口）。
- 导入抽屉「未连接云端」一行异步插入导致按钮跳位（真实用户手快可能点错）。

## 5. 质量方收货标准

- Q1/Q2 由质量方在**默认产品档**复跑通过；报告中每一步是确定期望值的断言。
- 学生端：4 个 part 各自取到音频，阅读目录不含该听力卷。
- 重连记录字段存在；本轮运行重连次数为 0，或每次都有合理解释。
- 三套测试数字与回报一致；阅读链保持 13/13。
