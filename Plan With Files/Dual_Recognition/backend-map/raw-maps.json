[
  {
    "subsystem": "SQLite schema and migrations (src-tauri/src/db.rs + its only consumer library_commands.rs)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "role": "The ENTIRE SQLite layer for authoring_hub.db: file-level rename migration, connection open + PRAGMAs, the one-and-only DDL constant SCHEMA_SQL (8 tables, 10 indexes), library_meta key/value store, and every query/mutation function. No other module contains DDL for this DB.",
        "sizeNote": "1349 lines / 53,620 bytes. Plan 17.12's \"50KB file\" figure is accurate."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "role": "Only cross-module consumer of db.rs. Holds the *_core impls behind the 8 Tauri library commands, the ExamRecord/LibraryItemRecord construction, the two double-write hooks (upsert_reading_job/upsert_writing_job), and migrate_existing_into_library — the single existing data-migration precedent.",
        "sizeNote": "802 lines; tests start at :492"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "CommandResult<T> alias (:90); library DTOs LibraryFilter/LibraryExamSummary/LibraryExamDetail/LibraryMetaPatch/LibraryStats (:206-265); `mod db;` (:54) and `mod library_commands;` (:66); app_root() = app_data_dir (:357-359); the .setup() hook that runs the migration (:1408-1414); invoke_handler registration of the 8 library commands (:1467-1474).",
        "sizeNote": "11009 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "role": "save_job() writes jobs/<id>/job.json then best-effort double-writes the DB at :37. This is the ONLY path by which reading content reaches SQLite.",
        "sizeNote": "105 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs",
        "role": "save_writing_job double-writes DB at :129; delete_writing_job hard-deletes the exams row at :189.",
        "sizeNote": "211 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "role": "delete_job_core removes the job dir then calls db::delete_exam_by_id at :149 (hard DELETE on exams only).",
        "sizeNote": "297 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "get_authoring_v2_core (:54) / apply_authoring_v2_patches_core (:123) — the workspace save path Phases 1-3 use. Contains ZERO references to db::, job_store::, library_commands:: or rusqlite. V2 edits never reach SQLite.",
        "sizeNote": "2551 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/util.rs",
        "role": "ensure_app_dirs (:63-78) enumerates the AppData subdirs that exist today: config, config/secrets, jobs, writing-jobs, logs, cache, cache/parser, cache/thumbnails, cache/preview-server. No assets/, source-evidence/ or tmp/ as plan 11.4 assumes.",
        "sizeNote": "389 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/export_nas_library.rs",
        "role": "DECOY — a second, unrelated SQLite DB (publish/library.db, const at :24) with its own schema created at :693-726 (library_meta + reading_assets). write_library_db (:728) deletes and recreates the file every publish; it has no migration story and shares the table name library_meta with authoring_hub.db. Do not confuse the two when grepping.",
        "sizeNote": "1727 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/",
        "role": "NOT SQL. Rust serde struct definitions (common.rs, document_ir_v2.rs, ielts_authoring_v2.rs, migration_v1.rs = V1<->V2 IR conversion, quality_report_v2.rs, …). A directory submodule using mod.rs — the convention to copy for a new library/ module.",
        "sizeNote": "10 files"
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/library/libraryStore.ts",
        "role": "Proof that the legacy exams-driven query is on the NEW production path: the converged Library route merges listJobs() (file scan) + listLibraryExams() (COALESCE query) + listTrashedExams() (library_items-only) at :55-66.",
        "sizeNote": "115 lines"
      }
    ],
    "publicApi": [
      {
        "symbol": "db_path",
        "file": "src-tauri/src/db.rs:27",
        "signature": "pub(crate) fn db_path(root: &Path) -> PathBuf",
        "notes": "root.join(\"authoring_hub.db\"). DB_NAME const at :24, LEGACY_DB_NAME=\"library.db\" at :22. root is always app.path().app_data_dir() (lib.rs:357-359)."
      },
      {
        "symbol": "migrate_db_file",
        "file": "src-tauri/src/db.rs:33",
        "signature": "pub(crate) fn migrate_db_file(root: &Path) -> CommandResult<()>",
        "notes": "The ONLY file-level migration: std::fs::rename(library.db -> authoring_hub.db) when the new name is absent. Idempotent; new file wins if both exist. Called from open_connection:53, so it runs on every open."
      },
      {
        "symbol": "open_connection",
        "file": "src-tauri/src/db.rs:51",
        "signature": "pub(crate) fn open_connection(root: &Path) -> CommandResult<Connection>",
        "notes": "THE only way in. No pool, no AppState-held connection — a fresh transient rusqlite Connection per call. Does: migrate_db_file -> Connection::open -> execute_batch(\"PRAGMA journal_mode=WAL; PRAGMA foreign_keys=ON; PRAGMA busy_timeout=5000;\") (:60-65) -> ensure_schema (:66). Errors are redacted to the literal \"open_db_failed\" (:58) with the real path only on stderr. 15 call sites, all in library_commands.rs (:177,215,223,232,255,266,275,370,375,387) + db.rs:278,773."
      },
      {
        "symbol": "ensure_schema",
        "file": "src-tauri/src/db.rs:71",
        "signature": "pub(crate) fn ensure_schema(conn: &Connection) -> CommandResult<()>",
        "notes": "Body is exactly `conn.execute_batch(SCHEMA_SQL)`. This IS the whole migration mechanism. Runs unconditionally on every open_connection."
      },
      {
        "symbol": "SCHEMA_SQL",
        "file": "src-tauri/src/db.rs:77-191",
        "signature": "const SCHEMA_SQL: &str = r#\" ... \"#;",
        "notes": "Single raw-string DDL blob, entirely CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT EXISTS. 8 tables: library_meta(:79), exams(:83), library_items(:108), library_item_revisions(:129), ingest_jobs(:143), source_assets(:158), publish_records(:170), job_artifact_index(:184). 10 indexes: :100,:101,:102,:103,:124,:125,:126,:140,:155,:181. One UNIQUE(library_item_id, revision_no) at :138. No views, no triggers, no AUTOINCREMENT. Real FK clauses exist only at :131, :172, :173."
      },
      {
        "symbol": "set_meta",
        "file": "src-tauri/src/db.rs:195",
        "signature": "pub(crate) fn set_meta(conn: &Connection, key: &str, value: &str) -> CommandResult<()>",
        "notes": "UPSERT into library_meta. The entire versioning primitive available in this codebase."
      },
      {
        "symbol": "get_meta",
        "file": "src-tauri/src/db.rs:205",
        "signature": "pub(crate) fn get_meta(conn: &Connection, key: &str) -> CommandResult<Option<String>>",
        "notes": "Exactly ONE production key exists: \"migration_done_v1\" (read library_commands.rs:388, written :487)."
      },
      {
        "symbol": "ExamRecord",
        "file": "src-tauri/src/db.rs:220-236",
        "signature": "pub(crate) struct ExamRecord { pub id: String, pub exam_id: Option<String>, pub title: String, pub subject: String, pub category: Option<String>, pub frequency: Option<String>, pub status: String, pub task_type: Option<String>, pub tags: Vec<String>, pub payload_json: String, pub source_hash: Option<String>, pub issue_errors: u32, pub issue_warnings: u32, pub created_at: String, pub updated_at: String }",
        "notes": "Flat row for legacy `exams`. status is the public enum draft|needs_review|ready|exported."
      },
      {
        "symbol": "upsert_exam_conn",
        "file": "src-tauri/src/db.rs:239",
        "signature": "pub(crate) fn upsert_exam_conn(conn: &Connection, record: &ExamRecord) -> CommandResult<()>",
        "notes": "INSERT ... ON CONFLICT(id) DO UPDATE, created_at deliberately not overwritten. Accepts &Transaction via Deref — that is how migrate_existing_into_library reuses it (library_commands.rs:479)."
      },
      {
        "symbol": "upsert_exam",
        "file": "src-tauri/src/db.rs:277",
        "signature": "pub(crate) fn upsert_exam(root: &Path, record: &ExamRecord) -> CommandResult<()>",
        "notes": "open_connection + upsert_exam_conn. The root-taking convenience wrapper pattern that lets save hooks avoid holding a Connection."
      },
      {
        "symbol": "upsert_library_item",
        "file": "src-tauri/src/db.rs:841",
        "signature": "pub(crate) fn upsert_library_item(conn: &Connection, record: &LibraryItemRecord) -> CommandResult<String>",
        "notes": "Returns the new revision id. Uses `conn.unchecked_transaction()` (:846) so it only needs &Connection — but that means it issues BEGIN DEFERRED and will FAIL if called while an outer transaction is open. Appends a NEW library_item_revisions row on every call (revision_no = MAX+1, :864) and points current_revision_id at it. record.task_type is silently dropped — see surprises."
      },
      {
        "symbol": "LibraryItemRecord",
        "file": "src-tauri/src/db.rs:817-836",
        "signature": "pub(crate) struct LibraryItemRecord { pub id: String, pub subject: String, pub content_type: String, pub title: String, pub category: Option<String>, pub difficulty: Option<String>, pub status: String, pub task_type: Option<String>, pub tags: Vec<String>, pub source_asset_id: Option<String>, pub linked_ingest_job_id: Option<String>, pub created_at: String, pub updated_at: String, pub revision_payload_json: String, pub schema_version: String, pub created_from_job_id: Option<String>, pub change_reason: Option<String> }",
        "notes": "#[derive(Clone)]. status is the internal enum draft|review_required|ready|published|archived."
      },
      {
        "symbol": "ensure_library_item_for_exam",
        "file": "src-tauri/src/db.rs:497",
        "signature": "pub(crate) fn ensure_library_item_for_exam(conn: &Connection, id: &str) -> CommandResult<bool>",
        "notes": "Lazy backfill legacy exams row -> library_items + rev 1 (change_reason \"legacy_exam_backfill\", :480). Returns false if no exams row exists. Sole caller library_commands.rs:258."
      },
      {
        "symbol": "list_exams",
        "file": "src-tauri/src/db.rs:510",
        "signature": "pub(crate) fn list_exams(conn: &Connection, filter: &LibraryFilter) -> CommandResult<Vec<LibraryExamSummary>>",
        "notes": "Wraps active_summary_source_sql() as a subquery. Dynamic AND clauses on subject/status/category, ORDER BY updated_at DESC, LIMIT default 200 clamped 1..1000 (:532)."
      },
      {
        "symbol": "get_exam",
        "file": "src-tauri/src/db.rs:552",
        "signature": "pub(crate) fn get_exam(conn: &Connection, id: &str) -> CommandResult<Option<LibraryExamDetail>>",
        "notes": "Uses active_detail_source_sql(), which additionally LEFT JOINs library_item_revisions and returns COALESCE(rev.payload_json, e.payload_json) (:393)."
      },
      {
        "symbol": "search_exams",
        "file": "src-tauri/src/db.rs:577",
        "signature": "pub(crate) fn search_exams(conn: &Connection, query: &str) -> CommandResult<Vec<LibraryExamSummary>>",
        "notes": "LOWER(...) LIKE '%q%' over title / exam_id / tags_json. No FTS table. LIMIT 200 hardcoded."
      },
      {
        "symbol": "get_stats",
        "file": "src-tauri/src/db.rs:601",
        "signature": "pub(crate) fn get_stats(conn: &Connection) -> CommandResult<LibraryStats>",
        "notes": "Runs the COALESCE subquery FOUR times (total + group_count on subject/status/category, :606 and :616)."
      },
      {
        "symbol": "update_exam_meta",
        "file": "src-tauri/src/db.rs:644",
        "signature": "pub(crate) fn update_exam_meta(conn: &Connection, id: &str, patch: &LibraryMetaPatch) -> CommandResult<Option<LibraryExamSummary>>",
        "notes": "Dynamic UPDATE exams (:684). If affected == 0 it returns Ok(None) WITHOUT touching library_items (:689-691) — exams is authoritative for edit existence. Only then calls update_library_item_meta (:692)."
      },
      {
        "symbol": "update_library_item_meta",
        "file": "src-tauri/src/db.rs:714",
        "signature": "fn update_library_item_meta(conn: &Connection, id: &str, patch: &LibraryMetaPatch) -> CommandResult<()>",
        "notes": "Private. Maps frequency->difficulty, task_type->category fallback (:732-735), status via to_library_item_status. No-ops when the item row is absent (:719)."
      },
      {
        "symbol": "delete_exam",
        "file": "src-tauri/src/db.rs:764",
        "signature": "pub(crate) fn delete_exam(conn: &Connection, id: &str) -> CommandResult<bool>",
        "notes": "HARD `DELETE FROM exams WHERE id=?`. Does not touch library_items."
      },
      {
        "symbol": "delete_exam_by_id",
        "file": "src-tauri/src/db.rs:772",
        "signature": "pub(crate) fn delete_exam_by_id(root: &Path, id: &str) -> CommandResult<()>",
        "notes": "open_connection + delete_exam. Called from job_commands.rs:149 (delete_job) and writing_store.rs:189 (delete_writing_job)."
      },
      {
        "symbol": "prune_orphan_exams",
        "file": "src-tauri/src/db.rs:781",
        "signature": "pub(crate) fn prune_orphan_exams(conn: &Connection, live_ids: &std::collections::HashSet<String>) -> CommandResult<usize>",
        "notes": "SELECT all exams ids, then row-by-row DELETE any id not in live_ids. Only called inside migrate_existing_into_library's transaction (library_commands.rs:483), i.e. exactly once per install."
      },
      {
        "symbol": "soft_delete_library_item",
        "file": "src-tauri/src/db.rs:903",
        "signature": "pub(crate) fn soft_delete_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "notes": "Sets deleted_at=now. Returns false if already deleted."
      },
      {
        "symbol": "restore_library_item",
        "file": "src-tauri/src/db.rs:914",
        "signature": "pub(crate) fn restore_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "notes": "Clears deleted_at. Returns false if not deleted."
      },
      {
        "symbol": "restore_exam_from_library_item",
        "file": "src-tauri/src/db.rs:1001",
        "signature": "pub(crate) fn restore_exam_from_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "notes": "Reverse backfill: re-materializes a missing exams row from library_items + current revision (helper exam_record_from_library_item at :924, which re-derives exam_id/task_type by digging into payload JSON at :944-972). Exists purely because the exams row can be hard-deleted while the item survives."
      },
      {
        "symbol": "list_trashed_items",
        "file": "src-tauri/src/db.rs:1013",
        "signature": "pub(crate) fn list_trashed_items(conn: &Connection) -> CommandResult<Vec<LibraryExamSummary>>",
        "notes": "The ONLY production query that reads library_items WITHOUT joining exams (:1015-1019). Fabricates NULL exam_id / NULL source_hash / 0 issue counts."
      },
      {
        "symbol": "active_summary_source_sql",
        "file": "src-tauri/src/db.rs:325",
        "signature": "fn active_summary_source_sql() -> String",
        "notes": "THE COALESCE mixer plan 17.12 wants deleted. `FROM exams e LEFT JOIN library_items li ON li.id = e.id WHERE li.deleted_at IS NULL` (:356-358) — exams is the DRIVING table. COALESCEs title/subject/category/difficulty->frequency/tags_json/created_at, CASE-normalizes li.status, takes source_hash and both issue counts from `e` ONLY (:347-349), and picks the greater of the two updated_at (:351-355). Consumed at :516, :586, :606, :616, :700."
      },
      {
        "symbol": "active_detail_source_sql",
        "file": "src-tauri/src/db.rs:362",
        "signature": "fn active_detail_source_sql() -> String",
        "notes": "Same as above plus `LEFT JOIN library_item_revisions rev ON rev.id = li.current_revision_id` and `COALESCE(rev.payload_json, e.payload_json) AS payload_json`. Consumed at :555."
      },
      {
        "symbol": "normalize_public_status / to_library_item_status",
        "file": "src-tauri/src/db.rs:309 / :317",
        "signature": "fn normalize_public_status(status: &str) -> &str   /   fn to_library_item_status(status: &str) -> &str",
        "notes": "review_required<->needs_review, published<->exported. A THIRD copy of the same mapping exists in library_commands.rs:160-168 as `fn to_library_item_status(exam_status: &str) -> &'static str` with a `_ => \"draft\"` catch-all."
      },
      {
        "symbol": "migrate_existing_into_library",
        "file": "src-tauri/src/library_commands.rs:386",
        "signature": "pub(crate) fn migrate_existing_into_library(root: &Path) -> CommandResult<usize>",
        "notes": "THE template for any new data migration. Pattern: `let mut conn = open_connection(root)?` -> early-return if get_meta(\"migration_done_v1\").is_some() (:388) -> scan jobs/*/job.json and writing-jobs/*/writing-job.json building all records with ZERO IO inside the tx (:392-463) -> `conn.transaction()` (:470, needs `mut`) -> upsert_exam_conn(&tx, r) per record -> prune_orphan_exams(&tx, ...) -> set_meta(&tx, \"migration_done_v1\", \"1\") -> tx.commit(). Any failure rolls back and leaves the flag unset so the next launch retries. NOTE it writes only `exams`, never library_items."
      },
      {
        "symbol": "upsert_reading_job / upsert_writing_job",
        "file": "src-tauri/src/library_commands.rs:92 / :139",
        "signature": "pub(crate) fn upsert_reading_job(root: &Path, job: &ImportJob) -> CommandResult<()>   /   pub(crate) fn upsert_writing_job(root: &Path, job: &WritingJob) -> CommandResult<()>",
        "notes": "The two double-write hooks plan 11.2-M4 deletes. Each does upsert_exam (own connection) then upsert_library_item_from_exam (:172, a SECOND connection) — two separate transient connections per save, no shared transaction. Reading hook skips the whole DB write if authoring-ir.json exists but fails to parse (:95-101). Sole callers: job_store.rs:37, writing_store.rs:129."
      },
      {
        "symbol": "upsert_library_item_from_exam",
        "file": "src-tauri/src/library_commands.rs:172",
        "signature": "fn upsert_library_item_from_exam(root: &Path, exam: &ExamRecord, schema_version: &str) -> CommandResult<()>",
        "notes": "Opens its own connection, honours soft-delete via is_library_item_soft_deleted (:280) to avoid resurrecting trashed items, sets source_asset_id = exam.source_hash (:196) and linked_ingest_job_id = exam.id (:197), change_reason \"ingest_save\" (:203)."
      }
    ],
    "dataFlow": "Location and lifecycle. One DB file: <app_data_dir>/authoring_hub.db (db.rs:24,27-29; app_data_dir at lib.rs:358). There is no pool and no long-lived Connection anywhere — every operation calls db::open_connection(root), which (1) attempts the legacy library.db -> authoring_hub.db rename, (2) Connection::open (creating the file if absent), (3) execute_batch of journal_mode=WAL + foreign_keys=ON + busy_timeout=5000, (4) execute_batch(SCHEMA_SQL). So schema convergence is lazy and unconditional: every single DB touch re-runs all 8 CREATE TABLE IF NOT EXISTS + 10 CREATE INDEX IF NOT EXISTS statements. There is NO PRAGMA user_version (grep over src-tauri/src returns zero hits), NO schema_version table (the identically-named thing at db.rs:134 is a *column* on library_item_revisions holding \"ReadingAuthoringIRV1\"/\"WritingExamSourceV1\"), and NO migration-history table. The only versioning primitive is the library_meta(key,value) table, which in production holds exactly one key: migration_done_v1.\n\nWrite path (reading). A Tauri command mutates an ImportJob -> job_store::update_job -> job_store::save_job (:33) writes jobs/<id>/job.json, then best-effort calls library_commands::upsert_reading_job (:37); errors only go to stderr. upsert_reading_job reads jobs/<id>/authoring-ir.json (falling back to the serialized job when the file is absent, and ABORTING the DB write entirely when the file exists but won't parse, library_commands.rs:95-101), builds an ExamRecord, calls db::upsert_exam (connection #1, writes `exams`), then upsert_library_item_from_exam (connection #2, writes library_items + a brand-new library_item_revisions row). Writing is the same shape via writing_store.rs:129. Both hooks run outside any shared transaction, so a crash between them forks the two tables.\n\nWrite path (V2 workspace) — the gap. The converged editor saves through apply_authoring_v2_patches. authoring_v2_commands.rs:123-168 loads the current authoring, applies patches, and persists via artifact_store::append_revision (file tree under jobs/<id>/authoring/revisions/). That file contains no `db::`, `job_store::`, `library_commands::` or `workflow_state::` reference at all, so a V2 edit never reaches SQLite. library_item_revisions.payload_json therefore only ever holds V1 authoring-ir (or raw job.json) content.\n\nMetadata write path runs the other direction: update_library_exam_meta_core (library_commands.rs:227) reads the summary, calls db::update_exam_meta (UPDATE exams first — bails with Ok(None) if zero rows matched, db.rs:689 — then UPDATE library_items), then best-effort write_back_meta_to_source (:293) which mutates job.json/writing-job.json and re-triggers save_job, which re-triggers the double-write hook, which appends yet another revision row.\n\nDelete path. delete_library_exam_core (:254) is soft: ensure_library_item_for_exam then soft_delete_library_item, leaving the exams row intact. But delete_job (job_commands.rs:142-156) and delete_writing_job (writing_store.rs:183-195) rm -rf the job dir and then HARD `DELETE FROM exams` — library_items and its revisions survive with deleted_at still NULL.\n\nRead path. list/get/search/stats and the post-update read-back all funnel through the two generated SQL strings at db.rs:325-399, whose FROM clause is `exams e LEFT JOIN library_items li ON li.id = e.id WHERE li.deleted_at IS NULL`. Because `exams` drives the join, a library_items row with no matching exams row is invisible to every production query except list_trashed_items. list_trashed_items (db.rs:1013) is the sole library_items-only query. Reads reach the frontend as LibraryExamSummary/Detail (lib.rs:215-243) through the 8 commands registered at lib.rs:1467-1474, each a spawn_blocking shell around a *_core.\n\nBootstrap. lib.rs .setup() (:1408-1414) calls ensure_app_dirs then `let _ = library_commands::migrate_existing_into_library(&root);` — return value AND error both discarded. That function's own open_connection is what first creates the DB and schema on a fresh install.\n\nSeparate DB, do not conflate: publish/library.db (export_nas_library.rs:24) is a publish artifact with its own library_meta + reading_assets schema (:693-726), deleted and rebuilt from scratch on each publish (:733-737). It never migrates and shares only the table name.",
    "gapsVsPlan": [
      {
        "claim": "Plan 11.2-M1: add library_items_v2, processing_jobs_v2, actionable_issues_v1, library_item_recovery_v1 while old exams/library_items/jobs stay readable (plan:2001-2004).",
        "reality": "Additive is the ONLY thing this codebase can do, so M1 is nearly free — append to SCHEMA_SQL and existing installs get the tables at the next open_connection. But the plan's own DDL is not copy-pasteable: plan 4.3 (:385,:398) and 4.4 (:428,:435) all use bare `CREATE TABLE`, which throws on the second ensure_schema call. Every statement must be rewritten as CREATE TABLE IF NOT EXISTS. Also the M1 list omits editor_journal_v1 (only in 4.4's SQL), and actionable_issues_v1 has NO DDL anywhere in the plan — only the Rust struct ActionableIssueV1 at plan:1481-1495 and bare mentions at plan:2003/:3173. The implementer must author that table themselves.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/db.rs:71-75 (ensure_schema runs SCHEMA_SQL every open), db.rs:77-191 (all DDL uses IF NOT EXISTS) vs plan lines 385, 398, 428, 435, 2003"
      },
      {
        "claim": "Plan 11.2-M2: \"迁移必须幂等，以 migration_version 标记\" (plan:2024).",
        "reality": "There is no migration_version anywhere. The existing marker is the boolean-ish library_meta row migration_done_v1 (read library_commands.rs:388, written :487). There is no PRAGMA user_version and no migrations table — grep for user_version across src-tauri/src returns nothing. A monotonic integer version does not exist and would have to be introduced.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/library_commands.rs:388 `if matches!(db::get_meta(&conn, \"migration_done_v1\")?, Some(_)) { return Ok(0); }` and :487 `db::set_meta(&tx, \"migration_done_v1\", \"1\")?;`"
      },
      {
        "claim": "Plan 17.12: \"删除 production query 对 legacy exams / new library_items 的 COALESCE 混合\".",
        "reality": "Confirmed and worse than a single query: two generated SQL strings, active_summary_source_sql (db.rs:325) and active_detail_source_sql (db.rs:362), are consumed at 6 call sites (db.rs:516, 555, 586, 606, 616, 700) covering list_exams, get_exam, search_exams, get_stats (x4 executions), and the post-update read-back. `exams` is the DRIVING table of the join, not the fallback, so library_items rows without an exams row are invisible. Removing the mixing is therefore not a cleanup — it changes which rows exist. source_hash, issue_errors and issue_warnings come from `exams` ONLY (db.rs:347-349) and have no column at all in library_items, so those three fields are lost the moment exams is dropped from the join.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/db.rs:356-358 `FROM exams e LEFT JOIN library_items li ON li.id = e.id WHERE li.deleted_at IS NULL`; consumed at db.rs:516, 555, 586, 606, 616, 700"
      },
      {
        "claim": "Plan 11.1: the five parallel sources of truth include \"jobs/<id>/authoring/revisions/* V2 内容\" and \"library_items + revisions 新 DB 内容\", forking only transiently.",
        "reality": "The fork is permanent, not transient. apply_authoring_v2_patches_core persists exclusively through artifact_store::append_revision; authoring_v2_commands.rs contains zero `db::`, `job_store::`, `library_commands::` or `workflow_state::` references. So the DB's library_item_revisions.payload_json holds V1 authoring-ir (or raw job.json) forever, and every V2 edit made by the already-shipped Phase 1-3 workspace is invisible to the DB. The frontend's claim that the workspace \"saves through get_authoring_v2 / apply_authoring_v2_patches\" is true, and that path is entirely file-based.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:149-156 (append_revision is the only persistence); grep of that file for db::/job_store::/library_commands::/workflow_state:: returns nothing; src-tauri/src/job_store.rs:37 is the only DB write trigger for reading content"
      },
      {
        "claim": "Plan 11.2-M3/M4 and 17.13: the new UI stops calling legacy library CRUD, then the double-writes and legacy exams queries get deleted.",
        "reality": "The already-shipped Phase 1-3 Library route still calls all of it. src/features/library/libraryStore.ts:55-66 fans out to listJobs() (a raw file scan of jobs/*/job.json, job_store.rs:58-84), listLibraryExams() (the COALESCE query), and listTrashedExams(), then merges by id. moveToTrash/restore (:105,:110) call deleteLibraryExam/restoreLibraryExam. So M4 is blocked by live frontend code, and there are currently THREE read sources on the converged route, not one.",
        "verdict": "partly-wrong",
        "evidence": "src/features/library/libraryStore.ts:55-66, :105, :110; commands registered at src-tauri/src/lib.rs:1467-1474"
      },
      {
        "claim": "Plan 17.12: \"SQL 拆到 library/schema.rs 或 migration 文件\"; \"不继续在一个 50KB 文件中追加\".",
        "reality": "Accurate and not started. db.rs is 53,620 bytes / 1349 lines and holds DDL, connection handling, and all data access. No library/ directory exists; lib.rs:44-87 declares flat modules, with only docx_ingest, ielts_grammar, pdf_ingest and schema as mod.rs-style directory submodules. There are zero .sql files and zero migrations/ directories in the repo.",
        "verdict": "confirmed",
        "evidence": "`ls -la src-tauri/src/db.rs` = 53620 bytes; src-tauri/src/lib.rs:44-87; `find . -name '*.sql'` and `find . -type d -name migrations` both empty"
      },
      {
        "claim": "Plan 11.4: the AppData layout already is authoring_hub.db + assets/sha256/<prefix>/<hash> + source-evidence/ + tmp/jobs/<job-id>/{input,render,local,cloud,reconcile}.",
        "reality": "Only authoring_hub.db matches. ensure_app_dirs creates exactly config, config/secrets, jobs, writing-jobs, logs, cache, cache/parser, cache/thumbnails, cache/preview-server. No assets/, no source-evidence/, no tmp/. Also cleanup.rs contains no DB references whatsoever, so plan 11.5's cleanup_on_startup has no existing DB-aware hook to extend.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/util.rs:63-78; src-tauri/src/db.rs:27-29; grep of src-tauri/src/cleanup.rs for db::/Connection/library_items/exams returns nothing"
      },
      {
        "claim": "Plan 4.3: library_items_v2.modality CHECK (modality IN ('reading','listening','writing')).",
        "reality": "Nothing blocks adding that table, but during the M1-M3 coexistence window a listening item can never be mirrored into the legacy tables: exams.subject CHECK is IN ('reading','writing') (db.rs:87) and library_items.subject CHECK is the same (db.rs:110). Since the production list query is exams-driven, a listening item created in v2 tables will be BOTH uninsertable into exams AND invisible in the Library list until the COALESCE query is replaced. Listening cannot be shipped incrementally behind the legacy read path.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/db.rs:87, :110, :356-358; plan line 387"
      },
      {
        "claim": "Plan 4.4: stop creating an immutable artifact per save; keep only current DS + one snapshot + last 100 journal entries.",
        "reality": "The DB side is worse than described. upsert_library_item unconditionally INSERTs a new library_item_revisions row on every call (db.rs:887-896, revision_no = MAX+1 at :864), and the double-write hook fires on every save_job — including the one that write_back_meta_to_source triggers after a metadata edit (library_commands.rs:339). There is no DELETE FROM library_item_revisions and no DELETE FROM library_items anywhere in db.rs; the only DELETEs are on `exams` (:766, :801). Revision rows and soft-deleted items grow without bound.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/db.rs:864, :887-896; full inventory of write statements in db.rs shows DELETE only at :766 and :801"
      },
      {
        "claim": "Stated as verified by the orchestrator: library_items.id == job.job_id.",
        "reality": "Confirmed at every construction site. Reading: ExamRecord.id = job.job_id (library_commands.rs:50); writing: id = job.job_id (:120); LibraryItemRecord.id = exam.id (:187). db.rs's own backfill path does the same (:460). Additionally linked_ingest_job_id and created_from_job_id are also set to that same id (library_commands.rs:197, :202; db.rs:474, :479), so those two columns carry no extra information today.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/library_commands.rs:50, :120, :187, :197, :202; src-tauri/src/db.rs:460, :474, :479"
      },
      {
        "claim": "Plan 11.2-M1 implies the Phase-1 six-table \"formal schema\" is a working foundation to build v2 on.",
        "reality": "Four of those six tables are dead schema: ingest_jobs, source_assets, publish_records and job_artifact_index are created and indexed but never INSERTed, SELECTed or referenced anywhere in the codebase — grepping each name across db.rs finds only the CREATE statement, its index, and the module doc comment at db.rs:10. Only library_items and library_item_revisions are live. So processing_jobs_v2 is not replacing a working ingest_jobs; it is the first real job table.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/db.rs:143-190 create them; per-name grep across all of src-tauri/src finds no INSERT/SELECT for any of the four"
      }
    ],
    "changePoints": [
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "SCHEMA_SQL (const, lines 77-191)",
        "whatToChange": "THE recipe for adding a table in this codebase, and it is one step: append `CREATE TABLE IF NOT EXISTS <name> (...);` plus `CREATE INDEX IF NOT EXISTS ...;` to this raw string, before the closing \"#. Nothing else is required — ensure_schema (:71) runs the whole blob from open_connection (:66) on every connection, so existing installs materialize the new tables the next time any DB call happens. Add all five (library_items_v2, processing_jobs_v2, actionable_issues_v1, library_item_recovery_v1, editor_journal_v1) here. Rewrite the plan's DDL: plan 4.3/4.4 use bare CREATE TABLE, which errors on the second ensure_schema. Author actionable_issues_v1 yourself from ActionableIssueV1 (plan:1481-1495) — the plan has no DDL for it. Follow the file's existing conventions: TEXT ISO8601 timestamps (never INTEGER epoch), TEXT PRIMARY KEY ids, *_json TEXT NOT NULL DEFAULT '{}' or '[]' for blobs, soft delete via a nullable deleted_at.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "open_connection / ensure_schema (lines 51-75)",
        "whatToChange": "If you need anything CREATE-TABLE-IF-NOT-EXISTS cannot express — adding a column to an already-shipped v2 table, backfilling a NOT NULL, renaming — you must introduce the mechanism, because none exists: zero ALTER TABLE statements in the entire repo (rust, sql and ts), zero PRAGMA user_version, no migrations table. Add a `fn run_migrations(conn: &Connection) -> CommandResult<()>` invoked from db.rs:66 immediately after ensure_schema, gated on a library_meta key (e.g. schema_version_v2 read via get_meta/set_meta, which already accept &Connection and work inside a Transaction). Do this in the same commit that adds the v2 tables — retrofitting a version baseline after installs exist in the field is what forces guesswork later. Do NOT add PRAGMAs here without deciding about foreign_keys: it is currently ON (:62) in production and OFF in the unit-test harness.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "module scope; new src-tauri/src/library/{mod.rs,schema.rs,repository.rs,migration.rs}",
        "whatToChange": "Plan 17.12's split. Convention to copy: a directory submodule with mod.rs re-exporting `pub mod x;` (src-tauri/src/schema/mod.rs:1-5), declared as a bare `mod library;` in the alphabetical list at lib.rs:44-87 — it belongs immediately before `mod library_commands;` at lib.rs:66. Move SCHEMA_SQL to library/schema.rs and keep db.rs::ensure_schema delegating to it so the ~15 existing open_connection call sites keep working unchanged. Everything in db.rs is pub(crate), so cross-module moves need no visibility changes. New repository fns should keep the file's two-signature idiom: `fn foo(conn: &Connection, ...)` for composability inside transactions plus an optional `fn foo_at(root: &Path, ...)` wrapper that opens its own connection (pattern at db.rs:277-280 and :772-776).",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "upsert_library_item (line 841, specifically `conn.unchecked_transaction()` at :846)",
        "whatToChange": "Trap for any v2 repository write you call from inside a migration. This fn takes &Connection but issues its own BEGIN DEFERRED via unchecked_transaction, so calling it (or anything copying the idiom) while an outer transaction is open fails with \"cannot start a transaction within a transaction\". The existing migration sidesteps this by calling only upsert_exam_conn inside its tx (library_commands.rs:479) — note it deliberately does NOT write library_items there. New v2 repository writers must expose a tx-safe form (plain execute, no unchecked_transaction) if they are to be reused by migration.rs. Also note that upsert_library_item INSERTs library_items with current_revision_id pointing at a revision row that does not exist yet (:867-884, revision inserted at :887) — this only works because current_revision_id has NO FK clause (db.rs:117 is a comment, not a REFERENCES). Do not add a real FK there, and do not copy that ordering into v2 tables that do have FKs.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/library_commands.rs",
        "symbol": "migrate_existing_into_library (line 386)",
        "whatToChange": "Copy this as the M2 template into library/migration.rs, guarded by a NEW library_meta key (do not reuse migration_done_v1). The shape that matters: `let mut conn` because conn.transaction() (:470) needs &mut, unlike the unchecked_transaction used elsewhere; all filesystem IO done BEFORE opening the tx (:392-463, explicitly commented at :392-393); set_meta inside the tx (:487) so a rollback also un-sets the done flag and the next launch retries; failures collected into a `skipped` vec and logged rather than aborting (:465-467). Two hazards for M2 specifically: (a) this existing function calls prune_orphan_exams (:483), which DELETEs any exams row whose id is not backed by a jobs/*/job.json or writing-jobs/*/writing-job.json file — once v2-native items exist without job dirs, re-running anything with that pruning would delete their legacy mirror rows; (b) plan 11.2's M2 pseudocode wants prefer_in_order(current_v2_revision, authoring_v2_shadow, convert_v1_authoring), and only the third of those is reachable from the DB — V2 content lives solely in the file tree via artifact_store, so migration.rs must read jobs/<id>/authoring/revisions/* off disk, not from library_item_revisions.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/lib.rs",
        "symbol": "run() .setup() closure (lines 1408-1414)",
        "whatToChange": "Register the new migration next to the existing `let _ = library_commands::migrate_existing_into_library(&root);` at :1412. Note that line discards both the count AND any error, so a failing migration is completely silent today — at minimum log the Err for the new one. Order matters: ensure_app_dirs (:1410) must precede any open_connection because Connection::open cannot create the parent directory.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "active_summary_source_sql (line 325) and active_detail_source_sql (line 362)",
        "whatToChange": "Plan 17.12's deletion target. When you repoint reads at library_items_v2, replacing these two functions is a behavioural change, not a refactor, for three reasons: exams is the join's DRIVING table so the row set shrinks/changes; source_hash, issue_errors and issue_warnings are read from `e` only (:347-349) and have no library_items counterpart, so LibraryExamSummary (lib.rs:215-237) loses three fields unless library_items_v2 gains columns for them; and the status CASE at :334-341 plus normalize_public_status (:309) are what keep the frontend's draft|needs_review|ready|exported contract alive, which is incompatible with plan 11.3's LibraryItemStatusV2 (Processing/ActionRequired/Ready/Publishing/Published/Failed/Archived). Add a v2 read path alongside these rather than editing them in place until the frontend contract moves.",
        "risk": "high",
        "blockedBy": "src/features/library/libraryStore.ts:60 still calls listLibraryExams, and src/pages/LibraryPage.tsx / LibraryExamDetail.tsx / Dashboard.tsx call list/search/get/stats. Plan 11.2-M4 cannot start until those move to the v2 commands."
      },
      {
        "file": "src-tauri/src/authoring_v2_commands.rs",
        "symbol": "apply_authoring_v2_patches_core (line 123) and get_authoring_v2_core (line 54)",
        "whatToChange": "The single highest-value convergence edit and the one the plan understates. These are the commands the shipped Phase 1-3 workspace saves through, and they persist ONLY to the file tree via artifact_store::append_revision (:149-156); the file has no db:: import at all. To make library_items_v2.canonical_ds_json authoritative, this is where the write must be added (or moved). The optimistic-concurrency contract already in place — base_revision compared against the loaded current_revision, erroring `revision_conflict:current={}:base={}` (:135-140) — maps directly onto the plan's current_edit_version, so reuse that error string rather than inventing one. Batch cap is 200 patches (:130-132).",
        "risk": "high"
      },
      {
        "file": "src-tauri/src/library_commands.rs",
        "symbol": "upsert_reading_job (line 92), upsert_writing_job (line 139), upsert_library_item_from_exam (line 172)",
        "whatToChange": "Plan 11.2-M4's \"删除 save_job -> upsert_reading_job\". Call sites to cut: job_store.rs:37 and writing_store.rs:129. Before cutting, know what each hook silently absorbs: upsert_reading_job SKIPS the entire DB write when authoring-ir.json exists but fails to parse (:95-101) — deliberately, to avoid clobbering a good DB payload with job.json; and each hook opens TWO separate transient connections (upsert_exam at :104/:141 and upsert_library_item_from_exam at :177), so exams and library_items are never written in one transaction. upsert_library_item_from_exam also encodes two mislabelled mappings you should not carry into v2: source_asset_id := exam.source_hash (:196, a bare SHA-256 hex, not a source_assets.id) and linked_ingest_job_id := exam.id (:197).",
        "risk": "high",
        "blockedBy": "src/features/library/libraryStore.ts merges listJobs() file-scan output into the Library rows, so job.json must keep being written until that merge is removed."
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "update_exam_meta (line 644, guard at :689-691)",
        "whatToChange": "Fix or bypass before any v2-only item can be edited. The function UPDATEs `exams` first and returns Ok(None) when zero rows matched, so update_library_item_meta (:692) is never reached and the Tauri command reports \"not found\" for any item that exists only in library_items(_v2). Every metadata edit today therefore requires a legacy exams row. Either invert the order or route v2 items through a new command.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "delete_exam (line 764) / delete_exam_by_id (line 772)",
        "whatToChange": "Hard `DELETE FROM exams` that leaves library_items and all its revisions behind with deleted_at still NULL — invisible to every production read (exams drives the join) and absent from list_trashed_items (which filters deleted_at IS NOT NULL). Call sites: job_commands.rs:149 (delete_job) and writing_store.rs:189 (delete_writing_job). The codebase already acknowledges the resulting mess with restore_exam_from_library_item (:1001) and the test restore_rehydrates_legacy_exam_row_deleted_by_old_flow (library_commands.rs:689). When v2 tables land, add the corresponding v2 delete here or these two entry points will orphan v2 rows the same way.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "mem_conn test helper (lines 1041-1045)",
        "whatToChange": "Add `conn.execute_batch(\"PRAGMA foreign_keys=ON;\")` before ensure_schema, or every FK you write in the new v2 DDL is unenforced in tests and enforced in production. SQLite defaults foreign_keys OFF; production turns it ON at db.rs:62 but this helper does not. This matters immediately for the plan's `processing_jobs_v2.library_item_id TEXT NOT NULL REFERENCES library_items_v2(id)` (plan:400): inserting a job before its item passes every unit test and fails on a user's machine.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "LibraryItemRecord.task_type (line 825) + upsert_library_item INSERT column list (lines 868-882)",
        "whatToChange": "Dead field: library_items has no task_type column (only exams does, db.rs:91), and the INSERT never binds record.task_type — it is silently dropped on every write even though three call sites populate it (library_commands.rs:194, db.rs:471, and the test at :1222). That silent drop is exactly why the read queries have to reconstruct task_type with `COALESCE(e.task_type, CASE WHEN subject='writing' THEN li.category END)` (db.rs:342-345) and why list_trashed_items fabricates it from category (:1016). Either add the column or delete the field; do not reproduce the workaround in v2.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "exams CHECK constraints (lines 87, 90) and library_items CHECK constraints (lines 110, 115)",
        "whatToChange": "Hard blocker for incremental listening support. subject is CHECKed IN ('reading','writing') in both tables, so nothing with plan 4.3's modality='listening' can be double-written to the legacy tables. Since the production list query is exams-driven, a listening item in v2 tables is both uninsertable into exams and invisible in the Library. Sequence listening AFTER the read path moves off active_summary_source_sql, or ship it with the reads. Note SQLite cannot relax a CHECK via ALTER TABLE — widening the legacy constraint would need a full table rebuild, which is a mechanism this codebase does not have.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "library_item_revisions growth (INSERT at :887-896, revision_no at :864)",
        "whatToChange": "Plan 4.4's bounded-recovery goal needs a pruning path that does not exist: db.rs contains no DELETE against library_item_revisions or library_items — the only DELETEs are on exams (:766, :801). Every save_job appends a revision, including the extra save_job that write_back_meta_to_source triggers after a metadata edit (library_commands.rs:339). If library_item_recovery_v1 (single row per item, PRIMARY KEY library_item_id) and editor_journal_v1 (AUTOINCREMENT, needs a keep-last-100 delete) are to replace this, write the pruning in the same change — and give editor_journal_v1 an index on (library_item_id, id) since the plan's DDL declares none.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/db.rs",
        "symbol": "to_library_item_status (:317) / normalize_public_status (:309) and the duplicate at library_commands.rs:160-168",
        "whatToChange": "Three copies of the status mapping exist: two in db.rs and a third `fn to_library_item_status(exam_status: &str) -> &'static str` in library_commands.rs:160-168 which differs by having a `_ => \"draft\"` catch-all. A fourth mapping is inlined as SQL CASE in both source-SQL builders (db.rs:336-338, :373-375), and a fifth direction lives in library_status_to_reading/library_status_to_writing (library_commands.rs:346-364). Adding plan 11.3's LibraryItemStatusV2 without consolidating these first will produce a sixth. Consolidate into library/schema.rs as part of the split.",
        "risk": "low"
      }
    ],
    "testHarness": "All tests are inline `#[cfg(test)] mod tests` — there is no src-tauri/tests/ directory and no integration-test crate. Package `ielts-author-studio`, lib `ielts_author_studio_lib` (src-tauri/Cargo.toml:1-10), rusqlite 0.31 with the `bundled` feature (:34), so tests need no system SQLite.\n\ndb.rs tests (module at :1037-1349, 12 tests). Two fixtures to copy verbatim:\n\n    fn mem_conn() -> Connection {\n        let conn = Connection::open_in_memory().unwrap();\n        ensure_schema(&conn).unwrap();\n        conn\n    }\n\n(db.rs:1041-1045 — note it omits PRAGMA foreign_keys=ON, so FKs are unenforced in every one of these tests) and `fn sample_record(id: &str, subject: &str, status: &str) -> ExamRecord` (:1047-1069) / `fn sample_library_item(id: &str, subject: &str, status: &str) -> LibraryItemRecord` (:1208-1237). Test names, which are the exact patterns to clone for v2: schema_is_idempotent (:1072 — calls ensure_schema twice; the direct guard against a bare CREATE TABLE slipping into SCHEMA_SQL, so add nothing to that constant without this passing), upsert_inserts_then_updates (:1079), list_filters_by_subject_and_status (:1099), search_matches_title_and_tags (:1131), stats_group_counts (:1143), update_meta_partial_and_delete (:1157), meta_get_set (:1180 — the template for a schema-version-key test), check_constraint_rejects_invalid_status_and_subject (:1190 — asserts CHECK violations surface as Err), library_item_upsert_creates_revisions (:1240 — asserts revision_no increments and current_revision_id follows), soft_delete_and_restore (:1278), active_queries_follow_soft_delete_and_normalize_statuses (:1296 — the one that pins the exams-driven join's visibility semantics; it will fail if you repoint reads at v2 tables, which is the signal that the change is real), migrate_db_file_renames_legacy (:1325 — uses std::env::temp_dir().join(format!(\"ielts-migtest-{}\", uuid::Uuid::new_v4().simple()))).\n\nlibrary_commands.rs tests (module at :492-802, 6 tests) cover the full file+DB round trip and are where a new migration belongs. Fixtures: make_reading_appdata() (:501-543) builds a temp AppData root at std::env::temp_dir().join(format!(\"ielts-lib-test-{}\", uuid::Uuid::new_v4().simple())) containing jobs/import-test-1/{job.json,authoring-ir.json} with a minimal ReadingAuthoringIRV1 payload (:526-536); make_writing_appdata() (:545-567); fn cleanup(root: &PathBuf) (:569-571) does fs::remove_dir_all. Tests: migrate_imports_reading_and_writing (:574 — asserts the return count is 2 AND that a second call returns 0, i.e. the migration_done_v1 idempotence assertion to duplicate for a new key), update_meta_writes_back_to_job_json_and_survives_resave (:598), delete_library_exam_moves_to_trash_and_restore_brings_it_back (:638), restore_rehydrates_legacy_exam_row_deleted_by_old_flow (:689 — deliberately simulates the hard-deleted-exams-row-with-surviving-item state), update_meta_keeps_cleaned_reading_jobs_cleaned (:713), migrate_prunes_orphan_db_rows (:754).\n\nNotable coverage holes an implementer should not assume are covered: no test opens a real on-disk authoring_hub.db and asserts the schema (only in-memory), so a WAL/pragma-ordering regression would not be caught; no test exercises foreign_keys=ON at all; no test covers concurrent connections despite busy_timeout=5000 being the entire concurrency strategy; and nothing tests the double-write hooks' partial-failure path (exams written, library_items not).\n\nRun with `cargo test -p ielts-author-studio db::` / `library_commands::` — but per the assignment I did not execute cargo, so I have not verified these pass on the current tree.",
    "surprises": [
      "The migration mechanism is not a mechanism. There is no user_version, no schema_version table, no migrations directory, no .sql file, and zero ALTER TABLE statements anywhere in the repo. \"Migration\" = ensure_schema (db.rs:71) re-running one CREATE-TABLE-IF-NOT-EXISTS blob on every single connection open. Adding a TABLE is therefore trivially safe; adding a COLUMN to a table that has already shipped has no supported path at all.",
      "exams is the DRIVING table of the production join, not the fallback: `FROM exams e LEFT JOIN library_items li ON li.id = e.id` (db.rs:356-357). Every reading of the plan that treats library_items as the new primary and exams as legacy compatibility has it backwards — a library_items row with no exams row is invisible to list/get/search/stats. list_trashed_items (db.rs:1013) is the only production query that reads library_items alone.",
      "source_hash, issue_errors and issue_warnings have NO column in library_items. They are read from `e` only (db.rs:347-349) and are hardcoded to NULL/0 in both the library_items-only paths (:985-987, :1017). Dropping exams from the join silently zeroes three LibraryExamSummary fields the frontend already renders.",
      "LibraryItemRecord.task_type (db.rs:825) is set by three call sites and persisted by none — library_items has no such column, and the INSERT at :868-882 never binds it. The read queries then reconstruct it with COALESCE(e.task_type, CASE WHEN subject='writing' THEN li.category END) (:342-345). A silent data loss that the query layer papers over.",
      "Four of the six Phase-1 \"formal schema\" tables are dead: ingest_jobs, source_assets, publish_records, job_artifact_index are created and indexed but never read or written anywhere in the codebase. So processing_jobs_v2 is not replacing a working ingest_jobs — it is the first real job table. Correspondingly, library_items.source_asset_id is populated with a bare SHA-256 hex (library_commands.rs:196) rather than a source_assets.id, and linked_ingest_job_id / created_from_job_id are both just the item's own id.",
      "The already-shipped V2 workspace never touches SQLite. apply_authoring_v2_patches_core (authoring_v2_commands.rs:123-168) persists only via artifact_store::append_revision; that file contains no db::, job_store::, library_commands:: or workflow_state:: reference. library_item_revisions.payload_json holds V1 authoring-ir content forever. The plan's \"short-time fork\" framing understates this: it is a permanent fork for every V2 edit.",
      "foreign_keys is ON in production (db.rs:62) and OFF in the db.rs test harness (mem_conn at :1041-1045 omits the pragma). Any FK in the new v2 DDL — e.g. the plan's processing_jobs_v2 -> library_items_v2 reference — is unenforced in every unit test and enforced on users' machines.",
      "upsert_library_item deliberately INSERTs library_items with current_revision_id pointing at a revision row that does not exist yet (:867-884 before :887-896). This only works because current_revision_id has no REFERENCES clause — db.rs:117 is a comment arrow, not a constraint. Adding a real FK there, or copying that write order into a v2 table that does have FKs, breaks under foreign_keys=ON.",
      "upsert_library_item opens its own transaction via conn.unchecked_transaction() (:846) while taking only &Connection, so it cannot be called from inside an outer transaction. migrate_existing_into_library avoids this by writing only `exams` inside its tx (library_commands.rs:479) — which is also why the existing one-time migration never populated library_items at all.",
      "Two separate transient connections per save. upsert_reading_job calls upsert_exam (own connection, :104) then upsert_library_item_from_exam (a second connection, :177). exams and library_items are never written atomically, and both failures are swallowed to stderr (job_store.rs:37-42).",
      "Deleting a job hard-deletes only the exams row. job_commands.rs:149 and writing_store.rs:189 call delete_exam_by_id, leaving library_items + all revisions with deleted_at NULL — invisible to reads and absent from the trash list. The codebase already ships a workaround for the resulting state (restore_exam_from_library_item, db.rs:1001) and a test named restore_rehydrates_legacy_exam_row_deleted_by_old_flow (library_commands.rs:689).",
      "Nothing ever deletes a library_items or library_item_revisions row. The complete DELETE inventory in db.rs is two statements, both on exams (:766, :801). Every save appends a revision — including the extra save_job that a metadata edit triggers through write_back_meta_to_source (library_commands.rs:339) — so revisions grow unbounded, which is precisely what plan 4.4 wants to stop.",
      "publish/library.db is a different database with a colliding table name. export_nas_library.rs:693-726 creates its own library_meta plus reading_assets, and write_library_db (:728-737) deletes and recreates the file on every publish. Grepping for library_meta or \"library.db\" hits both DBs; only authoring_hub.db has migrations.",
      "The startup migration's return value and error are both discarded: `let _ = library_commands::migrate_existing_into_library(&root);` (lib.rs:1412). A migration that fails on a user's machine is completely silent, and because the done-flag is set inside the transaction (library_commands.rs:487), the only evidence is that it silently retries next launch.",
      "migrate_existing_into_library also runs prune_orphan_exams (library_commands.rs:483), which DELETEs any exams row whose id has no backing jobs/*/job.json or writing-jobs/*/writing-job.json. It is safe today only because migration_done_v1 makes it run once ever. Once v2-native items exist without job directories, anything that re-runs or generalizes that migration will delete their legacy mirror rows.",
      "The status vocabulary is mapped in six places: two Rust fns in db.rs (:309, :317), a third in library_commands.rs:160-168 that differs by a `_ => \"draft\"` catch-all, inline SQL CASE in both source-SQL builders (:336-338, :373-375), and the reverse direction in library_status_to_reading/library_status_to_writing (library_commands.rs:346-364). Plan 11.3's LibraryItemStatusV2 is a seventh vocabulary, incompatible with the frontend's current draft|needs_review|ready|exported contract.",
      "exams.subject and library_items.subject are both CHECKed IN ('reading','writing') (db.rs:87, :110), so plan 4.3's modality='listening' cannot be mirrored into the legacy tables — and SQLite cannot relax a CHECK via ALTER TABLE. Listening cannot ship behind the current exams-driven read path.",
      "open_connection redacts its own failure to the literal string \"open_db_failed\" (db.rs:58), logging the real path only to stderr. Debugging a schema or permissions failure from the frontend error alone is impossible; check stderr."
    ]
  },
  {
    "subsystem": "job.json as source of truth + best-effort DB double write (src-tauri/src/job_store.rs and its 28 production write callers)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "role": "The whole ProcessingJob persistence layer. make_job/load_job/save_job/update_job/list_saved_jobs. Line 35 is the ONLY place job.json is written in production; lines 30 and 66 are the only places it is read.",
        "sizeNote": "105 lines total (3.7 KB) — 1 inline test. The smallest file in the subsystem and the single chokepoint."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "role": "Tauri *_core for create_import_job / list_jobs / get_job / update_job_meta / delete_job / import_source_file. Contains the second direct save_job call (:87) and the only DB delete (:149).",
        "sizeNote": "297 lines, no tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "role": "Both directions of the double write: upsert_reading_job (:92, file -> DB) and write_back_meta_to_source (:293, DB -> file). Also the startup migration that scans jobs/*/job.json (:386).",
        "sizeNote": "~800 lines, 6 inline tests including the two that pin the double-write semantics."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "role": "SQLite layer (authoring_hub.db). SCHEMA_SQL :77-191, upsert_exam :277, upsert_library_item :841 (appends a revision row on every call), active_summary_source_sql :325 / active_detail_source_sql :362 (exams-driven COALESCE), delete_exam_by_id :772.",
        "sizeNote": "1035 lines / 53 KB, 12 inline tests. Plan 17.12 wants this split."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/util.rs",
        "role": "job_dir :33, safe_job_dir :44, ensure_job_dirs :223 (15 eager subdirs), write_json :259 (non-atomic fs::write), read_json :246.",
        "sizeNote": "389 lines, 3 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs",
        "role": "The V2 on-disk layout: JobArtifactPaths :22, ensure_job_artifact_layout :142 (13 more eager subdirs), append_revision :240 (flock + atomic + create_new), current-revision.json is the V2 state pointer.",
        "sizeNote": "~600 lines / 30 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "5 update_job sites (:234 :277 :309 :329 :379 :459 :504 — 7 counting all) plus archive_current_draft_for_source_replacement :109 which creates a job-dir tree outside ensure_job_dirs.",
        "sizeNote": "~900 lines / 23 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "The main import path. load_job :1399 :2227 :2608 and update_job :1466 :1679 :1686 :2123 :2585. A single run_auto_pipeline does 4 update_job calls.",
        "sizeNote": "116 KB — the largest caller. Plan 17.8 wants it split."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "The converged workspace save path (get_authoring_v2 :54, apply_authoring_v2_patches :123, export_authoring_v2 :449). Contains ZERO references to job_store/ImportJob/job.json/db — verified by grep.",
        "sizeNote": "97 KB, 14 inline tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/workflow_state.rs",
        "role": "Two pure status-transition helpers that both end in update_job (:21 apply_preview_e2e_job_state, :75 update_validation_job_state). The closest thing to an existing state machine.",
        "sizeNote": "~230 lines, 4 tests. Best template for a DB-backed transition module."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "role": "write_authoring_project :38 (reads job via load_job :43 and embeds job metadata into authoring-project.json), cleanup_transient_job_artifacts :99 (update_job :154 -> Cleaned), minimize_process_artifacts_after_authoring :161.",
        "sizeNote": "213 lines."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs",
        "role": "The exact same file+DB double-write pattern for the writing subject: save_writing_job :122 -> upsert_writing_job. writing-jobs/<id>/writing-job.json.",
        "sizeNote": "~230 lines. Must be converted in lockstep or the library list breaks for writing rows."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "Type definitions (JobStatus :94, WorkflowStep :115, IssueCounts :127, ImportJob :135, SourceFile :158, JobDetail :313, LibraryExamSummary :215), all 59 Tauri command shells (:850-:1400), the command registry (:1415), app setup (:1408) and 4 production job_store calls in the CLI path (:459 :569 :583 :603).",
        "sizeNote": "449 KB / ~10600 lines, 136 inline tests. mod tests starts at :1480 — everything above is production."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/library/libraryTypes.ts",
        "role": "Frontend proof that job.json is still load-bearing: deriveStage :95 and STEP_PROGRESS :67 read job.status/job.currentStep/job.issueCounts; the DB summary is only a fallback.",
        "sizeNote": "179 lines."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/library/libraryStore.ts",
        "role": "The converged library page calls listJobs() AND listLibraryExams() AND listTrashedExams() and merges by id (:55-66). Delete goes only through deleteLibraryExam (:105).",
        "sizeNote": "115 lines."
      }
    ],
    "publicApi": [
      {
        "symbol": "save_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:33",
        "signature": "pub(crate) fn save_job(root: &Path, job: &ImportJob) -> CommandResult<()> {\n    validate_path_segment(\"job_id\", &job.job_id)?;\n    write_json(&job_dir(root, &job.job_id).join(\"job.json\"), job)?;\n    // 双写题库 DB（失败记日志但不阻断主流程，文件仍是导出源）。\n    if let Err(error) = crate::library_commands::upsert_reading_job(root, job) {\n        eprintln!(\n            \"[library] upsert_reading_job failed for {}: {}\",\n            job.job_id, error\n        );\n    }\n    Ok(())\n}",
        "notes": "The entire function, verbatim. Exactly three steps: validate the id as a path segment, write jobs/<id>/job.json via util::write_json (non-atomic fs::write, creates parents), then call upsert_reading_job and swallow any error into eprintln!. The DB is never allowed to fail the call. Note the asymmetry: it uses job_dir() (infallible, falls back to \"__invalid_job_id__\") while load_job uses safe_job_dir()."
      },
      {
        "symbol": "update_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:46",
        "signature": "pub(crate) fn update_job(\n    root: &Path,\n    job_id: &str,\n    mutator: impl FnOnce(&mut ImportJob),\n) -> CommandResult<ImportJob> {\n    let mut job = load_job(root, job_id)?;\n    mutator(&mut job);\n    job.updated_at = Utc::now();\n    save_job(root, &job)?;\n    Ok(job)\n}",
        "notes": "Read-modify-write with NO locking and NO optimistic-concurrency check. 26 of the 28 production write paths go through here. It is the only place updated_at is bumped."
      },
      {
        "symbol": "load_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:28",
        "signature": "pub(crate) fn load_job(root: &Path, job_id: &str) -> CommandResult<ImportJob> {\n    let dir = safe_job_dir(root, job_id)?;\n    read_json(&dir.join(\"job.json\"))\n}",
        "notes": "Hard-fails if job.json is missing or unparseable — no Option, no default. get_job therefore fails outright for a job whose job.json is gone, which is what the workspace page depends on."
      },
      {
        "symbol": "list_saved_jobs",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:58",
        "signature": "pub(crate) fn list_saved_jobs(\n    root: &Path,\n    filter: Option<JobFilter>,\n) -> CommandResult<Vec<ImportJob>>",
        "notes": "Body scans fs::read_dir(root/\"jobs\") and read_json::<ImportJob> on every jobs/*/job.json (:64-70). One unparseable job.json fails the whole listing (the `?` at :68). Filters status/search in memory, sorts by Reverse(updated_at)."
      },
      {
        "symbol": "make_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:7",
        "signature": "pub(crate) fn make_job(input: CreateJobInput) -> ImportJob",
        "notes": "job_id format is `format!(\"import-{}-{}\", now.format(\"%Y%m%d%H%M%S\"), suffix)` where suffix is the first 8 chars of a simple UUID (:9-11). Defaults: status Working, category Some(\"P1\"), frequency Some(\"medium\"), current_step Upload, issue_counts default."
      },
      {
        "symbol": "ImportJob",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:135",
        "signature": "pub struct ImportJob {\n    #[serde(rename = \"jobId\")] pub job_id: String,\n    pub title: String,\n    pub status: JobStatus,\n    pub category: Option<String>,\n    pub frequency: Option<String>,\n    pub tags: Vec<String>,\n    #[serde(rename = \"sourceFiles\")] pub source_files: Vec<SourceFile>,\n    #[serde(rename = \"activeLlmProfileId\")] pub active_llm_profile_id: Option<String>,\n    #[serde(rename = \"createdAt\")] pub created_at: DateTime<Utc>,\n    #[serde(rename = \"updatedAt\")] pub updated_at: DateTime<Utc>,\n    #[serde(rename = \"currentStep\")] pub current_step: WorkflowStep,\n    #[serde(rename = \"issueCounts\")] pub issue_counts: IssueCounts,\n}",
        "notes": "This is the whole on-disk job.json shape. JobStatus (lib.rs:94) = Working|NeedsReview|DraftSaved|ExportReady|Exported|Cleaned with serde aliases for older names (Draft/Uploaded/Parsed/SplitReady -> Working etc.). WorkflowStep (lib.rs:115) = Upload|DocumentReview|Split|Authoring|LlmReview|Preview|Export|Pack. IssueCounts (lib.rs:127) = {errors: u32, warnings: u32, needs_review: u32}."
      },
      {
        "symbol": "upsert_reading_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:92",
        "signature": "pub(crate) fn upsert_reading_job(root: &Path, job: &ImportJob) -> CommandResult<()> {\n    let payload = match reading_payload(root, job) {\n        Ok(v) => v,\n        Err(ReadingPayloadError::ReadFailed(e)) => {\n            eprintln!(\n                \"[library] upsert_reading_job skipped for {}: authoring-ir.json read failed (DB payload preserved): {}\",\n                job.job_id, e\n            );\n            return Ok(());\n        }\n    };\n    let record = exam_record_from_reading_job(job, payload);\n    upsert_exam(root, &record)?;\n    // Phase 1：同步写正式主模型 library_items + revisions（尊重软删除：已软删除则不复活）。\n    if let Err(e) = upsert_library_item_from_exam(root, &record, \"ReadingAuthoringIRV1\") {\n        eprintln!(\n            \"[library] upsert_library_item (reading) failed for {}: {}\",\n            job.job_id, e\n        );\n    }\n    Ok(())\n}",
        "notes": "The double-write body, verbatim. THREE independent failure swallow points: (1) an unreadable authoring-ir.json returns Ok(()) with no DB write at all, (2) upsert_library_item_from_exam errors are logged only, (3) save_job logs the outer error. Only upsert_exam can propagate. schema_version is hardcoded \"ReadingAuthoringIRV1\" even when the payload is actually a serialized ImportJob."
      },
      {
        "symbol": "reading_payload",
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:71",
        "signature": "fn reading_payload(root: &Path, job: &ImportJob) -> Result<Value, ReadingPayloadError> {\n    let ir_path = crate::util::job_dir(root, &job.job_id).join(\"authoring-ir.json\");\n    if !ir_path.exists() {\n        return Ok(serde_json::to_value(job).unwrap_or(Value::Null));\n    }\n    // 文件存在：读取必须成功，否则报错而非静默回退。\n    match crate::util::read_json_opt(&ir_path) {\n        Ok(Some(ir)) => Ok(ir),\n        Ok(None) => Ok(serde_json::to_value(job).unwrap_or(Value::Null)),\n        Err(e) => Err(ReadingPayloadError::ReadFailed(e)),\n    }\n}",
        "notes": "Proves the DB payload is the V1 authoring-ir.json, or the serialized ImportJob when that file is absent. It NEVER reads the V2 revision store (authoring/revisions/*.json)."
      },
      {
        "symbol": "exam_record_from_reading_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:47",
        "signature": "pub(crate) fn exam_record_from_reading_job(job: &ImportJob, payload: Value) -> ExamRecord",
        "notes": "ExamRecord.id = job.job_id (:50). This plus LibraryItemRecord.id = exam.id (:187) is the code proof of library_items.id == job.job_id. status_from_reading (:26) collapses 6 JobStatus values into 4: Working->draft, NeedsReview->needs_review, DraftSaved|ExportReady->ready, Exported|Cleaned->exported. issue_counts.needs_review is DROPPED — only errors and warnings reach the DB."
      },
      {
        "symbol": "upsert_library_item_from_exam",
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:172",
        "signature": "fn upsert_library_item_from_exam(\n    root: &Path,\n    exam: &ExamRecord,\n    schema_version: &str,\n) -> CommandResult<()>",
        "notes": "Skips the write entirely if is_library_item_soft_deleted (:178) so a trashed item is not resurrected by a later pipeline step. to_library_item_status (:160) maps draft->draft, needs_review->review_required, ready->ready, exported->published."
      },
      {
        "symbol": "write_back_meta_to_source",
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:293",
        "signature": "fn write_back_meta_to_source(\n    root: &Path,\n    subject: &str,\n    id: &str,\n    patch: &LibraryMetaPatch,\n) -> CommandResult<()>",
        "notes": "The reverse double write named in plan M4. For reading it does load_job -> mutate -> `crate::job_store::update_job(root, id, |j| { *j = job.clone(); })?` (:339-341), i.e. a whole-struct overwrite that re-triggers the forward double write. Special case at :332: if the job is already Cleaned, a library edit of status=\"exported\" must not downgrade it."
      },
      {
        "symbol": "upsert_library_item",
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:841",
        "signature": "pub(crate) fn upsert_library_item(\n    conn: &Connection,\n    record: &LibraryItemRecord,\n) -> CommandResult<String>",
        "notes": "Appends a NEW library_item_revisions row on every call (revision_no = existing max + 1, :864; INSERT at :887-896) carrying a full copy of record.revision_payload_json, then points current_revision_id at it. There is no dedup on identical payloads. Because save_job calls this transitively, every job status tick writes another full-payload revision."
      },
      {
        "symbol": "delete_exam_by_id",
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:772",
        "signature": "pub(crate) fn delete_exam_by_id(root: &Path, id: &str) -> CommandResult<()> {\n    let conn = open_connection(root)?;\n    delete_exam(&conn, id)?;\n    Ok(())\n}",
        "notes": "delete_exam (:764) is `DELETE FROM exams WHERE id=?` only. It does NOT touch library_items or library_item_revisions, so delete_job leaves those rows orphaned."
      },
      {
        "symbol": "active_summary_source_sql",
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:325",
        "signature": "FROM exams e\n         LEFT JOIN library_items li ON li.id = e.id\n         WHERE li.deleted_at IS NULL",
        "notes": "The FROM/JOIN clause verbatim (db.rs:356-358); active_detail_source_sql (:362) is identical plus a LEFT JOIN onto library_item_revisions for payload_json (:393-397). Consequence: `exams` is the driving table, so a library_items row with no exams row never appears in list_library_exams or get_library_exam. This blocks plan 17.12's \"delete legacy exams query\" until the driver is flipped."
      },
      {
        "symbol": "LibraryExamSummary",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:215",
        "signature": "pub struct LibraryExamSummary {\n    pub id: String,\n    #[serde(rename = \"examId\")] pub exam_id: Option<String>,\n    pub title: String,\n    pub subject: String,\n    pub category: Option<String>,\n    pub frequency: Option<String>,\n    pub status: String,\n    #[serde(rename = \"taskType\")] pub task_type: Option<String>,\n    pub tags: Vec<String>,\n    #[serde(rename = \"sourceHash\")] pub source_hash: Option<String>,\n    #[serde(rename = \"issueErrors\")] pub issue_errors: u32,\n    #[serde(rename = \"issueWarnings\")] pub issue_warnings: u32,\n    #[serde(rename = \"createdAt\")] pub created_at: String,\n    #[serde(rename = \"updatedAt\")] pub updated_at: String,\n}",
        "notes": "No currentStep and no needsReview — exactly the two fields src/features/library/libraryTypes.ts needs (STEP_PROGRESS/STEP_STAGE keyed on WorkflowStep at :67/:78, actionableFrom counting errors+needsReview at :89). This is why the converged library page still has to call list_jobs."
      },
      {
        "symbol": "ensure_job_dirs",
        "file": "F:/workspace/PDF2Test/src-tauri/src/util.rs:223",
        "signature": "pub(crate) fn ensure_job_dirs(path: &Path) -> CommandResult<()>",
        "notes": "Eagerly creates 15 subdirs for every job: uploads, sources, preview, preview/runtime, exports, extraction, authoring, authoring/revisions, authoring/patches, assets, assets/blobs, assets/metadata, assets/previews, export-history, legacy (:224-240)."
      },
      {
        "symbol": "ensure_job_artifact_layout",
        "file": "F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs:142",
        "signature": "pub(crate) fn ensure_job_artifact_layout(\n    root: &Path,\n    job_id: &str,\n) -> CommandResult<JobArtifactPaths>",
        "notes": "Creates 13 of the same dirs again (:147-163). Called from create_import_job_core, apply_authoring_v2_patches, export_authoring_v2 and inspect_job_artifacts. JobArtifactPaths::for_job (:40) is the canonical path map; current_revision_path() = authoring/current-revision.json, revision_path(n) = authoring/revisions/<n>.json, revision_meta_path(n) = authoring/revisions/<n>.meta.json, patch_path(n) = authoring/patches/<n>.jsonl."
      },
      {
        "symbol": "write_json",
        "file": "F:/workspace/PDF2Test/src-tauri/src/util.rs:259",
        "signature": "pub(crate) fn write_json<T: Serialize + ?Sized>(path: &Path, value: &T) -> CommandResult<()> {\n    if let Some(parent) = path.parent() {\n        fs::create_dir_all(parent).map_err(|error| error.to_string())?;\n    }\n    let data = serde_json::to_string_pretty(value).map_err(|error| error.to_string())?;\n    fs::write(path, data).map_err(|error| format!(\"write_json:{}:{}\", path.display(), error))\n}",
        "notes": "Plain fs::write — no temp file, no rename, no fsync. This is how the authoritative job.json is persisted, while the V2 revision store next to it uses flock + create_new + atomic rename (artifact_store.rs:249-258, :306, :318)."
      },
      {
        "symbol": "create_import_job_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs:77",
        "signature": "pub(crate) async fn create_import_job_core(\n    input: CreateJobInput,\n    app: AppHandle,\n) -> CommandResult<ImportJob> {\n    let root = app_root(&app)?;\n    ensure_app_dirs(&root)?;\n    let job = make_job(input);\n    let dir = job_dir(&root, &job.job_id);\n    ensure_job_dirs(&dir)?;\n    ensure_job_artifact_layout(&root, &job.job_id)?;\n    save_job(&root, &job)?;\n    Ok(job)\n}",
        "notes": "Verbatim. The save_job here already creates the exams row AND the library_items row + revision 1 (payload = serialized ImportJob, since authoring-ir.json does not exist yet). So a DB job row for the item already exists at creation time — plan 17.14's \"source import creates a DB job\" is partly already true."
      },
      {
        "symbol": "delete_job_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs:142",
        "signature": "pub(crate) async fn delete_job_core(job_id: String, app: AppHandle) -> CommandResult<()> {\n    let root = app_root(&app)?;\n    let dir = job_dir(&root, &job_id);\n    if dir.exists() {\n        fs::remove_dir_all(dir).map_err(|error| error.to_string())?;\n    }\n    // 同步删除题库 DB 中的记录（失败记日志但不阻断文件删除——文件已删，DB 孤儿可被迁移/重试清理）。\n    if let Err(error) = crate::db::delete_exam_by_id(&root, &job_id) {\n        eprintln!(\n            \"[library] delete_exam_by_id failed for {}: {}\",\n            job_id, error\n        );\n    }\n    Ok(())\n}",
        "notes": "Verbatim. Files first, DB best-effort second, and library_items is never touched. Only reachable from the legacy src/pages/JobList.tsx — the converged UI calls delete_library_exam instead."
      },
      {
        "symbol": "get_job_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs:100",
        "signature": "pub(crate) async fn get_job_core(job_id: String, app: AppHandle) -> CommandResult<JobDetail>",
        "notes": "Assembles JobDetail from load_job plus read_json_opt of 7 job-dir files: document-ir.json, split-candidates.json, authoring-ir.json, validation-report.json, preview/preview-assets.json, pipeline-report.json, vision-answer-candidates.json, plus source_review_status_for_job and load_llm_suggestions (:104-113). Called by src/features/editor/ExamWorkspacePage.tsx:37, so the converged workspace still needs job.json to exist."
      },
      {
        "symbol": "apply_authoring_v2_patches_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:123",
        "signature": "pub(crate) fn apply_authoring_v2_patches_core(root: &Path, input: Value) -> CommandResult<Value>",
        "notes": "The converged workspace save path. Sequence: load_current_authoring -> revision_conflict check against input.base_revision -> apply_patch loop -> mark_user_audit -> refresh_quality_report -> validate_authoring -> append_revision(RevisionSourceV2::User). It never calls save_job/update_job and never opens the DB (grep for job_store|update_job|save_job|ImportJob|job.json|db:: over this 97 KB file returns nothing). A user fixing every blocker in the workspace does not change ImportJob.status, exams.status or library_items.status."
      },
      {
        "symbol": "save_writing_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs:122",
        "signature": "pub(crate) fn save_writing_job(root: &Path, job: &WritingJob) -> CommandResult<()> {\n    validate_path_segment(\"writing_job_id\", &job.job_id)?;\n    write_json(\n        &writing_job_dir(root, &job.job_id).join(\"writing-job.json\"),\n        job,\n    )?;\n    // 双写题库 DB（失败记日志但不阻断主流程）。\n    if let Err(error) = crate::library_commands::upsert_writing_job(root, job) {\n        eprintln!(\n            \"[library] upsert_writing_job failed for {}: {}\",\n            job.job_id, error\n        );\n    }\n    Ok(())\n}",
        "notes": "Structurally identical twin of save_job for writing-jobs/<id>/writing-job.json. Same update_writing_job (:138) and list_writing_jobs (:150) dir-scan shape. Must be migrated in lockstep because both feed the same exams/library_items tables."
      }
    ],
    "dataFlow": "On-disk root is `app.path().app_data_dir()` (lib.rs:357). ensure_app_dirs (util.rs:63) creates config, config/secrets, jobs, writing-jobs, logs, cache, cache/parser, cache/thumbnails, cache/preview-server. The SQLite file is a sibling: `<root>/authoring_hub.db` (db.rs:24/:27), renamed from a legacy `library.db` on first open (db.rs:33).\n\nJOB DIRECTORY LAYOUT, `<root>/jobs/<job_id>/`. Directories (28 create_dir_all calls across two functions, 15 unique from util::ensure_job_dirs:224 and 13 overlapping from artifact_store::ensure_job_artifact_layout:147): uploads/, sources/, extraction/, preview/, preview/runtime/, exports/, export-history/, legacy/, assets/{blobs,metadata,previews}/, authoring/, authoring/revisions/, authoring/patches/. Two more appear lazily: `debug/` (pdf_facts_shadow.rs:2324) and `revisions/<rfc3339>-source-replaced/` (authoring_commands.rs:113-122, a sibling of `authoring/revisions`, not created by ensure_job_dirs).\n\nFiles at the job-dir root: job.json (the ImportJob), authoring-ir.json (V1 content), authoring-project.json (cleanup.rs:72), document-ir.json, split-candidates.json, validation-report.json, publish-readiness-report.json, pipeline-report.json, pipeline-report-summary.json, source-review.json (source_review.rs:94), cleanup-summary.json, llm-last-suggestion.json, llm-calls.jsonl, manual-transcription.txt, vision-transcription.txt, vision-transcription-output.json, vision-answer-output.json, vision-answer-candidates.json, and the V2 shadows document-ir-v2.shadow{,.error,.compare}.json + document-ir-v2.shadow.overlay.svg (pdf_facts_shadow.rs:19-22) and authoring-ir-v2.shadow{,.compare,.error}.json (ielts_grammar/mod.rs:56-58). Under authoring/: current-revision.json (the V2 revision pointer), revision.lock, revisions/<n>.json, revisions/<n>.meta.json, patches/<n>.jsonl. Under uploads/: `<sha256[..8]>-<sanitized-name>` staged via a `.staging-<uuid>-<name>` temp then renamed (job_commands.rs:174-192). Under preview/: preview-assets.json. Under export-history/: phase5-export.lock. Under llm-suggestions/: per-suggestion files (llm_suggestions.rs:283/:300).\n\nFORWARD WRITE (file -> DB), the only shape that exists: some *_core calls `update_job(root, job_id, |job| {...})` -> load_job reads job.json -> mutator -> `job.updated_at = Utc::now()` -> save_job -> `write_json(jobs/<id>/job.json)` (plain fs::write, non-atomic) -> `library_commands::upsert_reading_job` -> `reading_payload` reads authoring-ir.json (or serializes the ImportJob if that file is absent) -> `exam_record_from_reading_job` flattens ImportJob into ExamRecord with id = job_id -> `db::upsert_exam` INSERT..ON CONFLICT into `exams` -> `upsert_library_item_from_exam` -> skip if soft-deleted, else `db::upsert_library_item` which in one transaction upserts `library_items` AND inserts a brand-new `library_item_revisions` row (revision_no = max+1) holding a full copy of the payload, then repoints current_revision_id. Every failure after the file write is swallowed to stderr.\n\nREVERSE WRITE (DB -> file), one path only: `update_library_exam_meta` -> `update_library_exam_meta_core` (library_commands.rs:227) -> `db::update_exam_meta` UPDATEs `exams` then `update_library_item_meta` UPDATEs `library_items` (db.rs:692) -> `write_back_meta_to_source` (:293) -> load_job, apply title/category/frequency/tags/status, then `update_job(root, id, |j| { *j = job.clone(); })` which re-enters the forward write. Failure is logged, DB is not rolled back.\n\nREAD PATHS. job.json is read in exactly three literal places: job_store.rs:30 (load_job), job_store.rs:66 (list_saved_jobs full dir scan), library_commands.rs:407-411 (startup migration, direct read_json). Production load_job callers: job_commands.rs:104, authoring_commands.rs:159/:257/:291/:371/:400, auto_pipeline.rs:1399/:2227/:2608, cleanup.rs:43, llm_commands.rs:221, library_commands.rs:316, pdf_facts_shadow.rs:2315, lib.rs:603, plus update_job's own internal load at job_store.rs:51. The library list is read from the DB via `list_exams` (db.rs:510) but driven `FROM exams e LEFT JOIN library_items li` (:356-357).\n\nSTARTUP. lib.rs:1408-1413 setup: app_root -> ensure_app_dirs -> `let _ = library_commands::migrate_existing_into_library(&root)` (return value discarded). That migration (library_commands.rs:386) is gated on `library_meta.migration_done_v1`, scans jobs/*/job.json and writing-jobs/*/writing-job.json, builds ExamRecords outside the transaction, then in one transaction upsert_exam_conn each, `prune_orphan_exams` deletes `exams` rows whose id has no live job.json, and sets migration_done_v1. It only writes `exams`, never `library_items`.\n\nCONCURRENCY. run_auto_pipeline spawns a cloud worker in a thread::scope (auto_pipeline.rs:1420-1435); that worker does NOT touch job state (run_cloud_conversion_worker, :1226-1266, only sends over channels). Every DB touch opens a fresh transient WAL connection (db.rs:51, documented at db.rs:5-7) so save_job needs only `root: &Path`. There is no lock or version check around job.json — two concurrent update_job calls silently lose one mutation.",
    "gapsVsPlan": [
      {
        "claim": "11.1: `save_job()` 先写文件，再 best-effort 双写 DB",
        "reality": "Exactly right, and the swallowing is deeper than described: there are three independent failure-swallow points (unreadable authoring-ir.json returns Ok(()) with no DB write at all; upsert_library_item errors are eprintln-only; save_job's own call is eprintln-only). Only db::upsert_exam can propagate an error, and even that is caught by save_job.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/job_store.rs:33-44, src-tauri/src/library_commands.rs:92-113"
      },
      {
        "claim": "11.1: 元数据又先写 DB，再 best-effort 回写 JSON",
        "reality": "Confirmed verbatim. update_library_exam_meta_core writes exams + library_items first, then calls write_back_meta_to_source, whose failure is logged and never rolls back the DB.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/library_commands.rs:227-252, :293-344"
      },
      {
        "claim": "11.1: `library_items + revisions` is the \"新 DB 内容\" source, distinct from `exams.payload_json` (legacy) and `authoring/revisions/*` (V2)",
        "reality": "library_items_revisions.payload_json is a byte-for-byte copy of exams.payload_json (library_commands.rs:200 sets revision_payload_json = exam.payload_json), and both come from the V1 authoring-ir.json — or from the serialized ImportJob when that file does not exist. Neither table ever receives the V2 revision content. There are three content stores, not four, and the DB holds only V1.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/library_commands.rs:71-82, :103-106, :200; src-tauri/src/db.rs:887-896"
      },
      {
        "claim": "11.1: 任何一步失败都可能形成短时或永久分叉",
        "reality": "Confirmed, plus two divergence sources the plan does not name. (a) The workspace's own save path never writes job state or the DB at all, so the library row's status is permanently stale relative to the user's edits. (b) The two delete commands are asymmetric: delete_job removes files + the exams row but orphans library_items/library_item_revisions, while delete_library_exam soft-deletes only library_items and leaves both the files and the exams row.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:123-168 (no db/job_store refs anywhere in the file); src-tauri/src/job_commands.rs:142-156; src-tauri/src/db.rs:764-776; src-tauri/src/library_commands.rs:254-263"
      },
      {
        "claim": "11.2 M1: 建立 `library_items_v2`、`processing_jobs_v2`、`actionable_issues_v1`、`library_item_recovery_v1`",
        "reality": "None of those four tables or names exist. But an unused `ingest_jobs` table already exists with almost exactly the ProcessingJob shape the plan wants: id, kind, subject, status CHECK IN ('created','importing','parsing','review_required','authoring','ready_to_publish','published','archived','failed'), current_step, source_asset_id, linked_library_item_id, issue_counts_json, created_at, updated_at, plus idx_ingest_jobs_status. It is created by SCHEMA_SQL and never read or written by any Rust code (nor are source_assets, publish_records, job_artifact_index).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/db.rs:143-190; grep for ingest_jobs|source_assets|publish_records|job_artifact_index over src-tauri/src returns only db.rs comments and the DDL"
      },
      {
        "claim": "11.2 M4: 删除 `save_job -> upsert_reading_job` 和 `library meta -> write_back_meta_to_source`",
        "reality": "Both call sites exist exactly as named and are one line each to remove (job_store.rs:37 and library_commands.rs:245). The hard part is not the deletion — it is that 26 update_job sites and the whole library list currently depend on the file side being authoritative.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/job_store.rs:37, src-tauri/src/library_commands.rs:245"
      },
      {
        "claim": "11.2 M4 / 17.12: 删除 legacy `exams` 查询",
        "reality": "Blocked as stated. Every production library read is driven `FROM exams e LEFT JOIN library_items li` — list_exams (db.rs:515), get_exam (:554), get_exam_summary (:699), search_exams, get_stats. A library_items row without a matching exams row is invisible to all of them. The exams table cannot be dropped from the query path until the driving table is flipped to library_items; only list_trashed_items (:1013) already reads library_items directly.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/db.rs:325-360, :362-399, :510-550, :552-576, :697-712, :1013-1031"
      },
      {
        "claim": "17.14: `job_store.rs` 替换为 `processing/repository.rs`",
        "reality": "Not started. There is no processing/ directory, no repository.rs, and no LibraryRepository/LibraryRepositoryV2 symbol anywhere in src-tauri/src. job_store.rs is still the only persistence layer for ProcessingJob.",
        "verdict": "wrong",
        "evidence": "grep for list_library_items|processing/repository|LibraryRepository|processing_jobs_v2|library_items_v2 over src-tauri/src returns nothing; ls src-tauri/src shows no processing/ dir"
      },
      {
        "claim": "17.14: source import 创建 DB job",
        "reality": "Already true for the library tables, and richer than the plan assumes: create_import_job_core calls save_job, which creates the exams row AND the library_items row + revision_no 1 before any source file is attached. import_source_file then update_jobs again, creating revision 2. What is missing is a row in the (unused) ingest_jobs table and any notion of the job living only there.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/job_commands.rs:77-89 -> job_store.rs:33-44 -> library_commands.rs:92-113 -> db.rs:841-900"
      },
      {
        "claim": "17.14: `list_saved_jobs` 被 `list_library_items` 取代",
        "reality": "No function named list_library_items exists; the DB lister is db::list_exams behind library_commands::list_library_exams_core. More importantly the replacement cannot happen as a rename: LibraryExamSummary has no currentStep and no needsReview, which are precisely the fields the converged library page uses for stage, progress and actionable count. The frontend therefore still calls listJobs() alongside listLibraryExams() and merges by id.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/lib.rs:215-237; src-tauri/src/db.rs:510; src/features/library/libraryStore.ts:55-66; src/features/library/libraryTypes.ts:67-124, :151"
      },
      {
        "claim": "17.14: `job.json` 不再是权威状态",
        "reality": "Not started, and job.json is currently more load-bearing than the plan implies: it is written non-atomically with a plain fs::write while the V2 revision store sitting one directory below it uses flock + create_new + atomic rename. The authoritative state file has strictly weaker durability than the shadow content it governs. list_saved_jobs also hard-fails the entire library listing on a single unparseable job.json.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/util.rs:259-265 vs src-tauri/src/artifact_store.rs:249-258, :306, :318-321; src-tauri/src/job_store.rs:64-70"
      },
      {
        "claim": "11.4: 不再为每个题目保留 preview/legacy/export-history/patches/revisions 的深层目录树",
        "reality": "Confirmed as the current state and slightly worse: those directories are created eagerly for every job by two separate functions (15 dirs in util::ensure_job_dirs, 13 overlapping in artifact_store::ensure_job_artifact_layout), both called from create_import_job_core. A third revision tree, jobs/<id>/revisions/<timestamp>-source-replaced/, is created lazily by archive_current_draft_for_source_replacement and is not the same directory as authoring/revisions.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/util.rs:223-244; src-tauri/src/artifact_store.rs:142-165; src-tauri/src/job_commands.rs:84-86; src-tauri/src/authoring_commands.rs:109-152"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "save_job (line 33)",
        "whatToChange": "The single chokepoint. Either (a) delete the write_json line and keep the function as a DB-only writer, or (b) keep it as a thin adapter over the new repository during migration. Deleting line 37 (upsert_reading_job) alone is what plan M4 asks for, but doing that first makes the DB stop tracking status entirely — the DB write must be promoted to primary in the same change. Note the file write is currently non-atomic (util::write_json -> fs::write) and there is no lock, so the new repository can only improve durability.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "update_job (line 46)",
        "whatToChange": "Read-modify-write with no lock and no version check; 26 of 28 production write paths funnel through it. Replace with a repository method that takes an expected version (mirroring the revision_conflict pattern already used at authoring_v2_commands.rs:135 and artifact_store.rs:261). This is the highest-leverage single edit: keeping the `update_job(root, job_id, |job| {...})` signature shape lets all 26 callers compile unchanged while the body moves to SQL.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "list_saved_jobs (line 58)",
        "whatToChange": "Replace the fs::read_dir scan of jobs/*/job.json with a DB query. Two behaviours must be preserved or deliberately changed: the `?` at line 68 currently fails the whole listing on one bad job.json, and the sort is Reverse(updated_at). The plan calls the replacement `list_library_items`, which does not exist — the nearest existing function is db::list_exams (db.rs:510), which cannot serve this today (see the LibraryExamSummary change point).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "load_job (line 28)",
        "whatToChange": "Repoint at the DB. It hard-fails on a missing job.json (no Option), and get_job depends on that, so the DB read must return a full ImportJob including source_files, current_step and issue_counts — none of which exist in the current library tables. During migration, keep a file fallback for jobs created before the cutover.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "upsert_reading_job (line 92) + reading_payload (line 71) + exam_record_from_reading_job (line 47) + upsert_library_item_from_exam (line 172)",
        "whatToChange": "The whole forward double write. Delete once the DB is primary. Three behaviours must be re-homed first: status_from_reading (:26) collapses 6 JobStatus values to 4 and DROPS issue_counts.needs_review; the soft-delete guard (:178) prevents a later pipeline step from resurrecting a trashed item; and reading_payload's absent-file branch (:73-75) writes the serialized ImportJob as the exam payload while still labelling it schema_version \"ReadingAuthoringIRV1\" (:106).",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "write_back_meta_to_source (line 293), called from update_library_exam_meta_core (line 245)",
        "whatToChange": "Delete the reverse write (plan M4). Preserve the Cleaned-preservation rule at :332-336 — a library edit of status=\"exported\" must not downgrade an already-Cleaned job — by moving it into the DB status transition. Also note the reading branch does a whole-struct overwrite `*j = job.clone()` (:340), which clobbers any concurrent mutation between its load_job (:316) and the update_job (:339).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "upsert_library_item (line 841)",
        "whatToChange": "Stop appending a revision per save. It INSERTs a new library_item_revisions row on every call (revision_no = max+1, :864, INSERT :887-896) with a full copy of the payload. Because save_job reaches this, a single run_auto_pipeline (4 update_job calls) writes 4 full-payload revision rows, and every export writes 2 more. Split metadata upsert from content revision, and add a payload-hash dedup.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "active_summary_source_sql (line 325) and active_detail_source_sql (line 362)",
        "whatToChange": "Flip the driving table from `exams` to `library_items`. Today both are `FROM exams e LEFT JOIN library_items li ... WHERE li.deleted_at IS NULL` (:356-358, :394-397), so any item with no exams row is invisible to list_exams, get_exam, get_exam_summary, search_exams and get_stats. This is the blocker for plan 17.12's \"delete the COALESCE mixing\" and for making the DB standalone.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "ingest_jobs table (SCHEMA_SQL line 143)",
        "whatToChange": "Use it, or replace it deliberately. It already declares the ProcessingJob shape the plan wants (status CHECK with 9 states, current_step, issue_counts_json, linked_library_item_id, source_asset_id) and has zero readers and zero writers. Deciding between extending this table and adding processing_jobs_v2 should happen before any migration is written, or the schema will carry two dead job tables.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "LibraryExamSummary (line 215)",
        "whatToChange": "Add currentStep (WorkflowStep) and a needsReview count, or introduce the V2 row type. Without these the DB literally cannot answer what the converged library page asks: src/features/library/libraryTypes.ts keys STEP_PROGRESS (:67) and STEP_STAGE (:78) on WorkflowStep and computes actionableCount as errors + needsReview (:89). This is the precondition for dropping the listJobs() call in libraryStore.ts:56.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "symbol": "create_import_job_core (line 77)",
        "whatToChange": "Make the DB row the creation act. Today it does make_job -> ensure_job_dirs -> ensure_job_artifact_layout -> save_job, so the id is minted in Rust (make_job:11, `import-<ts>-<uuid8>`) and the DB rows appear only as a side effect of save_job. Also drop the 28 eager create_dir_all calls per job (plan 11.4) — the tmp workspace should be created on demand.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "symbol": "import_source_file_core (line 158, update_job at 203)",
        "whatToChange": "source_files currently lives only inside job.json. The DB has an unused source_assets table (db.rs:158) and library_items.source_asset_id, but nothing writes them; ExamRecord carries only source_hash = first source file's sha256 (library_commands.rs:48). Moving SourceFile into the DB is a schema addition, not a repoint. The staging protocol at :174-192 (.staging-<uuid> then rename, dedup on existing final_path) is worth preserving as-is.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "symbol": "update_job_meta_core (line 117, update_job at 123)",
        "whatToChange": "Once metadata lives in the DB this command and update_library_exam_meta become the same operation applied to the same row. Converge them; JobMetaPatch (lib.rs:193) and LibraryMetaPatch differ in fields (JobMetaPatch has activeLlmProfileId, LibraryMetaPatch has status/task_type).",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "symbol": "get_job_core (line 100)",
        "whatToChange": "Split it. The `job:` field must come from the DB while the 7 read_json_opt sidecars (document-ir.json, split-candidates.json, authoring-ir.json, validation-report.json, preview/preview-assets.json, pipeline-report.json, vision-answer-candidates.json) stay as disposable workspace reads. src/features/editor/ExamWorkspacePage.tsx:37 calls this, so it must keep working through the whole migration.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "symbol": "delete_job_core (line 142)",
        "whatToChange": "Fix the asymmetry before or during the migration. It hard-deletes the job dir and the exams row but never touches library_items or library_item_revisions, leaving orphans that prune_orphan_exams (db.rs:781) does not clean because it only prunes exams. Meanwhile delete_library_exam (library_commands.rs:254), the one the converged UI actually calls, soft-deletes library_items and leaves the files and the exams row forever. A single DB-backed delete + tmp-dir reclaim replaces both.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "apply_authoring_v2_patches_core (line 123)",
        "whatToChange": "This path must START writing job/library state — today it writes neither. It is the converged workspace's only save, and it has zero references to job_store, ImportJob, job.json or db (verified by grep over the whole 97 KB file). After append_revision succeeds it should transition the DB item's status/issue counts from the freshly computed quality report (refresh_quality_report, :146/:744) so the library row reflects the user's edits. Same for export_authoring_v2_core (:449).",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "update_job at 1466, 1679, 1686, 2123, 2585; load_job at 1399, 2227, 2608",
        "whatToChange": "The main import path, 5 write sites. :1466 sets DocumentReview + needs_review from the source review; :1679 Working/Split; :1686 DraftSaved/Authoring; :2123 the terminal next_status/next_step + a 6-term needs_review sum (:2126-2131) + error/warning counts recomputed from the validation report; :2585 the same for run_cloud_review, immediately followed by a full `job = load_job(...)` re-read at :2608. All of these are the natural progress events the plan wants emitted from a DB-backed queue instead. The job clone handed to the cloud worker at :1423 must stay a snapshot — the worker itself writes no job state.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "symbol": "update_job at 234, 277, 309, 329, 379, 459, 504",
        "whatToChange": "7 write sites, one per V1 authoring command (parse_document, apply_manual_transcription, apply_vision_transcription, resolve_source_review, run_rule_split, build_authoring_ir, update_authoring_ir). Each computes status + current_step + issue_counts inline from a freshly read source-review/authoring-review. Two extra concerns: the mutator at :234-248 calls source_review_status() *inside* the closure (so a filesystem read happens while the job struct is held), and archive_current_draft_for_source_replacement (:109) moves 6 files into jobs/<id>/revisions/<ts>-source-replaced/, a directory outside both ensure_job_dirs and authoring/revisions.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "update_job at 252, 441, 734; load_job at 221",
        "whatToChange": "3 write sites: llm_run_group_core (:252, status from a 0.85 confidence threshold, step LlmReview), apply_llm_suggestion_core (:441) and apply_vision_answer_candidates_core (:734), the latter two both computing needs_review = authoring_review + source_review. Same repoint as the authoring_commands sites.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/workflow_state.rs",
        "symbol": "apply_preview_e2e_job_state (line 6, update_job at 21) and update_validation_job_state (line 59, update_job at 75)",
        "whatToChange": "Best template for the new DB transition module: validation_job_state (:46) is already a pure (bool, u32) -> (JobStatus, WorkflowStep) function with no IO. Keep that purity and give it a DB-writing wrapper. Only 2 write sites here, and count_issues (:85) is the shared severity counter used by the auto_pipeline sites too.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/preview_commands.rs",
        "symbol": "update_job at 45 and 60 (generate_preview_assets_core, line 37)",
        "whatToChange": "Two write sites in one function: the validation-failure branch (:45, NeedsReview/Authoring, then returns Err) and the success branch (:60, status from review_issues + humanVerified, step Preview). The failure branch writes state and then errors out, which a transactional DB write must reproduce deliberately rather than roll back.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "cleanup_transient_job_artifacts (line 99, update_job at 154), write_authoring_project (line 38, load_job at 43), minimize_process_artifacts_after_authoring (line 161)",
        "whatToChange": "Two couplings. (1) update_job at :154 sets Cleaned/Export — the terminal state that write_back_meta_to_source has a special case for. (2) write_authoring_project embeds jobId/title/category/frequency/tags/status/currentStep/createdAt/updatedAt into authoring-project.json (:50-60) and is called from both cleanup entry points, so the job record leaks into a fourth on-disk artifact. Both cleanup functions declare job.json in their `kept` lists (:148, :208), which must be updated. This module is also the seed for plan 11.5's cleanup_on_startup/cleanup_after_canonical_commit — neither exists today; there is no startup or crash-recovery cleanup at all.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/export_pack.rs",
        "symbol": "update_job at 180 (export_reading_assets_with_options_core, line 130) and 277 (export_reading_js_core, line 197)",
        "whatToChange": "Both set Exported/Export and are immediately followed by cleanup_transient_job_artifacts, which writes Cleaned — two full file+DB round trips per export. In the DB model this is one publish transition. The :277 site is inside a per-job loop, so a batch of N jobs currently does 2N writes.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/export_nas_library.rs",
        "symbol": "update_job at 1512 (inside export_nas_library_core, line 1446)",
        "whatToChange": "Per-written-job Exported/Export write followed by cleanup_transient_job_artifacts in the same loop (:1512-1530). Note publish_nas_package_v2 (nas_package_v2.rs) does NOT write job state at all — grep for job_store over that file returns nothing — so the V1 and V2 publishers already disagree about whether publishing changes job status. Converge them.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "migrate_existing_into_library (line 386), called from lib.rs:1412",
        "whatToChange": "This is the existing (and only) file->DB migration, and it is the natural place to hang plan 11.2 M2. Current gaps to fix: it writes only `exams`, never library_items (so migrated items have no revision rows); it is gated on a single boolean `migration_done_v1` rather than a migration_version (plan wants idempotence with a version marker and no clobbering of later user edits); prune_orphan_exams (db.rs:781) prunes only exams; and its result is discarded with `let _ =` at lib.rs:1412 so a failed migration is invisible.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs",
        "symbol": "save_writing_job (line 122), update_writing_job (line 138), list_writing_jobs (line 150), load_writing_job (line 117)",
        "whatToChange": "The identical twin double write for writing-jobs/<id>/writing-job.json -> upsert_writing_job -> the same exams/library_items tables. It must be migrated in lockstep: the library list merges reading and writing rows through the same LibraryExamSummary, so converting only the reading side leaves the DB half-authoritative.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "CLI path: make_job at 459 and 569, save_job at 583, load_job at 603",
        "whatToChange": "generate_reading_source_from_path (:439) builds an ImportJob purely in memory and never persists it (no save_job) — it works today only because the pipeline functions take &ImportJob. run_auto_pipeline_from_path (:502) does persist (save_job :583, load_job :603) into a temp or --app-root root. Both are reachable from main.rs via run_cli_or_app -> run_cli (dispatch at :725-736). If ImportJob stops being constructible without a DB, both CLI entry points break, and they are what the repo uses for headless PDF regression runs.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "debug_document_ir_v2_overlay_core (line 2314, load_job at 2315)",
        "whatToChange": "The load_job call at :2315 is a bare existence guard — its result is discarded (`load_job(root, job_id)?;`). Replace with a DB existence check, or drop it in favour of safe_job_dir at :2316.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/util.rs",
        "symbol": "write_json (line 259), ensure_job_dirs (line 223)",
        "whatToChange": "write_json is a plain fs::write with no temp+rename and no fsync; it is what persists the authoritative job.json. Plan 17.15 wants an atomic helper in util/atomic.rs — artifact_store.rs already has write_canonical_json_atomic (:172) and write_bytes_atomic, so the extraction target already exists. ensure_job_dirs' 15 eager dirs plus ensure_job_artifact_layout's 13 (28 create_dir_all per job) are what plan 11.4 wants replaced by on-demand tmp/jobs/<job-id>/{input,render,local,cloud,reconcile}.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/features/library/libraryStore.ts",
        "symbol": "load (line 54) and mergeRows (line 13); libraryTypes.ts deriveStage (line 95), STEP_PROGRESS (line 67), buildRow (line 137)",
        "whatToChange": "The frontend half of the double source. libraryStore merges listJobs() + listLibraryExams() + listTrashedExams() by id and libraryTypes derives stage/progress/actionableCount from the job first and the DB summary only as fallback. Once the backend row carries currentStep + needsReview, delete the listJobs() call (:56), drop the ImportJob branch of deriveStage (:97-111) and the `job?.` preferences in buildRow (:147, :151, :153, :154). The 2s poll at :11/:95 is the placeholder the plan wants replaced by a backend event.",
        "risk": "medium"
      }
    ],
    "testHarness": "There is no `src-tauri/tests/` directory — every test is an inline `#[cfg(test)] mod tests`. Test counts by file: lib.rs 136, pdf_facts_shadow.rs 20, authoring_v2_commands.rs 14, db.rs 12, library_commands.rs 6, workflow_state.rs 4, auto_pipeline.rs 4, job_store.rs 1. In lib.rs, `mod tests` starts at line 1480, so every job_store reference at or above 1479 is production and everything below is a test.\n\nHelpers to copy (all in lib.rs mod tests): `fn temp_test_root() -> PathBuf { env::temp_dir().join(format!(\"epic8-test-{}\", Uuid::new_v4().simple())) }` (:1576-1578), `fn test_job() -> ImportJob` wrapping make_job with a P1/medium fixture (:1540-1548), `fn test_source(file_type: &str) -> SourceFile` (:1550-1560), `fn parser_fixture(name: &str)` resolving `CARGO_MANIFEST_DIR/../fixtures/parser/<name>` (:1580-1586). Tests do not clean up via a Drop guard; they call `let _ = fs::remove_dir_all(root);` at the end (e.g. job_store.rs:103).\n\nThe canonical state assertion to copy is `assert_eq!(load_job(&root, &job.job_id).unwrap().status, JobStatus::Cleaned);` (lib.rs:5013-5016). It appears in this exact shape at lib.rs:5014, :7319, :7593, :7632, :7678, :7682, :7747, :8114, and as `let saved = load_job(&root, &job.job_id).unwrap();` at :4350, :4667, :4733, :4834, :4894, :5096, :5135, :10196. workflow_state.rs has 6 more at :162, :169, :174, :198, :213, :218. Those ~20 assertions are the ones a DB-backed store must re-point; they are the de facto contract for what status/currentStep/issueCounts each command produces.\n\nNamed tests that pin this subsystem specifically:\n- `job_store.rs:92 legacy_job_load_does_not_require_phase1_directories` — save_job then load_job on a root where ensure_job_dirs was never called, asserting `!job_dir(&root, &job.job_id).join(\"authoring\").exists()`. Proves write_json's create_dir_all is the only directory requirement; a DB store must keep this \"no eager layout\" property.\n- `library_commands.rs:501 make_reading_appdata()` — builds a temp appData with jobs/import-test-1/{job.json,authoring-ir.json} by calling util::write_json directly (:520-521). Several tests write job.json bypassing save_job (:717, :722), which is exactly the pattern that will need a repository seeding helper.\n- `library_commands.rs:615-621` — asserts `\"job.json title must be synced from library edit\"` after update_library_exam_meta_core. This is the test that will fail first, and by design, when write_back_meta_to_source is deleted.\n- `library_commands.rs:624-625` and `:673` — \"模拟「后续任务保存」：直接调 save_job（不经题库），双写应保留新 title 而非覆盖回旧值\", i.e. a test asserting the double write does not clobber a library edit.\n- `library_commands.rs:780` — resets migration_done_v1 and re-runs the migration to assert prune_orphan_exams drops rows with no job.json. Directly encodes \"job.json is the liveness oracle\" and must be rewritten.\n- `workflow_state.rs:95-127` — imports `job_store::{load_job, make_job, save_job}`, makes a job, save_job, then calls the transition helpers and re-reads with load_job. The tightest existing round-trip template.\n- `auto_pipeline.rs` tests use `run_auto_pipeline_core_with_gateway` (auto_pipeline.rs:1381) with an injected `FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>` gateway — the seam to reuse for testing pipeline state transitions without network. `run_cloud_review_core_with_gateway` (:2217) is the same shape.\n- db.rs's 12 tests operate on a `Connection` directly (open_connection on a temp root), so repository-level tests can follow that pattern rather than going through *_core.\n\nNothing outside Rust reads job.json — grep for `job.json` across src/, scripts/ and tools/ in .ts/.tsx/.js/.mjs/.cjs/.py returns nothing — so the migration has no sidecar or script consumers to coordinate with, only the Tauri command surface.",
    "surprises": [
      "The converged workspace's save path writes neither job state nor the DB. `apply_authoring_v2_patches_core` (authoring_v2_commands.rs:123-168) does load_current_authoring -> conflict check -> patches -> refresh_quality_report -> validate -> append_revision, and stops. A grep for job_store|update_job|save_job|ImportJob|job.json|db:: over the whole 97 KB file returns nothing. So a user who opens the workspace and resolves every blocker leaves ImportJob.status, exams.status and library_items.status untouched, and the library row keeps showing the stale stage. `export_authoring_v2_core` (:449) and `publish_nas_package_v2` (nas_package_v2.rs, also zero job_store refs) have the same hole — while the V1 publishers (export_pack.rs:180/:277, export_nas_library.rs:1512) do write Exported. The V1 and V2 publish paths already disagree.",
      "An `ingest_jobs` table with almost exactly the plan's ProcessingJob shape already exists and is completely dead. db.rs:143-155 declares id, kind, subject, status CHECK IN ('created','importing','parsing','review_required','authoring','ready_to_publish','published','archived','failed'), current_step, source_asset_id, linked_library_item_id, issue_counts_json, created_at, updated_at, plus idx_ingest_jobs_status. Nothing reads or writes it. Same for source_assets (:158), publish_records (:170) and job_artifact_index (:184). Plan 11.2 M1 proposes creating processing_jobs_v2 from scratch without acknowledging these four empty tables.",
      "Every job status tick writes a full copy of the authoring IR into the DB. save_job -> upsert_reading_job -> upsert_library_item unconditionally INSERTs a new library_item_revisions row (revision_no = max+1, db.rs:864, INSERT :887-896) whose payload_json is the entire authoring-ir.json. One run_auto_pipeline does 4 update_job calls (auto_pipeline.rs:1466, :1679, :1686, :2123); one export does 2 (Exported then Cleaned). There is no payload dedup, so the revisions table grows by a full document per state transition, and none of those revisions correspond to a user edit.",
      "The library query is driven by the legacy table, not the new one. active_summary_source_sql and active_detail_source_sql are both `FROM exams e LEFT JOIN library_items li ON li.id = e.id WHERE li.deleted_at IS NULL` (db.rs:356-358, :394-397). A library_items row with no matching exams row is invisible to list_exams, get_exam, get_exam_summary, search_exams and get_stats. `exams` is therefore load-bearing today, which directly contradicts plan 17.12's \"delete production queries against legacy exams\" — that deletion has to be preceded by flipping the driver.",
      "The two delete paths are asymmetric and neither is complete. delete_job (job_commands.rs:142-156) removes the job directory and the `exams` row but never touches library_items or library_item_revisions, leaving orphans that prune_orphan_exams (db.rs:781) cannot reach because it only prunes exams. delete_library_exam (library_commands.rs:254-263) soft-deletes only library_items, leaving both the on-disk job directory and the exams row in place forever. The converged UI calls only the second one (src/features/library/libraryStore.ts:105), so in the current product nothing on disk is ever reclaimed.",
      "The authoritative state file has weaker durability than the shadow content it governs. job.json is persisted by util::write_json (:259-265) — plain fs::write, no temp file, no rename, no fsync, no lock, and update_job has no version check. One directory below it, authoring/revisions/*.json is written under an exclusive flock on authoring/revision.lock with create_new + an atomic rename for current-revision.json (artifact_store.rs:249-258, :306, :318-321). Also, a single unparseable job.json fails the entire library listing because of the `?` at job_store.rs:68.",
      "An unreadable authoring-ir.json makes the DB write silently vanish. reading_payload returns Err only when the file exists but cannot be read (library_commands.rs:80), and upsert_reading_job then logs and `return Ok(())` (:95-101) — no exams write, no library_items write, and save_job reports success. The comment says this is deliberate (preserve the existing DB payload rather than overwrite it with job.json), but the effect is that job.json and the DB diverge with no error surfaced anywhere.",
      "status_from_reading throws away information on the way into the DB. It collapses 6 JobStatus values into 4 (library_commands.rs:26-33: DraftSaved and ExportReady both become \"ready\"; Exported and Cleaned both become \"exported\"), and ExamRecord carries only issue_errors and issue_warnings — issue_counts.needs_review is dropped entirely (:61-62). Combined with LibraryExamSummary having no currentStep (lib.rs:215-237), the DB cannot currently reconstruct the job state that libraryTypes.ts:95-124 needs, which is exactly why libraryStore.ts still calls listJobs().",
      "library_items.id == job.job_id is confirmed in code, and the id is minted in Rust, not the DB: make_job builds `import-<YYYYMMDDHHMMSS>-<uuid8>` (job_store.rs:9-11), exam_record_from_reading_job sets ExamRecord.id = job.job_id (library_commands.rs:50), and LibraryItemRecord.id = exam.id (:187). ExamRecord.exam_id is a separate, oddly derived field: `job.category.as_ref().map(|_| job.job_id.clone())` (:51), i.e. it is Some(job_id) whenever a category exists and None otherwise.",
      "A third revision tree exists that nobody documents. archive_current_draft_for_source_replacement (authoring_commands.rs:109-152) creates jobs/<id>/revisions/<rfc3339-with-punctuation-stripped>-source-replaced/ and fs::renames 6 files into it (authoring-ir.json, split-candidates.json, pipeline-report.json, validation-report.json, publish-readiness-report.json, source-review.json) plus a revision-meta.json. This is a sibling of `authoring/revisions` (the V2 store), is not created by ensure_job_dirs, and is not in any cleanup list, so it accumulates on every source replacement.",
      "The startup migration's result is discarded. lib.rs:1412 is `let _ = library_commands::migrate_existing_into_library(&root);`. A failed migration leaves migration_done_v1 unset (correct, it retries next launch) but the app starts with a partially populated library and no user-visible signal. The migration also writes only `exams`, never library_items, so every migrated item has zero revision rows and get_exam falls back to e.payload_json via the COALESCE at db.rs:393.",
      "The plan's target symbols do not exist yet at all: no processing/ directory, no repository.rs, no LibraryRepository or LibraryRepositoryV2, no list_library_items, no library_items_v2 / processing_jobs_v2 / actionable_issues_v1 / library_item_recovery_v1, no cleanup_on_startup and no crash-recovery path (cleanup.rs only has the two export-triggered functions). Plan 17.14 reads as if job_store.rs were being renamed; it is being written from nothing, against 28 live write paths."
    ]
  },
  {
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "save_job (job_store.rs:33-44)",
        "whatToChange": "DOUBLE-WRITE POINT #1. Lines 36-42 call crate::library_commands::upsert_reading_job(root, job) and swallow the error. Delete that block so save_job only writes jobs/<id>/job.json. WARNING: ~13 production call sites rely on this side effect as the ONLY thing that populates exams/library_items for reading (authoring_commands.rs:234,277,309,329,379,459,504; auto_pipeline.rs:1466,1679,1686,2123,2585; cleanup.rs:154; export_nas_library.rs:1512; export_pack.rs:180,277; job_commands.rs:87,123,203; llm_commands.rs:252,441,734; lib.rs:583). Removing it before the repository writes on its own empties the library list.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs",
        "symbol": "save_writing_job (writing_store.rs:122-136)",
        "whatToChange": "DOUBLE-WRITE POINT #2. Lines 128-134 call crate::library_commands::upsert_writing_job(root, job) best-effort. Delete. Production callers that depend on the side effect: lib.rs:1222 (create), lib.rs:1252 (update via update_writing_job), export_writing_library.rs:186, library_commands.rs:314.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "upsert_reading_job (92-113) + reading_payload (71-82) + ReadingPayloadError (84-87) + exam_record_from_reading_job (47-66) + status_from_reading (26-33)",
        "whatToChange": "Delete the whole reading double-write ladder. Note the two writes inside are NOT atomic with each other: upsert_exam(root,&record) at line 104 opens its own connection and autocommits; upsert_library_item_from_exam at 106 opens a SECOND connection and only logs failure. Any failure between them is the permanent fork described in plan 11.1.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "upsert_writing_job (139-150) + exam_record_from_writing_job (117-136) + status_from_writing (35-41)",
        "whatToChange": "Same as the reading ladder: upsert_exam at line 141 then best-effort upsert_library_item_from_exam at 143, two connections, two transactions. Delete.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "upsert_library_item_from_exam (172-207)",
        "whatToChange": "This is the only ExamRecord -> LibraryItemRecord adapter. It opens its own connection at line 177 (so it can never join the caller's transaction) and it hardcodes schema_version from a &str parameter ('ReadingAuthoringIRV1' / 'WritingExamSourceV1'), which is a lie for early-pipeline revisions whose payload is job.json rather than authoring-ir.json. db.rs:458-482 library_item_record_from_exam does the same conversion but infers schema_version from the payload (db.rs:440-456) - two divergent derivations. Replace both with one repository function that takes &Transaction and derives schema_version from the payload.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "update_library_exam_meta_core (227-252)",
        "whatToChange": "DOUBLE-WRITE POINT #3 (the reverse direction). Line 245 calls write_back_meta_to_source, error only logged (246-250). Delete that call. Also: lines 234/239/247 issue three separate autocommit statements on one connection with no transaction (get_exam, update_exam_meta, get_exam_summary) - wrap in one tx. Today one title edit produces 4 uncoordinated writes to exams/library_items plus 2 file writes plus 1 new content revision.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "write_back_meta_to_source (293-344) + library_status_to_reading (346-355) + library_status_to_writing (357-364)",
        "whatToChange": "Delete all three. This is 'DB -> best effort source file' from plan 17.13. Two extra defects die with it: (a) lines 316/339-341 load the job, mutate the local copy, then call job_store::update_job whose mutator does *j = job.clone() - update_job re-loads from disk and the mutator throws that fresh read away, so any concurrent pipeline write between line 316 and line 339 is silently lost; (b) the reading branch re-enters save_job (line 339) and the writing branch re-enters save_writing_job (line 314), so the reverse write re-triggers the forward double write.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "update_exam_meta (644-694) + update_library_item_meta (714-762)",
        "whatToChange": "UPDATE exams (line 684-688) and UPDATE library_items (line 692 -> 757-760) are two separate autocommit statements; the second is skipped entirely when library_item_exists is false (719-721), so metadata can land in exams only. Single-direction target: keep only the library_items UPDATE, inside the caller's transaction, and return the row read back from library_items. Also note patch.status is written to exams.status raw with no validation while the library_items path maps it through to_library_item_status (742).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "active_summary_source_sql (325-360) and active_detail_source_sql (362-399)",
        "whatToChange": "THE central read change. Both are 'FROM exams e LEFT JOIN library_items li ON li.id = e.id WHERE li.deleted_at IS NULL' - legacy exams is the DRIVING table, so a library_items row with no exams sibling is invisible to every active query. Every column is a COALESCE(li.x, e.x) mix, status is a nested CASE, updated_at is a max() of the two, and issue_errors/issue_warnings/source_hash/exam_id come ONLY from exams (li has no such columns). Rewrite as FROM library_items li LEFT JOIN library_item_revisions rev ON rev.id = li.current_revision_id WHERE li.deleted_at IS NULL, and add real columns for issue counts + source ref to library_items (or the new V2 table) because they do not exist there today. This is plan 17.12's 'delete the COALESCE mixing'.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "list_exams (510-550), get_exam (552-575), search_exams (577-599), get_stats (601-642), get_exam_summary (697-712)",
        "whatToChange": "Five readers, six embedded uses of the COALESCE view (get_stats builds it 4x: once for total at 604-607 and once per group_count at 614-617). All must re-point at library_items. get_exam's payload is COALESCE(rev.payload_json, e.payload_json) so the detail body is the V1 authoring IR copy, never the V2 canonical DS. get_stats does 4 full scans of a non-indexable subquery per call; list_exams ORDER BY updated_at DESC and search_exams LOWER(tags_json) LIKE both sort/filter on computed expressions so no index applies.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "upsert_library_item (841-900)",
        "whatToChange": "Two blocking problems. (1) It ALWAYS inserts a new library_item_revisions row (887-896, revision_no = max+1) with a full payload copy - no content comparison. Because save_job fires it, one localOnly import of one PDF produces ~6 full-payload revisions (create + import_source_file + parse + split + authoring + final), and a pure metadata rename produces one more. Add a content/hash guard so metadata-only writes touch no revision. (2) It calls conn.unchecked_transaction() (845-847) which issues BEGIN; calling it from inside an outer Connection::transaction (as plan 11.2 M2 requires) fails with 'cannot start a transaction within a transaction'. Change the signature to take &rusqlite::Transaction. Also record.task_type (declared db.rs:825) is never bound in the INSERT params (878-882) - dead field; writing task_type survives only because library_commands.rs:124 stuffs it into category.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "upsert_exam (277-280) and upsert_exam_conn (239-274)",
        "whatToChange": "The two legacy-table writers. upsert_exam is the transient-connection wrapper used by the double-write hooks; upsert_exam_conn is also used by migration (library_commands.rs:479) and by restore_exam_from_library_item (db.rs:1008). Once reads move off exams, delete upsert_exam entirely and keep upsert_exam_conn only if a downgrade path is wanted.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "delete_exam (764-769) and delete_exam_by_id (772-776)",
        "whatToChange": "ASYMMETRY BUG to fix, not just remove. job_commands.rs:149 (delete_job) and writing_store.rs:189 (delete_writing_job) hard-DELETE the exams row and leave the library_items row + its whole revision chain behind with deleted_at NULL. That orphan is invisible in the active list (exams is the driving table) AND invisible in trash (deleted_at is NULL) - unreachable garbage. Worse, a later delete_library_exam on the same id succeeds (library_item_exists short-circuits) and puts it in trash, and restore_library_exam then re-INSERTS an exams row from the stale revision, resurrecting a library entry whose files were deleted. Replace with one repository delete that soft-deletes library_items, plus a real purge that deletes revisions then the item (needed because PRAGMA foreign_keys=ON + library_item_revisions.library_item_id REFERENCES library_items(id) blocks a bare item delete).",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "prune_orphan_exams (781-807)",
        "whatToChange": "File-system-driven DB deletion: deletes every exams row whose id is not in a live_ids set built by scanning jobs/ and writing-jobs/. Delete it - it is the exact inverse of the target direction. Until then note the hazard at library_commands.rs:405 and 443: both directory scans are `if let Ok(entries) = read_dir(...)` with `.flatten()`, so an IO/permission failure yields a silently empty or partial live_ids and this function then wipes the corresponding exams rows. It only prunes exams, never library_items.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "migrate_existing_into_library (386-490)",
        "whatToChange": "Currently the ONLY thing this writes is exams (upsert_exam_conn at 479) - it never creates a library_items row or a revision, so on a fresh install after migration library_items is completely EMPTY and every active query serves pure legacy data through the LEFT JOIN. This is where plan 11.2 M2 has to be implemented. Two constraints: (a) the outer tx is conn.transaction() (470-472) so any item writer must accept &Transaction (see upsert_library_item note); (b) the marker is a single boolean library_meta.migration_done_v1 (388, 487) with no migration_version, so it cannot express the plan's 'do not overwrite user-edited V2 rows' idempotency - replace with a versioned key.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "delete_library_exam_core (254-263)",
        "whatToChange": "Depends on db::ensure_library_item_for_exam, which backfills the item FROM the legacy exams row and returns false when no exams row exists (db.rs:497-506) - so delete silently no-ops (returns Ok(false), which the frontend ignores at libraryStore.ts:105) for any row that is in the list via listJobs but missing from exams. Also the backfill path itself calls upsert_library_item, i.e. deleting an item can create a content revision. Target: soft-delete straight on library_items inside one tx, error (not false) when the item does not exist.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "ensure_library_item_for_exam (497-506) + library_item_record_from_exam (458-482) + get_legacy_exam_record (401-438) + library_item_exists (484-495) + infer_revision_schema_version (440-456)",
        "whatToChange": "The whole legacy-backfill cluster. Keep only inside the one-shot migration (plan 17.13: 'migration reads old file/job, never writes back'); remove from the delete path. Note change_reason is hardcoded 'legacy_exam_backfill' (480) vs 'ingest_save' in library_commands.rs:203 - the two only reasons ever written.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "restore_exam_from_library_item (1001-1010) + exam_record_from_library_item (924-999)",
        "whatToChange": "REVERSE-DIRECTION WRITE (library_items -> exams). Delete both. exam_record_from_library_item also re-derives exam_id/task_type by pattern-matching JSON payload keys (944-972) and fabricates source_hash=None, issue_errors=0, issue_warnings=0 (985-987), so a restore silently zeroes the issue counts of the restored row. And normalize_public_status (309-315) does not map 'archived', while exams.status CHECK (db.rs:90) forbids it - restoring an archived item would fail the CHECK.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "restore_library_exam_core (265-272)",
        "whatToChange": "Drop the `let _ = restore_exam_from_library_item(&conn, id)?` at line 269; keep only restore_library_item. Wrap in a tx once the two writes become one.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "is_library_item_soft_deleted (280-289)",
        "whatToChange": "The only raw SQL outside db.rs. It is the anti-resurrection guard for library_items, but it is checked at line 178 AFTER upsert_exam already ran at line 104/141 - so exams keeps being updated for a trashed item while library_items freezes. Since the active view prefers COALESCE(li.title, e.title), a delete -> pipeline-save -> restore sequence makes the title/status visibly regress to the pre-delete values. Move the guard into the repository so one check covers the single write.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "open_connection (51-68)",
        "whatToChange": "Every *_core function opens a fresh transient connection, and each open runs migrate_db_file (53), the three PRAGMAs (60-65), and the ENTIRE SCHEMA_SQL batch of 11 CREATE TABLE + 9 CREATE INDEX statements via ensure_schema (66). One save_job does this twice. A LibraryRepository should own one connection (or pool) in AppState and run schema/migration exactly once at startup. Note AppState (lib.rs:1407 .manage(AppState)) is currently an empty marker, so there is nowhere to put a connection yet.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "SCHEMA_SQL (77-191)",
        "whatToChange": "ingest_jobs (143), source_assets (158), publish_records (170), job_artifact_index (184) are created but never read or written by any code in the crate (verified by grep across src/). library_items.source_asset_id is filled with a raw sha256 from exam.source_hash (library_commands.rs:196), not an id into source_assets, despite the comment at line 118. exams.status CHECK allows only draft|needs_review|ready|exported while library_items.status CHECK also allows 'archived', so archived is unreachable while exams is still written. Plan 11.2 M1's four new tables (library_items_v2, processing_jobs_v2, actionable_issues_v1, library_item_recovery_v1) do not exist at all.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "apply_authoring_v2_patches_core (123-168), append_revision call at 149-156",
        "whatToChange": "MISSING WRITE, not a double write. This is where the new workspace commits canonical content, and it touches NO database: append_revision (artifact_store.rs:240-272) writes jobs/<id>/authoring/revisions/<n>.json under a file lock, then the function returns. grep for save_job|update_job|library_commands|db:: across authoring_v2_commands.rs returns nothing. So V2 edits never reach exams, library_items, or library_item_revisions - title/status/updated_at in the library list do not move when the user edits in the workspace. The convergence has to add the repository commit here (and in get_authoring_v2_core / load_current_authoring at 725-742, which reads the revision file, falling back to the V2 shadow file).",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "list_library_exams (1317-1328), get_library_exam (1330-1338), update_library_exam_meta (1340-1352), delete_library_exam (1354-1362), search_library_exams (1364-1375), get_library_stats (1377-1383), restore_library_exam (1385-1393), list_trashed_exams (1395-1401), registration block (1467-1474), startup migration (1409-1413)",
        "whatToChange": "The eight thin shells are already the right shape (app_root + spawn_blocking + *_core) so they can be re-pointed at a repository without touching the invoke surface. Consumer audit before deleting any: the NEW frontend uses only list_library_exams, list_trashed_exams, delete_library_exam, restore_library_exam (src/features/library/libraryStore.ts:2). get_library_exam and update_library_exam_meta are used only by src/pages/LibraryExamDetail.tsx, search_library_exams only by src/pages/LibraryPage.tsx, get_library_stats only by src/pages/Dashboard.tsx + src/pages/LibraryPage.tsx - all reachable only under #/legacy/... (src/app/router.ts:11-24). Startup line 1412 discards the migration result with `let _ =`.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/features/library/libraryStore.ts",
        "symbol": "load (54-71) + mergeRows (13-32), and libraryTypes.ts buildRow (137-163) / deriveStage (95-124)",
        "whatToChange": "The new library is currently FILE-FIRST, which inverts the plan's direction and must be flipped in the same change as the backend. load() merges listJobs() (a jobs/ directory scan) with listLibraryExams() + listTrashedExams(); buildRow prefers job.title over summary.title (line 147) and job.updatedAt over summary.updatedAt (line 154); deriveStage branches on job.status/job.currentStep first and only falls through to summary.status when no job exists (95-124). Consequence for sequencing: deleting the save_job -> DB hook alone changes nothing visible here, but dropping listJobs() is what makes the repository's correctness load-bearing. Also LibraryStageV1 'failed' is unreachable - neither JobStatus nor the library status enum has a failed state, so plan 11.3's Failed must be added to the repository status first.",
        "risk": "medium"
      }
    ],
    "dataFlow": "CURRENT, verified end to end.\n\nWRITE PATH A - forward double write (file first, DB best-effort). job_store::save_job (job_store.rs:33-44) writes jobs/<id>/job.json, then calls library_commands::upsert_reading_job (92-113) and swallows any error. upsert_reading_job reads jobs/<id>/authoring-ir.json via reading_payload (71-82; if the file exists but fails to parse it SKIPS the DB write entirely and returns Ok to protect the existing DB payload, library_commands.rs:95-101), builds an ExamRecord with id = job.job_id (47-66), calls db::upsert_exam (db.rs:277) which OPENS ITS OWN CONNECTION and autocommits an INSERT..ON CONFLICT into `exams`, then calls upsert_library_item_from_exam (172-207) which OPENS A SECOND CONNECTION, checks is_library_item_soft_deleted (280-289), and calls db::upsert_library_item (db.rs:841-900) which in its own unchecked_transaction upserts `library_items` AND unconditionally INSERTs a new `library_item_revisions` row carrying a full copy of the payload. So: 1 file + 2 connections + 3 statements in 2 uncoordinated transactions per save. writing_store::save_writing_job (122-136) is the mirror image via upsert_writing_job (139-150). One localOnly import of one PDF runs this ~6 times (job_commands.rs:87 create, :203 import_source_file, auto_pipeline.rs:1466 parse, :1679 split, :1686 authoring, :2123 final), i.e. ~6 full-payload revision rows and 12 connections, each connection re-running the whole CREATE TABLE/INDEX batch (db.rs:66).\n\nWRITE PATH B - reverse write (DB first, file best-effort). update_library_exam_meta_core (227-252): open connection, get_exam to learn subject (234), db::update_exam_meta (239) which UPDATEs `exams` (db.rs:684-688) and then separately UPDATEs `library_items` (db.rs:692 -> 757-760) with no wrapping transaction, then write_back_meta_to_source (245, body 293-344) mutates job.json / writing-job.json and re-enters save_job / save_writing_job - which runs WRITE PATH A again. Net effect of renaming one title: UPDATE exams, UPDATE library_items, write job.json, UPSERT exams, UPSERT library_items, INSERT a brand-new content revision. Failure of the file half is only logged (246-250).\n\nWRITE PATH C - the V2 canonical content, which never reaches the DB. The new workspace saves via apply_authoring_v2_patches_core (authoring_v2_commands.rs:123-168) -> append_revision (artifact_store.rs:240) -> jobs/<id>/authoring/revisions/<n>.json under a file lock. Reads come from load_current_authoring (authoring_v2_commands.rs:725-742): current revision file, else the V2 shadow file. authoring_v2_commands.rs contains zero references to save_job, update_job, library_commands or db::. So there are FIVE content stores and the DB holds a copy of only one of them (the V1 authoring-ir.json, or job.json itself before authoring-ir exists).\n\nDELETE / RESTORE. delete_library_exam_core (254-263) = ensure_library_item_for_exam (backfill from legacy exams if needed) + soft_delete_library_item; the jobs/<id> directory and both JSON stores stay on disk. restore_library_exam_core (265-272) = restore_library_item + restore_exam_from_library_item, the latter re-INSERTING an `exams` row from library_items + its current revision (db.rs:1001-1010). Separately, delete_job (job_commands.rs:142-156) and delete_writing_job (writing_store.rs:183-196) rm -rf the directory and hard-DELETE only the `exams` row (db::delete_exam_by_id), leaving the library_items row and its whole revision chain orphaned.\n\nREAD PATH. All five readers (list_exams db.rs:510, get_exam :552, search_exams :577, get_stats :601, get_exam_summary :697) select from one of two generated subqueries, active_summary_source_sql (325-360) / active_detail_source_sql (362-399). Both are `FROM exams e LEFT JOIN library_items li ON li.id = e.id [LEFT JOIN library_item_revisions rev ON rev.id = li.current_revision_id] WHERE li.deleted_at IS NULL`. Legacy `exams` is the DRIVING table; every user-visible field is COALESCE(li.x, e.x) with library_items winning; status is a nested CASE that maps review_required->needs_review and published->exported; updated_at is max(li, e); issue_errors/issue_warnings/source_hash/exam_id come ONLY from exams because library_items has no such columns; the detail payload is COALESCE(rev.payload_json, e.payload_json). list_trashed_items (1013-1031) is the one query that reads library_items alone, and it fabricates exam_id=NULL, source_hash=NULL, issue_errors=0, issue_warnings=0.\n\nTARGET, single direction. One LibraryRepository owning a single Connection in AppState (today lib.rs:1407 manages an empty AppState marker), schema+migration run once at startup instead of on every open_connection. One transaction per user intent:\n\n  commit_canonical(&mut self, item_id, ds: &Value, meta: MetaDelta, reason) -> Result<RevisionId>\n    tx = conn.transaction()\n    SELECT status, deleted_at, current_revision_id, content_hash FROM library_items WHERE id=?  -- refuse if deleted_at IS NOT NULL\n    if hash(ds) != current content_hash { INSERT library_item_revisions (revision_no+1, payload=ds, schema_version=ds.schemaVersion, change_reason=reason) }\n    UPDATE library_items SET title/category/difficulty/status/tags_json/issue_errors/issue_warnings/current_revision_id/updated_at\n    tx.commit()\n\nwith the three metadata-only intents (rename, retag, status) sharing the same tx but taking the no-new-revision branch, and delete/restore/purge as their own single-statement transactions on library_items only. That requires: (1) db::upsert_library_item's signature changing from &Connection + unchecked_transaction() to &Transaction, otherwise it cannot nest inside this tx or inside migration's conn.transaction(); (2) library_items gaining real issue_errors/issue_warnings/source-reference/content_hash columns, since today those values exist only on the legacy exams row; (3) the V2 revision commit at authoring_v2_commands.rs:149 calling commit_canonical, because that is the only place the canonical DS actually changes; (4) file writes becoming derived output (job.json as a cache, or dropped) with no path from a file write back into the DB.",
    "gapsVsPlan": [
      {
        "claim": "11.1: `save_job()` writes files first, then best-effort double-writes the DB; metadata writes the DB first, then best-effort writes back to JSON. Any step can fork.",
        "evidence": "job_store.rs:33-44, library_commands.rs:92-113, library_commands.rs:227-252, library_commands.rs:293-344",
        "reality": "Exactly right, and worse than stated: the 'DB' half is itself two uncoordinated writes on two separate connections (upsert_exam at library_commands.rs:104 then upsert_library_item_from_exam at :106, which opens its own connection at :177), so the fork can also occur between exams and library_items.",
        "verdict": "confirmed"
      },
      {
        "claim": "11.1: the five stores are job.json, authoring-ir.json, authoring/revisions/*, exams.payload_json, library_items+revisions.",
        "evidence": "job_store.rs:35; authoring_commands.rs:417 and :503; artifact_store.rs:272 via authoring_v2_commands.rs:149; db.rs:93; db.rs:129-139",
        "reality": "All five exist. But the plan reads as if the DB mirrors the canonical content; it does not. library_item_revisions.payload_json is a copy of authoring-ir.json (V1), or of job.json itself when authoring-ir.json does not exist yet (library_commands.rs:73-74). The authoring/revisions/* V2 store - the one the new workspace edits - has no DB representation at all.",
        "verdict": "partly-wrong"
      },
      {
        "claim": "11.2 M2: migration inserts library_items_v2 rows (insert_library_item_v2 / insert_shell_item) preferring current V2 revision, then the V2 shadow, then converted V1.",
        "evidence": "library_commands.rs:386-490, specifically the only write at :479 (upsert_exam_conn) plus :483 prune and :487 marker",
        "reality": "migrate_existing_into_library writes ONLY the legacy `exams` table. It never inserts a library_items row, never a revision, and never looks at the V2 revision store or shadow. After a fresh migration library_items is empty and every active query serves pure legacy data through the LEFT JOIN. library_items rows appear later only as a side effect of save_job or of delete_library_exam's backfill.",
        "verdict": "wrong"
      },
      {
        "claim": "11.2 M2: migration must be idempotent, marked by `migration_version`, and must not overwrite user-edited V2 rows.",
        "evidence": "library_commands.rs:388 and :487 (library_meta.migration_done_v1), db.rs:195-215",
        "reality": "Idempotency exists but it is a single boolean key `migration_done_v1`, not a version. There is no migration_version anywhere in the crate (grep returns nothing), so a second migration wave cannot be expressed, and the test at library_commands.rs:783 has to DELETE the marker row to re-run.",
        "verdict": "partly-wrong"
      },
      {
        "claim": "11.2 M1: add library_items_v2, processing_jobs_v2, actionable_issues_v1, library_item_recovery_v1 while keeping old tables readable.",
        "evidence": "grep for those four names across src-tauri/src returns nothing; db.rs:77-191 is the whole schema",
        "reality": "None of the four exist. What does exist from an earlier phase: exams, library_meta, library_items, library_item_revisions, plus four tables that are created and then never read or written by any code - ingest_jobs (db.rs:143), source_assets (:158), publish_records (:170), job_artifact_index (:184).",
        "verdict": "wrong"
      },
      {
        "claim": "11.2 M3: the new UI only reads/writes the new tables; job_store and legacy library_commands are no longer called by the new UI.",
        "evidence": "src/features/library/libraryStore.ts:2 and :55-65; src/features/library/libraryTypes.ts:147,154,95-124; src/features/editor/useCanonicalEditor.ts:2,67,107",
        "reality": "The opposite is true today. The new library page calls listJobs() (a jobs/ directory scan) alongside listLibraryExams(), and buildRow prefers job.title over summary.title (libraryTypes.ts:147), job.updatedAt over summary.updatedAt (:154), and derives the user-visible stage from job.status/job.currentStep first (:95-124). The new workspace uses only getAuthoringV2/applyAuthoringV2Patches, which touch no table at all. The new UI is file-first, not table-first.",
        "verdict": "wrong"
      },
      {
        "claim": "11.2 M4 / 17.13: delete `save_job -> upsert_reading_job`, delete `library meta -> write_back_meta_to_source`, delete legacy `exams` queries.",
        "evidence": "job_store.rs:36-42, library_commands.rs:245, db.rs:325-399 with readers at db.rs:514, 553, 582, 604, 614, 698",
        "reality": "All three targets exist and are precisely identifiable. The third is much bigger than one line: `exams` is the DRIVING table of both generated subqueries, so removing it means rewriting the FROM clause and finding new homes for issue_errors, issue_warnings, source_hash and exam_id, none of which exist as columns on library_items.",
        "verdict": "confirmed"
      },
      {
        "claim": "17.13: new thin Tauri commands calling LibraryRepository.",
        "evidence": "lib.rs:1317-1401, lib.rs:1467-1474; grep for LibraryRepository returns nothing",
        "reality": "No LibraryRepository type exists. The eight commands are, however, already thin in the right shape (app_root + spawn_blocking + a *_core in library_commands.rs), so they can be re-pointed without changing the invoke surface. AppState (lib.rs:1407) is an empty marker struct, so there is no place to hold a repository connection yet.",
        "verdict": "partly-wrong"
      },
      {
        "claim": "17.13: during the legacy-data phase, only READ the old file/job - stop writing back to it.",
        "evidence": "library_commands.rs:293-344 (write-back), db.rs:1001-1010 (restore_exam_from_library_item), db.rs:781-807 (prune_orphan_exams), job_commands.rs:149, writing_store.rs:189",
        "reality": "Confirmed as a gap, and there are four reverse/legacy writers, not one: write_back_meta_to_source (DB -> JSON), restore_exam_from_library_item (library_items -> exams), prune_orphan_exams (filesystem -> DELETE FROM exams), and delete_exam_by_id from the two file-deletion commands.",
        "verdict": "confirmed"
      },
      {
        "claim": "11.3: LibraryItemStatusV2 = Processing | ActionRequired | Ready | Publishing | Published | Failed | Archived.",
        "evidence": "db.rs:115 CHECK (draft|review_required|ready|published|archived), db.rs:90 CHECK (draft|needs_review|ready|exported), library_commands.rs:160-168, db.rs:309-323, src/features/library/libraryTypes.ts:5-13",
        "reality": "Three enums are in play and none is the target. library_items allows draft|review_required|ready|published|archived; exams allows only draft|needs_review|ready|exported - narrower, so 'archived' is unreachable while exams is still written (the exams UPDATE in update_exam_meta runs first and would hit the CHECK). There is no Processing, Publishing or Failed state anywhere, which is why the new UI's 'failed' stage tab can never match a row.",
        "verdict": "partly-wrong"
      },
      {
        "claim": "The verified fact library_items.id == job.job_id.",
        "evidence": "library_commands.rs:50 (ExamRecord.id = job.job_id), library_commands.rs:187 (LibraryItemRecord.id = exam.id), library_commands.rs:197 and :202 (linked_ingest_job_id and created_from_job_id also = exam.id)",
        "reality": "Confirmed for both subjects. The id is the job id three times over. Note library_items.id is declared 'stable UUID' at db.rs:109 but is actually the import-<timestamp>-<suffix> string from job_store.rs:11 (or writing-<timestamp>-<suffix>, writing_store.rs:102).",
        "verdict": "confirmed"
      },
      {
        "claim": "17.12: move data access out of db.rs into a repository; split the SQL out.",
        "evidence": "db.rs is 1349 lines / 53 KB; all library SQL is in db.rs except one raw query at library_commands.rs:281-283",
        "reality": "Accurate and cheap to do: SQL is already almost fully centralised in db.rs, with exactly one leak (is_library_item_soft_deleted's raw SELECT in library_commands.rs).",
        "verdict": "confirmed"
      }
    ],
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "role": "The subsystem itself: the two double-write hooks (upsert_reading_job 92-113, upsert_writing_job 139-150), the ExamRecord->LibraryItemRecord adapter (upsert_library_item_from_exam 172-207), the eight *_core command bodies (211-377), the reverse metadata write-back (write_back_meta_to_source 293-344), and the one-shot startup migration (migrate_existing_into_library 386-490). Tests at 492-802.",
        "sizeNote": "802 lines / 31 KB - about 39% of it is the inline test module"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "role": "All SQL. Full schema in SCHEMA_SQL (77-191); the two generated COALESCE read views (active_summary_source_sql 325-360, active_detail_source_sql 362-399); the five readers (list_exams 510, get_exam 552, search_exams 577, get_stats 601, get_exam_summary 697); legacy writers (upsert_exam_conn 239, upsert_exam 277, update_exam_meta 644, delete_exam 764, delete_exam_by_id 772, prune_orphan_exams 781); the V2 writers (upsert_library_item 841, soft_delete_library_item 903, restore_library_item 914); and the two legacy<->item bridges (ensure_library_item_for_exam 497, restore_exam_from_library_item 1001). Tests 1037-1349.",
        "sizeNote": "1349 lines / 53 KB - plan 17.12 targets splitting this"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "role": "Reading job file store. save_job (33-44) is double-write trigger #1; update_job (46-56) is the load-mutate-save wrapper that every pipeline stage funnels through; list_saved_jobs (58-84) is the directory scan the new library UI still calls as listJobs.",
        "sizeNote": "105 lines - small, entirely rewritable"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs",
        "role": "Writing job file store, mirror of job_store. save_writing_job (122-136) is double-write trigger #2; delete_writing_job (183-196) hard-deletes the exams row via db::delete_exam_by_id at :189 and leaves library_items orphaned.",
        "sizeNote": "211 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "role": "create_import_job_core (77-89, save_job at :87), update_job_meta_core (117-140), import_source_file_core (158-209, update_job at :203) and delete_job_core (142-156, db::delete_exam_by_id at :149 - the exams-only hard delete).",
        "sizeNote": "297 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "The canonical-content commit path used by the new workspace, and the proof it bypasses the library entirely: get_authoring_v2_core (54-57), apply_authoring_v2_patches_core (123-168), load_current_authoring (725-742). No reference to save_job, update_job, library_commands or db:: anywhere in the file.",
        "sizeNote": "2551 lines / 95 KB - plan 17.16 wants it split five ways"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs",
        "role": "The V2 file revision store the DB knows nothing about: recover_current_revision (210) and append_revision (240-272) writing jobs/<id>/authoring/revisions/<n>.json under revision.lock.",
        "sizeNote": "888 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "Public types (JobStatus 92-111, ImportJob 134-155, LibraryFilter 205-212, LibraryExamSummary 214-237, LibraryExamDetail 239-243, LibraryMetaPatch 245-254, LibraryStats 256-265), the eight library command shells (1317-1401), the startup migration call (1409-1413) and the invoke_handler registration (1467-1474).",
        "sizeNote": "11009 lines / 438 KB - plan 18 P8-T04 targets splitting it"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "Seven update_job call sites (234, 277, 309, 329, 379, 459, 504) - the V1 authoring path that writes authoring-ir.json (417, 503) and therefore determines what payload the DB double write copies.",
        "sizeNote": "629 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "Five update_job call sites (1466, 1679, 1686, 2123, 2585) - the main source of double-write volume during one import.",
        "sizeNote": "116 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/library/libraryStore.ts",
        "role": "Frontend proof that the new library is file-first: merges listJobs() with listLibraryExams() + listTrashedExams() (55-65), 2s polling while any row is processing (11, 93-97), moveToTrash/restore ignoring the returned boolean (104-112)."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/library/libraryTypes.ts",
        "role": "buildRow (137-163) and deriveStage (95-124) - shows job.json winning over the DB summary for title, updatedAt and stage, and that no data source can produce the 'failed' stage."
      }
    ],
    "publicApi": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:92",
        "notes": "DOUBLE-WRITE ENTRY #1, called only from job_store.rs:37. Returns Ok(()) and merely logs when authoring-ir.json is unreadable (95-101) - a deliberate 'preserve the DB payload' skip. Also returns Ok when the library_items half fails (106-111).",
        "signature": "pub(crate) fn upsert_reading_job(root: &Path, job: &ImportJob) -> CommandResult<()>",
        "symbol": "upsert_reading_job"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:139",
        "notes": "DOUBLE-WRITE ENTRY #2, called only from writing_store.rs:129.",
        "signature": "pub(crate) fn upsert_writing_job(root: &Path, job: &WritingJob) -> CommandResult<()>",
        "symbol": "upsert_writing_job"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:172",
        "notes": "Private. Opens its OWN connection at line 177 so it can never join a caller's transaction. schema_version is a caller-supplied literal, unlike db::infer_revision_schema_version.",
        "signature": "fn upsert_library_item_from_exam(\n    root: &Path,\n    exam: &ExamRecord,\n    schema_version: &str,\n) -> CommandResult<()>",
        "symbol": "upsert_library_item_from_exam"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:293",
        "notes": "Private. The reverse write. Reading branch ends in job_store::update_job with `*j = job.clone()` (339-341), which discards update_job's own fresh disk read - a lost-update window. Writing branch calls save_writing_job directly (314).",
        "signature": "fn write_back_meta_to_source(\n    root: &Path,\n    subject: &str,\n    id: &str,\n    patch: &LibraryMetaPatch,\n) -> CommandResult<()>",
        "symbol": "write_back_meta_to_source"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:227",
        "notes": "Calls write_back_meta_to_source at line 245 with the error only logged. Three uncoordinated statements on one connection, no transaction.",
        "signature": "pub(crate) fn update_library_exam_meta_core(\n    root: &Path,\n    id: &str,\n    patch: LibraryMetaPatch,\n) -> CommandResult<Option<LibraryExamSummary>>",
        "symbol": "update_library_exam_meta_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:254",
        "notes": "Returns Ok(false) - not an error - when neither a library_items row nor a legacy exams row exists. The frontend discards the boolean (libraryStore.ts:105).",
        "signature": "pub(crate) fn delete_library_exam_core(root: &Path, id: &str) -> CommandResult<bool>",
        "symbol": "delete_library_exam_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:265",
        "notes": "Two writes, no transaction: restore_library_item then the reverse-direction restore_exam_from_library_item (line 269).",
        "signature": "pub(crate) fn restore_library_exam_core(root: &Path, id: &str) -> CommandResult<bool>",
        "symbol": "restore_library_exam_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:211",
        "signature": "pub(crate) fn list_library_exams_core(\n    root: &Path,\n    filter: Option<LibraryFilter>,\n) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "list_library_exams_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:219",
        "signature": "pub(crate) fn get_library_exam_core(\n    root: &Path,\n    id: &str,\n) -> CommandResult<Option<LibraryExamDetail>>",
        "symbol": "get_library_exam_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:366",
        "notes": "No consumer in the new frontend; only src/pages/LibraryPage.tsx:37.",
        "signature": "pub(crate) fn search_library_exams_core(\n    root: &Path,\n    query: &str,\n) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "search_library_exams_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:374",
        "notes": "No consumer in the new frontend; only src/pages/Dashboard.tsx:16 and src/pages/LibraryPage.tsx:52.",
        "signature": "pub(crate) fn get_library_stats_core(root: &Path) -> CommandResult<LibraryStats>",
        "symbol": "get_library_stats_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:274",
        "signature": "pub(crate) fn list_trashed_exams_core(root: &Path) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "list_trashed_exams_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:386",
        "notes": "Called once at startup from lib.rs:1412 with the result discarded (`let _ =`). Gated on library_meta.migration_done_v1. Writes ONLY the exams table.",
        "signature": "pub(crate) fn migrate_existing_into_library(root: &Path) -> CommandResult<usize>",
        "symbol": "migrate_existing_into_library"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:841",
        "notes": "Uses conn.unchecked_transaction() at 845, so it CANNOT be nested inside an outer transaction. ALWAYS inserts a new revision row (887-896) with no content comparison. record.task_type is declared but never bound.",
        "signature": "pub(crate) fn upsert_library_item(\n    conn: &Connection,\n    record: &LibraryItemRecord,\n) -> CommandResult<String>",
        "symbol": "db::upsert_library_item"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:277",
        "notes": "Transient-connection wrapper used by the double-write hooks; delegates to upsert_exam_conn (239).",
        "signature": "pub(crate) fn upsert_exam(root: &Path, record: &ExamRecord) -> CommandResult<()>",
        "symbol": "db::upsert_exam"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:239",
        "notes": "Also called with a &Transaction deref inside migration (library_commands.rs:479) and by restore_exam_from_library_item (db.rs:1008). ON CONFLICT preserves created_at.",
        "signature": "pub(crate) fn upsert_exam_conn(conn: &Connection, record: &ExamRecord) -> CommandResult<()>",
        "symbol": "db::upsert_exam_conn"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:644",
        "notes": "UPDATE exams (684-688) then, separately and unwrapped, update_library_item_meta -> UPDATE library_items (757-760). Silently skips the library_items half when no item row exists (719-721).",
        "signature": "pub(crate) fn update_exam_meta(\n    conn: &Connection,\n    id: &str,\n    patch: &LibraryMetaPatch,\n) -> CommandResult<Option<LibraryExamSummary>>",
        "symbol": "db::update_exam_meta"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:510",
        "notes": "Default limit 200 clamped to 1..1000 (532). Filters on the normalized public status.",
        "signature": "pub(crate) fn list_exams(\n    conn: &Connection,\n    filter: &LibraryFilter,\n) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "db::list_exams"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:552",
        "notes": "Payload is COALESCE(rev.payload_json, e.payload_json) - the V1 authoring IR, never the V2 canonical DS.",
        "signature": "pub(crate) fn get_exam(conn: &Connection, id: &str) -> CommandResult<Option<LibraryExamDetail>>",
        "symbol": "db::get_exam"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:577",
        "notes": "LIKE over title, exam_id and the raw tags_json string; hard LIMIT 200; full scan.",
        "signature": "pub(crate) fn search_exams(\n    conn: &Connection,\n    query: &str,\n) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "db::search_exams"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:601",
        "notes": "Builds the COALESCE subquery four times per call (total + three group_counts at 613-634).",
        "signature": "pub(crate) fn get_stats(conn: &Connection) -> CommandResult<LibraryStats>",
        "symbol": "db::get_stats"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:497",
        "notes": "Backfills a library_items row FROM the legacy exams row (and thereby creates a revision). Returns false when no exams row exists - which is what makes delete depend on the legacy table.",
        "signature": "pub(crate) fn ensure_library_item_for_exam(conn: &Connection, id: &str) -> CommandResult<bool>",
        "symbol": "db::ensure_library_item_for_exam"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:1001",
        "notes": "REVERSE WRITE: library_items + current revision -> INSERT into exams. Short-circuits to Ok(true) if an exams row already exists (1002-1004), which is why a stale library_items row can win the COALESCE after restore.",
        "signature": "pub(crate) fn restore_exam_from_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "symbol": "db::restore_exam_from_library_item"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:903",
        "signature": "pub(crate) fn soft_delete_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "symbol": "db::soft_delete_library_item"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:914",
        "signature": "pub(crate) fn restore_library_item(conn: &Connection, id: &str) -> CommandResult<bool>",
        "symbol": "db::restore_library_item"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:1013",
        "notes": "The one query that reads library_items alone. Fabricates exam_id=NULL, source_hash=NULL, issue_errors=0, issue_warnings=0 (1015-1018).",
        "signature": "pub(crate) fn list_trashed_items(conn: &Connection) -> CommandResult<Vec<LibraryExamSummary>>",
        "symbol": "db::list_trashed_items"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:764",
        "notes": "Hard DELETE of the exams row only; library_items and its revisions survive. Wrapper delete_exam_by_id (772) is called from job_commands.rs:149 and writing_store.rs:189.",
        "signature": "pub(crate) fn delete_exam(conn: &Connection, id: &str) -> CommandResult<bool>",
        "symbol": "db::delete_exam"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:781",
        "notes": "Filesystem-driven DB deletion, migration-only. Deletes every exams row absent from live_ids; never touches library_items.",
        "signature": "pub(crate) fn prune_orphan_exams(\n    conn: &Connection,\n    live_ids: &std::collections::HashSet<String>,\n) -> CommandResult<usize>",
        "symbol": "db::prune_orphan_exams"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:51",
        "notes": "Runs migrate_db_file + three PRAGMAs + the full SCHEMA_SQL batch on EVERY call. Errors are desensitized to the literal \"open_db_failed\".",
        "signature": "pub(crate) fn open_connection(root: &Path) -> CommandResult<Connection>",
        "symbol": "db::open_connection"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:220",
        "notes": "The legacy flat row. Carries issue_errors/issue_warnings/source_hash/exam_id, which have NO equivalent column on library_items - the reason the read views still need the exams join.",
        "signature": "pub(crate) struct ExamRecord {\n    pub id: String,\n    pub exam_id: Option<String>,\n    pub title: String,\n    pub subject: String, // \"reading\" | \"writing\"\n    pub category: Option<String>,\n    pub frequency: Option<String>,\n    pub status: String, // 统一枚举：draft|needs_review|ready|exported\n    pub task_type: Option<String>,\n    pub tags: Vec<String>,\n    pub payload_json: String,\n    pub source_hash: Option<String>,\n    pub issue_errors: u32,\n    pub issue_warnings: u32,\n    pub created_at: String, // ISO8601\n    pub updated_at: String,\n}",
        "symbol": "db::ExamRecord"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs:817",
        "notes": "task_type is never bound by upsert_library_item's INSERT (params at 878-882) - dead field. No deleted_at field, so an upsert cannot resurrect a trashed row via ON CONFLICT.",
        "signature": "#[derive(Clone)]\npub(crate) struct LibraryItemRecord {\n    pub id: String,           // 稳定 UUID（首版用 job_id，后续保持）\n    pub subject: String,      // reading|writing\n    pub content_type: String, // reading_exam|writing_task\n    pub title: String,\n    pub category: Option<String>,\n    pub difficulty: Option<String>, // 原 frequency\n    pub status: String,             // draft|review_required|ready|published|archived\n    pub task_type: Option<String>,\n    pub tags: Vec<String>,\n    pub source_asset_id: Option<String>,\n    pub linked_ingest_job_id: Option<String>,\n    pub created_at: String,\n    pub updated_at: String,\n    // revision 内容\n    pub revision_payload_json: String,\n    pub schema_version: String, // ReadingAuthoringIRV1 | WritingExamSourceV1\n    pub created_from_job_id: Option<String>,\n    pub change_reason: Option<String>,\n}",
        "symbol": "db::LibraryItemRecord"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:33",
        "notes": "The single choke point for reading double writes.",
        "signature": "pub(crate) fn save_job(root: &Path, job: &ImportJob) -> CommandResult<()>",
        "symbol": "job_store::save_job"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:46",
        "notes": "load -> mutate -> save_job, refreshing updated_at (53). Every pipeline stage uses this, so every stage triggers a double write.",
        "signature": "pub(crate) fn update_job(\n    root: &Path,\n    job_id: &str,\n    mutator: impl FnOnce(&mut ImportJob),\n) -> CommandResult<ImportJob>",
        "symbol": "job_store::update_job"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/writing_store.rs:122",
        "signature": "pub(crate) fn save_writing_job(root: &Path, job: &WritingJob) -> CommandResult<()>",
        "symbol": "writing_store::save_writing_job"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:123",
        "notes": "The new workspace's save. Writes only a file revision (append_revision at 149-156). Contains no DB call of any kind.",
        "signature": "pub(crate) fn apply_authoring_v2_patches_core(root: &Path, input: Value) -> CommandResult<Value>",
        "symbol": "apply_authoring_v2_patches_core"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:54",
        "notes": "Reads jobs/<id>/authoring/revisions/<n>.json, falling back to the V2 shadow file (load_current_authoring 725-742). Never reads library_item_revisions.",
        "signature": "pub(crate) fn get_authoring_v2_core(root: &Path, job_id: &str) -> CommandResult<Value>",
        "symbol": "get_authoring_v2_core"
      }
    ],
    "subsystem": "library repository and legacy exams double write (src-tauri/src/library_commands.rs + db.rs)",
    "surprises": [
      "The new workspace's saves never reach the library at all. apply_authoring_v2_patches_core writes only jobs/<id>/authoring/revisions/<n>.json (authoring_v2_commands.rs:149 -> artifact_store.rs:240); grep for save_job|update_job|library_commands|db:: across authoring_v2_commands.rs returns nothing. So the DB copy (library_item_revisions.payload_json) is a copy of the V1 authoring-ir.json, and the plan's premise that the DB is one of two mirrors of the canonical content is wrong - it is a mirror of a different, older document.",
      "migrate_existing_into_library never creates a library_items row. Its only write is upsert_exam_conn (library_commands.rs:479). On a fresh install library_items is EMPTY after migration, and every active query returns pure legacy data through the LEFT JOIN. Plan 11.2 M2's insert_library_item_v2 loop does not exist in any form.",
      "`exams`, not library_items, is the DRIVING table of every active query (db.rs:356-357, :394-395). A library_items row without an exams sibling is invisible to list_exams, get_exam, search_exams and get_stats. This makes the legacy table load-bearing for the entire library surface, not a compatibility shim.",
      "Orphan + resurrection path. delete_job (job_commands.rs:149) and delete_writing_job (writing_store.rs:189) hard-delete only the exams row, leaving the library_items row and its whole revision chain with deleted_at NULL - invisible in the active list (no exams sibling) and invisible in trash (not soft-deleted). A later delete_library_exam on that id succeeds and puts it in trash, and restore_library_exam then re-INSERTs an exams row from the stale revision (db.rs:1001-1010), resurrecting a library entry whose files were deleted.",
      "Silent title/status regression through the trash. upsert_exam (library_commands.rs:104) runs BEFORE the soft-delete guard at :178, so a trashed item's exams row keeps absorbing pipeline updates while its library_items row is frozen. Because the read view is COALESCE(li.title, e.title) with library_items winning, delete -> any pipeline save -> restore makes the title and status revert to their pre-delete values. The existing restore test (library_commands.rs:638-686) re-saves the job and never asserts the title, so this is untested.",
      "Every save appends a full content revision. db::upsert_library_item unconditionally INSERTs into library_item_revisions (db.rs:887-896) with no content comparison, and it is driven by save_job. One localOnly import of one PDF produces ~6 full-payload revision rows (job_commands.rs:87, :203; auto_pipeline.rs:1466, 1679, 1686, 2123), a metadata-only rename produces a 7th, and even delete_library_exam can produce one via the ensure_library_item_for_exam backfill.",
      "Early revisions carry a false schema_version. upsert_reading_job hardcodes \"ReadingAuthoringIRV1\" (library_commands.rs:106) but reading_payload falls back to serializing the ImportJob itself when authoring-ir.json does not exist yet (:73-74) - so the first revisions of every item are labelled as authoring IR while actually containing job.json. db.rs:440-456 infers schema_version from the payload instead, so the crate has two contradictory derivations.",
      "prune_orphan_exams can delete every exams row. It builds live_ids by scanning jobs/ and writing-jobs/ with `if let Ok(entries) = read_dir(...)` and `.flatten()` (library_commands.rs:405, 443), silently dropping IO failures, then hard-deletes every exams row not in the set (db.rs:799-804). ensure_app_dirs (lib.rs:1410) creates both directories before the call so the normal path is safe, but a permission or enumeration failure turns into DB deletion. It is gated by the one-shot migration_done_v1 marker, so it fires exactly on the run most likely to be a fresh or moved appData root.",
      "unchecked_transaction() is a landmine for the migration work plan 11.2 M2 asks for. upsert_library_item issues BEGIN via conn.unchecked_transaction() (db.rs:845); calling it inside migrate_existing_into_library's conn.transaction() (library_commands.rs:470) would fail with 'cannot start a transaction within a transaction'. The signature has to become &Transaction before any item can be written during migration.",
      "Four Phase-1 tables are dead. ingest_jobs (db.rs:143), source_assets (:158), publish_records (:170) and job_artifact_index (:184) are created on every open_connection and never read or written anywhere in the crate. library_items.source_asset_id is filled with a raw sha256 from the source file (library_commands.rs:196), not an id into source_assets, despite the comment at db.rs:118.",
      "The status CHECKs disagree, and the narrower one runs first. library_items allows 'archived' (db.rs:115) but exams does not (db.rs:90), and update_exam_meta UPDATEs exams before library_items (db.rs:684 then :692), so 'archived' is unreachable while the double write stands. patch.status is also written into exams.status with no validation - an unexpected value from the frontend surfaces as a raw CHECK-constraint error string.",
      "The new library UI is file-first, the opposite of the plan's direction. libraryStore.ts merges listJobs() (a directory scan) with listLibraryExams(), and buildRow prefers job.title (libraryTypes.ts:147) and job.updatedAt (:154) over the DB summary while deriveStage branches on job.status/job.currentStep first (:95-124). Consequence for sequencing: removing the save_job -> DB hook alone changes nothing the user sees; dropping listJobs() is the step that makes repository correctness load-bearing.",
      "Four of the eight library commands have no consumer in the converged UI. get_library_exam and update_library_exam_meta are used only by src/pages/LibraryExamDetail.tsx, search_library_exams only by src/pages/LibraryPage.tsx, get_library_stats only by src/pages/Dashboard.tsx and src/pages/LibraryPage.tsx - all reachable only through #/legacy/... (src/app/router.ts:11-24). The metadata write-back (and its whole reverse direction) therefore has no new-UI caller to preserve.",
      "There is no permanent delete. Nothing purges a trashed library item: no purge/hard_delete/empty_trash symbol exists in src-tauri/src, and PRAGMA foreign_keys=ON plus library_item_revisions.library_item_id REFERENCES library_items(id) (db.rs:63, :131) means a naive DELETE of an item row would fail while revisions exist. Any repository redesign has to introduce a purge that deletes revisions first.",
      "open_connection re-runs the entire schema on every call (db.rs:66). One save_job opens two connections, so a single import of one PDF executes the 11 CREATE TABLE + 9 CREATE INDEX batch about a dozen times. AppState (lib.rs:1407) is an empty marker struct, so there is currently nowhere for a repository to hold a long-lived connection.",
      "The library's 'failed' stage can never appear. src/features/library/libraryTypes.ts:5-13 defines a 'failed' stage and a 'failed' filter tab, but neither JobStatus (lib.rs:92-111) nor the library_items status CHECK has a failed state, so no data source can produce it. Plan 11.3's Processing / Publishing / Failed all need new repository states."
    ],
    "testHarness": "All tests are inline `#[cfg(test)] mod tests` in the same files; there is no src-tauri/tests/ directory and no [[test]] target in Cargo.toml (package `ielts-author-studio`, lib `ielts_author_studio_lib`). Filter with `cargo test -p ielts-author-studio library_commands::` or `... db::`.\n\nTWO STYLES TO COPY.\n(1) db.rs uses pure in-memory SQLite - `fn mem_conn() -> Connection { let conn = Connection::open_in_memory().unwrap(); ensure_schema(&conn).unwrap(); conn }` (db.rs:1041-1045) plus builders `sample_record(id, subject, status)` (1047-1069) and `sample_library_item(id, subject, status)` (1208-1238). This is the right template for LibraryRepository unit tests: no filesystem, no app root.\n(2) library_commands.rs uses real temp directories and real files - `make_reading_appdata()` (501-543) creates std::env::temp_dir()/ielts-lib-test-<uuid> with a full job.json plus a minimal ReadingAuthoringIRV1 authoring-ir.json, `make_writing_appdata()` (545-567) the writing equivalent, `cleanup(&root)` (569-571) removes the tree. Note these tests write to the real temp dir and are not isolated per thread; there is no #[serial] guard.\n\nEXISTING TESTS, and which ones the convergence deliberately breaks.\nlibrary_commands.rs (6): `migrate_imports_reading_and_writing` (573) asserts migration returns 2 and is idempotent; `update_meta_writes_back_to_job_json_and_survives_resave` (597) asserts job.json.title is synced from a library edit AND that a later save_job does not overwrite the DB - DIES with the reverse write-back, and its second half is the closest thing to a regression test for the fork; `delete_library_exam_moves_to_trash_and_restore_brings_it_back` (637) asserts the job dir survives a soft delete, that a resave does not revive a trashed item (669-677 - keep this assertion, it is the anti-resurrection contract), and that restore returns it to the active list; `restore_rehydrates_legacy_exam_row_deleted_by_old_flow` (688) exists purely to pin the reverse library_items -> exams write - DELETE with restore_exam_from_library_item; `update_meta_keeps_cleaned_reading_jobs_cleaned` (712) pins the Cleaned-preservation branch inside write_back_meta_to_source - DIES with it, but the underlying rule (editing metadata must not downgrade a published/cleaned item) needs re-expressing against the repository status enum; `migrate_prunes_orphan_db_rows` (753) pins filesystem-driven pruning - DELETE.\ndb.rs (13): `schema_is_idempotent` (1071), `upsert_inserts_then_updates` (1078, also pins created_at preservation), `list_filters_by_subject_and_status` (1098), `search_matches_title_and_tags` (1130), `stats_group_counts` (1142), `update_meta_partial_and_delete` (1156), `meta_get_set` (1179), `check_constraint_rejects_invalid_status_and_subject` (1189), `library_item_upsert_creates_revisions` (1239 - asserts a new revision per save, i.e. it pins exactly the revision-spam behaviour that must change; rewrite it to assert no new revision for a metadata-only write), `soft_delete_and_restore` (1277), `active_queries_follow_soft_delete_and_normalize_statuses` (1295 - the single best template for a repository read test: it walks list/get/search/stats/trash/restore in one pass), `migrate_db_file_renames_legacy` (1324).\n\nWHAT IS NOT COVERED and needs new tests. No test asserts the exams/library_items pair stays consistent when one half fails. No test covers the delete_job -> orphaned library_items path (job_commands.rs:149). No test covers title divergence across delete -> save -> restore. No test asserts anything about the V2 revision store reaching the DB, because it does not. No test exercises the eight lib.rs command shells (they need a Tauri AppHandle) - the *_core split at library_commands.rs:209 is what makes them testable, so keep that boundary."
  },
  {
    "subsystem": "cleanup, environment, settings and model profiles (src-tauri: cleanup.rs / environment.rs / diagnostics.rs / llm_commands.rs, plus their real dependencies llm_profiles.rs, llm_gateway.rs, job_commands.rs)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "role": "Two artifact-reduction routines plus the AuthoringProjectV1 manifest writer. Deletes a HARDCODED file/dir list under jobs/<job_id>/ and jobs-scoped entries in <root>/cache/parser. No startup/exit/cancel reaper, no DB awareness.",
        "sizeNote": "213 lines / 8.6 KB — the smallest file in the assignment; no inline tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/diagnostics.rs",
        "role": "The ENTIRE settings persistence layer that exists in Rust: one bool, keepFullProcessArtifacts, in <root>/config/diagnostics-settings.json. There is no other backend app-settings store.",
        "sizeNote": "30 lines / 911 bytes; no inline tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "role": "Sidecar/binary discovery, EPIC8_* env-flag readers, and environment_preflight_report() — a synchronous 17-check report that spawns 5-7 subprocesses. Also owns the feature flags document_ir_v2_shadow_enabled / authoring_v2_shadow_enabled / quality_gate_v2_enabled, all hardcoded true.",
        "sizeNote": "706 lines / 24.8 KB; no inline tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "role": "Profile CRUD core (save/delete/test) + base-URL security validation + the provider allow-list, mixed with V1 LLM suggestion apply and vision-answer adoption business logic.",
        "sizeNote": "750 lines / 29.7 KB; no inline tests — everything is tested from lib.rs"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs",
        "role": "NOT in the assignment but is the actual LlmProfile schema owner: file layout, OS keyring adapter, plaintext fallback, redaction for UI, and the synthetic placeholder profile. Any settings refactor must move this too.",
        "sizeNote": "258 lines / 8.7 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "role": "The other half of the provider-enum mismatch. require_openai_compatible_provider() is the single routing gate; all four gateway commands funnel through it via openai_chat_completions_endpoint().",
        "sizeNote": "54 KB; only 4 lines mention provider (135-153)"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "role": "Thin async cores for list_llm_profiles / run_environment_preflight / get+save_diagnostics_settings (lines 272-297).",
        "sizeNote": "10.3 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "SaveLlmProfileInput struct (335-352), all #[tauri::command] wrappers (1046-1160), the invoke_handler list (1415-1475), the setup() hook (1408-1414), and ALL 136 tests covering this subsystem.",
        "sizeNote": "449 KB — the tests for all four assigned modules live here, not beside them"
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/settings/SettingsPage.tsx",
        "role": "New (Phase 1-3) settings page. Already narrows providers to 2 and hides forceJson/temperature/enabled. NOTE: this file was being rewritten by another agent while I read it (193 lines at 14:32 -> 412 lines at 14:38); line numbers below are from the 412-line version.",
        "sizeNote": "412 lines / 15.7 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/settings/appSettings.ts",
        "role": "keepSourceFiles / nasDestination / localConcurrency / cloudConcurrency / developerMode live ONLY in localStorage. Its own header comment (lines 5-7) admits the backend has no matching commands.",
        "sizeNote": "89 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src/types/settings.ts",
        "role": "LlmProvider union still lists 4 providers (line 2) and types SaveLlmProfileInput.provider, so an unsupported provider is still type-legal.",
        "sizeNote": "52+ lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src/pages/Settings.tsx",
        "role": "Old settings page. Now DEAD CODE — no file in src/ imports \"pages/Settings\". Still contains the AnthropicCompatible/Custom <option>s (289-292) and the 700ms autosave the plan objects to (151-178).",
        "sizeNote": "~330 lines"
      }
    ],
    "publicApi": [
      {
        "symbol": "cleanup_transient_job_artifacts",
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:99",
        "signature": "pub(crate) fn cleanup_transient_job_artifacts(\n    root: &Path,\n    job_id: &str,\n    export_summary: Value,\n) -> CommandResult<Value>",
        "notes": "The \"publish succeeded\" cleanup. Writes authoring-project.json first, then if keepFullProcessArtifacts is false: removes dirs [\"cache\",\"preview\",\"llm-suggestions\"] (cleanup.rs:121) and files document-ir.json, split-candidates.json, pipeline-report.json, pipeline-report-summary.json, llm-last-suggestion.json, llm-calls.jsonl, vision-transcription-output.json, vision-answer-output.json, vision-transcription.txt, manual-transcription.txt, validation-report.json, publish-readiness-report.json (cleanup.rs:124-137), then cleanup_parser_cache_for_job. Sets job.status = JobStatus::Cleaned and current_step = WorkflowStep::Export (cleanup.rs:154-157). Returns CleanupSummaryV1 and also writes cleanup-summary.json ONLY in the retention branch (cleanup.rs:117)."
      },
      {
        "symbol": "minimize_process_artifacts_after_authoring",
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:161",
        "signature": "pub(crate) fn minimize_process_artifacts_after_authoring(\n    root: &Path,\n    job_id: &str,\n    reason: &str,\n) -> CommandResult<Value>",
        "notes": "Runs right after draft generation, NOT at publish. Same dir list; file list differs from the export variant: it ALSO deletes publish-readiness-report.json/validation-report.json but NOT pipeline-report-summary.json (compare cleanup.rs:124-137 vs 184-196). Does not change job status. Returns ArtifactMinimizationV1."
      },
      {
        "symbol": "write_authoring_project",
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:38",
        "signature": "pub(crate) fn write_authoring_project(\n    root: &Path,\n    job_id: &str,\n    export_summary: Option<Value>,\n) -> CommandResult<Value>",
        "notes": "Hard dependency on V1: `let authoring: Value = read_json(&dir.join(\"authoring-ir.json\"))?;` (cleanup.rs:45) uses read_json, not read_json_opt. If authoring-ir.json is absent this errors and BOTH cleanup entry points fail. Emits schemaVersion \"AuthoringProjectV1\" to jobs/<id>/authoring-project.json."
      },
      {
        "symbol": "validation_summary",
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:28",
        "signature": "pub(crate) fn validation_summary(report: &Value) -> Value",
        "notes": "Also consumed by export_pack.rs:173."
      },
      {
        "symbol": "DiagnosticsSettings",
        "file": "F:/workspace/PDF2Test/src-tauri/src/diagnostics.rs:6",
        "signature": "#[derive(Debug, Serialize, Deserialize, Clone, Default)]\npub struct DiagnosticsSettings {\n    #[serde(rename = \"keepFullProcessArtifacts\")]\n    pub keep_full_process_artifacts: bool,\n}",
        "notes": "One field, defaults to false. Path: root.join(\"config\").join(\"diagnostics-settings.json\") (diagnostics.rs:12-14). Missing file returns Default (diagnostics.rs:18-20)."
      },
      {
        "symbol": "load_diagnostics_settings / write_diagnostics_settings",
        "file": "F:/workspace/PDF2Test/src-tauri/src/diagnostics.rs:16,24",
        "signature": "pub(crate) fn load_diagnostics_settings(root: &Path) -> CommandResult<DiagnosticsSettings>\npub(crate) fn write_diagnostics_settings(\n    root: &Path,\n    settings: &DiagnosticsSettings,\n) -> CommandResult<DiagnosticsSettings>",
        "notes": "Only three production readers: cleanup.rs:105, cleanup.rs:167, runtime_validation.rs:382 (where it gates writing publish-readiness-report.json)."
      },
      {
        "symbol": "environment_preflight_report",
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:313",
        "signature": "pub(crate) fn environment_preflight_report() -> Value",
        "notes": "Returns {schemaVersion:\"EnvironmentPreflightV1\", ok, errors, warnings, checks[], generatedAt}. `ok` = errors == 0 where errors counts only checks with severity==\"error\" (environment.rs:683-689) — and NO check in the file ever uses \"error\" (all are \"warning\"/\"info\"; see the severity list at environment.rs:321-673). So ok is unconditionally true. Fully synchronous: spawns node --version, every python candidate twice (once at 360-364, again inside resolve_python_command at 398), a pypdf import, and on macOS sips."
      },
      {
        "symbol": "ResolvedCommand",
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:10",
        "signature": "#[derive(Debug, Clone)]\npub(crate) struct ResolvedCommand {\n    pub(crate) program: String,\n    pub(crate) args: Vec<String>,\n    pub(crate) source: String,\n}",
        "notes": "Has fn display() -> String (environment.rs:26)."
      },
      {
        "symbol": "environment feature flags",
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:219,223,227",
        "signature": "pub(crate) fn document_ir_v2_shadow_enabled() -> bool { true }\npub(crate) fn authoring_v2_shadow_enabled() -> bool { true }\npub(crate) fn quality_gate_v2_enabled() -> bool { true }",
        "notes": "Hardcoded true, no env read. These three are the V2 kill switches the convergence relies on; they are already permanently on."
      },
      {
        "symbol": "environment env-flag readers",
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:177,187,211,215,231",
        "signature": "pub(crate) fn runtime_gate_strict_mode() -> bool          // EPIC8_RUNTIME_GATE_STRICT, default true\npub(crate) fn node_validator_diagnostics_enabled() -> bool // EPIC8_NODE_VALIDATOR_DIAGNOSTICS, default false\npub(crate) fn cloud_pdf_vision_enabled() -> bool           // EPIC8_ENABLE_CLOUD_PDF_VISION, default false\npub(crate) fn local_ocr_enabled() -> bool                  // EPIC8_ENABLE_LOCAL_OCR, default false\npub(crate) fn pdf_renderer_setting() -> String             // EPIC8_PDF_RENDERER, default \"auto\"",
        "notes": "Every user-facing toggle the plan wants in Settings is currently an environment variable with no command to set it. pdf_renderer_setting accepts only auto|none|sips|pdfium|poppler|pymupdf (environment.rs:236-239)."
      },
      {
        "symbol": "SUPPORTED_LLM_PROVIDERS",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:78",
        "signature": "const SUPPORTED_LLM_PROVIDERS: [&str; 4] = [\n    \"OpenAiCompatible\",\n    \"AnthropicCompatible\",\n    \"Ollama\",\n    \"Custom\",\n];",
        "notes": "THE WRITE SIDE of the §7.14 mismatch. Enforced at llm_commands.rs:89-91: `if !SUPPORTED_LLM_PROVIDERS.contains(&input.provider.as_str()) { return Err(format!(\"unsupported_llm_provider:{}\", input.provider)); }`"
      },
      {
        "symbol": "require_openai_compatible_provider",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:135",
        "signature": "fn require_openai_compatible_provider(profile: &Value) -> CommandResult<()> {\n    let provider = profile\n        .get(\"provider\")\n        .and_then(Value::as_str)\n        .map(str::trim)\n        .filter(|value| !value.is_empty())\n        .ok_or_else(|| \"llm_provider_missing\".to_string())?;\n    if !matches!(provider, \"OpenAiCompatible\" | \"Ollama\") {\n        return Err(format!(\n            \"llm_provider_unsupported_for_openai_route:{}\",\n            provider\n        ));\n    }\n    Ok(())\n}",
        "notes": "THE READ SIDE of the §7.14 mismatch — the matches! is at llm_gateway.rs:146. Called only from openai_chat_completions_endpoint (llm_gateway.rs:156), which is called only from openai_post_once (llm_gateway.rs:378). All four gateway commands (classify_group/extract_group/test_profile, transcribe_pdf_images, extract_pdf_image_answers, generate_pdf_reading_outline — dispatch at llm_gateway.rs:36-48) go through it, so the gate is complete but fires at HTTP-request time, not at save time."
      },
      {
        "symbol": "save_llm_profile_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:85",
        "signature": "pub(crate) fn save_llm_profile_core(\n    root: &Path,\n    input: SaveLlmProfileInput,\n) -> CommandResult<Value>",
        "notes": "Rebuilds the profile as a fresh json! object with exactly these keys (llm_commands.rs:137-151): profileId, name, provider, baseUrl, model, temperature, timeoutMs, forceJson, enabled, hasApiKey, apiKeySecretRef, secretStorageBackend, secretStorageMessage. Any other field on an existing profile is DROPPED on save. profileId defaults to format!(\"profile-{}\", Uuid::new_v4().simple()) (llm_commands.rs:93-95). Upsert semantics: retain(!= id) then insert(0, profile) (158-160)."
      },
      {
        "symbol": "SaveLlmProfileInput",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:335",
        "signature": "#[derive(Debug, Serialize, Deserialize, Clone)]\npub struct SaveLlmProfileInput {\n    #[serde(rename = \"profileId\")]\n    pub profile_id: Option<String>,\n    pub name: String,\n    pub provider: String,\n    #[serde(rename = \"baseUrl\")]\n    pub base_url: String,\n    pub model: String,\n    #[serde(rename = \"apiKey\")]\n    pub api_key: Option<String>,\n    pub temperature: f64,\n    #[serde(rename = \"timeoutMs\")]\n    pub timeout_ms: u64,\n    #[serde(rename = \"forceJson\")]\n    pub force_json: bool,\n    pub enabled: bool,\n}",
        "notes": "provider is a bare String, not an enum — nothing type-level stops an unsupported value. No capability fields (supportsVisionImages etc.) exist here."
      },
      {
        "symbol": "validate_llm_base_url",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:30",
        "signature": "fn validate_llm_base_url(base_url: &str) -> CommandResult<()>",
        "notes": "Rejects '@' in URL (credentials), non-http(s) schemes, empty host; allows plain http only for localhost/::1, private/loopback/link-local IPv4 literals, host.docker.internal, and *.local (llm_commands.rs:61-68). Duplicated with a slightly different implementation in llm_gateway.rs:161-188 (that one also rejects fragments and handles IPv6 unicast-link-local). Two divergent copies of the same policy."
      },
      {
        "symbol": "delete_llm_profile_core / test_llm_profile_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:165,173",
        "signature": "pub(crate) fn delete_llm_profile_core(root: &Path, profile_id: &str) -> CommandResult<Vec<Value>>\npub(crate) fn test_llm_profile_core(root: &Path, profile_id: &str) -> CommandResult<Value>",
        "notes": "test returns {ok, message, latencyMs} (llm_commands.rs:209-211) and never surfaces a typed error — a provider mismatch appears only as message = \"LLM gateway failed: llm_provider_unsupported_for_openai_route:AnthropicCompatible\". It reads the keyring 2N+1 times per call (load_profiles at 175, find_profile->load_profiles at 184, load_llm_api_key at 197)."
      },
      {
        "symbol": "llm_run_group_core / apply_llm_suggestion_core / apply_vision_answer_candidates_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:214,263,453",
        "signature": "pub(crate) fn llm_run_group_core(root: &Path, job_id: &str, group_id: &str, profile_id: &str, mode: &str) -> CommandResult<Value>\npub(crate) fn apply_llm_suggestion_core(root: &Path, job_id: &str, suggestion_id: &str, selected_paths: Vec<String>, question_ids: Option<Vec<String>>, user_confirmed: bool) -> CommandResult<Value>\npub(crate) fn apply_vision_answer_candidates_core(root: &Path, job_id: &str, decisions: Value) -> CommandResult<Value>",
        "notes": "All three are V1-only: they read and write jobs/<id>/authoring-ir.json directly and never touch the V2 revision store. §17.11 wants the first two removed from the new UI. apply_llm_suggestion_core's quote verification is already conditional on document-ir.json still existing (llm_commands.rs:346) because cleanup deletes it."
      },
      {
        "symbol": "llm_profiles storage layout",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs:10,14,25,44,48",
        "signature": "fn profiles_path(root: &Path) -> PathBuf      // root/config/llm-profiles.json\nfn secret_path(root: &Path, profile_id: &str) -> PathBuf // root/config/secrets/<id>.key\nfn os_secret_service() -> &'static str { \"com.ielts.author.studio.llm\" }\npub(crate) fn os_secret_ref(profile_id: &str) -> String   // \"os-secret:<service>:<id>\"\npub(crate) fn file_secret_ref(profile_id: &str) -> String // \"profile-secret:<id>\"",
        "notes": "llm-profiles.json is a bare JSON array of untyped objects — Vec<Value>, no struct, no schema file in contracts/."
      },
      {
        "symbol": "save_profile_secret",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs:172",
        "signature": "pub(crate) fn save_profile_secret(\n    root: &Path,\n    profile_id: &str,\n    api_key: Option<&str>,\n) -> CommandResult<(bool, String, String)>",
        "notes": "Returns (has_api_key, backend, message) where backend is \"os\"|\"file\"|\"none\". keyring::Entry first; on failure it errors with os_secret_unavailable_plaintext_fallback_disabled unless EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK is set (llm_profiles.rs:200-206). DANGER: an empty-after-trim key takes the else-branch at 178-186 and DELETES the stored secret."
      },
      {
        "symbol": "load_profiles",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs:235",
        "signature": "pub(crate) fn load_profiles(root: &Path) -> CommandResult<Vec<Value>>",
        "notes": "When config/llm-profiles.json is absent it SYNTHESIZES one profile (llm_profiles.rs:238-241): {\"profileId\":\"profile-local-placeholder\",\"name\":\"Local JSON Gateway\",\"provider\":\"OpenAiCompatible\",\"baseUrl\":\"http://localhost:11434/v1\",\"model\":\"local-structurer\",\"temperature\":0,\"timeoutMs\":60000,\"forceJson\":true,\"enabled\":true}. Every returned profile passes through redact_profile_for_ui, which does an OS keyring read per profile (llm_profiles.rs:137)."
      },
      {
        "symbol": "redact_profile_for_ui",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs:129",
        "signature": "pub(crate) fn redact_profile_for_ui(root: &Path, mut profile: Value) -> Value",
        "notes": "Removes apiKey; injects hasApiKey, apiKeySecretRef (removed when backend is \"none\"), secretStorageBackend, and secretStorageMessage — the last is exactly the \"storage backend 技术文案\" §14.4 says must not be shown. It is still returned on every profile; only the new UI declines to render it."
      },
      {
        "symbol": "select_llm_profile",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:115",
        "signature": "pub(crate) fn select_llm_profile(\n    root: &Path,\n    job: &ImportJob,\n    requested_profile_id: Option<String>,\n) -> Option<String>",
        "notes": "Auto-selection scoring is (hasApiKey?2:0, !placeholder?1:0, provider==\"OpenAiCompatible\"?1:0) at auto_pipeline.rs:161-165. OpenAiCompatible is only the LOWEST-weight tiebreak, so an AnthropicCompatible profile with an API key outranks an OpenAiCompatible one without — it gets selected and then dies in the gateway. Missing `enabled` is treated as true (auto_pipeline.rs:128, 139)."
      },
      {
        "symbol": "tauri command surface (settings/diagnostics/profiles)",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:1046-1096",
        "signature": "#[tauri::command] async fn list_llm_profiles(app: AppHandle) -> CommandResult<Vec<Value>>\n#[tauri::command] async fn run_environment_preflight(_app: AppHandle) -> CommandResult<Value>\n#[tauri::command] async fn get_diagnostics_settings(app: AppHandle) -> CommandResult<DiagnosticsSettings>\n#[tauri::command] async fn save_diagnostics_settings(settings: DiagnosticsSettings, app: AppHandle) -> CommandResult<DiagnosticsSettings>\n#[tauri::command] async fn save_llm_profile(input: SaveLlmProfileInput, app: AppHandle) -> CommandResult<Value>\n#[tauri::command] async fn delete_llm_profile(profile_id: String, app: AppHandle) -> CommandResult<Vec<Value>>\n#[tauri::command] async fn test_llm_profile(profile_id: String, app: AppHandle) -> CommandResult<Value>",
        "notes": "Registered at lib.rs:1440-1447. Only test_llm_profile uses spawn_blocking (lib.rs:1093); run_environment_preflight does not, despite spawning 5-7 subprocesses. There is NO command for cleanup, no command for app settings (keepSourceFiles / concurrency), and no get_app_settings/save_app_settings anywhere in the invoke_handler list (lib.rs:1415-1475)."
      }
    ],
    "dataFlow": "SETTINGS WRITE PATH. The frontend has two disjoint stores. (a) Model connection goes to Rust: SettingsPage.saveAndTest -> tauriCommands.saveLlmProfile({input}) -> save_llm_profile (lib.rs:1076) -> ensure_app_dirs -> save_llm_profile_core (llm_commands.rs:85). That function (1) checks input.provider against SUPPORTED_LLM_PROVIDERS, (2) validate_llm_base_url, (3) if api_key.is_some() calls save_profile_secret -> keyring::Entry(\\\"com.ielts.author.studio.llm\\\", profile_id).set_password, falling back to <root>/config/secrets/<id>.key only under EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK, (4) rebuilds a fixed 13-key JSON object, (5) load_profiles -> retain -> insert(0) -> save_profiles writes the whole array to <root>/config/llm-profiles.json. (b) Everything else the plan calls a setting — keepSourceFiles, nasDestination, localConcurrency, cloudConcurrency, developerMode — never leaves the browser: appSettings.ts writes localStorage key \\\"ielts-author-studio.app-settings.v1\\\" (appSettings.ts:11, 59-73). The only true backend setting is keepFullProcessArtifacts -> save_diagnostics_settings -> <root>/config/diagnostics-settings.json.\n\nSETTINGS READ PATH. list_llm_profiles -> load_profiles -> for each profile redact_profile_for_ui, which performs one OS keyring get_password per profile (llm_profiles.rs:137) and stamps hasApiKey/secretStorageBackend/secretStorageMessage. Because find_profile also calls load_profiles, a single test_llm_profile does 2N+1 keyring reads.\n\nGATEWAY PATH. A profile is chosen either explicitly (UI passes profileId) or by select_llm_profile (auto_pipeline.rs:115) from job.active_llm_profile_id then by score. The chosen id is resolved with find_profile, reduced to a 7-field payload by make_llm_input / profile_payload (llm_suggestions.rs:139-147, 154-164) — provider, baseUrl, model, temperature, timeoutMs, forceJson, profileId — and the secret is fetched separately by load_llm_api_key and passed as an out-of-band argument, never inside the payload. run_llm_gateway (llm_gateway.rs:23) writes a redacted copy of the input to jobs/<id>/cache/llm/<cmd>-input-<ts>.json, dispatches on command_name to one of four routes, appends a record to jobs/<id>/llm-calls.jsonl, and writes the output next to the input. Every route reaches openai_post_once -> openai_chat_completions_endpoint -> require_openai_compatible_provider, so a non-OpenAI/Ollama provider fails at request time with llm_provider_unsupported_for_openai_route.\n\nCLEANUP PATH. Two functions, six call sites, all inside other commands — there is no cleanup command and no scheduler. minimize_process_artifacts_after_authoring fires at the END of draft generation: authoring_commands.rs:458 (\\\"build_authoring_ir\\\"), auto_pipeline.rs:2186 (\\\"run_auto_pipeline\\\"), and on publish-gate failure at export_pack.rs:147, export_pack.rs:254, export_nas_library.rs:1118. cleanup_transient_job_artifacts fires only on successful V1 export: export_pack.rs:184 (single reading-assets), export_pack.rs:303 (reading-js batch), export_nas_library.rs:1516 (NAS library). Both first call write_authoring_project (which read_json's authoring-ir.json), then consult load_diagnostics_settings and return early if keepFullProcessArtifacts. Deletion is a literal list against jobs/<job_id>/ plus a prefix scan of <root>/cache/parser for entries starting with job_id (cleanup.rs:76-97). Nothing consults the SQLite library_items table, no asset reference counting, no age policy.\n\nWHAT SURVIVES CLEANUP (verified against the dirs ensure_job_dirs/ensure_job_artifact_layout create): job.json, authoring-ir.json, authoring-project.json, source-review.json, uploads/, exports/, and — unlisted by the summary but never deleted — sources/, extraction/, assets/{blobs,metadata,previews}/, authoring/{revisions,patches}/, export-history/, legacy/, authoring-ir-v2.shadow.json, authoring-ir-v2.shadow.{error,compare}.json, document-ir-v2.shadow.json, document-ir-v2.shadow.{error,overlay.svg,compare}.json. The V2 workspace is therefore cleanup-safe today: get_authoring_v2 -> build_editor_session -> load_current_authoring reads authoring/revisions/<n>.json or falls back to authoring-ir-v2.shadow.json (authoring_v2_commands.rs:725-742), and refresh_quality_report reads document-ir-v2.shadow.json (744-748) — none of which cleanup touches.",
    "gapsVsPlan": [
      {
        "claim": "§7.14: \"当前 UI 可选择 AnthropicCompatible 和 Custom，但 gateway 路由只接受 OpenAiCompatible 和 Ollama\"",
        "reality": "Directionally right but understated: the mismatch is not only in the UI, it is in the Rust write path. llm_commands.rs:78-83 accepts all four strings and persists them; llm_gateway.rs:146 rejects two of them at HTTP time. So the invalid state is durably storable, auto-selectable, and only reported as a free-text test-connection failure.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_commands.rs:78-83 and :89-91 (accept) vs src-tauri/src/llm_gateway.rs:146-151 (reject); src/types/settings.ts:2 and src/pages/Settings.tsx:289-292 (UI union still 4-wide)"
      },
      {
        "claim": "§11.5: cleanup_on_startup deletes tmp jobs by age (completed 24h / failed 7d / orphaned 24h) and GCs unreferenced assets after a 7-day grace period",
        "reality": "Does not exist in any form. The only startup work is lib.rs setup(): ensure_app_dirs plus library_commands::migrate_existing_into_library. No age tracking, no orphan detection, no asset reference counting anywhere in cleanup.rs.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/lib.rs:1408-1414 (whole setup hook); src-tauri/src/cleanup.rs:1-213 (no time or DB logic; grep for older_than/retention/prune over src-tauri/src returns only db.rs:781 prune_orphan_exams, unrelated to job dirs)"
      },
      {
        "claim": "§11.5: cleanup_after_canonical_commit keeps only [source_if_retention_enabled, assets_referenced_by_canonical_ds, minimal_processing_summary]",
        "reality": "cleanup_transient_job_artifacts is the nearest thing but is keyed to V1 export success, keeps a hardcoded list rather than a reference set, never inspects assets, and its own reported `kept` array is incomplete — it omits sources/, extraction/, assets/*, authoring/revisions/, authoring/patches/, export-history/, legacy/ and every *.shadow.* file, all of which are retained.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/cleanup.rs:121-139 (deletion lists), :148 (the inaccurate `kept` literal) vs src-tauri/src/util.rs:223-244 and src-tauri/src/artifact_store.rs:147-164 (the 13-15 dirs actually created per job)"
      },
      {
        "claim": "§11.5: cleanup_on_close_best_effort flushes editor buffers, marks running jobs interrupted, deletes disposable render cache — and \"应用正常关闭不是唯一触发点\"",
        "reality": "No exit hook at all. lib.rs:1404-1477 registers no on_window_event and no RunEvent handler; tauri::Builder goes straight from invoke_handler to .run(). Nothing marks interrupted jobs and nothing deletes render cache on close or after a crash.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/lib.rs:1403-1478 (entire run(); grep for RunEvent/on_window_event in lib.rs returns nothing)"
      },
      {
        "claim": "§17.17: \"启动、成功、取消、退出四个触发点\"",
        "reality": "Two categories exist, neither of them the plan's four. Success is only V1 export (export_pack.rs:184/303, export_nas_library.rs:1516); the V2 publisher never calls cleanup and never touches job status at all, so once §17.24 retires V1 export the success trigger vanishes. There is no cancel path (no cancel command exists) and no startup/exit path. The de-facto third trigger the plan does not mention is \"immediately after draft generation\" (authoring_commands.rs:458, auto_pipeline.rs:2186).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/nas_package_v2.rs (grep cleanup|minimize returns only its own local cleanup_transaction_artifacts at :1069; grep update_job|JobStatus:: returns nothing); src-tauri/src/authoring_commands.rs:458; src-tauri/src/auto_pipeline.rs:2186"
      },
      {
        "claim": "§17.17: \"清理失败记录日志，不中断用户关闭\"",
        "reality": "The opposite today. Every call site propagates with `?`, including inside `let _ = ...?`, so an I/O failure aborts the caller. remove_file_if_exists / remove_dir_if_exists turn any fs error into Err (util.rs:281-295). At export_pack.rs:184 the export files are already on disk and job.status is already Exported (export_pack.rs:180-183) when cleanup runs, so a single locked file makes a successful publish report failure.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/export_pack.rs:180-184; src-tauri/src/authoring_commands.rs:458; src-tauri/src/auto_pipeline.rs:2186; src-tauri/src/util.rs:281-295"
      },
      {
        "claim": "§17.17: \"当前 kept 列表不再固定保留 job/project/source-review/uploads/exports\"",
        "reality": "Confirmed — the list is a literal in the summary JSON and the retention behaviour is a literal in code, with no policy input.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/cleanup.rs:148 and :208"
      },
      {
        "claim": "§17.23: \"环境检测在应用启动后台运行一次\"",
        "reality": "Not implemented. environment_preflight_report runs on demand from the settings page mount (SettingsPage.tsx:97) and nowhere else, and it runs synchronously on the Tauri async runtime — run_environment_preflight is the one command in this group that does NOT use spawn_blocking, while it spawns 5-7 subprocesses (node, each python candidate probed twice, pypdf import).",
        "verdict": "wrong",
        "evidence": "src-tauri/src/lib.rs:1051-1054 vs :1089-1096 (test_llm_profile's spawn_blocking); src-tauri/src/job_commands.rs:278-280; src-tauri/src/environment.rs:316, 360-364, 398-399"
      },
      {
        "claim": "§17.23: \"只有 blocking 项显示用户摘要\"",
        "reality": "Unimplementable against the current report. Not one of the 17 checks is emitted with severity \"error\" — all are \"warning\" or \"info\" — so report.ok is always true and the new page's blockingChecks filter (severity === \"error\") can never match. The alert block is dead code until the backend classifies blockers.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/environment.rs:321-673 (every severity literal) and :683-696 (errors/warnings counting); src/features/settings/SettingsPage.tsx:113-115, 201-203"
      },
      {
        "claim": "§17.23: \"不让可选 Python/Poppler 缺失阻止 born-digital PDF 主链\"",
        "reality": "Already satisfied. python and python:pypdf are severity \"warning\" with messages that explicitly say Rust parsers are primary, and no gate reads the preflight result.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/environment.rs:370-396 (python), :410-426 (pypdf)"
      },
      {
        "claim": "§14.2: \"不受支持 provider -> 删除\"",
        "reality": "Done on the new page only. SettingsPage.tsx narrows to two providers with an explicit comment citing llm_gateway.rs:146, but src/types/settings.ts:2 still exports the 4-wide LlmProvider union that types SaveLlmProfileInput.provider, and the Rust allow-list is untouched — so the invalid value is still type-legal and still accepted.",
        "verdict": "partly-wrong",
        "evidence": "src/features/settings/SettingsPage.tsx:28-32 vs src/types/settings.ts:2 and src-tauri/src/llm_commands.rs:78-83"
      },
      {
        "claim": "§14.2: advanced settings show 请求超时 / 云端并发数 / 本地并发数 / Ollama 模式 / 过程文件保留 / 环境诊断",
        "reality": "Only two of the six have a backend. Request timeout is profile.timeoutMs (clamped 1000..300000 at llm_gateway.rs:121-128) and 过程文件保留 is keepFullProcessArtifacts. Ollama is a provider string. Cloud and local concurrency have NO Rust representation whatsoever — no command, no config file; appSettings.ts keeps them in localStorage and its own comment says the backend will get them at P9.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/lib.rs:1415-1475 (full invoke_handler, no app-settings command); src/features/settings/appSettings.ts:5-7, 20-21"
      },
      {
        "claim": "§14.3: \"第一版普通用户只维护一个 active profile\"",
        "reality": "Cannot hold with the current store. When config/llm-profiles.json is absent load_profiles synthesizes the profile-local-placeholder entry, and save_llm_profile_core builds its new array from that same load_profiles result — so the very first real save persists the placeholder alongside the real profile, permanently. delete_llm_profile_core has the same effect. select_llm_profile has to special-case the id (auto_pipeline.rs:155) and the new UI has to filter it out by string (SettingsPage.tsx:26, 74).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_profiles.rs:236-247; src-tauri/src/llm_commands.rs:152-161; src-tauri/src/auto_pipeline.rs:52,155; src/features/settings/SettingsPage.tsx:26,74"
      },
      {
        "claim": "§14.4: \"不要在 Base URL 每输入一个字符时 700ms 自动保存并立即使配置生效\"",
        "reality": "Fixed by construction, not by deletion. The 700ms debounce still exists at src/pages/Settings.tsx:151-178, but that file is now unreferenced — no file under src/ imports \"pages/Settings\", and it is not in router.ts's LEGACY_PAGES list. The new page saves only on explicit 保存并测试.",
        "verdict": "already-done",
        "evidence": "src/pages/Settings.tsx:151-178 (dead); src/app/App.tsx:5 imports only features/settings/SettingsPage; src/app/router.ts:11-24 (LEGACY_PAGES has no \"settings\")"
      },
      {
        "claim": "§14.4: \"API Key 继续使用现有安全存储，但不展示 storage backend 技术文案\"",
        "reality": "Half done. Storage is unchanged and correct (keyring first, plaintext only behind EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK). But secretStorageBackend and secretStorageMessage are still injected into every profile the backend returns and still persisted into llm-profiles.json; only the new UI declines to render them.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_profiles.rs:158-168; src-tauri/src/llm_commands.rs:149-150"
      },
      {
        "claim": "§14.1: \"保留原始 PDF 以便后续核对 [开关]\"",
        "reality": "The toggle exists in the UI and writes localStorage, but it is a no-op: nothing in Rust ever deletes uploads/ or sources/, and nothing reads a source-retention flag. Setting it to false changes no backend behaviour.",
        "verdict": "partly-wrong",
        "evidence": "src/features/settings/SettingsPage.tsx:277-279 and appSettings.ts:16-17 vs src-tauri/src/cleanup.rs:121-139, :184-198 (uploads/ and sources/ appear in neither deletion list; grep keep_source_files over src-tauri returns nothing)"
      },
      {
        "claim": "§17.25: new schemas incl. actionable-issue / publish-check-result; profile & diagnostics contracts",
        "reality": "Nothing in this subsystem has a schema today. contracts/ holds only common-v2, content-doc-v2, document-ir-v2, ielts-authoring-ir-v2, listening-*, quality-report-v2, reading-exam-source-v2. LlmProfile is Vec<Value>, DiagnosticsSettings is a 1-field struct, EnvironmentPreflightV1 is an ad-hoc json! literal.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/contracts/ listing; src-tauri/src/schema/ listing (9 modules, none for settings)"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "SUPPORTED_LLM_PROVIDERS (line 78-83) and save_llm_profile_core's check (line 89-91)",
        "whatToChange": "Cut the array to [\"OpenAiCompatible\", \"Ollama\"] so the save path and llm_gateway.rs:146 share one definition. Better: replace the const with a `pub(crate) enum LlmProvider` used by both modules and by SaveLlmProfileInput.provider, so the compiler enforces parity instead of two string lists. The existing test at lib.rs:10538-10551 uses \"MistralNative\" and will still pass; add a case asserting \"AnthropicCompatible\" is now rejected.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/types/settings.ts",
        "symbol": "export type LlmProvider (line 2)",
        "whatToChange": "Narrow to \"OpenAiCompatible\" | \"Ollama\". This is what makes the §7.14 fix type-enforced across the frontend — SettingsPage already only emits those two, but the union currently lets any caller (including devFallbackBackend and the dead pages/Settings.tsx) pass the others.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/pages/Settings.tsx",
        "symbol": "provider <select> (lines 287-294) and the 700ms autosave effect (151-178)",
        "whatToChange": "Delete the file — it is already unreferenced (nothing imports \"pages/Settings\", and \"settings\" is not in router.ts's LEGACY_PAGES). Leaving it keeps the AnthropicCompatible/Custom options and the debounced autosave alive as a copy-paste hazard and will keep tripping greps for §7.14/§14.4.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "symbol": "environment_preflight_report (line 313) severity assignment throughout, and the errors/warnings tally at 683-696",
        "whatToChange": "Introduce a real \"error\" severity for the checks that actually block the born-digital main chain, or add an explicit `blocking: bool` field. Today no check is ever \"error\", so `ok` is hardcoded-true in effect and the new page's blocking-alert path (SettingsPage.tsx:113-115, 201-203) is unreachable. §17.23's \"只有 blocking 项显示用户摘要\" cannot be built until this classification exists. Per §17.23 keep Python/pypdf/sidecars as non-blocking.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "symbol": "environment_preflight_report python probing (lines 360-364 and 398-399)",
        "whatToChange": "resolve_python_command() at 398 re-probes the same candidates already probed at 360-364, doubling subprocess spawns. Probe once and reuse. Needed before this can run on startup at all.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "run_environment_preflight (lines 1051-1054) and the setup() hook (1408-1414)",
        "whatToChange": "Two edits for §17.23. (1) Wrap the core in tauri::async_runtime::spawn_blocking exactly as test_llm_profile does at 1093 — it currently blocks the async runtime while spawning 5-7 processes. (2) Kick the report off once from setup() into a cached OnceLock/State and have the command serve the cache, so the settings page stops paying for it on every mount (SettingsPage.tsx:97).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "write_authoring_project (line 45: read_json(&dir.join(\"authoring-ir.json\"))?)",
        "whatToChange": "THE blocking dependency for the whole convergence. Both cleanup entry points call this first, and it hard-errors if the V1 authoring-ir.json is missing. The moment the canonical DS stops writing authoring-ir.json, every cleanup call — and therefore build_authoring_ir, run_auto_pipeline, and every export — fails. Rebuild the summary from the V2 revision store (authoring/revisions/<n>.json, or authoring-ir-v2.shadow.json) and make the V1 read optional via read_json_opt.",
        "risk": "high",
        "blockedBy": "Depends on the Canonical DS decision (§4) — which artifact becomes the authoritative input for the processing summary."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "cleanup_transient_job_artifacts (99) and minimize_process_artifacts_after_authoring (161) — the hardcoded dir/file lists at 121-139 and 181-198",
        "whatToChange": "Invert the policy per §17.17: instead of a deletion allowlist, compute the retain set from (a) the library_items row for this id, (b) asset ids referenced by the current DS, (c) the source-retention flag. Note the two lists silently differ today (minimize deletes publish-readiness-report.json and validation-report.json but not pipeline-report-summary.json) — that divergence is unexplained and should collapse into one policy function. Also fix the `kept`/`removed` literals at 148-149 and 208-209, which under-report reality by 8 directories.",
        "risk": "high",
        "blockedBy": "Needs the LibraryRepositoryV2 / canonical-DS asset reference API from §11.2/§11.6, which does not exist yet."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "call sites: authoring_commands.rs:458, auto_pipeline.rs:2186, export_pack.rs:147/184/254/303, export_nas_library.rs:1118/1516",
        "whatToChange": "Change the propagation contract to satisfy §17.17's \"清理失败记录日志，不中断用户关闭\". Every one of these uses `?` (including under `let _ =`), so a locked file fails the surrounding command — worst at export_pack.rs:184, where the export files are already written and job.status is already Exported. Swallow-and-log instead, and return the cleanup outcome as a field rather than as the command's Result.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "new: cleanup_on_startup / cleanup_on_close_best_effort (§11.5 pseudocode)",
        "whatToChange": "Both are absent. Startup needs the reaper wired into lib.rs setup() (currently only ensure_app_dirs + migrate_existing_into_library at 1410-1412); exit needs an on_window_event/RunEvent handler, which lib.rs:1404-1477 does not register at all. Age-based deletion also needs a durable per-job completed/failed timestamp — job.json has created_at/updated_at but no terminal-state time, so the 24h/7d rules have nothing to key on yet.",
        "risk": "high",
        "blockedBy": "Requires the durable ProcessingJob record from §12.2/§17.14; job.json is not authoritative and carries no completion timestamp."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "publish_nas_package_v2_core",
        "whatToChange": "The V2 publisher — the one the converged product uses — calls neither cleanup function and never touches job status. Its only cleanup is cleanup_transaction_artifacts (line 1069), which is package-staging temp removal, unrelated. Once §17.24 retires V1 export, the success-side cleanup trigger and the JobStatus::Cleaned transition both disappear silently. Wire the canonical-commit cleanup here.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "delete_library_exam_core (line 254)",
        "whatToChange": "Soft-deletes the DB row only; jobs/<id>/ (uploads with the original PDFs, all revisions, all shadow artifacts) stays on disk forever with no reaper. Contrast delete_job_core (job_commands.rs:142-156), which hard-deletes the directory. This asymmetry is precisely what §11.5's orphan sweep is for; without it, trash grows unbounded.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "deletion lists — missing shadow sidecars",
        "whatToChange": "document-ir-v2.shadow.overlay.svg, document-ir-v2.shadow.compare.json, document-ir-v2.shadow.error.json, authoring-ir-v2.shadow.compare.json and authoring-ir-v2.shadow.error.json are never deleted (constants at pdf_facts_shadow.rs:19-22 and ielts_grammar/mod.rs:56-58). Overlay SVGs in particular are large debug output. Keep document-ir-v2.shadow.json and authoring-ir-v2.shadow.json — the V2 workspace needs them (authoring_v2_commands.rs:733, 746) — and add the compare/overlay/error sidecars to the diagnostics-only set.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "select_llm_profile (line 115), scoring tuple at 161-165",
        "whatToChange": "Two problems for a one-active-profile product. (1) provider==\"OpenAiCompatible\" is only the lowest tiebreak, so an AnthropicCompatible profile with a key beats an OpenAiCompatible one without and then dies in the gateway — filter unroutable providers out entirely instead of merely deprioritizing. (2) missing `enabled` defaults to true (128, 139), so the synthetic placeholder is always selectable. If §14.1's global 云端识别 switch is meant to be authoritative, that switch needs a representation the backend reads directly, not an inferred any-enabled-profile heuristic.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs",
        "symbol": "load_profiles (line 236-241)",
        "whatToChange": "Stop synthesizing profile-local-placeholder inside the loader, or stop feeding load_profiles' output back into save_profiles. Today the first real save persists the placeholder into llm-profiles.json permanently (llm_commands.rs:152-161), which is why auto_pipeline.rs:155 and SettingsPage.tsx:26/74 both hardcode the id to filter it. §14.3's single-active-profile model needs the store to hold only real profiles.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs",
        "symbol": "save_profile_secret (lines 178-186)",
        "whatToChange": "Latent key-wipe: api_key = Some(\"\") trims to empty, takes the else branch, and DELETES the stored secret while reporting success. No current caller sends it (SettingsPage.tsx:137 sends undefined; pages/Settings.tsx:161 omits the field), but a settings rewrite that sends the controlled input value verbatim will silently destroy the user's key. Make the wipe an explicit clear_profile_secret call and treat Some(\"\") as no-change.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs",
        "symbol": "redact_profile_for_ui (line 137) called per-profile from load_profiles",
        "whatToChange": "Every list_llm_profiles does one OS keyring read per profile, and test_llm_profile_core does 2N+1 (llm_commands.rs:175, 184, 197). On macOS a keychain read can prompt. Cache the has-secret probe or have callers ask for one profile. Matters more once §5.5 concurrency runs several jobs that each call find_profile.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "validate_llm_base_url (line 30) vs llm_gateway.rs openai_chat_completions_endpoint (line 155)",
        "whatToChange": "Two divergent implementations of the same http/https private-host policy: the gateway copy additionally rejects URL fragments and handles IPv6 unicast-link-local; the save-side copy does not. Extract one shared validator so a URL that saves cannot fail at request time for a shape reason.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "save_llm_profile_core profile literal (lines 137-151)",
        "whatToChange": "The rebuild-from-scratch pattern silently drops any field not in the 13-key literal. environment.rs:552-557 advertises profileCapabilityFields [supportsVisionImages, supportsPdfFileInput, maxPdfBytes, maxVisionImages] to the UI, but a repo-wide grep shows NO code reads any of them (only pdf_facts_shadow.rs:1083 uses an unrelated local maxPdfBytes) and save would erase them anyway. Either implement capability gating for the vision/PDF routes and add the fields to SaveLlmProfileInput, or delete the advertisement — it currently promises a feature that cannot be configured.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "test_llm_profile_core return value (lines 209-211)",
        "whatToChange": "Returns {ok, message: \"LLM gateway failed: <raw error string>\", latencyMs}. §15.2 and §17.19 want typed errors the UI does not string-parse; today a provider mismatch, a 401, and a DNS failure are indistinguishable except by prefix. Return a typed code alongside the message.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/diagnostics.rs",
        "symbol": "DiagnosticsSettings (line 6)",
        "whatToChange": "This is the natural home for the settings §14.1/§14.2 actually need in Rust: keepSourceFiles, localConcurrency, cloudConcurrency, developerMode, cloudRecognitionEnabled. They currently live only in localStorage (appSettings.ts:15-24), whose own comment (lines 5-7) says to swap the read/write points for get_app_settings/save_app_settings once Rust owns them. Adding fields to this serde struct is backward compatible (missing keys need #[serde(default)]); the two commands already exist at lib.rs:1056-1067 and can be renamed/extended rather than replaced.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "job.status = JobStatus::Cleaned (line 155)",
        "whatToChange": "Cleaned is a terminal JobStatus (lib.rs:110) that the library layer folds into \"exported\" (library_commands.rs:31) with a special guard against downgrading (library_commands.rs:330-333) and the frontend renders as 已发布并清理过程文件 (src/features/library/libraryTypes.ts:108,133). If §11.3 replaces these statuses with library-item states, this write plus all three of those readers move together.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "minimize at authoring_commands.rs:458 deleting split-candidates.json and document-ir.json",
        "whatToChange": "Regeneration is lossy after minimization: build_authoring_ir_core reads document-ir.json at :403 and split-candidates.json at :404, both read_json_opt, so a re-run with allowOverwrite falls back to make_dynamic_split_candidates(..., None) and produces a strictly worse draft. Same for run_rule_split_core:374. If §17.17 keeps minimizing at draft time, either keep enough physical evidence to regenerate (document-ir-v2.shadow.json is retained and may suffice) or block regeneration explicitly instead of degrading it.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/source_review.rs",
        "symbol": "source_review_status (lines 120-130)",
        "whatToChange": "With doc == None — the post-cleanup state — it returns the saved source-review.json if present, otherwise computes required=false/resolved=true from an empty document. So a job whose source-review.json is missing silently becomes \"nothing to review\" and sails through publish_readiness_gate (runtime_validation.rs:371). auto_pipeline.rs:1465 does write the file today, so the hole is narrow, but any new import path that skips it inherits a silent pass.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "mod tests (1480-…, 136 #[test] fns)",
        "whatToChange": "All four assigned modules have ZERO inline tests; every test that exercises them lives in lib.rs's test module. Splitting cleanup.rs into library/cleanup.rs and profile CRUD into settings/model_profiles.rs per §17.11/§17.17 requires updating the use-lists at lib.rs:1494-1496 and 1521, and the cleanest sequencing is to move the relevant tests down into the new modules in the same commit.",
        "risk": "low"
      }
    ],
    "testHarness": "All tests for this subsystem are `#[cfg(test)] mod tests` inside src-tauri/src/lib.rs (136 #[test] fns starting at lib.rs:1480). cleanup.rs, environment.rs, diagnostics.rs, llm_commands.rs, llm_profiles.rs and llm_gateway.rs each contain zero test modules (verified by grepping for #[cfg(test)]). There is no src-tauri/tests/ integration directory.\n\nHelpers to copy: `fn temp_test_root() -> PathBuf` (lib.rs:1576) returns env::temp_dir().join(format!(\"epic8-test-{}\", Uuid::new_v4().simple())) and every test ends with `let _ = fs::remove_dir_all(root);`. `fn env_test_lock() -> &'static Mutex<()>` (lib.rs:1858) serializes tests that mutate process env — mandatory for anything touching EPIC8_*. `fn make_publishable_fixture(root: &Path) -> (ImportJob, Value)` (lib.rs:1863) builds a job that can reach the publish gate. `fn parser_fixture(name: &str)` (lib.rs:1580) resolves fixtures/parser/<name>. `fn live_llm_profile_from_env()` (lib.rs:1811) gates real-API tests behind EPIC8_LIVE_LLM_*.\n\nExact tests to extend or mirror:\n- lib.rs:8085 `cleanup_respects_diagnostics_artifact_retention` — the only direct cleanup_transient_job_artifacts test. Asserts cleaned == false, cleanup-summary.json exists, document-ir.json / split-candidates.json / validation-report.json survive, and status stays Working. There is NO test of the cleaned == true branch of this function; add one.\n- lib.rs:4438 `auto_pipeline_retains_process_artifacts_only_when_diagnostics_enabled` — retention-on path through run_auto_pipeline, including the <root>/cache/parser retention assertion (4479-4490).\n- lib.rs:10061 `files_pdf_samples_auto_pipeline_minimizes_artifacts_and_preserves_review_gate` — the authoritative minimization contract. Enumerates the 8 files that must be gone (10159-10175), asserts cache/, preview/, llm-suggestions/ are gone (10176-10178), asserts no <root>/cache/parser entry is prefixed with the job id (10179-10193), and asserts authoring-ir.json / authoring-project.json / source-review.json survive (10144-10158). Requires exactly 4 PDFs in Files/ (asserts samples.len() == 4 at 10083-10087) and skips with a message if the dir is empty. Uses run_auto_pipeline_core_with_gateway with a closure gateway that panics if called — the pattern for testing without network.\n- lib.rs:4381 `auto_pipeline_persists_llm_review_in_authoring_ir_after_minimization`.\n- lib.rs:7566 `export_core_publish_gate_failure_writes_no_export_or_cleanup`.\n- lib.rs:10517 `llm_profile_save_rejects_public_http_unknown_provider_and_accepts_localhost` — the provider/base-URL gate. Constructs SaveLlmProfileInput literals directly and asserts on error substrings (unsupported_llm_provider, llm_profile_base_url_insecure_http, llm_profile_base_url_credentials_in_url). This is the test to extend when narrowing SUPPORTED_LLM_PROVIDERS; it already covers \"MistralNative\" but not \"AnthropicCompatible\".\n- lib.rs:2059 `environment_preflight_reports_required_dependency_names` — asserts schemaVersion plus 17 required check names, with cfg!(target_os) branches for renderer:macos-sips and renderer:windows-pdfium. Any preflight restructuring must keep these names or update this list. Note it asserts nothing about severity, which is how the all-\"warning\" state went unnoticed.\n- lib.rs:2011 `secret_file_helpers_reject_unsafe_profile_ids`, lib.rs:2115 `plaintext_secret_fallback_is_disabled_by_default`, lib.rs:2137 `plaintext_secret_fallback_requires_explicit_opt_in` — secret storage; the last two take env_test_lock and set/remove EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK.\n- lib.rs:2040 `llm_cache_input_redacts_api_key` and lib.rs:2162 `make_llm_input_never_contains_api_key` — the two guards that keep secrets out of on-disk cache and gateway payloads. Any change to the profile payload shape must keep both green.\n\nNot covered anywhere, and worth adding alongside the changes: the cleaned == true branch of cleanup_transient_job_artifacts; cleanup behaviour when authoring-ir.json is absent (V2-only job); cleanup error propagation on an undeletable file; any assertion that preflight severities are classifiable; any test of select_llm_profile's provider preference.",
    "surprises": [
      "The provider-enum mismatch is worse than §7.14 describes. The plan frames it as a UI-only problem, but src-tauri/src/llm_commands.rs:78-83 accepts AnthropicCompatible and Custom and persists them to config/llm-profiles.json. auto_pipeline.rs:select_llm_profile (115-172) then treats provider==\"OpenAiCompatible\" as only the lowest of three tiebreaks, so an AnthropicCompatible profile that has an API key OUTRANKS an OpenAiCompatible one that does not, gets auto-selected, and fails in the gateway with llm_provider_unsupported_for_openai_route. Narrowing the frontend dropdown alone does not close this — an existing config file keeps the broken profile.",
      "The V2 publisher never triggers cleanup and never touches job status. nas_package_v2.rs contains no reference to cleanup::* (its cleanup_transaction_artifacts at :1069 is package-staging temp removal) and no reference to update_job or JobStatus at all. Every cleanup_transient_job_artifacts call site is V1 export (export_pack.rs:184/303, export_nas_library.rs:1516). So the moment §17.24 retires V1 export, the success-side cleanup trigger and the JobStatus::Cleaned transition both vanish with no compile error.",
      "No preflight check ever has severity \"error\". All 17 use \"warning\" or \"info\" (environment.rs:321-673), and report.ok is computed as errors == 0 (683-689) — so ok is unconditionally true. The new SettingsPage's blocking-item alert filters on severity === \"error\" (SettingsPage.tsx:113-115), which means §17.23's \"only blocking items get a user summary\" is currently implemented as \"nothing is ever shown\". The existing test (lib.rs:2059) only checks check names, never severities, which is why this went unnoticed.",
      "cleanup.rs's own reported `kept` array is factually wrong. cleanup.rs:148 lists 6 entries, but ensure_job_dirs (util.rs:223-244) and ensure_job_artifact_layout (artifact_store.rs:147-164) create 13-15 directories per job, and cleanup deletes only 3 of them. sources/, extraction/, assets/{blobs,metadata,previews}/, authoring/{revisions,patches}/, export-history/, legacy/ and every *.shadow.* sidecar (including document-ir-v2.shadow.overlay.svg) are silently retained. Anyone reading the summary JSON to reason about disk usage will be misled.",
      "`let _ = cleanup(...)?` does NOT swallow the error. The `?` binds to the call, so an I/O failure aborts the caller. At export_pack.rs:180-184 this happens after the export files are written and after job.status is already set to Exported — a single locked file turns a successful publish into a reported failure with the side effects already committed. §17.17's \"cleanup failure must not interrupt\" is currently inverted at all 8 call sites.",
      "Deleting a library item leaves the entire job directory on disk permanently. delete_library_exam_core (library_commands.rs:254-263) is a DB soft-delete only; delete_job_core (job_commands.rs:142-156) is the only path that removes jobs/<id>/. With no startup reaper (lib.rs setup() at 1408-1414 does only ensure_app_dirs + migrate_existing_into_library) and no exit hook (lib.rs:1404-1477 registers no RunEvent/on_window_event), trashed items keep their original PDFs and full revision history forever.",
      "environment.rs advertises four LLM capability fields that nothing reads and that save would erase anyway. environment.rs:552-557 emits profileCapabilityFields [supportsVisionImages, supportsPdfFileInput, maxPdfBytes, maxVisionImages]; a repo-wide grep finds no reader (the only other hit is an unrelated local constant at pdf_facts_shadow.rs:1083), and save_llm_profile_core's fixed 13-key literal (llm_commands.rs:137-151) would drop them on the next save. The vision and PDF-outline routes therefore run against whatever model is configured with no capability check.",
      "Only ONE real setting exists in Rust. config/diagnostics-settings.json holds a single bool. keepSourceFiles, nasDestination, localConcurrency, cloudConcurrency and developerMode live in localStorage (appSettings.ts:11-24) with no backend counterpart, and appSettings.ts's own header comment (lines 5-7) says so. Notably keepSourceFiles is already a no-op: cleanup never deletes uploads/ or sources/, and grep for keep_source_files over src-tauri returns nothing — so the §14.1 toggle changes no backend behaviour today.",
      "run_environment_preflight is the only command in this group that skips spawn_blocking (lib.rs:1051-1054) while being by far the most blocking — 5-7 synchronous subprocess spawns, with the python candidates probed twice because resolve_python_command at environment.rs:398 re-runs the probes already done at 360-364. On Windows `py -3 --version` with no Python installed can stall on the App Execution Alias. The settings page pays this on every mount (SettingsPage.tsx:97).",
      "src/pages/Settings.tsx is already dead code — no file under src/ imports \"pages/Settings\", and \"settings\" is absent from router.ts's LEGACY_PAGES (11-24), so unlike the other retired pages it is not even reachable via #/legacy/. It still holds the AnthropicCompatible/Custom options (289-292) and the 700ms autosave §14.4 objects to (151-178), which makes both look unfixed to anyone grepping.",
      "METHOD NOTE, not a code defect: src/features/settings/SettingsPage.tsx was being actively rewritten by another agent while I read it — 193 lines and brace-unbalanced at 14:32, 412 lines and complete at 14:38. I re-read it and my findings reflect the 412-line version. Anyone else reporting that file as truncated is seeing the same race, not a real bug.",
      "Two divergent copies of the base-URL security policy exist: llm_commands.rs:30-76 (save-side) and llm_gateway.rs:161-188 (request-side). The gateway copy additionally rejects URL fragments and handles IPv6 unicast-link-local; the save copy does not. A URL can therefore pass save and fail at request time for a shape reason rather than a connectivity one."
    ]
  },
  {
    "subsystem": "ielts_grammar local recognition — src-tauri/src/ielts_grammar/ (entry points, V1-vs-V2 input reality, module breakdown, and which plan 6.8 hard-closure checks already exist)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "role": "The whole orchestrator + task-group/passage/response/slot builders. Only 3 fns + 3 consts are visible outside the module (lines 26, 56-58, 60, 76). Contains build_authoring_v2_shadow (the entry point), build_passage, group_lines, rebind_lines_to_physical, build_responses_and_slots, build_stimulus, refresh_group_quality, build_shadow_compare.",
        "sizeNote": "3030 lines total; production code ends at line 2165 (#[cfg(test)] mod tests starts 2166). 19 inline tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "The entire hard gate. evaluate_quality (line 27) plus 60+ private validators. This is the ONLY file in the subsystem that reads DocumentIRV2 geometry (regions/spans/glyphs/tables/vectorPaths) — and it reads it only to build the coverage ledger, never for recognition.",
        "sizeNote": "4468 lines; production ends ~3649. 25 inline tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/completion.rs",
        "role": "Completion-family structure recovery: recover_completion_structure (38), completion_context_nodes_with_slots (546), completion_table_node (1019), completion_flowchart_node (1134), answer_slot_node (1224), slot-marker/blank-run scanners (338-494).",
        "sizeNote": "1685 lines; production ends ~1251. 12 inline tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "role": "The 8-PDF acceptance harness. process_fixture (1282) hardcodes the V1 chain: parse_source_document -> make_dynamic_split_candidates -> make_dynamic_authoring_ir -> build_authoring_v2_shadow. Any entry-signature change breaks this file first.",
        "sizeNote": "2207 lines, all #[cfg(test)]. 12 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_signature.rs",
        "role": "Task-type classification from instruction text. infer_instruction_signature (17), infer_task_type_from_cues (161) — pure substring cues, task_type_from_kind_hint (232) reads V1 kindHint.",
        "sizeNote": "529 lines; production ends ~390. 8 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs",
        "role": "Defines SemanticLine (8) — the ONLY substrate every recognizer sees — plus the two projections semantic_lines_from_v1_document (104) and semantic_lines_from_v2_shadow (161), and collect_instruction_zone (29).",
        "sizeNote": "481 lines; production ends ~361. 7 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_bank.rs",
        "role": "Shared option-bank detection: detect_option_bank (14, gated on explicit 'list of ...' title OR matching semantics), detect_completion_option_bank (52, requires declared A-terminal alphabet + one source row per label), option_bank_value (440).",
        "sizeNote": "466 lines; production ends ~273. 6 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_run.rs",
        "role": "Local option-run detection from line text: detect_option_runs (20), parse_option_line (146), materialize_run (186), run_matches_alphabet (114).",
        "sizeNote": "335 lines; production ends ~246. 5 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/question_number.rs",
        "role": "Questions N-M expression parsing: parse_question_expression (17), expand_expression (49), question_expression_end (76).",
        "sizeNote": "364 lines; production ends ~309. 4 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/prompt_assembler.rs",
        "role": "Stem assembly: assemble_prompt (16). Prefers the V1 candidate prompt string outright (24-40); geometric fallback is a line-index window between question anchors.",
        "sizeNote": "164 lines; production ends ~121. 1 test."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/reading.rs",
        "role": "Passage ContentDoc emission: passage_nodes (5) = one paragraph node per SemanticLine, visual_passage_lines (41) = coarse text-based exclusion filter, is_paper_section_header (62).",
        "sizeNote": "176 lines; production ends ~111. 3 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/anchors.rs",
        "role": "Question-number anchoring. detect_question_anchors (14) + parse_leading_question_number (59) — strictly line-first: parses digits off the start of SemanticLine.text.",
        "sizeNote": "136 lines; production ends ~92. 2 tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/diagram.rs",
        "role": "diagram_candidate (7) — hotspot emission for DiagramLabel/PlanMapLabel completion; reads normalized rects out of source_anchor./displayBBox/normalized (52-57).",
        "sizeNote": "122 lines; production ends ~88. 1 test."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/issue_codes.rs",
        "role": "53 stable issue-code string constants — the actual vocabulary the 6.8 blocker list must be reconciled against.",
        "sizeNote": "53 lines, no tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/evidence.rs",
        "role": "SourceAnchorV2 construction/normalization: source_anchor_from_job (5), anchor_from_value (41).",
        "sizeNote": "94 lines, no tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/answer_key.rs",
        "role": "answer_key_from_v1 (3) and answer_value_for_slot (17) — V2 answers are entirely derived from the V1 authoring answerKey.",
        "sizeNote": "53 lines, no tests."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "Main product path. Calls build_authoring_v2_shadow at 207 and 2050, write_authoring_v2_shadow at 288 (via write_pipeline_authoring_v2_shadow, 275), and build_authoring_v2_shadow again at 2434 in the cloud-review path. Also line 1487-1490 is where DocumentIRV2 is materialized — PDF only.",
        "sizeNote": "116127 bytes"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs",
        "role": "Produces both required V1 inputs: make_dynamic_split_candidates (4193) and make_dynamic_authoring_ir (8345). split_section_evidence_for_blocks (542) is where the 120-char textPreview cap enters the recognition substrate (via dynamic_block_text_preview, 513).",
        "sizeNote": "547026 bytes — the file 17.6 wants frozen"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "Second write_authoring_v2_shadow call site (431), inside build_authoring_ir_core. Also parse_document_core (154) — the only place DOCX gets a DocumentIRV2 shadow (186-190).",
        "sizeNote": "22934 bytes"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "The workspace save path Phases 1-3 already use. get_authoring_v2_core (54), apply_authoring_v2_patches_core (123), refresh_quality_report (744) -> evaluate_quality (748). Re-runs the quality gate but never re-runs recognition.",
        "sizeNote": "97587 bytes"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/document_ir_v2.rs",
        "role": "The geometry the plan wants recognition to consume. PageNodeV2 (295) carries glyphs/spans/lines/regions/vector_paths/tables/reading_order/reading_order_graph. SpanNodeV2 (64) has style+bbox. LineNodeV2 (87) has baseline, indentation_pt, hanging_indent_pt, inline_gaps_pt. RegionNodeV2 (139) has kind+column_index+reading_order_rank. TableNodeV2 (241)/TableCellV2 (190) have rows/cols/row_span/col_span/content_region_ids.",
        "sizeNote": "Everything 6.5/6.6/6.7/6.10 need already exists here."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "role": "Produces the V1 document-ir.json that recognition actually depends on. PDF path (535-590) is pdf_extract text-only, page-level semantic_text_chunks (269), with document_block (310) fabricating bbox = [72, y0, 520, y0+36] and page 595x842.",
        "sizeNote": "104508 bytes"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "role": "Writes document-ir-v2.shadow.json (SHADOW_ARTIFACT_FILE, line 19). write_pdf_facts_shadow_with_v1 (474) does NOT mutate the V1 doc — it only bridges diagram-region assets V1->V2.",
        "sizeNote": "157352 bytes"
      }
    ],
    "publicApi": [
      {
        "symbol": "write_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:60",
        "signature": "pub(crate) fn write_authoring_v2_shadow(\n    job_dir: &Path,\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n    output_path: &Path,\n) -> CommandResult<Value>",
        "notes": "Thin wrapper: calls build_authoring_v2_shadow, writes authoring-ir-v2.shadow.json, then writes authoring-ir-v2.shadow.compare.json. Two call sites: auto_pipeline.rs:288, authoring_commands.rs:431."
      },
      {
        "symbol": "build_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:76",
        "signature": "pub(crate) fn build_authoring_v2_shadow(\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n) -> CommandResult<Value>",
        "notes": "THE recognition entry point. Of its five inputs, only `split` (V1 split-candidates) is structurally required — the group loop at mod.rs:134 iterates split[\"questionGroupCandidates\"]; an empty/missing array yields zero task groups. `physical_shadow` (DocumentIRV2) is Option and never drives structure. `v1_authoring` supplies answerKey, fixed options and passage htmlBlocks fallback. `v1_document` supplies V1 SemanticLines. Returns the full IeltsAuthoringIRV2 JSON, schema-validated at mod.rs:378."
      },
      {
        "symbol": "evaluate_quality",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:27 (re-exported at mod.rs:26)",
        "signature": "pub(crate) fn evaluate_quality(authoring: &Value, physical_shadow: Option<&Value>) -> Value",
        "notes": "Second public entry point and the only one the converged workspace already calls: authoring_v2_commands.rs:748 inside refresh_quality_report. Operates on an already-built IeltsAuthoringIRV2 — it does NOT re-run recognition. Returns QualityReportV2 with state in {blocked, review_required, ready}."
      },
      {
        "symbol": "SHADOW_ARTIFACT_FILE / SHADOW_COMPARE_FILE / SHADOW_ERROR_FILE",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:56-58",
        "signature": "pub(crate) const SHADOW_ARTIFACT_FILE: &str = \"authoring-ir-v2.shadow.json\";\npub(crate) const SHADOW_COMPARE_FILE: &str = \"authoring-ir-v2.shadow.compare.json\";\npub(crate) const SHADOW_ERROR_FILE: &str = \"authoring-ir-v2.shadow.error.json\";",
        "notes": "Imported and aliased as AUTHORING_V2_SHADOW_* in auto_pipeline.rs:11-13 and authoring_commands.rs:13-15. authoring_v2_commands.rs:34 hardcodes the same string independently."
      },
      {
        "symbol": "SemanticLine",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs:8",
        "signature": "pub(crate) struct SemanticLine {\n    pub id: String,\n    pub text: String,\n    pub source_anchor: Value,\n    pub page_index: i32,\n    pub order: usize,\n    pub role: String,\n    pub bbox: Option<[f64; 4]>,\n}",
        "notes": "The single substrate every recognizer consumes. This is the bottleneck: no spans, no style, no baseline, no indentation, no hanging indent, no region id, no column. Widening this struct is the highest-leverage single change for 6.5/6.6/6.7."
      },
      {
        "symbol": "semantic_lines_from_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs:161",
        "signature": "pub(crate) fn semantic_lines_from_v2_shadow(shadow: &Value) -> Vec<SemanticLine>",
        "notes": "The complete extent of DocumentIRV2's contribution to recognition: reads only pages[].pageIndex, pages[].lines[].text, .id, .bbox and .sourceAnchors. Everything else in PageNodeV2 (spans, glyphs, regions, tables, vectorPaths, readingOrder, indentation_pt, hanging_indent_pt, baseline, style) is discarded here."
      },
      {
        "symbol": "semantic_lines_from_v1_document",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs:104",
        "signature": "pub(crate) fn semantic_lines_from_v1_document(document: Option<&Value>) -> Vec<SemanticLine>",
        "notes": "Projects V1 pages[].blocks[] (text or html), using blockId as the line id and the V1 block bbox."
      },
      {
        "symbol": "collect_instruction_zone",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs:29",
        "signature": "pub(crate) fn collect_instruction_zone(\n    lines: &[SemanticLine],\n    heading_index: usize,\n    expected_numbers: &[u32],\n) -> InstructionZone",
        "notes": "Scans forward from heading_index, stopping at is_task_boundary / is_option_run_start / is_new_task_heading (243-261). Returns text, line_ids, source_anchors, start/end index, confidence, warnings."
      },
      {
        "symbol": "detect_question_anchors",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/anchors.rs:14",
        "signature": "pub(crate) fn detect_question_anchors(\n    lines: &[SemanticLine],\n    expected_numbers: &[u32],\n) -> Vec<QuestionAnchor>",
        "notes": "Line-first, exactly as 6.5 describes the current state. parse_leading_question_number (anchors.rs:59) takes up to 3 leading ASCII digits off the trimmed line, scores 0.62 for a bare number / 0.72 unpunctuated / 0.90 with . ) : , then filters by expected_numbers.contains(&number). No span iteration, no bbox scoring, no header/footer or year/measurement rejection."
      },
      {
        "symbol": "assemble_prompt",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/prompt_assembler.rs:16",
        "signature": "pub(crate) fn assemble_prompt(\n    candidate_prompt: Option<&str>,\n    question_number: u32,\n    question_anchor: Option<&QuestionAnchor>,\n    next_question_anchor: Option<&QuestionAnchor>,\n    lines: &[SemanticLine],\n    fallback_anchor: Option<Value>,\n) -> PromptResult",
        "notes": "Short-circuits on the V1 candidate prompt string when it is non-empty and not a known placeholder (24-40). Geometric path is a plain line-index slice lines[start..option_start] where option_start comes from detect_option_runs. source_coverage is binary 1.0/0.0, not a ratio."
      },
      {
        "symbol": "detect_option_runs",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_run.rs:20",
        "signature": "pub(crate) fn detect_option_runs(lines: &[SemanticLine]) -> Vec<OptionRun>",
        "notes": "parse_option_line (146) requires the label to be the leading token of line.text (single uppercase letter, or lowercase roman i/v/x). One continuation absorb only: an empty-text label absorbs the immediately next non-label prose line (26-46). No x-column stability, no hanging-indent matching, no multi-line wrap."
      },
      {
        "symbol": "detect_option_bank",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_bank.rs:14",
        "signature": "pub(crate) fn detect_option_bank(\n    lines: &[SemanticLine],\n    instruction_text: &str,\n    allow_reuse: Option<bool>,\n    matching_semantics: bool,\n) -> Option<OptionBankCandidate>",
        "notes": "Returns None unless the instruction contains one of 6 literal 'list of ...'/'choose from the box' markers OR matching_semantics is true (option_bank.rs:33). Then takes the first complete run with >=2 options."
      },
      {
        "symbol": "detect_completion_option_bank",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_bank.rs:52",
        "signature": "pub(crate) fn detect_completion_option_bank(\n    lines: &[SemanticLine],\n    instruction_text: &str,\n    allow_reuse: Option<bool>,\n    expected_slot_count: usize,\n) -> Option<OptionBankCandidate>",
        "notes": "Requires a bank cue, >=3 declared letter labels, every declared label present with non-empty text, and (absent a visible 'List of ...' heading) one distinct source row per label. Infers allow_reuse from cardinality when expected_slot_count > options.len() (option_bank.rs:157-166)."
      },
      {
        "symbol": "recover_completion_structure",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/completion.rs:38",
        "signature": "pub(crate) fn recover_completion_structure(\n    task_type: &TaskTypeV2,\n    lines: &[SemanticLine],\n    instruction_line_ids: &[String],\n    expected_numbers: &[u32],\n) -> CompletionStructureCandidate",
        "notes": "Returns { container_kind, context_lines, slot_line_ids: BTreeMap<u32,String>, slot_lines }. closes_slots(expected) (completion.rs:31) is the completion hard-closure predicate consumed at mod.rs:281 and mod.rs:1541/1558."
      },
      {
        "symbol": "infer_instruction_signature",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_signature.rs:17",
        "signature": "pub(crate) fn infer_instruction_signature(\n    text: &str,\n    expression: &QuestionNumberExpressionV2,\n    kind_hint: Option<&str>,\n    evidence_anchors: Vec<Value>,\n) -> SignatureResult",
        "notes": "kind_hint is the V1 candidate's kindHint field (mod.rs:195). Emits warning 'task_type_conflict:instruction=..;structure_hint=..' when the two disagree, which quality.rs:1345 promotes to the blocking TASK_TYPE_CONFLICT."
      },
      {
        "symbol": "passage_nodes / visual_passage_lines",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/reading.rs:5 / 41",
        "signature": "pub(crate) fn passage_nodes(\n    title: &str,\n    lines: &[SemanticLine],\n    source_anchors: Vec<Value>,\n) -> Vec<Value>\n\npub(crate) fn visual_passage_lines(lines: &[SemanticLine]) -> Vec<SemanticLine>",
        "notes": "passage_nodes emits one flat paragraph node per line, no paragraph labelling. visual_passage_lines is a last-resort text-prefix filter only."
      }
    ],
    "dataFlow": "Auto-import (the product path, auto_pipeline.rs:run_auto_pipeline_core_with_gateway, line 1381):\n\n1. parse_source_document (auto_pipeline.rs:1449) writes document-ir.json — V1 DocumentIRV1. For PDF this is pdf_extract text only: parser.rs:535 pdf_extract::extract_text_by_pages, chunked per page by semantic_text_chunks (parser.rs:269, blank lines or the markers 'reading passage '/'questions '/'question '/'answer key'/'answers'), and every block gets a FABRICATED bbox from parser.rs:317-319 — [72, y0, 520, min(y0+36,794)] with y0 = 72 + (ordinal % 16) * 42 — on a hardcoded 595x842 page.\n\n2. auto_pipeline.rs:1487-1519 writes document-ir-v2.shadow.json (DocumentIRV2, real geometry from pdf_geometry.rs) — but only when main_source_file(&job).filter(|s| s.file_type == \"pdf\"). DOCX gets nothing here.\n\n3. auto_pipeline.rs:1675 make_dynamic_split_candidates(&job_id, &job, doc.as_ref()) -> split-candidates.json. Input is the V1 doc only (authoring_pipeline.rs:4193-4199 -> dynamic_document_blocks(doc)). DocumentIRV2 is not a parameter. Each questionGroupCandidate carries groupId, heading, instructionText, kindHint, range (block ids) and sectionEvidence[] built by split_section_evidence_for_blocks (authoring_pipeline.rs:542) whose textPreview is dynamic_block_text_preview = text.chars().take(120) (authoring_pipeline.rs:513-516).\n\n4. auto_pipeline.rs:1684 make_dynamic_authoring_ir(&job, &split, doc.as_ref()) -> authoring-ir.json (V1 authoring: groups, passage.htmlBlocks, answerKey, questionOrder).\n\n5. auto_pipeline.rs:2027 write_pipeline_authoring_v2_shadow -> write_authoring_v2_shadow -> build_authoring_v2_shadow(job, &ir, &split, doc.as_ref(), physical_shadow.as_ref()).\n\nInside build_authoring_v2_shadow (mod.rs:76):\n- mod.rs:105-107 physical_lines = physical_shadow.map(semantic_lines_from_v2_shadow).unwrap_or_default()\n- mod.rs:108 v1_lines = semantic_lines_from_v1_document(v1_document)\n- mod.rs:109-113 fallback_lines = physical_lines if non-empty else v1_lines\n- mod.rs:119 build_passage(...) — passage line set comes from split[\"passageCandidates\"][0][\"range\"] block ids matched against v1_lines (mod.rs:428-442); fallbacks are v1_authoring./passage/htmlBlocks then visual_passage_lines(fallback_lines). Emits \"paragraphMap\": {} unconditionally (mod.rs:535).\n- mod.rs:134-140 THE LOOP: for candidate in split[\"questionGroupCandidates\"]\n  - mod.rs:146 group_lines(candidate, v1_lines, fallback_lines, physical_lines, ...) builds the group's SemanticLine vector, in priority order: (a) candidate.sectionEvidence[] using evidence.textPreview + evidence.bbox (mod.rs:566-616), (b) v1_lines filtered by candidate range block ids (mod.rs:617-623), (c) the whole fallback_lines document (mod.rs:624-626). Then rebind_lines_to_physical (mod.rs:627) overwrites ONLY source_anchor and bbox by matching the alphanumeric-lowercase text key against consecutive physical lines on the same page, falling back to bbox intersection (mod.rs:635-687). line.text is never replaced.\n  - mod.rs:184 heading_index, 190 collect_instruction_zone, 197 infer_instruction_signature (task type decided HERE), 205 detect_question_anchors, 207 detect_option_runs, 208 detect_option_bank / 218 detect_completion_option_bank, 273 recover_completion_structure, 284 build_responses_and_slots, 309 build_stimulus.\n- mod.rs:375 quality::evaluate_quality(&authoring, physical_shadow) — the only place DocumentIRV2 geometry beyond flat lines is read, and only for the coverage ledger (quality.rs:2981 source_coverage_summary -> significant_physical_nodes 3311 over spans/lines/regions/glyphs/vectorPaths/tables/annotations/imagePlacements).\n- mod.rs:377 refresh_group_quality, mod.rs:378 serde_json::from_value::<IeltsAuthoringIRV2> validation.\n\nSame chain runs three more times per job: auto_pipeline.rs:201-208 (has_reliable_question_groups, for the vision-transcription gate), auto_pipeline.rs:2050 (the v2 quality gate), auto_pipeline.rs:2432-2440 (cloud review). Each rebuilds split + V1 authoring + the full V2 shadow from scratch.\n\nWorkspace save path (already converged per Phases 1-3): get_authoring_v2 -> get_authoring_v2_core (authoring_v2_commands.rs:54) -> build_editor_session (711) -> load_current_authoring (725) which reads the highest revision or falls back to authoring-ir-v2.shadow.json. apply_authoring_v2_patches -> apply_authoring_v2_patches_core (123) applies patches then refresh_quality_report (146) -> evaluate_quality (748) with document-ir-v2.shadow.json read directly. Recognition is NOT re-run on save.",
    "gapsVsPlan": [
      {
        "claim": "6.1: ielts_grammar still starts from V1 questionGroupCandidates rather than DocumentIRV2 geometry.",
        "reality": "Exactly right, and the group loop is a hard dependency, not a preference. build_authoring_v2_shadow iterates split[\"questionGroupCandidates\"] and produces one taskGroup per candidate; there is no other group-discovery path. An empty array yields zero task groups and quality.rs:57-70 raises QUESTION_RANGE_UNPARSED. physical_shadow is Option<&Value> and is passed to only two things: semantic_lines_from_v2_shadow (flat line list) and evaluate_quality (coverage ledger).",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:76-84 (signature), mod.rs:134-140 (the loop), mod.rs:105-107 and instruction_zone.rs:161-206 (the only DocumentIRV2 consumption in recognition), mod.rs:375"
      },
      {
        "claim": "6.1: physical layer is mainly converted into SemanticLine.",
        "reality": "Understated. semantic_lines_from_v2_shadow reads only pages[].pageIndex + lines[].{id,text,bbox,sourceAnchors}. SemanticLine has 7 fields and carries no span, style, baseline, indentation_pt, hanging_indent_pt, inline_gaps_pt, region id, region kind, column_index or reading_order rank — all of which already exist in the DocumentIRV2 schema.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/instruction_zone.rs:8-16 and 161-206 vs src-tauri/src/schema/document_ir_v2.rs:64-74 (SpanNodeV2), 87-107 (LineNodeV2), 139-157 (RegionNodeV2), 295-323 (PageNodeV2)"
      },
      {
        "claim": "6.1: table/region/vector cannot directly become question structure.",
        "reality": "Confirmed. Zero reads of \"tables\", \"regions\", \"vectorPaths\", \"spans\" or \"glyphs\" exist anywhere in the recognition modules. The only reads are inside quality.rs (3230, 3341, 3382, 3401, 3497, 3514, 3545, 3558) and all feed significant_physical_nodes / physical_ignored_reasons for the coverage ledger.",
        "verdict": "confirmed",
        "evidence": "grep over src-tauri/src/ielts_grammar/*.rs; hits only in quality.rs:3230-3558"
      },
      {
        "claim": "6.1: V1 candidate errors constrain the V2 grammar search range.",
        "reality": "Confirmed and worse than stated in two respects that the plan does not mention. (a) group_lines' primary substrate is candidate.sectionEvidence[].textPreview, which authoring_pipeline.rs:513-516 truncates to 120 characters; rebind_lines_to_physical replaces only source_anchor and bbox, never text, so a long stem/option/note row stays truncated. mod.rs test at 2386 is literally named/fed 'Truncated preview', so this is known and deliberate. (b) For PDF, the V1 bbox those evidence lines carry is fabricated by parser.rs:317-319, so the bbox-intersection rebind fallback matches against synthetic geometry.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/authoring_pipeline.rs:513-516 and 552; src-tauri/src/ielts_grammar/mod.rs:566-616 (textPreview -> SemanticLine.text), mod.rs:635-687 (rebind never touches text), mod.rs:2386-2401 (test); src-tauri/src/parser.rs:310-330"
      },
      {
        "claim": "6.2: introduce QuestionLayoutGraphV1 / PageLayoutGraph / QuestionBlockCandidateV1 / OptionBankCandidateV1 / VisualStimulusCandidateV1 / UnassignedEvidence / InstructionZoneCandidate / RecognitionIssueCode.",
        "reality": "None of these eight identifiers exist anywhere in src-tauri/src. There is no intermediate layout graph of any kind. The closest existing analogues are option_bank::OptionBankCandidate (option_bank.rs:7), completion::CompletionStructureCandidate (completion.rs:19) and instruction_zone::InstructionZone (instruction_zone.rs:19) — all line-list based.",
        "verdict": "confirmed",
        "evidence": "grep -rn over src-tauri/src/ --include=*.rs: 0 hits for each of the 8 names"
      },
      {
        "claim": "6.3: task-type classification must not precede question-boundary recovery.",
        "reality": "Currently violated. Order inside the loop is: collect_instruction_zone (mod.rs:190) -> infer_instruction_signature, which decides task_type (mod.rs:197-203) -> detect_question_anchors (205) -> detect_option_runs (207) -> detect_option_bank (208) -> recover_completion_structure (273). Classification also feeds back into detection: detect_option_bank's matching_semantics argument and the is_completion_task gate for the completion bank and completion structure are all derived from the already-decided task_type.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:190, 197, 203, 205, 207, 208-224, 273-282"
      },
      {
        "claim": "6.4: add an inferred SemanticRegionRole per region (Passage / QuestionInstruction / QuestionPrompt / OptionRun / SharedOptionBank / CompletionStimulus / AnswerKey / HeaderFooter / Unknown) with the listed scoring features.",
        "reality": "SemanticRegionRole does not exist; no region-role scoring exists. A PHYSICAL region kind already exists and is emitted by the shadow: PhysicalRegionKindV2 = {Text, Title, List, Table, Figure, Diagram, Form, Header, Footer, PageNumber, Rule, Unknown}, plus RegionNodeV2.column_index / section_index / reading_order_rank / confidence. That is a usable substrate, but it is a different (physical) taxonomy and recognition never reads it.",
        "verdict": "partly-wrong",
        "evidence": "0 hits for SemanticRegionRole; src-tauri/src/schema/document_ir_v2.rs:121-134 (PhysicalRegionKindV2), 139-157 (RegionNodeV2)"
      },
      {
        "claim": "6.5: current anchor detection parses numbers off the start of whole lines; replace with detect_question_number_tokens over page.spans with the listed score terms.",
        "reality": "The description of the current state is exactly right (anchors.rs:59-90 parses up to 3 leading ASCII digits off the trimmed line text). detect_question_number_tokens and QuestionNumberToken do not exist. Of the plan's eight score terms, only 'within the instruction-declared range' exists — as a hard filter, not a weight (anchors.rs:24-26 expected_numbers.contains). There is no monotonic-sequence bonus, no left-margin/gutter term, no font-size consistency term, no footer/page-number penalty, no unit/year/percent penalty, no passage-interior penalty. The threshold is not 0.55; scores are fixed at 0.62/0.72/0.90 (+0.10 if punctuation-split prompt text follows) and are never compared against a cutoff.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/anchors.rs:14-42 and 59-90; 0 hits for detect_question_number_tokens / QuestionNumberToken"
      },
      {
        "claim": "6.6: replace stem assembly with assemble_question_stem using geometric_interval, baseline tolerance, hanging indent and vertical-gap thresholds; must support 5 shapes including cross-page.",
        "reality": "assemble_question_stem and StemCandidate do not exist. assemble_prompt (prompt_assembler.rs:16) short-circuits on the V1 candidate prompt string first (24-40), and its fallback is a flat line-index window lines[anchor.line_index .. option_start]. No baseline tolerance, no same-column/hanging-indent test, no vertical-gap threshold, no cross-page continuation. Of the 5 shapes: same-line and number-on-its-own-line work (anchors.rs:80-82 accepts a bare number and the window picks up following lines); wrapped and big-indent work only incidentally because every line in the window is concatenated; cross-page is not handled at all. source_coverage is binary 1.0/0.0 (prompt_assembler.rs:80), not an assigned-visible-text ratio.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/prompt_assembler.rs:16-88; 0 hits for assemble_question_stem / StemCandidate"
      },
      {
        "claim": "6.7: option recognition should find a label token then collect right-of and hanging-indent continuations; a legal run needs contiguous labels, a stable label x column, non-empty text per label, containment in one block/bank region, and must not promote 'Paragraph A' / 'Section B' / a passage's leading letter.",
        "reality": "OptionLabelToken and assemble_option do not exist. parse_option_line (option_run.rs:146-175) still requires the label to be the leading token of line.text. There is exactly one continuation rule and it is not indent-based: an empty-text label absorbs the immediately following non-label, non-bare-number line (option_run.rs:30-46). Of the five evidence requirements: contiguous labels YES (materialize_run, 187-194), non-empty text per label YES (194), stable label x column NO (bbox is never consulted in this module), containment in one block/bank region NO, false-positive rejection PARTIAL — monotonic-sequence gating means an isolated 'A study of ...' is marked incomplete (test option_run.rs:296) but 'Paragraph A' / 'Section B' are not named or excluded anywhere.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/option_run.rs:20-62, 146-204, 296-300; 0 hits for OptionLabelToken / assemble_option"
      },
      {
        "claim": "6.8: a validate_basic_task hard-closure function dispatching per task family, and 7 named blocker codes.",
        "reality": "No validate_basic_task exists and there is no per-task-family dispatch — the checks are spread across evaluate_group (quality.rs:1300), validate_options (1622), validate_cardinality (1759), validate_completion_host (1888), validate_answers (2114), validate_visual_task (2324). Most of the substance exists under different code names; two requirements genuinely do not. See the per-check breakdown in surprises and changePoints. Blocker-code mapping: QUESTION_PROMPT_MISSING -> exists as PROMPT_EMPTY (issue_codes.rs:19, raised quality.rs:1432); QUESTION_PROMPT_BOUNDARY_AMBIGUOUS -> exists as PROMPT_BOUNDARY_AMBIGUOUS (issue_codes.rs:20, raised 1449); OPTION_LABEL_MISSING -> no dedicated code, covered indirectly by OPTION_ALPHABET_MISMATCH (issue_codes.rs:22) and only when instructionSignature.optionAlphabet was parsed; OPTION_TEXT_MISSING -> no dedicated code, raised as OPTION_RUN_INCOMPLETE via option_has_renderable_content (quality.rs:2781); OPTION_RUN_INCOMPLETE -> exists verbatim (issue_codes.rs:21); SHARED_OPTION_BANK_MISSING -> exists as OPTION_BANK_MISSING (issue_codes.rs:23, raised 1678); SIGNIFICANT_SOURCE_TEXT_UNASSIGNED -> exists as SIGNIFICANT_REGION_UNASSIGNED (issue_codes.rs:53, raised quality.rs:141-157) but document-scoped, not per task group.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/issue_codes.rs:7-53; quality.rs:1300-1568, 1622-1757, 1759-1886, 2781-2795; 0 hits for validate_basic_task"
      },
      {
        "claim": "6.8: require_unique_option_labels.",
        "reality": "Does not exist in any form. option_labels (quality.rs:2300) collects into a BTreeSet, so a duplicate label is silently deduplicated. It is caught only accidentally: if optionAlphabet was parsed and a duplicate displaced a missing label, the set comparison at 1700/1728 fails as OPTION_ALPHABET_MISMATCH. With no declared alphabet, duplicates pass.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/ielts_grammar/quality.rs:2300-2307, 1698-1702, 1725-1732"
      },
      {
        "claim": "6.8: require_question_source_coverage(task, 0.92).",
        "reality": "No per-task source-coverage check exists. Two things resemble it and neither is it: (a) quality.rs:182 has_low_task_score = any task score < 0.92 — but that is the blended evaluate_group score (weights at quality.rs:1550-1560), and it only downgrades state to review_required, never to blocked; (b) taskGroups[].quality.sourceCoverage is written by refresh_group_quality (mod.rs:2094) as a fake binary: 1.0 if the group has any sourceAnchors at all, else 0.0. Real coverage is computed only document-wide, in source_coverage_summary (quality.rs:2981), and blocks at the document level when unassigned_ids is non-empty (quality.rs:141-157).",
        "verdict": "wrong",
        "evidence": "src-tauri/src/ielts_grammar/quality.rs:180-193, 1550-1568, 2981-3085, 141-157; mod.rs:2093-2097"
      },
      {
        "claim": "6.8: require_exact_fixed_response_set for TrueFalseNotGiven / YesNoNotGiven.",
        "reality": "Partially present, but as generation not validation. fixed_response_option_bank (mod.rs:1278-1290) synthesizes exactly ['TRUE','FALSE','NOT GIVEN'] / ['YES','NO','NOT GIVEN'] with allowReuse=true, and validate_options (quality.rs:1648-1651) does include tfng/ynng in choice_required so the bank must be closed and renderable. But there is no check that the set is EXACTLY those three — a bank of 2 or 5 members passes, because expected_labels_from_alphabet (2285) only handles 'X-Y' letter ranges and TFNG signatures do not carry one.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:1287-1290, 1307-1327; quality.rs:1639-1651, 1694-1702, 2285-2298"
      },
      {
        "claim": "6.8: require_every_statement_nonempty (per-statement, not per-group).",
        "reality": "Effectively enforced, but through a different mechanism than PROMPT_EMPTY. PROMPT_EMPTY is group-scoped: group_prompt_text (quality.rs:2755) concatenates every stimulus and every responseGroup prompt, so one non-empty prompt anywhere satisfies it. The per-statement closure comes from mod.rs:1044-1047 emitting the literal '[prompt pending review]' placeholder for an empty per-question prompt, which node_contains_prompt_placeholder / group_has_ambiguous_prompt (quality.rs:2770-2818) detect and quality.rs:1449 raises as the blocking PROMPT_BOUNDARY_AMBIGUOUS. Note the gap: when suppress_structural_completion_prompt is set for a completion task, prompt is Vec::new() and no placeholder is emitted (mod.rs:1040-1042) — that case relies entirely on validate_completion_host's inline-slot closure instead.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:1040-1051, 1466; quality.rs:1428-1454, 2755-2769, 2770-2779, 2798-2818"
      },
      {
        "claim": "6.8: require_every_item_prompt / require_shared_option_bank / require_nonempty_bank_options for Matching* and Classification.",
        "reality": "require_shared_option_bank and require_nonempty_bank_options both exist: validate_options raises blocking OPTION_BANK_MISSING for matching/classification when neither inline options nor a complete bank are present (quality.rs:1673-1691), and bank_complete demands items.len() >= 2 and all(option_has_renderable_content) (1668-1670). require_every_item_prompt is the same per-statement situation as above — covered via the placeholder -> PROMPT_BOUNDARY_AMBIGUOUS route, not by a dedicated check.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/quality.rs:1639-1691, 1668-1670, 2781-2795"
      },
      {
        "claim": "6.9: Matching Headings must explicitly separate Paragraph A-G -> passage.paragraphMap, List of Headings i-x -> task.optionBank, Questions 14-20 -> response groups/slots; and classification requires has_list_of_headings_title && has_roman_option_run && has_paragraph_targets.",
        "reality": "paragraphMap is hardcoded empty at mod.rs:535 ('paragraphMap': {}), so the passage side of the separation does not exist even though the schema supports it (ielts_authoring_v2.rs:75 pub paragraph_map: Option<BTreeMap<String,String>>) and reading_source_v2.rs:113 already forwards it to the runtime. The bank side works (roman labels are parsed by option_run.rs:155-158 and test option_run.rs:316). The three-way classification conjunction does not exist: infer_task_type_from_cues (instruction_signature.rs:170-172) returns MatchingHeadings on the substring 'list of headings' or 'correct heading for each paragraph' alone, with no roman-run and no paragraph-target evidence.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:531-537; src-tauri/src/schema/ielts_authoring_v2.rs:75; src-tauri/src/reading_source_v2.rs:34, 113; instruction_signature.rs:170-172"
      },
      {
        "claim": "6.9: any region consumed by option_bank_candidate must not enter the passage ContentDoc.",
        "reality": "No such coupling exists for the passage. build_passage runs at mod.rs:119, BEFORE the group loop that detects any option bank (mod.rs:134), and its line set comes from split['passageCandidates'][0]['range'] block ids. The only defence is visual_passage_lines' coarse text filter (reading.rs:41-60), used only as a third-level fallback. The equivalent subtraction IS implemented for the completion stimulus: completion_option_bank_line_ids (completion.rs:142) removes bank rows from context_lines (completion.rs:99).",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:119-130, 383-442, 481-483; reading.rs:41-60; completion.rs:92-99, 142"
      },
      {
        "claim": "6.10 tables: prefer DocumentIRV2.pages[].tables[] via compile_table_stimulus; stop creating fake one-question-per-row tables.",
        "reality": "compile_table_stimulus does not exist and pages[].tables[] is never read by recognition. completion_table_node (completion.rs:1019) reconstructs a table by sorting context_lines + slot_lines by (page_index, order) and windowing around rows that contain answer markers — i.e. from flat line text. The plan's target data is fully available and typed: TableNodeV2 (document_ir_v2.rs:241-255, rows/cols/cells/detection_mode/topology_confidence) and TableCellV2 (190-206, row/col/row_span/col_span/content_region_ids). Partial credit on the 'no fake table' half: mod.rs:1541-1556 refuses to promote a table unless structure.closes_slots(expected_numbers), keeping source rows for manual repair instead (test mod.rs:2700 incomplete_table_does_not_fabricate_per_question_prompts).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/completion.rs:1019-1035; mod.rs:1540-1557; src-tauri/src/schema/document_ir_v2.rs:190-255; 0 hits for compile_table_stimulus"
      },
      {
        "claim": "6.10 diagram: hybrid diagram node with assetId + hotspots[{hotspotId, slotId, normalizedRect}].",
        "reality": "Already implemented in the plan's exact shape. diagram_candidate (diagram.rs:7-49) emits {type:'diagram', id, assetId, hotspots:[{hotspotId, slotId, normalizedRect, labelAnchor}], display}. The gap is the source of the rects: normalized_rect (diagram.rs:52-57) reads source_anchor./displayBBox/normalized or /bbox/normalized and matches the hotspot to a line by line.text.trim_start().starts_with(number) (diagram.rs:25-27) — i.e. still line-text matching, and the asset chosen is just the first non-empty assetId in the document (mod.rs:1570-1576), not a task-scoped crop.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/ielts_grammar/diagram.rs:7-57; mod.rs:1570-1584"
      },
      {
        "claim": "6.11: unassigned-evidence ledger with per-node labels (assigned_to_instruction / assigned_to_prompt:q5 / assigned_to_option:q5:A / assigned_to_option_bank:task2 / assigned_to_stimulus:task3 / ignored_header_footer / unassigned), and a blocker when a question range contains large unassigned areas.",
        "reality": "A ledger exists and is emitted as quality.coverageLedger + quality.coverageStatus (quality.rs:189-201), built by source_coverage_summary (2981-3085) over significant_physical_nodes (3311) with dispositions {assigned, ignored_with_reason, unassigned} and a targetIds list of the authoring node ids that referenced each source node, plus reasons from physical_ignored_reasons (3184). Two deltas from the plan: (a) the labels are targetIds, not the plan's semantic role strings, so 'lost the whole stem but got the task type right' is only visible by inspecting which node ids appear; (b) the blocker is document-scoped — SIGNIFICANT_REGION_UNASSIGNED fires when any unassigned significant node exists anywhere (quality.rs:141-157). There is no per-task-group 'large unassigned area between question numbers' check.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/quality.rs:126-201, 2981-3085, 3184-3250, 3311-3420; issue_codes.rs:53"
      },
      {
        "claim": "17.7: new entry pub fn recognize_local(document: &DocumentIRV2) -> LocalRecognitionOutcome, no longer accepting V1 split or v1_authoring.",
        "reality": "Neither recognize_local nor LocalRecognitionOutcome exists. The current entry takes 5 arguments of which split is load-bearing and v1_authoring supplies answers plus fixed options plus a passage fallback. There is no typed input at all — every recognizer works on serde_json::Value, not DocumentIRV2. Four production call sites plus one test call site pass the V1 tuple: auto_pipeline.rs:207, 288, 2050, 2434; authoring_commands.rs:431; pdf_geometry.rs:3163.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/ielts_grammar/mod.rs:60-84; 0 hits for recognize_local / LocalRecognitionOutcome"
      },
      {
        "claim": "17.7: do not keep a single 100KB aggregate file; split mod.rs per the migration map.",
        "reality": "Direction is right, magnitude is understated. mod.rs is 112627 bytes / 3030 lines (2165 production). quality.rs is larger still at 157234 bytes / 4468 lines (~3649 production) — the plan's map splits it into reliability.rs + current_ds_validation.rs, which is the bigger job. real_pdf_acceptance.rs adds another 88055 bytes of test-only code that transitively depends on the entry signature.",
        "verdict": "partly-wrong",
        "evidence": "ls -la src-tauri/src/ielts_grammar/; wc -l per file"
      },
      {
        "claim": "Frontend Phases 1-3 fact: the workspace saves through the existing get_authoring_v2 / apply_authoring_v2_patches.",
        "reality": "Confirmed. lib.rs:1009 get_authoring_v2 -> get_authoring_v2_core (authoring_v2_commands.rs:54); lib.rs:1025 apply_authoring_v2_patches -> apply_authoring_v2_patches_core (123); both registered at lib.rs:1435, 1437. Worth noting for the refactor: this path already reads document-ir-v2.shadow.json directly (authoring_v2_commands.rs:35, 746) and calls only evaluate_quality — it never touches build_authoring_v2_shadow or any V1 artifact, so an entry-point rewrite does not disturb it.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/lib.rs:1008-1027, 1435-1437; src-tauri/src/authoring_v2_commands.rs:34-35, 54-56, 123-146, 725-742, 744-756"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "build_authoring_v2_shadow (line 76) and write_authoring_v2_shadow (line 60)",
        "whatToChange": "The 17.7 entry-point rewrite. Replace the 5-arg (job, v1_authoring, split, v1_document, physical_shadow) signature with a DocumentIRV2-first one. Practical staging: keep the current fn as a legacy adapter and add recognize_local(&DocumentIRV2) beside it, so the 6 call sites and 19+12 tests can migrate individually rather than in one commit. Note there is no typed input today — everything is serde_json::Value, so a &DocumentIRV2 parameter also forces deserialization at the boundary.",
        "risk": "high",
        "blockedBy": "5 production call sites (auto_pipeline.rs:207, 288, 2050, 2434; authoring_commands.rs:431), 1 test call site (pdf_geometry.rs:3163), and the whole real_pdf_acceptance.rs harness (process_fixture, line 1367)."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "the questionGroupCandidates loop, lines 134-140",
        "whatToChange": "This single `for candidate in split[\"questionGroupCandidates\"]` is the literal breakpoint 6.1 names. Replace with instruction-zone discovery over DocumentIRV2 pages (6.3 step 2) driving group creation. Everything inside the loop body reads `candidate` for groupId (141), heading (155), instructionText (159), kindHint (194), sectionEvidence (via group_lines, 146) and block ids (expression_from_candidate, 165; candidate_block_ids, 173) — each of those needs a geometry-derived replacement.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "group_lines, line 555",
        "whatToChange": "Replace the three-tier V1 substrate (sectionEvidence.textPreview -> v1_lines by block id -> whole document) with 6.6's geometric question-block expansion over DocumentIRV2 lines. This is where the 120-char truncation enters (evidence.textPreview at 576-578) and where the fabricated V1 bbox enters (evidence.bbox at 585-596). Deleting tier (a) alone recovers full text via v1_lines, which is a cheap independent win.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs",
        "symbol": "SemanticLine (line 8) and semantic_lines_from_v2_shadow (line 161)",
        "whatToChange": "Highest leverage per unit of risk in the whole subsystem. Widen the projection to carry what 6.5/6.6/6.7 need and DocumentIRV2 already has: LineNodeV2.baseline, .indentation_pt, .hanging_indent_pt, .inline_gaps_pt, .source_order, .confidence; the line's span ids with SpanNodeV2.bbox + .style (font size, bold) for number/label token detection; the owning RegionNodeV2 id, .kind and .column_index. Purely additive — every existing consumer keeps compiling. Do this before touching anchors/prompt/option.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/anchors.rs",
        "symbol": "detect_question_anchors (line 14) and parse_leading_question_number (line 59)",
        "whatToChange": "6.5's token/geometry-first rewrite. Iterate span tokens rather than line prefixes; turn expected_numbers from a hard filter (line 24-26) into a +0.30 weight; add the 7 missing score terms (monotonic sequence, left-margin/gutter position, adjacency to right-of or next-line body text, font-size consistency, footer/page-number penalty, unit/year/percent penalty, passage-interior penalty); apply the 0.55 cutoff. Keep QuestionAnchor.line_index or every downstream consumer breaks.",
        "risk": "high",
        "blockedBy": "QuestionAnchor.line_index is consumed as a line index by prompt_assembler.rs:51-64, mod.rs:option_run_for_question (1589), and mod.rs:question_anchor (1858). A token-level anchor needs a line/block back-reference or those three change together."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/prompt_assembler.rs",
        "symbol": "assemble_prompt, line 16",
        "whatToChange": "6.6. Two separable pieces: (a) drop or demote the V1 candidate_prompt short-circuit at lines 24-40, which currently bypasses geometry entirely whenever V1 produced any prompt string; (b) replace the flat lines[start..option_start] window with the geometric interval + baseline-tolerance same-row scan + hanging-indent/vertical-gap wrap take_while, and add cross-page continuation. Also replace the binary source_coverage at line 80 with a real assigned-visible-text ratio, which is what 6.8's require_question_source_coverage needs as an input.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_run.rs",
        "symbol": "parse_option_line (146), detect_option_runs (20), materialize_run (186)",
        "whatToChange": "6.7. Split label-token detection from run assembly: find label tokens by span geometry (single uppercase letter or lowercase roman) rather than requiring the leading position in line.text; collect text_right_of within baseline tolerance; replace the one-line absorb at 30-46 with hanging_indent_matches continuation. Add label-x-column stability and same-block/bank-region containment to the materialize_run incompleteness rule (192-194). Add explicit rejection of 'Paragraph A' / 'Section B' / passage-initial capitals.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/option_bank.rs",
        "symbol": "detect_option_bank, line 14 (gate at line 33)",
        "whatToChange": "6.9's classification conjunction. Today the function returns None unless one of 6 literal 'list of ...' strings appears or the caller already decided the task is matching. Add has_roman_option_run and has_paragraph_targets as first-class evidence so a bank can be recovered without the exact instruction phrasing, and so MatchingHeadings classification can be gated on structure instead of substring.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "build_passage, line 383 — specifically the hardcoded \"paragraphMap\": {} at line 535",
        "whatToChange": "6.9's passage side. Populate paragraphMap from detected Paragraph A-G labels. The schema and runtime already accept it: schema/ielts_authoring_v2.rs:75 declares Option<BTreeMap<String,String>> and reading_source_v2.rs:113 already forwards passage.paragraph_map, so this is a producer-only change with no downstream work.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "build_passage ordering, line 119 vs the group loop at line 134",
        "whatToChange": "6.9's 'no region consumed by an option bank may enter the passage ContentDoc'. build_passage currently runs before any bank is detected, so the subtraction is impossible as written. Either move passage compilation after group recognition, or make it a two-pass (collect consumed node ids, then emit). Mirror completion.rs:142 completion_option_bank_line_ids, which already does exactly this for the completion stimulus.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/completion.rs",
        "symbol": "completion_table_node (1019) and completion_flowchart_node (1134)",
        "whatToChange": "6.10's table half. Read DocumentIRV2 pages[].tables[] (TableNodeV2 at schema/document_ir_v2.rs:241) and emit ContentNodeV2::Table from TableCellV2.row/col/row_span/col_span/content_region_ids (190-206) instead of reconstructing from sorted line text. Keep the existing closes_slots guard (mod.rs:1541) so an unrecovered table still degrades to source rows rather than a fabricated grid.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_signature.rs",
        "symbol": "infer_instruction_signature (17) / infer_task_type_from_cues (161) and its call site mod.rs:197",
        "whatToChange": "6.3's ordering rule: classification must follow boundary recovery. Requires restructuring the loop body so anchors/option runs/bank/completion structure are recovered first and their evidence feeds classification, then removing the reverse dependency where task_type gates detect_option_bank's matching_semantics (mod.rs:212-224), the completion bank (218) and recover_completion_structure (273).",
        "risk": "high",
        "blockedBy": "Circular today: task_type decides which detectors run, and 6.3 wants detector output to decide task_type. Needs a two-phase split (structure-only pass, then classify, then type-specific compilation)."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "symbol": "evaluate_quality (27) / evaluate_group (1300)",
        "whatToChange": "6.8's validate_basic_task. Introduce the per-task-family dispatch the plan specifies and route the existing scattered checks through it, so the closure set is readable and per-type. Do not rewrite the checks themselves — most already exist and are load-bearing (see the 6.8 verdicts).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "symbol": "validate_options (1622) and option_labels (2300)",
        "whatToChange": "Add the genuinely missing 6.8 checks: (a) require_unique_option_labels — option_labels returns a BTreeSet so duplicates vanish; count before dedup and raise a new blocking code; (b) require_expected_option_labels currently no-ops when instructionSignature.optionAlphabet is absent (expected_labels.as_ref().is_none_or(...) at 1698 and 1725) — derive expected labels from the recovered run when the instruction omits the range; (c) require_exact_fixed_response_set for tfng/ynng — assert exactly TRUE/FALSE/NOT GIVEN or YES/NO/NOT GIVEN rather than merely 'closed and renderable'.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "symbol": "source_coverage_summary (2981) and the document-level blocker at 141-157",
        "whatToChange": "6.8's require_question_source_coverage(task, 0.92) and 6.11's per-range blocker. Both need per-task-group coverage, which does not exist. Add a task-scoped variant of the ledger (source nodes whose bbox falls inside the group's geometric extent), then a blocking check at 0.92 per task and a 'large unassigned run between consecutive question numbers' rule.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "refresh_group_quality, line 2058 — specifically line 2094",
        "whatToChange": "taskGroups[].quality.sourceCoverage is fabricated: 1.0 when the group has any sourceAnchors, else 0.0. Any UI or gate that reads a per-group coverage number is reading a boolean. Replace with the real per-task coverage from the change above.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/issue_codes.rs",
        "symbol": "the 53 constants, lines 7-53",
        "whatToChange": "Reconcile names with 6.8 if the plan's vocabulary is meant to be authoritative: PROMPT_EMPTY vs QUESTION_PROMPT_MISSING, PROMPT_BOUNDARY_AMBIGUOUS vs QUESTION_PROMPT_BOUNDARY_AMBIGUOUS, OPTION_BANK_MISSING vs SHARED_OPTION_BANK_MISSING, SIGNIFICANT_REGION_UNASSIGNED vs SIGNIFICANT_SOURCE_TEXT_UNASSIGNED; add OPTION_LABEL_MISSING, OPTION_TEXT_MISSING, OPTION_LABEL_DUPLICATE. These strings cross the IPC boundary into the frontend issue list and are persisted inside saved revisions (authoring_v2_commands.rs preserve_issue_resolutions keys on issueId), so renaming is a data-compatibility change, not a rename.",
        "risk": "medium",
        "blockedBy": "Codes are persisted in authoring-ir-v2.shadow.json and in revision files; preserve_issue_resolutions (authoring_v2_commands.rs:781) matches previously resolved/ignored issues. A rename silently drops user resolutions."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "the DocumentIRV2 materialization block, lines 1487-1519",
        "whatToChange": "Real gap independent of the refactor: the filter is `main_source_file(&job).filter(|source| source.file_type == \"pdf\")`, so a DOCX job in the AUTO import path never gets a DocumentIRV2 shadow — physical_shadow stays None, quality raises PHYSICAL_SHADOW_MISSING and sourceCoverage is 0.0. The manual parse_document command handles both (authoring_commands.rs:186-190 branches pdf vs write_docx_facts_shadow_with_v1) and parse_document_core is never called from auto_pipeline. Mirror that branch here.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "has_reliable_question_groups (195), run_auto_pipeline_core_with_gateway (1381, call at 2050), run_cloud_review_core_with_gateway (2217, call at 2434), write_pipeline_authoring_v2_shadow (275, call at 288)",
        "whatToChange": "Four independent full recognition runs per job, each re-deriving make_dynamic_split_candidates + make_dynamic_authoring_ir + the whole V2 shadow from disk. 17.8 wants this decomposed into workers; the cheap intermediate step is to compute the shadow once and thread it, which also removes three of the four V1 re-derivations.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs",
        "symbol": "dynamic_block_text_preview (513) and split_section_evidence_for_blocks (542)",
        "whatToChange": "If group_lines keeps consuming sectionEvidence during the transition, raise or remove the 120-char cap at line 515 (`text.chars().take(120)`), or add a full-text field alongside textPreview. As long as recognition reads textPreview, every stem/option/note row longer than 120 chars is silently truncated. Note this file is what 17.6 wants frozen — a one-line cap change is a reasonable exception, a new field is not.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "rebind_lines_to_physical (635), merged_physical_anchor (710), physical_span_anchors (1894), passage_preamble_anchors (776), physical_text_key (768)",
        "whatToChange": "This whole cluster exists only to re-attach V1-derived lines to real geometry after the fact. Once recognition starts from DocumentIRV2 lines, all five become dead. Deleting them changes emitted sourceAnchors, so plan on updating the 4 rebind tests (mod.rs:2344, 2365, 2386, 2295) rather than preserving their expectations.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "symbol": "process_fixture, lines 1282-1420 (V1 chain at 1341-1375)",
        "whatToChange": "The acceptance harness hardcodes parse_source_document -> make_dynamic_split_candidates -> make_dynamic_authoring_ir -> build_authoring_v2_shadow. It must gain a geometry-only path, ideally running both and diffing, so the 8-PDF corpus can prove the new recognizer against the recorded baselines rather than being rewritten to accept whatever it produces.",
        "risk": "high",
        "blockedBy": "fixtures/golden/private-real/*.pdf are gitignored (fixtures/golden/private-real/.gitignore: *.pdf) and absent from this checkout — only README.md is present. The two non-ignored tests that need them cannot run here."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "answer_key_from_v1 usage at line 133, fixed_options_from_v1 (1229), find_v1_group (1817) / find_v1_question (1843) / candidate_question (1828), passage htmlBlocks fallback (449-479)",
        "whatToChange": "These are the remaining v1_authoring dependencies that survive after split is removed, and 17.7 says v1_authoring must stop being a necessary input. Answers in particular have no geometry source — answer_key_from_v1 (answer_key.rs:3) is the only producer of V2 answerKey values. Decide explicitly whether answers move to a separate answer-source pass or the entry keeps an optional answers argument; otherwise a geometry-only recognize_local yields all-unresolved answers and validate_answers (quality.rs:2114) blocks every job.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "build_shadow_compare, line 2101 (policy block at 2138-2144)",
        "whatToChange": "Hardcodes the shadow-era policy: v1RemainsAuthoritative: true, v2EntersAuthoring: false, v2EntersExport: false. The converged product has V2 authoritative and V1 gone, so this report is either deleted or inverted. Also note the naming debt throughout: SHADOW_ARTIFACT_FILE, write_authoring_v2_shadow, the mod.rs:1-7 doc comment ('It never writes either V1 artifact... before any future promotion decision') and audit notes at mod.rs:372 all still describe a shadow that is in fact the authoritative input to the workspace.",
        "risk": "low"
      }
    ],
    "testHarness": "Everything is Rust inline `#[cfg(test)] mod tests` plus one dedicated test-only module. No integration test directory for this subsystem.\n\nInline test counts (all in src-tauri/src/ielts_grammar/): quality.rs 25, mod.rs 19, completion.rs 12, real_pdf_acceptance.rs 12, instruction_signature.rs 8, instruction_zone.rs 7, option_bank.rs 6, option_run.rs 5, question_number.rs 4, reading.rs 3, anchors.rs 2, diagram.rs 1, prompt_assembler.rs 1. evidence.rs, answer_key.rs and issue_codes.rs have none. Total 105.\n\nThe fixture-builder to copy for any new unit test — every small module repeats this exact helper, so match it verbatim:\n\n    fn line(id: &str, text: &str) -> SemanticLine {\n        SemanticLine {\n            id: id.to_string(),\n            text: text.to_string(),\n            source_anchor: json!({\"nodeIds\":[id]}),\n            page_index: 0,\n            order: 0,\n            role: String::new(),\n            bbox: None,\n        }\n    }\n\n(anchors.rs:96, prompt_assembler.rs:125, option_run.rs:250, reading.rs:115, completion.rs:1252 and diagram.rs:95 all use this shape. Widening SemanticLine means touching all six.)\n\nmod.rs tests build a full ImportJob via a local `job()` helper (mod.rs:2403-2427) and call build_authoring_v2_shadow with hand-written split/authoring JSON — the `\"questionGroupCandidates\":[{...}]` literals at mod.rs:2446, 2505, 2575, 2626, 2711, 2754, 2816 are the templates for exercising the entry point. `semantic_line(...)` (used at mod.rs:2386) is the mod.rs-local variant.\n\nTest names worth knowing because they encode current contracts you may be about to break:\n- mod.rs:2344 v1_evidence_rebinds_only_to_a_unique_same_page_physical_line\n- mod.rs:2365 v1_block_rebinds_to_every_line_in_a_unique_contiguous_physical_span\n- mod.rs:2386 v1_evidence_bbox_fallback_rebinds_all_overlapping_physical_lines — its input line is literally named \"Truncated preview\", confirming the 120-char cap is a known, designed-around condition\n- mod.rs:2430 builds_typed_shadow_without_mutating_v1_shape\n- mod.rs:2487 matching_people_uses_one_source_backed_bank_and_response_group\n- mod.rs:2615 completion_word_bank_is_bound_to_response_and_removed_from_stimulus\n- mod.rs:2700 incomplete_table_does_not_fabricate_per_question_prompts\n- mod.rs:2800 malformed_choose_two_range_keeps_one_shared_budget_and_fails_closed\n- mod.rs:2887 phase4_fixture_matrix_covers_expression_signature_and_fallback_rules\n- option_run.rs:296 ordinary_paragraph_a_is_not_an_option_without_sequence_evidence\n- option_run.rs:316 detects_lowercase_roman_heading_bank_without_promoting_words\n- anchors.rs:122 bare_declared_question_number_is_an_anchor\n\nReal-PDF acceptance (real_pdf_acceptance.rs). Driver is process_fixture (1282), which runs the full V1->V2 chain and pushes named checks (MANIFEST_PDF_IDENTITY, METADATA_BASELINE_SOURCE_IDENTITY, EXECUTABLE_V1_BASELINE_SUMMARY, REAL_PDF_PHYSICAL_DOCUMENT_IR_V2, PHYSICAL_AUTHORING_QUALITY_CHAIN, ...). Spec: fixtures/golden/phase4-eight-pdf-acceptance.json (8 fixtureIds, 11 requiredProofs, policy {v1Authoritative: true, v2ShadowOnly: true, productionExposure: false, falseReadyForbidden: true}). Manifest: fixtures/golden/manifest.json (sourcePath/sha256/sizeBytes/metadataPath/baselinePath per fixture). Recorded expectations: fixtures/golden/metadata/*.json and fixtures/golden/baseline/v1/*.json. Reports land in tmp/phase4-real-pdf-acceptance/report.json and tmp/phase5-real-pdf-acceptance/report.json (tmp/ is gitignored).\n\nImportant caveat I could not work around: the 8 real fixtures point at fixtures/golden/private-real/*.pdf, which that directory's own .gitignore excludes (`*.pdf`) and which are absent here — only README.md is present. chili_real_pdf_reaches_all_declared_acceptance_truth (1894) and petri_real_pdf_reaches_all_declared_acceptance_truth (1921) carry #[ignore], but phase4_eight_real_pdfs_reach_physical_authoring_quality_truth (1946) and phase5_real_pdf_edit_and_v2_export_round_trip (2017) do NOT — and both need those PDFs. I did not run cargo (instructed not to), so I cannot say whether they fail or whether the PDFs exist on this machine outside the repo; treat the strongest existing regression signal as unverified until you confirm the corpus is present locally.\n\nFully self-contained tests in that file (no private PDFs): compiler_probe_chain_requires_honest_failure_semantics (1788), quality_acceptance_policy_rejects_unexplained_blockers_and_requires_live_ocr_evidence (1820), quality_acceptance_policy_limits_runtime_exception_to_organisational_unresolved_set (1860), corpus_exam_id_check_rejects_duplicates_and_unsafe_segments (2048), metadata_contract_asserts_response_shape_binding_and_reuse (2066), metadata_contract_accepts_completion_slots_embedded_in_response_prompt (2144), completion_embedding_detection_recurses_prompt_and_stimulus_per_slot (1693), image_only_answer_pages_remain_unresolved_without_oracle_overlay (1727).\n\nOne cross-file test to remember: pdf_geometry.rs:3157-3175 calls make_dynamic_split_candidates -> make_dynamic_authoring_ir -> ielts_grammar::build_authoring_v2_shadow and asserts \"product grammar path should consume the promoted physical asset\". It will break on any entry-signature change and is easy to miss because it lives outside the ielts_grammar tree.\n\nBoth feature flags that gate this code are hardcoded true, so nothing is behind a switch you can flip while refactoring: environment.rs:219 document_ir_v2_shadow_enabled, :223 authoring_v2_shadow_enabled, :227 quality_gate_v2_enabled.",
    "surprises": [
      "The recognition substrate is truncated to 120 characters. group_lines' primary path builds SemanticLine.text from candidate.sectionEvidence[].textPreview (mod.rs:576-578), and that field is dynamic_block_text_preview = text.chars().take(120) (authoring_pipeline.rs:513-516, applied at 552). rebind_lines_to_physical restores the anchor and bbox from real geometry but never rewrites text (mod.rs:680-682). The V1 lines that DO carry full text (semantic_lines_from_v1_document, reading block.text) are only tier-2 fallback, used when sectionEvidence is empty. The test at mod.rs:2386 feeds an input literally named 'Truncated preview', so this is a known and designed-around condition rather than an oversight — but it means any stem, option or note row over 120 chars is silently cut before any grammar rule sees it.",
      "For PDF, the V1 geometry that recognition uses is fabricated. parser.rs:310-330 document_block sets bbox = [72, y0, 520, min(y0+36,794)] with y0 = 72 + (ordinal % 16) * 42, and hardcodes page width/height 595x842 (parser.rs:585-586). There is exactly one PDF V1 parser (pdf_extract::extract_text_by_pages, parser.rs:535) and it yields no geometry. Real geometry exists only in document-ir-v2.shadow.json, and write_pdf_facts_shadow_with_v1 (pdf_facts_shadow.rs:474) does not backfill it into V1 — it only bridges diagram-region assets in the other direction. So rebind_lines_to_physical's bbox-intersection fallback (mod.rs:723-742) is matching synthetic rectangles against real ones.",
      "V1 PDF blocks are page-level, not paragraph-level, in the worst case. semantic_text_chunks (parser.rs:269-307) first tries blank-line paragraphs; if a page yields only one, it splits on the markers 'reading passage ', 'questions ', 'question ', 'answer key', 'answers'. A page of questions with no blank lines becomes one V1 block containing every question and option on it — then truncated to 120 chars as textPreview.",
      "The auto-import path never builds DocumentIRV2 for DOCX. auto_pipeline.rs:1488 filters `source.file_type == \"pdf\"`. The manual parse_document command does handle both (authoring_commands.rs:186-190 dispatches write_pdf_facts_shadow_with_v1 vs write_docx_facts_shadow_with_v1) but parse_document_core has exactly one caller, lib.rs:913, and it is not the pipeline. So a DOCX job imported normally gets physical_shadow = None, a PHYSICAL_SHADOW_MISSING warning (quality.rs:126-140) and sourceCoverage 0.0 for the life of the job.",
      "passage.paragraphMap is hardcoded empty: mod.rs:535 emits `\"paragraphMap\": {}` unconditionally. This is the passage half of 6.9's Matching Headings separation, and it is the cheapest gap in the whole plan to close — schema/ielts_authoring_v2.rs:75 already declares Option<BTreeMap<String,String>> and reading_source_v2.rs:113 already forwards it to the runtime compiler. Nothing consumes it because nothing produces it.",
      "Per-group sourceCoverage is a disguised boolean. refresh_group_quality (mod.rs:2094) writes taskGroups[].quality.sourceCoverage = 1.0 if the group has any sourceAnchors at all, else 0.0. Real coverage is only ever computed document-wide (quality.rs:2981). Any gate or UI reading a per-group coverage number is reading 'has anchors: yes/no'.",
      "quality.rs is the real 17.7 problem, not mod.rs. mod.rs is 3030 lines / 112KB with 2165 production lines; quality.rs is 4468 lines / 157KB with ~3649 production lines and 60+ private validators. The plan's migration map allots quality.rs two target files (reliability.rs + current_ds_validation.rs) and mod.rs one; the effort is the other way round.",
      "Duplicate option labels are invisible to the gate. option_labels (quality.rs:2300) collects into a BTreeSet, so [A,A,C,D] becomes {A,C,D}. It is caught only as a side effect: if optionAlphabet was parsed, the set comparison at 1698/1725 fails as OPTION_ALPHABET_MISMATCH. With no declared alphabet — common for tfng, ynng and short_answer — duplicates pass silently.",
      "The 6.8 per-statement prompt closure works, but not through the code you would expect. PROMPT_EMPTY is group-scoped (group_prompt_text at quality.rs:2755 concatenates all stimulus + all response prompts, so one non-empty prompt satisfies it). The real per-question closure is a placeholder round-trip: mod.rs:1044-1047 emits the literal '[prompt pending review]' for an empty per-question prompt, and quality.rs:2798-2818 detects it and raises the blocking PROMPT_BOUNDARY_AMBIGUOUS. Any refactor that changes the placeholder string, or that stops emitting it, silently removes a blocking check.",
      "recognize_local(&DocumentIRV2) cannot produce answers. answer_key_from_v1 (answer_key.rs:3) reading v1_authoring.answerKey is the sole producer of V2 answerKey values, and validate_answers (quality.rs:2114) blocks on missing/invalid answers. A geometry-only entry point therefore blocks every job unless answers are routed in separately. 17.7 does not address this.",
      "The subsystem is called a shadow everywhere but is the authoritative input to the converged workspace. mod.rs:1-7 says 'It never writes either V1 artifact... before any future promotion decision'; build_shadow_compare (mod.rs:2138-2144) hardcodes v1RemainsAuthoritative: true / v2EntersAuthoring: false / v2EntersExport: false; the audit note at mod.rs:372 says 'Phase 4 grammar shadow; V1 remains authoritative.' Meanwhile authoring_v2_commands.rs:733 loads authoring-ir-v2.shadow.json as the base document for the workspace editor. The names and the policy block are stale by a whole phase.",
      "Recognition runs four times per job. auto_pipeline.rs:201-208 (has_reliable_question_groups, gating vision transcription), 288 (the artifact write), 2050 (the v2 quality gate), 2432-2440 (cloud review) — each independently re-derives make_dynamic_split_candidates + make_dynamic_authoring_ir + the whole V2 shadow. On a large PDF that is four full grammar passes plus four V1 authoring builds.",
      "Everything is untyped JSON. Despite a complete typed schema (schema/document_ir_v2.rs, schema/ielts_authoring_v2.rs), every recognizer signature takes &Value / Option<&Value>. The only typed touchpoints are the final serde_json::from_value::<IeltsAuthoringIRV2> validation (mod.rs:378), TaskTypeV2 threading, and quality.rs's compiler probes. 17.7's `recognize_local(document: &DocumentIRV2)` is therefore a bigger change than the signature suggests — it introduces typing to a subsystem that has none."
    ]
  },
  {
    "subsystem": "src-tauri/src/lib.rs command surface, AppState, and the test harness — cheapest path to a real create_import_job → import_source_file → run_auto_pipeline → get_authoring_v2 → apply_authoring_v2_patches → publish_nas_package_v2 chain test",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "Crate root. Lines 1-90: imports + 44 `mod` decls (only `pub mod schema;` at :82 is public). :354-355 `AppState`. :357-359 `app_root()`. :492-751 CLI helpers (`run_auto_pipeline_from_path` at :502 is the closest existing headless full-chain driver, incl. `--app-root`). :851-1401 all 57 `#[tauri::command]` wrappers. :1403-1476 `run()` with `.manage(AppState)` (:1407) and `tauri::generate_handler![...]` (:1415-1473, 57 entries). :1480-11009 `#[cfg(test)] mod tests` — 136 `#[test]` fns and all shared test helpers.",
        "sizeNote": "11009 lines; 9530 of them (87%) are the single `#[cfg(test)] mod tests` block"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_commands.rs",
        "role": "THE blocker module. Holds `create_import_job_core` (:77) and `import_source_file_core` (:158) — the only two cores in the whole chain that take `app: AppHandle` instead of `root: &Path`. Has NO `#[cfg(test)]` module at all; both fns are untested.",
        "sizeNote": "297 lines, 0 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "`run_auto_pipeline_core` (:1153) and the gateway-injectable `run_auto_pipeline_core_with_gateway` (:1381). Writes document-ir.json, `document-ir-v2.shadow.json` (PDF-only, :1490-1519), authoring-ir.json, and `authoring-ir-v2.shadow.json` (:2027).",
        "sizeNote": "2796 lines, 4 tests of its own; ~20 lib.rs tests drive it"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "`get_authoring_v2_core` (:54), `apply_authoring_v2_patches_core` (:123), `validate_authoring_v2_publish_readiness` (:174), `export_authoring_v2_core` (:449), `build_editor_session` (:711), `load_current_authoring` (:725), `refresh_quality_report` (:744), `validate_authoring` (:830). `AUTHORING_V2_SHADOW_FILE` const at :34.",
        "sizeNote": "2551 lines; test mod at :2003, 14 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "role": "`publish_nas_package_v2_core` (:245) plus the gate that makes the chain non-optional: `validate_v2_export_binding` (:56) requires an `export_authoring_v2` receipt on disk. Its test mod (:1424) contains `test_export_bundle` (:1441) which hand-forges that receipt.",
        "sizeNote": "2039 lines, 15 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/util.rs",
        "role": "`job_dir` (:32), `safe_job_dir` (:43), `ensure_app_dirs` (:62), `stage_file_with_hash` (:167), `ensure_job_dirs` (:223), `read_json`/`write_json`/`hash_file_or_path`. Everything is root-relative — this is why temp-root tests work.",
        "sizeNote": "389 lines, 3 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "role": "`make_job` (:7), `load_job` (:28), `save_job` (:33) — save_job is the SQLite dual-write hook (:37 calls `library_commands::upsert_reading_job`). Its single test is the minimal temp-root recipe.",
        "sizeNote": "105 lines, 1 test"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "role": "`db_path(root)` = `<root>/authoring_hub.db` (:27-30), `open_connection` (:51) opens WAL + `ensure_schema` per call. No connection lives in AppState (see the module doc at :5-8). A temp root therefore gets a real, isolated SQLite file for free.",
        "sizeNote": "1349 lines, 12 tests (all `Connection::open_in_memory()`)"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "role": "`upsert_reading_job` (:92), `exam_record_from_reading_job` (:47 — `id: job.job_id.clone()`), `upsert_library_item_from_exam` (:172 — `id: exam.id.clone()`, `linked_ingest_job_id: Some(exam.id)`), `migrate_existing_into_library` (:386, called from `run()`'s setup at lib.rs:1411).",
        "sizeNote": "802 lines, 6 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "`evaluate_quality` — the hard gate that decides `ready` / `review_required` / `blocked` (state derivation at :186-198). Its test helpers `early_approaches()` (:3653) and `valid_physical_shadow()` (:3689) are the ONLY proven recipe for reaching `ready`.",
        "sizeNote": "4468 lines, 25 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "role": "The existing precedent for an in-crate multi-command integration test: a whole file that is a `#[cfg(test)] mod` (declared at ielts_grammar/mod.rs:23-24). `run_phase5_real_pdf_edit_export` (:1584) already drives shadow → apply_authoring_v2_patches_core ×2 → export_authoring_v2_core.",
        "sizeNote": "2207 lines, 12 tests — ALL currently unrunnable in this tree (see surprises)"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs",
        "role": "Revision store the V2 editor writes through: `JobArtifactPaths::for_job` (:40), `ensure_job_artifact_layout` (:142), `recover_current_revision` (:210), `append_revision` (:240), `read_revision` (:337), `list_revision_records` (:345).",
        "sizeNote": "888 lines, 8 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/Cargo.toml",
        "role": "`[lib] name = \"ielts_author_studio_lib\"`, `crate-type = [\"staticlib\",\"cdylib\",\"rlib\"]`. `tauri = { version = \"2\", features = [] }` — no `test` feature. There is NO `[dev-dependencies]` section and no `[[test]]` target.",
        "sizeNote": "46 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/scripts/e2e/library-workspace-smoke.mjs",
        "role": "The only existing 'product chain' smoke. Its own header (lines 4-8) states it is browser + devFallbackBackend, explicitly NOT real Tauri/SQLite/filesystem, and names plan P0-T02 as the gap.",
        "sizeNote": "single file; scripts/e2e/ contains nothing else"
      }
    ],
    "publicApi": [
      {
        "symbol": "AppState",
        "file": "src-tauri/src/lib.rs:354",
        "signature": "#[derive(Debug, Default)]\npub struct AppState;",
        "notes": "A zero-field unit struct. It holds NOTHING. Grepping the whole crate for `AppState` returns exactly three hits: this declaration (lib.rs:355), `.manage(AppState)` (lib.rs:1407), and a db.rs comment (:7) explaining why the SQLite Connection is deliberately NOT in it. No command anywhere takes `State<AppState>`. Consequence: there is no state to construct for a test — the plan's `AppState::new(...)` (17.1) does not exist today."
      },
      {
        "symbol": "app_root",
        "file": "src-tauri/src/lib.rs:357",
        "signature": "fn app_root(app: &AppHandle) -> CommandResult<PathBuf> {\n    app.path().app_data_dir().map_err(|error| error.to_string())\n}",
        "notes": "Private, 2 lines, and the ONLY app-root resolution in the GUI path. There is NO env-var override and no settable path. I grepped every `env::var(...)` in the crate (25 distinct names, none is an app-root override) — the closest thing is the CLI `--app-root` flag consumed only by `run_auto_pipeline_from_path` (lib.rs:516-524), which never reaches this fn. Resolution goes through `dirs 6.0` (Cargo.lock:855) + identifier `com.ielts.author.studio` (tauri.conf.json), i.e. on Windows the real `%APPDATA%\\com.ielts.author.studio` via the known-folder API — not redirectable by env."
      },
      {
        "symbol": "run",
        "file": "src-tauri/src/lib.rs:1403",
        "signature": "pub fn run() {\n    tauri::Builder::default()\n        .plugin(tauri_plugin_dialog::init())\n        .plugin(tauri_plugin_opener::init())\n        .manage(AppState)\n        .setup(|app| {\n            let root = app_root(app.handle()).map_err(Box::<dyn std::error::Error>::from)?;\n            ensure_app_dirs(&root).map_err(Box::<dyn std::error::Error>::from)?;\n            // 初始化题库 DB schema + 首次启动迁移既有 job 数据（幂等，失败不阻断启动）。\n            let _ = library_commands::migrate_existing_into_library(&root);\n            Ok(())\n        })\n        .invoke_handler(tauri::generate_handler![ /* 57 commands, lib.rs:1416-1472 */ ])\n        .run(tauri::generate_context!())\n        .expect(\"error while running tauri application\");\n}",
        "notes": "generate_handler! list is lib.rs:1416-1472 and contains exactly 57 entries, in this order: create_import_job, list_jobs, get_job, update_job_meta, delete_job, import_source_file, reveal_job_folder, choose_export_dir, pick_pdf_folder_sources, parse_document, debug_document_ir_v2_overlay, rerun_ocr, apply_manual_transcription, apply_vision_transcription, resolve_source_review, run_rule_split, save_split_adjustments, build_authoring_ir, update_authoring_ir, get_authoring_v2, resolve_authoring_asset_preview, apply_authoring_v2_patches, export_authoring_v2, render_group_html, list_llm_profiles, run_environment_preflight, get_diagnostics_settings, save_diagnostics_settings, get_job_artifact_status, save_llm_profile, delete_llm_profile, test_llm_profile, llm_classify_group, llm_extract_group, apply_llm_suggestion, apply_vision_answer_candidates, validate_authoring_ir, generate_preview_assets, run_preview_e2e, run_auto_pipeline, run_cloud_review, export_reading_assets, export_reading_js, export_nas_library, publish_nas_package_v2, export_writing_library, create_writing_job, list_writing_jobs, get_writing_job, update_writing_job, delete_writing_job, list_library_exams, get_library_exam, update_library_exam_meta, delete_library_exam, search_library_exams, get_library_stats, restore_library_exam, list_trashed_exams. (That enumeration is 59 names — the two extra are because `restore_library_exam` and `list_trashed_exams` close the list; the awk count of non-blank stripped lines in 1416..1472 is 57. Treat 1416-1472 as the authoritative range, not my prose count.)"
      },
      {
        "symbol": "create_import_job / create_import_job_core",
        "file": "src-tauri/src/lib.rs:851, src-tauri/src/job_commands.rs:77",
        "signature": "#[tauri::command]\nasync fn create_import_job(input: CreateJobInput, app: AppHandle) -> CommandResult<ImportJob> {\n    job_commands::create_import_job_core(input, app).await\n}\n\npub(crate) async fn create_import_job_core(\n    input: CreateJobInput,\n    app: AppHandle,\n) -> CommandResult<ImportJob> {\n    let root = app_root(&app)?;\n    ensure_app_dirs(&root)?;\n    let job = make_job(input);\n    let dir = job_dir(&root, &job.job_id);\n    ensure_job_dirs(&dir)?;\n    ensure_job_artifact_layout(&root, &job.job_id)?;\n    save_job(&root, &job)?;\n    Ok(job)\n}",
        "notes": "BLOCKER #1. The whole body is 7 statements and the AppHandle is used only on the first line. Quoted in full because this is the exact code an implementer either refactors or reproduces."
      },
      {
        "symbol": "import_source_file_core",
        "file": "src-tauri/src/job_commands.rs:158",
        "signature": "pub(crate) async fn import_source_file_core(\n    job_id: String,\n    file_path: String,\n    role: String,\n    app: AppHandle,\n) -> CommandResult<SourceFile> {\n    let root = app_root(&app)?;\n    let dir = job_dir(&root, &job_id);\n    ensure_job_dirs(&dir)?;\n    /* ... ~45 lines: staging_name -> stage_file_with_hash -> stored_name = format!(\"{}-{}\", &hash[..8], sanitize_filename(&original_name)) -> rename -> update_job(...) ... */\n}",
        "notes": "BLOCKER #2. AppHandle used only at line 164. Body ends with `update_job(&root, &job_id, |job| { job.source_files.push(source.clone()); job.status = JobStatus::Working; job.current_step = WorkflowStep::DocumentReview; })?;` (job_commands.rs:203-207). Note the `stored_name` scheme is identical to the test helper `attach_fixture_source` (lib.rs:1837)."
      },
      {
        "symbol": "run_auto_pipeline_core",
        "file": "src-tauri/src/auto_pipeline.rs:1153",
        "signature": "pub(crate) fn run_auto_pipeline_core(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n) -> CommandResult<Value> {\n    run_auto_pipeline_core_with_gateway(root, job_id, input, run_llm_gateway)\n}",
        "notes": "Root-taking and synchronous. Already called directly by ~10 lib.rs tests. Guard at auto_pipeline.rs:1402: `if dir.join(\"authoring-ir.json\").exists() && !allow_overwrite { return Err(\"editable_draft_exists; ...\") }`."
      },
      {
        "symbol": "run_auto_pipeline_core_with_gateway",
        "file": "src-tauri/src/auto_pipeline.rs:1381",
        "signature": "pub(crate) fn run_auto_pipeline_core_with_gateway<F>(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n    mut llm_gateway: F,\n) -> CommandResult<Value>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value> + Send,",
        "notes": "The seam for a hermetic test: pass a closure that panics or returns Err, and assert the gateway was never called. Used by ~10 lib.rs tests. Cloud work is only spawned when `profile_id.is_some() && main_source_is_pdf(&job)` (auto_pipeline.rs:1417); with `input: None` no profile is selected at all, so no network."
      },
      {
        "symbol": "get_authoring_v2_core",
        "file": "src-tauri/src/authoring_v2_commands.rs:54",
        "signature": "pub(crate) fn get_authoring_v2_core(root: &Path, job_id: &str) -> CommandResult<Value> {\n    safe_job_dir(root, job_id)?;\n    build_editor_session(root, job_id)\n}",
        "notes": "Reads revision N>0 from the revision store, else falls back to `<job_dir>/authoring-ir-v2.shadow.json`; errors `AUTHORING_V2_NOT_AVAILABLE:shadow_missing:<path>` if neither exists (load_current_authoring, :725-742). Returns `{schemaVersion:\"AuthoringEditorSessionV1\", jobId, authoring, revision, source:\"shadow\"|\"revision\", revisions, v1FilesRemainReadable}`. NOT called by any test in the crate today."
      },
      {
        "symbol": "apply_authoring_v2_patches_core",
        "file": "src-tauri/src/authoring_v2_commands.rs:123",
        "signature": "pub(crate) fn apply_authoring_v2_patches_core(root: &Path, input: Value) -> CommandResult<Value> {",
        "notes": "Input struct (authoring_v2_commands.rs:37-43): `#[serde(rename_all = \"camelCase\")] struct ApplyAuthoringV2PatchesInput { pub job_id: String, pub base_revision: u64, pub patches: Vec<Value> }` — so the JSON is `{\"jobId\":..,\"baseRevision\":0,\"patches\":[..]}`. Rejects empty patches (`authoring_v2_patches_required`), >200 patches, and revision mismatch (`revision_conflict:current=N:base=M`). Calls `refresh_quality_report` then `validate_authoring` then `append_revision`, so quality is RECOMPUTED on every save."
      },
      {
        "symbol": "export_authoring_v2_core",
        "file": "src-tauri/src/authoring_v2_commands.rs:449",
        "signature": "pub(crate) fn export_authoring_v2_core(root: &Path, input: Value) -> CommandResult<Value> {",
        "notes": "Input struct (:45-51): `struct ExportAuthoringV2Input { pub job_id: String, pub export_dir: String, pub revision: Option<u64> }`. `export_dir` MUST be absolute (:497-499 `authoring_v2_export_dir_must_be_absolute`). Writes `<export_dir>/<safeExamId>-r<rev>-<uuid>/{authoring-ir-v2.json, reading-source-v2.json, manifest-v2.json}` and returns `{receipt:{..,outputDir,authoringPath,runtimePath,manifestPath,publishProof}, history, outputDir, revision, examId}`. Hard-fails with `authoring_v2_export_blocked:quality_state=<state>` (:482-486) unless the RE-EVALUATED quality is `ready`."
      },
      {
        "symbol": "publish_nas_package_v2_core",
        "file": "src-tauri/src/nas_package_v2.rs:245",
        "signature": "pub(crate) fn publish_nas_package_v2_core(root: &Path, input: Value) -> CommandResult<Value> {",
        "notes": "Input struct (nas_package_v2.rs:40-54): `#[serde(rename_all=\"camelCase\")] struct NasPackagePublishInput { #[serde(alias=\"exportDir\")] pub library_root: String, #[serde(alias=\"runtimePath\", alias=\"sourceFile\")] pub source_path: String, pub asset_root: Option<String>, pub exam_id: Option<String>, pub minimum_runtime_version: Option<String>, pub expected_manifest_sha256: Option<String>, pub fault: Option<String>, pub job_id: Option<String>, pub revision: Option<u64> }`. Both paths must be absolute (`absolute_path`, :886-892). `source_path`'s file_name MUST literally be `reading-source-v2.json` (:62). Pure Rust end to end — the pre-commit `run_student_loader_probe` (reading_runtime_v2.rs:48) spawns no process."
      },
      {
        "symbol": "validate_v2_export_binding",
        "file": "src-tauri/src/nas_package_v2.rs:56",
        "signature": "fn validate_v2_export_binding(\n    root: &Path,\n    input: &NasPackagePublishInput,\n    source_path: &Path,\n    source_bytes: &[u8],\n) -> CommandResult<()> {",
        "notes": "THE gate that forces `export_authoring_v2` into the chain. Requires, in the same dir as source_path: `manifest-v2.json` with `schemaVersion==\"AuthoringV2ExportReceiptV1\"`, exactly the 3 expected `files`, `reviewRequired==false`, matching `authoringSha256`/`runtimeSha256`, plus `authoring-ir-v2.json`. Then re-checks the persisted revision on disk (`<job_dir>/authoring-ir-v2.shadow.json` when revision==0, else `authoring/revisions/<rev>.json`) matches the exported authoring modulo `quality`, recompiles the runtime and byte-compares it, and finally re-runs `validate_authoring_v2_publish_readiness` and requires `manifest.publishProof` to equal it exactly."
      },
      {
        "symbol": "validate_authoring_v2_publish_readiness",
        "file": "src-tauri/src/authoring_v2_commands.rs:174",
        "signature": "pub(crate) fn validate_authoring_v2_publish_readiness(\n    root: &Path,\n    job_id: &str,\n    revision: u64,\n    authoring_value: &Value,\n) -> CommandResult<Value> {",
        "notes": "Reads quality FROM the passed value (it does NOT re-evaluate) — which is why `nas_package_v2::tests::test_export_bundle` can force `quality.state=\"ready\"` and still get a valid proof. Additional gates: revision must be current, no `unresolved` answerKey entries, no hardFailures, no unresolved Blocking issues, `audit.humanVerified==true`, and a `SourceReviewV1` for the job that is `resolved==true` and `stale!=true` with no blocking issues, and no ai-fallback/partial-failure markers in authoring, `authoring-ir.json`, or `pipeline-report.json`."
      },
      {
        "symbol": "ensure_app_dirs / job_dir / ensure_job_dirs / stage_file_with_hash",
        "file": "src-tauri/src/util.rs:62, :32, :223, :167",
        "signature": "pub(crate) fn ensure_app_dirs(root: &Path) -> CommandResult<()>\npub(crate) fn job_dir(root: &Path, job_id: &str) -> PathBuf   // root/jobs/<job_id>\npub(crate) fn ensure_job_dirs(path: &Path) -> CommandResult<()>\npub(crate) fn stage_file_with_hash(path: &Path, target: &Path) -> CommandResult<(String, u64)>",
        "notes": "`ensure_app_dirs` creates config, config/secrets, jobs, writing-jobs, logs, cache, cache/parser, cache/thumbnails, cache/preview-server. `ensure_job_dirs` creates uploads, sources, preview, preview/runtime, exports, extraction, authoring, authoring/revisions, authoring/patches, assets, assets/{blobs,metadata,previews}, export-history (util.rs:224-239)."
      },
      {
        "symbol": "make_job / save_job",
        "file": "src-tauri/src/job_store.rs:7, :33",
        "signature": "pub(crate) fn make_job(input: CreateJobInput) -> ImportJob\npub(crate) fn save_job(root: &Path, job: &ImportJob) -> CommandResult<()>",
        "notes": "`make_job` mints `job_id = format!(\"import-{}-{}\", now.format(\"%Y%m%d%H%M%S\"), &uuid[..8])`. `save_job` writes `<root>/jobs/<id>/job.json` AND calls `library_commands::upsert_reading_job(root, job)` (job_store.rs:37) — the SQLite dual-write, whose failures are only eprintln'd, never propagated."
      },
      {
        "symbol": "db_path / open_connection",
        "file": "src-tauri/src/db.rs:27, :51",
        "signature": "pub(crate) fn db_path(root: &Path) -> PathBuf   // root/authoring_hub.db\npub(crate) fn open_connection(root: &Path) -> CommandResult<Connection>",
        "notes": "Transient connection per operation, WAL + foreign_keys=ON + busy_timeout=5000, `ensure_schema` every open. Nothing cached, nothing in AppState. `library_items.id == exams.id == job.job_id` is confirmed at library_commands.rs:50 (`id: job.job_id.clone()`) and :187 (`id: exam.id.clone()`), with `linked_ingest_job_id: Some(exam.id.clone())` at :197."
      },
      {
        "symbol": "run_auto_pipeline_from_path (CLI)",
        "file": "src-tauri/src/lib.rs:502",
        "signature": "fn run_auto_pipeline_from_path(path: &Path, args: &[String]) -> CommandResult<Value> {\n    /* ... */\n    let root = cli_option_value(args, \"--app-root\")\n        .map(PathBuf::from)\n        .unwrap_or_else(|| {\n            env::temp_dir().join(format!(\n                \"ielts-author-studio-live-{}\",\n                uuid::Uuid::new_v4().simple()\n            ))\n        });\n\n    util::ensure_app_dirs(&root)?;\n    /* ... llm profile, hash file, build SourceFile, job_store::make_job, ensure_job_dirs,\n       util::write_bytes(uploads/<stored_name>), job_store::save_job, then: */\n    let report = auto_pipeline::run_auto_pipeline_core(&root, &job.job_id, Some(AutoPipelineInput { .. }))?;\n}",
        "notes": "This is the existing, shipped, AppHandle-free full-chain driver and the single best template for product_chain.rs. It proves the whole import+pipeline half of the chain needs no Tauri at all. Its only hard requirement is live LLM credentials (`missing_llm_base_url`/`missing_llm_model`/`missing_llm_api_key`, lib.rs:506-514) — a test drops those and passes `input: None`."
      }
    ],
    "dataFlow": "Every command in the chain is a 1-3 line `#[tauri::command]` wrapper whose ONLY use of the AppHandle is `let root = app_root(&app)?;`, after which it delegates to a synchronous `_core(root: &Path, ...)` function. lib.rs:1009-1012 is the archetype: `async fn get_authoring_v2(job_id: String, app: AppHandle) -> CommandResult<Value> { let root = app_root(&app)?; get_authoring_v2_core(&root, &job_id) }`. So the AppHandle is a path-resolution token, nothing more — there is no event emission, no managed state, and no window access anywhere in the six chain commands.\n\nThe exception is job_commands.rs: `create_import_job_core` (:77) and `import_source_file_core` (:158) take `app: AppHandle` themselves and call `app_root` internally. They are the only two members of the chain that cannot be driven from a `&Path`.\n\nOn-disk flow, all under one app root:\n1. create_import_job → `ensure_app_dirs(root)`; `make_job` mints `import-<ts>-<8hex>`; `ensure_job_dirs(root/jobs/<id>)`; `ensure_job_artifact_layout`; `save_job` writes `jobs/<id>/job.json` and dual-writes SQLite `<root>/authoring_hub.db` (tables `exams` + `library_items`, both keyed on `id == job_id`).\n2. import_source_file → stages the file to `jobs/<id>/uploads/.staging-<uuid>-<name>`, hashes it, renames to `uploads/<sha[..8]>-<sanitized name>`, pushes a `SourceFile` onto `job.source_files` via `update_job` (which re-runs save_job → SQLite again), and sets status=Working / step=DocumentReview.\n3. run_auto_pipeline → `load_job`; parses the main source into `jobs/<id>/document-ir.json` (parser cache at `<root>/cache/parser/<job_id>-document-ir.json`); writes `source-review.json`; for a **pdf** main source only, writes the physical `jobs/<id>/document-ir-v2.shadow.json` via `write_pdf_facts_shadow_with_v1` (auto_pipeline.rs:1490-1519, soft-fails to `...error.json`); builds split-candidates + `authoring-ir.json`; then at auto_pipeline.rs:2027 writes `jobs/<id>/authoring-ir-v2.shadow.json` (also soft-failing); then runs `validate_for_runtime_gate` → `validation-report.json` and the V2 quality gate.\n4. get_authoring_v2 → `recover_current_revision`; revision 0 ⇒ reads `authoring-ir-v2.shadow.json`, validates it as `IeltsAuthoringIRV2`, returns an `AuthoringEditorSessionV1` envelope.\n5. apply_authoring_v2_patches → applies the patch vocabulary, `mark_user_audit(revision+1)`, **recomputes** `quality` via `evaluate_quality(authoring, physical_shadow)` (only using `document-ir-v2.shadow.json` if `physical_shadow_matches_authoring`), re-validates, then `append_revision` writes `jobs/<id>/authoring/revisions/<N>.json` + `<N>.meta.json`, `patches/<N>.jsonl`, and `authoring/current-revision.json` under an fs2 exclusive lock on `authoring/revision.lock`.\n6. export_authoring_v2 (REQUIRED, though not named in the assignment) → refreshes quality again, hard-blocks unless `ready`, computes the publish proof, compiles `ReadingExamSourceV2`, and atomically materialises `<export_dir>/<examId>-r<rev>-<uuid>/{authoring-ir-v2.json, reading-source-v2.json, manifest-v2.json}` plus `jobs/<id>/export-history/phase5-v2-<rev>-<uuid>.json`.\n7. publish_nas_package_v2 → takes `libraryRoot` + `sourcePath = <that dir>/reading-source-v2.json`, re-verifies the receipt against the persisted revision, takes an fs2 lock on the NAS control dir, stages `<exam>.js` + `resources/<exam>/*` + `manifest.js`, runs the in-process student loader probe, and commits manifest-last with a journal and backups.",
    "gapsVsPlan": [
      {
        "claim": "Plan 17.1: `.manage(AppState::new(...))` — implying AppState is a real state object to be constructed.",
        "reality": "`AppState` is a unit struct with no fields, no `impl`, and no `new()`. It is referenced exactly 3 times in the crate: declared at lib.rs:355, `.manage(AppState)` at lib.rs:1407, and mentioned in a db.rs comment at :7 explaining why the SQLite Connection was deliberately kept OUT of it. No command takes `State<AppState>`.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/lib.rs:354-355, src-tauri/src/lib.rs:1407, src-tauri/src/db.rs:5-8"
      },
      {
        "claim": "Plan 17.1: lib.rs should become a thin entry point; `#[cfg(test)] mod tests` with thousands of lines should be split per module.",
        "reality": "Confirmed and understated. lib.rs is 11009 lines, of which lines 1480-11009 (9530, 87%) are one `#[cfg(test)] mod tests` block holding 136 of the crate's ~470 tests, plus every shared test helper (`temp_test_root`, `parser_fixture`, `attach_fixture_source`, `make_publishable_fixture`, `mock_openai_server`, `env_test_lock`, `phase4_pdf_fixtures`, `verify_all_authoring_items`, ...). Those helpers are private to that module, so any new test file must either duplicate them or the module must be split first.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/lib.rs:1480-11009; 136 occurrences of `#[test]` in lib.rs"
      },
      {
        "claim": "Plan 5.3: the new converged commands take no AppHandle at all (e.g. `async fn get_workspace_item(item_id: String) -> CommandResult<WorkspaceItemV1>`).",
        "reality": "Every one of the 57 registered commands today takes `app: AppHandle` (or `_app`), purely to call `app_root(&app)`. Removing the AppHandle from the signature is not a cosmetic change: it requires a root resolvable without an AppHandle, which does not exist yet.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/lib.rs:851-1401 (all wrappers), src-tauri/src/lib.rs:357-359"
      },
      {
        "claim": "Plan P0-T02: build a real Tauri smoke as three Node scripts (scripts/e2e/tauri-library-import.mjs, tauri-workspace-edit.mjs, tauri-publish.mjs) that exercise real commands, SQLite and filesystem.",
        "reality": "None of those three files exist. `scripts/e2e/` contains exactly one file, `library-workspace-smoke.mjs`, whose own header states it is a browser + devFallbackBackend test and 'not a real Tauri/SQLite/filesystem chain', explicitly naming P0-T02 as the outstanding gap. So the plan's chosen mechanism is unbuilt — and a Rust in-crate test is far cheaper than driving a real Tauri window from CDP.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/scripts/e2e/ (single file), scripts/e2e/library-workspace-smoke.mjs:4-8"
      },
      {
        "claim": "The named chain is create_import_job → import_source_file → run_auto_pipeline → get_authoring_v2 → apply_authoring_v2_patches → publish_nas_package_v2.",
        "reality": "`publish_nas_package_v2` cannot be reached from `apply_authoring_v2_patches`. It refuses any source whose file name is not literally `reading-source-v2.json` and whose sibling `manifest-v2.json` is not a valid `AuthoringV2ExportReceiptV1` with matching hashes and a byte-identical `publishProof`. Only `export_authoring_v2` produces that. `export_authoring_v2` is a mandatory 7th step in the chain.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/nas_package_v2.rs:62-71, :82-100, :140-152, :200-202; src-tauri/src/authoring_v2_commands.rs:533-566"
      },
      {
        "claim": "(Assignment premise) The chain can be driven against a temp dir.",
        "reality": "True for 5 of the 7 steps and false for 2. `run_auto_pipeline_core`, `get_authoring_v2_core`, `apply_authoring_v2_patches_core`, `export_authoring_v2_core`, `publish_nas_package_v2_core` all take `root: &Path` and are already called with temp roots by existing tests. `create_import_job_core` and `import_source_file_core` take `app: AppHandle` and resolve the root themselves.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/job_commands.rs:77-89 and :158-209 vs auto_pipeline.rs:1153, authoring_v2_commands.rs:54/:123/:449, nas_package_v2.rs:245"
      },
      {
        "claim": "(Assignment premise) `library_items.id == job.job_id`.",
        "reality": "Confirmed in code, and it holds for the compatibility `exams` table too.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/library_commands.rs:50 (`id: job.job_id.clone()`), :187 (`id: exam.id.clone()`), :197 (`linked_ingest_job_id: Some(exam.id.clone())`)"
      },
      {
        "claim": "(Implicit) An out-of-crate integration test at src-tauri/tests/product_chain.rs is a viable location.",
        "reality": "It is not viable without widening visibility. `src-tauri/tests/` does not exist, and an integration test links the rlib and sees only `pub` items. Every module in the chain is a private `mod` (lib.rs:43-86); the only `pub mod` is `schema` (lib.rs:82). Every `_core` fn is `pub(crate)`. The complete public surface of the crate is: `CommandResult`, the 20 data structs/enums (JobStatus … SaveLlmProfileInput), `AppState`, `run_cli_or_app()`, `run()`, and `pub mod schema`. Nothing in that list can drive the chain.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/lib.rs:43-86 (mod decls), :82 (`pub mod schema;`), and `grep -E '^pub (fn|struct|enum|type|mod)' lib.rs` → lines 82,90,94,115,127,135,158,177,187,193,206,215,240,246,257,268,273,279,286,301,307,313,336,355,752,1403"
      },
      {
        "claim": "(Implicit) `tauri::test`/a mock AppHandle would let the real command bodies be called.",
        "reality": "Two independent problems. (a) Cargo.toml declares `tauri = { version = \"2\", features = [] }` and has NO `[dev-dependencies]` section at all, so `tauri::test` is not compiled in; enabling it is a Cargo.toml edit. (b) Even with a mock app, `app.path().app_data_dir()` resolves via `dirs 6.0` + the bundle identifier to the user's REAL roaming AppData directory. On Windows `dirs` uses the known-folder API, so `%APPDATA%` cannot redirect it. A mock AppHandle would make the test write to `%APPDATA%\\com.ielts.author.studio` and pollute the developer's live app data.",
        "verdict": "wrong",
        "evidence": "src-tauri/Cargo.toml (no [dev-dependencies], tauri features = []), src-tauri/Cargo.lock:4354-4355 (tauri 2.11.2), Cargo.lock:855-857 (dirs 6.0.0), src-tauri/tauri.conf.json (identifier com.ielts.author.studio), src-tauri/src/lib.rs:357-359"
      },
      {
        "claim": "(Implicit) The named chain plus a temp dir is enough to reach a committed publish.",
        "reality": "The binding constraint is not plumbing, it is the quality gate. `export_authoring_v2_core` recomputes `quality` (`refresh_quality_report`, authoring_v2_commands.rs:474) and hard-blocks unless the state is `ready`; `evaluate_quality` only returns `ready` when there are zero issues, zero hardFailures, every taskScore ≥ 0.92, documentScore ≥ 0.95, sourceCoverage ≥ 0.995, AND a matching physical `DocumentIRV2` shadow explains every significant source node. The only place in the entire crate proven to reach `ready` is the checked-in golden fixture `fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json` paired with the hand-built `valid_physical_shadow()`. An existing test asserts the opposite for that same fixture without the shadow: setting `quality.state=\"ready\"` by hand still yields `authoring_v2_export_blocked:quality_state=review_required`.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/ielts_grammar/quality.rs:186-198 (state derivation), :4256-4262 (`ready_fixture_proves_...`), :3689-3745 (`valid_physical_shadow`), :4279-4295 (`missing_malformed_and_empty_physical_shadows_cannot_be_ready`); src-tauri/src/authoring_v2_commands.rs:474, :482-486, :2422-2458 (`export_rejects_non_ready_quality_state_before_materialization`)"
      }
    ],
    "changePoints": [
      {
        "file": "src-tauri/src/job_commands.rs",
        "symbol": "create_import_job_core (:77-89) and import_source_file_core (:158-209)",
        "whatToChange": "Split each into a root-taking inner fn plus a 2-line AppHandle shim, matching the pattern every other command already uses. Concretely: add `pub(crate) fn create_import_job_at_root(root: &Path, input: CreateJobInput) -> CommandResult<ImportJob>` containing the current body from `ensure_app_dirs(&root)?;` onward, and `pub(crate) fn import_source_file_at_root(root: &Path, job_id: &str, file_path: &str, role: &str) -> CommandResult<SourceFile>` containing the body from `let dir = job_dir(&root, &job_id);` onward. Then `create_import_job_core` becomes `{ let root = app_root(&app)?; create_import_job_at_root(&root, input) }` and likewise for import. Make them `fn` not `async fn` — neither body awaits anything. This is the single highest-leverage change: it is the ONLY thing standing between the existing test harness and a full-chain test, and it also unblocks the plan's 5.3 goal of AppHandle-free commands.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/lib.rs",
        "symbol": "new `#[cfg(test)] mod product_chain;` declaration next to `#[cfg(test)] mod cross_repo_contract_fixture;` (:51-53)",
        "whatToChange": "Add `#[cfg(test)] mod product_chain;` and create `src-tauri/src/product_chain.rs` as a test-only module (NOT src-tauri/tests/product_chain.rs — see the visibility gap). Two precedents to copy exactly: `#[cfg(test)] mod cross_repo_contract_fixture;` at lib.rs:51-52 and `#[cfg(test)] mod real_pdf_acceptance;` at ielts_grammar/mod.rs:23-24. Both are whole test-only files that `use crate::...` freely and get full `pub(crate)` access. Cost: two lines in lib.rs, zero refactoring of the 9530-line tests block.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "temp root helper",
        "whatToChange": "Copy the one-liner used by 63 existing lib.rs tests: `fn temp_test_root() -> PathBuf { env::temp_dir().join(format!(\"product-chain-{}\", Uuid::new_v4().simple())) }` (model: lib.rs:1576-1578). Do NOT add `tempfile` as a dev-dependency — the crate has no `[dev-dependencies]` section and every existing test does manual `env::temp_dir()` + `let _ = fs::remove_dir_all(root);` at the end. Follow that convention.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "stage 1-2: create + import",
        "whatToChange": "After the job_commands.rs split, call `create_import_job_at_root(&root, CreateJobInput { title: Some(..), category: Some(\"P1\"), frequency: Some(\"medium\"), tags: Some(vec![..]), llm_profile_id: None })` then `import_source_file_at_root(&root, &job.job_id, fixture_path_str, \"MainQuestion\")`. Use a fixture that is checked in and committed: `fixtures/parser/complex-reading.pdf` (1398 B) or `.txt` (455 B), reached via `PathBuf::from(env!(\"CARGO_MANIFEST_DIR\")).join(\"..\").join(\"fixtures\").join(\"parser\").join(name)` (model: `parser_fixture`, lib.rs:1580-1586). Then assert the SQLite side: `db::open_connection(&root)` and confirm a `library_items` row with `id == job.job_id` (that identity is guaranteed by library_commands.rs:50/:187). If you decline the refactor, the fallback is to reproduce the two bodies inline using the already-`pub(crate)` primitives `job_store::make_job`, `util::ensure_app_dirs`, `util::ensure_job_dirs`, `artifact_store::ensure_job_artifact_layout`, `util::stage_file_with_hash`, `job_store::save_job`, `job_store::update_job` — but then the test proves the helpers, not the commands.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "stage 3: run_auto_pipeline",
        "whatToChange": "Call `auto_pipeline::run_auto_pipeline_core_with_gateway(&root, &job.job_id, None, |_r,_j,cmd,_i,_k| panic!(\"product chain must not call the LLM gateway: {cmd}\"))` rather than `run_auto_pipeline_core`, so the test is provably hermetic. With `input: None` no profile is selected (`cloud_diagnostics_opted_in = !local_only && options.profile_id.is_some()`, auto_pipeline.rs:1395) and no cloud thread is spawned (:1417). Model to copy verbatim: `auto_pipeline_local_only_skips_all_pdf_cloud_calls` (lib.rs:9908-10056) which asserts `gateway_calls == 0`. Then assert the two shadow artifacts the next stages depend on actually exist: `jobs/<id>/authoring-ir-v2.shadow.json` and (pdf only) `jobs/<id>/document-ir-v2.shadow.json`, and print `authoring-ir-v2.shadow.error.json` / `document-ir-v2.shadow.error.json` on failure — both writes are soft-failing (auto_pipeline.rs:275-315 and :1490-1519), so a missing shadow is silent today.",
        "risk": "medium",
        "blockedBy": "No existing test asserts that `authoring-ir-v2.shadow.json` is present and IeltsAuthoringIRV2-valid after `run_auto_pipeline_core` on a checked-in fixture. I could not verify it without running cargo (forbidden by my instructions). The only in-crate proof that a pipeline-produced shadow feeds the V2 editor is `real_pdf_acceptance::run_phase5_real_pdf_edit_export`, which uses a private-real PDF that is absent from this tree. Budget one iteration here."
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "stage 4-5: get_authoring_v2 + apply_authoring_v2_patches",
        "whatToChange": "`authoring_v2_commands::get_authoring_v2_core(&root, &job.job_id)` — assert `source == \"shadow\"` and `revision == 0`. Then `apply_authoring_v2_patches_core(&root, json!({\"jobId\": job.job_id, \"baseRevision\": 0, \"patches\": [{\"op\":\"replaceText\",\"nodeId\": <first text node id>,\"from\":0,\"to\":0,\"text\":\"chain-edit\"}]}))` and assert the returned `revision == 1` and that `jobs/<id>/authoring/revisions/1.json` + `authoring/current-revision.json` exist. Copy the patch shape and the answer-resolution-then-text-edit sequence verbatim from `real_pdf_acceptance::run_phase5_real_pdf_edit_export` (real_pdf_acceptance.rs:1627-1652) — that is the only working example of this call pair in the repo. Note `get_authoring_v2_core` has zero test coverage today; this stage is genuinely new ground.",
        "risk": "medium"
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "stage 6: export_authoring_v2 (the mandatory step the assignment omits)",
        "whatToChange": "`export_authoring_v2_core(&root, json!({\"jobId\": job.job_id, \"exportDir\": <ABSOLUTE temp path>, \"revision\": revision}))` and take `outputDir` from the result. `exportDir` must be absolute or you get `authoring_v2_export_dir_must_be_absolute` (authoring_v2_commands.rs:497-499).",
        "risk": "high",
        "blockedBy": "This is where the test will actually fail first. `export_authoring_v2_core` recomputes quality and returns `authoring_v2_export_blocked:quality_state=review_required` unless `evaluate_quality` says `ready`, which additionally requires: a physical `document-ir-v2.shadow.json` that passes `physical_shadow_matches_authoring` (matching schemaVersion/jobId/documentId and at least one shared sourceFileId — authoring_v2_commands.rs:757-778) AND explains every significant source node, plus `audit.humanVerified == true` and a resolved, non-stale `SourceReviewV1`. A freshly parsed fixture will not satisfy this. Two escape hatches, pick one deliberately: (A) split the test in two — a `product_chain_import_to_edit` test that ends at stage 5 and asserts the real `review_required` state as the honest current behaviour, plus a `product_chain_publish` test that seeds the job dir from the golden `early-approaches-authoring-v2.json` + a `valid_physical_shadow`-style DocumentIRV2 (quality.rs:3689-3745) so quality genuinely evaluates to `ready`; or (B) skip stage 6 and hand-forge the receipt exactly as `nas_package_v2::tests::test_export_bundle` does (nas_package_v2.rs:1441-1441..1495), which sets `quality.state=\"ready\"`/`issues=[]` and calls `validate_authoring_v2_publish_readiness` directly — that fn reads quality from the value instead of recomputing it. (A) is the more honest product-chain proof; (B) is cheaper and already known to work."
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "stage 7: publish_nas_package_v2",
        "whatToChange": "`nas_package_v2::publish_nas_package_v2_core(&root, json!({\"libraryRoot\": <ABSOLUTE nas dir under the temp root>, \"sourcePath\": output_dir.join(\"reading-source-v2.json\")}))` and assert `result[\"status\"] == \"committed\"` plus `nas/manifest.js` and `nas/<examId>.js` on disk. Both paths must be absolute; `sourcePath`'s file name must be exactly `reading-source-v2.json`. Copy `publish_normalizes_publish_child_to_one_transaction_root` verbatim (nas_package_v2.rs:1540-1567) — it is the closest existing assertion set. No Node, no pdfium, no network is involved in this stage.",
        "risk": "low"
      },
      {
        "file": "src-tauri/src/product_chain.rs (new)",
        "symbol": "cleanup + isolation",
        "whatToChange": "End with `let _ = fs::remove_dir_all(root);` (the universal convention). Do NOT call `env::set_var` anywhere: `cargo test` runs threads in one process and only three lib.rs tests guard env mutation behind `env_test_lock()` (lib.rs:1858-1861, used at :2116, :2138, :3427). In particular do not touch `EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK` (the CLI path at lib.rs:546 does, which is exactly why the CLI recipe cannot be lifted verbatim into a test).",
        "risk": "low"
      },
      {
        "file": "src-tauri/Cargo.toml",
        "symbol": "[dependencies] tauri features / absent [dev-dependencies]",
        "whatToChange": "NO CHANGE NEEDED, and this is the point worth stating explicitly to whoever plans this work: adding `tauri = { features = [\"test\"] }` or a `[dev-dependencies]` block buys nothing, because a mock AppHandle still resolves `app_data_dir()` to the developer's real `%APPDATA%`. Leave the manifest alone; the split in job_commands.rs is strictly cheaper and strictly safer.",
        "risk": "low"
      }
    ],
    "testHarness": "There is no `src-tauri/tests/`, no `benches/`, no `examples/`, no `[[test]]` target and no `[dev-dependencies]`. All ~470 tests are in-crate `#[cfg(test)]` modules. Two shapes exist: a trailing `mod tests` inside a production file (the norm — lib.rs:1480, auto_pipeline.rs:2669, authoring_v2_commands.rs:2003, nas_package_v2.rs:1424, db.rs:1037, library_commands.rs:492, util.rs:310, job_store.rs:86), and a whole file that is itself test-only (`#[cfg(test)] mod cross_repo_contract_fixture;` at lib.rs:51-52 and `#[cfg(test)] mod real_pdf_acceptance;` at ielts_grammar/mod.rs:23-24). The second shape is the one to copy for product_chain.\n\nThe uniform recipe: a temp dir under `env::temp_dir()` with a UUID suffix, `ensure_app_dirs`, hand-built `ImportJob` via `make_job`, `save_job`, optional fixture bytes copied into `jobs/<id>/uploads/`, then direct calls to `_core(&root, ...)` functions, then `let _ = fs::remove_dir_all(root);`. NO AppHandle is ever constructed anywhere in the crate. NO test is `async`: there is no `#[tokio::test]`, no `block_on`, and no `tauri::async_runtime` call in any test — because every `_core` in the chain except the two in job_commands.rs is synchronous. `tauri::async_runtime::spawn_blocking` appears only in the production command wrappers.\n\nThree representative test functions, quoted verbatim.\n\n(1) The minimal temp-root + real-SQLite pattern — src-tauri/src/job_store.rs:91-104. Note it does not even call `ensure_app_dirs`, and `save_job` silently exercises the `authoring_hub.db` dual-write:\n\n```rust\n    #[test]\n    fn legacy_job_load_does_not_require_phase1_directories() {\n        let root = env::temp_dir().join(format!(\"phase1-job-store-{}\", Uuid::new_v4().simple()));\n        let job = make_job(CreateJobInput {\n            title: Some(\"Legacy job\".to_string()),\n            ..Default::default()\n        });\n        save_job(&root, &job).unwrap();\n        let loaded = load_job(&root, &job.job_id).unwrap();\n        assert_eq!(loaded.job_id, job.job_id);\n        assert_eq!(loaded.title, \"Legacy job\");\n        assert!(!job_dir(&root, &job.job_id).join(\"authoring\").exists());\n        let _ = fs::remove_dir_all(root);\n    }\n```\n\n(2) The canonical pipeline test — src-tauri/src/lib.rs:4319-4415 (head shown; it continues with ~30 assertions on the report and on which artifacts must and must not exist). This is the exact skeleton stages 1-3 of product_chain should follow:\n\n```rust\n    #[test]\n    fn auto_pipeline_llm_failure_keeps_text_import_in_llm_review() {\n        let root = temp_test_root();\n        ensure_app_dirs(&root).unwrap();\n        let mut job = test_job();\n        attach_fixture_source(&root, &mut job, \"complex-reading.txt\", \"MainQuestion\");\n        save_job(&root, &job).unwrap();\n\n        let report = run_auto_pipeline_core(&root, &job.job_id, None).unwrap();\n\n        assert!(report\n            .pointer(\"/llm/failures\")\n            .and_then(Value::as_array)\n            .map(|failures| !failures.is_empty())\n            .unwrap_or(false));\n        assert_eq!(\n            report.get(\"status\").and_then(Value::as_str),\n            Some(\"NeedsReview\")\n        );\n        let saved = load_job(&root, &job.job_id).unwrap();\n        assert_eq!(saved.status, JobStatus::NeedsReview);\n        assert_eq!(saved.current_step, WorkflowStep::Authoring);\n        assert!(job_dir(&root, &job.job_id)\n            .join(\"authoring-ir.json\")\n            .exists());\n        assert!(job_dir(&root, &job.job_id)\n            .join(\"authoring-project.json\")\n            .exists());\n\n        let _ = fs::remove_dir_all(root);\n    }\n```\n\nand its two supporting helpers, src-tauri/src/lib.rs:1576-1586 and :1829-1856:\n\n```rust\n    fn temp_test_root() -> PathBuf {\n        env::temp_dir().join(format!(\"epic8-test-{}\", Uuid::new_v4().simple()))\n    }\n\n    fn parser_fixture(name: &str) -> PathBuf {\n        PathBuf::from(env!(\"CARGO_MANIFEST_DIR\"))\n            .join(\"..\")\n            .join(\"fixtures\")\n            .join(\"parser\")\n            .join(name)\n    }\n\n    fn attach_fixture_source(root: &Path, job: &mut ImportJob, fixture_name: &str, role: &str) {\n        let fixture = parser_fixture(fixture_name);\n        let original_name = fixture\n            .file_name()\n            .and_then(|value| value.to_str())\n            .unwrap()\n            .to_string();\n        let (hash, size, bytes) = hash_file_or_path(&fixture).unwrap();\n        let stored_name = format!(\"{}-{}\", &hash[..8], sanitize_filename(&original_name));\n        ensure_job_dirs(&job_dir(root, &job.job_id)).unwrap();\n        write_bytes(\n            &job_dir(root, &job.job_id)\n                .join(\"uploads\")\n                .join(&stored_name),\n            &bytes.unwrap(),\n        )\n        .unwrap();\n        job.source_files.push(SourceFile {\n            file_id: format!(\"file-{}\", Uuid::new_v4().simple()),\n            original_name,\n            stored_name,\n            file_type: file_type_from_name(fixture_name).to_string(),\n            sha256: hash,\n            size_bytes: size,\n            role: role.to_string(),\n            imported_at: Utc::now(),\n        });\n    }\n```\n\n`attach_fixture_source` reproduces `import_source_file_core`'s `stored_name` scheme (`<sha[..8]>-<sanitized>`) byte for byte — which is precisely why nothing has yet forced the AppHandle out of that command.\n\n(3) The publish-side pattern — src-tauri/src/nas_package_v2.rs:1540-1567. Copy the assertions; note the receipt comes from `test_export_bundle` (:1441), which hand-forges `manifest-v2.json` with `quality.state=\\\"ready\\\"` instead of going through `export_authoring_v2_core`:\n\n```rust\n    #[test]\n    fn publish_normalizes_publish_child_to_one_transaction_root() {\n        let root = temp_root();\n        let (source_path, _source) = test_export_bundle(&root);\n\n        let nas_parent = root.join(\"nas\");\n        let publish_child = nas_parent.join(\"publish\");\n        let result = publish_nas_package_v2_core(\n            &root,\n            json!({\n                \"libraryRoot\": publish_child,\n                \"sourcePath\": source_path\n            }),\n        )\n        .unwrap();\n        assert_eq!(result[\"status\"], \"committed\");\n        assert!(nas_parent.join(\"manifest.js\").is_file());\n        assert!(nas_parent.join(\"early-approaches.js\").is_file());\n        assert!(nas_parent\n            .join(CONTROL_DIR_NAME)\n            .join(\"export.lock\")\n            .is_file());\n        assert!(!publish_child.join(\"manifest.js\").exists());\n        assert!(!publish_child.join(CONTROL_DIR_NAME).exists());\n        let _ = fs::remove_dir_all(root);\n    }\n```\n\nEnvironment/tooling notes for whoever runs it. pdfium resolves under `cargo test` via an explicit `CARGO_MANIFEST_DIR` fallback (pdf_geometry.rs:81-89) to `src-tauri/lib/pdfium-windows/pdfium.dll`, which is present — so PDF fixtures work under `cargo test` but would NOT work if the test binary is invoked directly (Cargo sets that env var for the child process). A single cached `Pdfium` instance behind a Mutex serialises PDF tests (pdf_geometry.rs:26-42). Node is not involved: `node_validator_diagnostics_enabled()` defaults to false (environment.rs:187-195), and the runtime gate the pipeline runs is pure Rust (`mode: \\\"static-rust\\\"`, runtime_validation.rs:309-319). Run the new test by exact name — do not expect a green `cargo test` baseline (see surprises).",
    "surprises": [
      "`cargo test` is NOT green in this working tree, and it is not the new test's fault. All 8 fixtures in `fixtures/golden/phase4-eight-pdf-acceptance.json` (chili-peppers, conformity, fishbourne-roman-palace, listening-to-the-ocean, organisational-design, petri-dish, sleep-study, western-celebrity) live under `fixtures/golden/private-real/*.pdf`, which `fixtures/golden/private-real/.gitignore:2` excludes and which are ABSENT from disk (only `.gitignore` and `README.md` are there). `lib.rs::registered_phase4_pdf_corpus_reaches_frozen_v1_parse_split_and_authoring` (lib.rs:9189-9190) is not `#[ignore]`d and calls `phase4_pdf_fixtures()` (lib.rs:1588), which asserts every entry is `available` and then hashes the file — it must panic. The same holds for the 12 tests in `ielts_grammar/real_pdf_acceptance.rs`, including `phase5_real_pdf_edit_and_v2_export_round_trip`. Run the new test by exact name (`cargo test product_chain`) and do not try to establish a green baseline first.",
      "`real_pdf_acceptance.rs` uses the REPOSITORY ROOT as its app root: `fn repo_root() -> PathBuf { PathBuf::from(env!(\"CARGO_MANIFEST_DIR\")).join(\"..\") }` (:21-23), and `run_phase5_real_pdf_edit_export` then does `root.join(\"jobs\").join(&job_id)` (:1594) plus `root.join(\"tmp/phase5-real-pdf-acceptance/...\")`. So that test suite writes job artifacts into `F:/workspace/PDF2Test/jobs/` and `tmp/` (both gitignored). Do not copy this; product_chain should use a temp root.",
      "The two AppHandle-bound cores are the ONLY untested module in the chain. `job_commands.rs` has no `#[cfg(test)]` block at all — `create_import_job_core` and `import_source_file_core` have never been executed by any test. That is the direct consequence of the AppHandle in their signature, and it is the concrete coverage hole a product-chain test closes.",
      "`AppState` is decorative. Zero fields, no `impl`, no `State<AppState>` consumer anywhere. db.rs:5-8 documents the deliberate decision to keep the SQLite Connection out of it (transient per-operation connections keyed on `root: &Path` instead), which is exactly what makes temp-root testing free. The plan's `AppState::new(...)` would be a new concept, not a rename.",
      "`get_authoring_v2` is the only command in the named chain with zero direct test coverage in the crate. `get_authoring_v2_core` is referenced exactly twice: the import at lib.rs:8 and the call at lib.rs:1011. No test calls it. By contrast `apply_authoring_v2_patches_core` and `export_authoring_v2_core` are each exercised by `real_pdf_acceptance.rs` (currently unrunnable) and `publish_nas_package_v2_core` by 8 tests in its own module.",
      "`export_authoring_v2` is a hidden mandatory link. The assignment's chain jumps from `apply_authoring_v2_patches` to `publish_nas_package_v2`, but `validate_v2_export_binding` (nas_package_v2.rs:56) makes that impossible: the source file must be named `reading-source-v2.json`, sit next to a hash-matching `AuthoringV2ExportReceiptV1` manifest and an `authoring-ir-v2.json`, and carry a `publishProof` byte-identical to a freshly recomputed one. Only `export_authoring_v2_core` produces that bundle.",
      "The quality gate, not the AppHandle, is the real cost driver. `evaluate_quality` (quality.rs) returns `ready` only with zero issues, zero hardFailures, all taskScores ≥ 0.92, documentScore ≥ 0.95, sourceCoverage ≥ 0.995 and a physical DocumentIRV2 shadow that explains every significant source node (quality.rs:186-198). An existing test proves that hand-setting `quality.state=\"ready\"` does not help, because export recomputes it: `export_rejects_non_ready_quality_state_before_materialization` asserts the error is exactly `authoring_v2_export_blocked:quality_state=review_required` (authoring_v2_commands.rs:2422-2458). And `missing_malformed_and_empty_physical_shadows_cannot_be_ready` (quality.rs:4279-4295) proves no physical shadow means never ready. The only proven `ready` recipe in the repo is `early-approaches-authoring-v2.json` + `valid_physical_shadow()` (quality.rs:3689-3745).",
      "The physical shadow that `ready` depends on is PDF-only. `run_auto_pipeline_core` writes `document-ir-v2.shadow.json` inside `if let (Some(source), Some(document)) = (main_source_file(&job).filter(|source| source.file_type == \"pdf\"), ...)` (auto_pipeline.rs:1487-1490). A `.txt`/`.md`/`.docx` main source produces no physical shadow, so a txt-based product_chain test can NEVER reach `ready` and can never publish. If the test must publish, the fixture must be a PDF.",
      "Both V2 shadow writes fail SILENTLY. `write_pipeline_authoring_v2_shadow` (auto_pipeline.rs:275-315) deletes the shadow and writes an `authoring-ir-v2.shadow.error.json` instead of propagating, and the physical-shadow path does the same with `document-ir-v2.shadow.error.json` (:1502-1517). `run_auto_pipeline` therefore returns Ok with no V2 shadow at all, and the failure only surfaces two commands later as `AUTHORING_V2_NOT_AVAILABLE:shadow_missing:<path>`. A product_chain test should assert both shadows exist immediately after stage 3 and dump the `.error.json` contents on failure.",
      "`authoring_v2_shadow_enabled()` and `quality_gate_v2_enabled()` are hardcoded `true` (environment.rs:223-229) — they read no env var despite looking like feature flags. There is no way to switch the quality gate off for a test.",
      "`run_auto_pipeline_core` refuses to run twice: `if dir.join(\"authoring-ir.json\").exists() && !allow_overwrite { return Err(\"editable_draft_exists; pass allowOverwrite=true before regenerating draft\") }` (auto_pipeline.rs:1402-1406). A retry loop in the test must pass `allow_overwrite: Some(true)`.",
      "`publish_nas_package_v2` silently retargets its output: `normalize_nas_library_root` strips a trailing `publish/` child so `libraryRoot: <x>/nas/publish` writes into `<x>/nas` (proved by `publish_normalizes_publish_child_to_one_transaction_root`, nas_package_v2.rs:1540-1567). Assert on the normalised parent, not the path you passed in.",
      "The shipped CLI already contains a complete headless chain driver with an explicit root override: `run_auto_pipeline_from_path` honours `--app-root` (lib.rs:516-524) and otherwise mints its own temp dir. It is the strongest existing evidence that the backend needs no Tauri — but it cannot be lifted verbatim into a test because it demands live LLM credentials (lib.rs:506-514) and calls `env::set_var(\"EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK\", \"1\")` (lib.rs:546), which is process-global and unsafe under the parallel test harness.",
      "Only 3 of 136 lib.rs tests guard env mutation behind `env_test_lock()` (lib.rs:1858-1861, used at :2116, :2138, :3427). Any new test that sets an env var without that lock will flake against those three.",
      "`tempfile 3.27` and `tokio 1.52` are already in Cargo.lock as transitive deps of tauri, but neither is a declared dependency, so `use tempfile::...` in a test will not compile. Every test rolls its own temp dir by hand — follow that."
    ]
  },
  {
    "subsystem": "LLM gateway and cloud outline (src-tauri/src/llm_gateway.rs, src-tauri/src/llm_suggestions.rs, plus their only real drivers auto_pipeline.rs / llm_commands.rs)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "role": "The whole transport + all four prompts + all four response validators in one file. Single entry point run_llm_gateway (llm_gateway.rs:23-69) dispatches on a command_name string (llm_gateway.rs:36-48) to 4 handlers; openai_post/openai_post_once (290-437) is the only HTTP path; validate_cloud_outline_output (1176-1365) is the de-facto CloudReadingOutlineV1 schema.",
        "sizeNote": "1365 lines / 54 KB. No struct definitions at all - everything is serde_json::Value."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs",
        "role": "Request-body builders (make_llm_input, make_vision_transcription_input, make_vision_answer_extraction_input, make_cloud_paper_generation_input), the deterministic local fallback, and the V1 group-suggestion auto-apply gate (llm_suggestion_auto_apply_issues, llm_suggestion_quote_mismatches). The string \"CloudReadingOutlineV1\" exists in exactly one place in the repo: llm_suggestions.rs:235.",
        "sizeNote": "737 lines / 28 KB. Also all serde_json::Value; no typed contract."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "The orchestrator that owns every cloud call. Holds the Mutex<FnMut> (auto_pipeline.rs:1415), the concurrent cloud worker (1226-1275), the outline generate/report/compare chain (1052-1150, 825-1012), and the second entry point run_cloud_review_core_with_gateway (2217+) which is what the new UI actually calls.",
        "sizeNote": "~2790 lines of code + tests / 116 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "role": "Profile CRUD + base-URL hardening (validate_llm_base_url, 29-75), SUPPORTED_LLM_PROVIDERS (79-83, still 4 variants), test_llm_profile_core (173-211), llm_run_group_core (213-258), apply_llm_suggestion_core (260+).",
        "sizeNote": "~700 lines / 30 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "role": "Produces the PdfImageExtractionV1 that every cloud call consumes. extract_pdf_images_for_vision (2454-2516), image_count_from_extraction (1845-1857), and stabilize_pdf_image_extraction_fields (~1995-2045) which already computes coverage / coverageStatus / partialCoverage / missingPageIndexes.",
        "sizeNote": "104 KB; only the image-extraction part is relevant."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/ielts_authoring_v2.rs",
        "role": "Already contains real Rust types for everything plan 7.3 wants to reuse: TaskTypeV2 (202), QuestionNumberExpressionV2 (225), TaskGroupV2 (242-258), OptionBankV2 (317-325), ResponseGroupV2 (337-353), AnswerSlotV2 (402-417).",
        "sizeNote": "16 KB, all #[serde(deny_unknown_fields)] camelCase."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/common.rs",
        "role": "SourceAnchorV2 (67-89) and RectV2 (19-29). RectV2 requires unit + origin + pageRotation, so plan 7.3's bare CloudEvidence.bbox: Option<[f64;4]> cannot be deserialized straight into it.",
        "sizeNote": "8 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "collect_publish_gate_markers (333-435) walks the authoring doc AND authoring-ir.json AND pipeline-report.json and blocks V2 publish on any non-empty `failure` or partial status under a path containing \"cloud\"/\"llm\"/\"ai\"/\"vision\". Any new cloud candidate surface inherits this gate automatically.",
        "sizeNote": "97 KB; only lines 260-440 matter here."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/llm_profiles.rs",
        "role": "Profile store (config/llm-profiles.json) and secret storage (OS keyring, then file, then refuse). load_llm_api_key / find_profile are what every cloud call uses to get the key.",
        "sizeNote": "8.6 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "role": "The existing TTL/cleanup hook for raw cloud output: cleanup_transient_job_artifacts (99-160) and minimize_process_artifacts_after_authoring (161+) delete cache/, llm-suggestions/, llm-calls.jsonl.",
        "sizeNote": "8.6 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/Cargo.toml",
        "role": "reqwest 0.12 with default-features=false, features=[\"blocking\",\"json\",\"rustls-tls\"]. No direct tokio. profile.release sets panic = \"abort\".",
        "sizeNote": "40 lines."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/import/useImportFiles.ts",
        "role": "The live cloud scheduler is here, in TypeScript: runPool(created, localConcurrency, runAutoPipeline{executionMode:\"localOnly\"}) then runPool(cloudEligible, cloudConcurrency, runCloudReview) (lines 147-158). This is the semaphore plan 26.3-D asks for - it already exists, frontend-side.",
        "sizeNote": "~170 lines."
      }
    ],
    "publicApi": [
      {
        "symbol": "run_llm_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:23",
        "signature": "pub(crate) fn run_llm_gateway(\n    root: &Path,\n    job_id: &str,\n    command_name: &str,\n    input: &Value,\n    api_key: Option<&str>,\n) -> CommandResult<Value>",
        "notes": "The ONLY public entry. command_name is a stringly-typed dispatch (llm_gateway.rs:36-48) over exactly 5 values: \"classify_group\" | \"extract_group\" | \"test_profile\" -> group LLM, \"transcribe_pdf_images\", \"extract_pdf_image_answers\", \"generate_pdf_reading_outline\"; anything else -> Err(\"unsupported_llm_gateway_command:{}\"). Side effects on every call: writes cache/llm/{command}-input-{ms}.json (redacted) before, cache/llm/{command}-output-{ms}.json after, and appends one llm_call record to jobs/{job_id}/llm-calls.jsonl (49-68) with commandName/jobId/model/ok/latencyMs/errorClass/recordedAt. No requestId is generated or recorded anywhere."
      },
      {
        "symbol": "parse_llm_json_content",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:219",
        "signature": "pub(crate) fn parse_llm_json_content(content: &str) -> CommandResult<Value>",
        "notes": "Plan 7.6's extract_single_json_value already exists here and is stricter than the plan's pseudocode. Tries whole-string parse; else brace/bracket-balanced scan (balanced_json_end, 251-288, string- and escape-aware) collecting every parseable candidate, then keeps only outermost ones. Exactly one outermost -> Ok. Zero -> Err(\"llm_json_parse_failed:{first_error}\"). Two or more -> Err(\"llm_json_parse_failed:ambiguous_wrapped_json\"). No markdown fence stripping is needed because the scan ignores prose."
      },
      {
        "symbol": "validate_llm_suggestion_output",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:775",
        "signature": "pub(crate) fn validate_llm_suggestion_output(\n    output: &mut Value,\n    mode: &str,\n    profile: &Value,\n    payload: &Value,\n) -> CommandResult<()>",
        "notes": "Also used from llm_commands/tests. Requires kind in allowed_question_kind, numeric confidence, array patch/questions, evidence.sourceBlockIds array, evidence.quotes array of {blockId,text} non-empty; patch ops restricted to op=replace and path in {\"/kind\",\"/layout/template\"} (any other path -> Err). Then normalize_confidence_fail_closed (204-217) collapses out-of-range confidence to 0.0 and appends a confidence_out_of_range warning, and stamps evidence.mode/source=\"openai-compatible-rust\"/model/usage."
      },
      {
        "symbol": "validate_cloud_outline_output",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:1176",
        "signature": "fn validate_cloud_outline_output(\n    output: &mut Value,\n    profile: &Value,\n    payload: &Value,\n) -> CommandResult<()>",
        "notes": "THIS IS the CloudReadingOutlineV1 schema; there is no Rust struct. Private, so the contract is not reusable outside this file today."
      },
      {
        "symbol": "redact_llm_input_for_cache",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:83",
        "signature": "pub(crate) fn redact_llm_input_for_cache(input: &Value) -> Value",
        "notes": "Removes only a top-level \"apiKey\" and inserts apiKeySource:\"process-env\". The real key never travels in `input` anyway (it is the separate api_key arg), so this is belt-and-braces."
      },
      {
        "symbol": "make_cloud_paper_generation_input",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:212",
        "signature": "pub(crate) fn make_cloud_paper_generation_input(\n    profile: &Value,\n    job: &ImportJob,\n    profile_id: &str,\n    source: &crate::SourceFile,\n    pdf_path: &Path,\n    extraction: &Value,\n) -> Value",
        "notes": "Builds {mode:\"generate_pdf_reading_outline\", job{jobId,title,category,frequency,tags}, profile(profile_payload), sourceFile{fileId,originalName,fileType,sha256,sizeBytes}, pdfPath, pages, extractionWarnings, outputContract{schema:\"CloudReadingOutlineV1\", jsonOnly, shape, rules[8]}}. llm_suggestions.rs:235 is the only occurrence of the literal \"CloudReadingOutlineV1\" in the repo."
      },
      {
        "symbol": "make_vision_transcription_input",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:166",
        "signature": "pub(crate) fn make_vision_transcription_input(\n    profile: &Value,\n    job: &ImportJob,\n    profile_id: &str,\n    extraction: &Value,\n) -> Value",
        "notes": "mode \"transcribe_pdf_images\"; carries pages + extractionWarnings and nothing else. No outputContract (unlike the answer/outline builders)."
      },
      {
        "symbol": "make_vision_answer_extraction_input",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:181",
        "signature": "pub(crate) fn make_vision_answer_extraction_input(\n    profile: &Value,\n    job: &ImportJob,\n    profile_id: &str,\n    extraction: &Value,\n) -> Value",
        "notes": "outputContract.schema = \"PdfImageAnswerKeyV1\"."
      },
      {
        "symbol": "make_llm_input",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:108",
        "signature": "pub(crate) fn make_llm_input(\n    profile: &Value,\n    job: &ImportJob,\n    group: &Value,\n    profile_id: &str,\n    mode: &str,\n) -> Value",
        "notes": "V1 group-repair path. repairContract.schema = \"Epic8LlmGroupRepairV1\" with allowedPatchPaths [\"/kind\",\"/layout/template\"] and highConfidenceAutoApplyThreshold 0.85."
      },
      {
        "symbol": "deterministic_llm_output",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:88",
        "signature": "pub(crate) fn deterministic_llm_output(group: &Value, mode: &str, warning: String) -> Value",
        "notes": "The local fallback the plan (17.10) wants relabelled. Emits confidence 0.64, evidence{mode,source:\"rust-local-fallback\",fallback:true}, warnings[warning,\"low-confidence-review-required\",\"fallback-output-never-auto-applies\"]. It IS saved as an llm suggestion today (llm_commands.rs:232+251, auto_pipeline.rs:1728+1753)."
      },
      {
        "symbol": "llm_suggestion_auto_apply_issues",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:451",
        "signature": "pub(crate) fn llm_suggestion_auto_apply_issues(\n    ir: &Value,\n    suggestion: &Value,\n    selected_paths: &[String],\n) -> Vec<String>",
        "notes": "Returns a flat Vec<String> of issue codes (confidence_below_auto_apply_threshold, evidence_block_not_in_group:*, non_provider_evidence_source:*, blocking_warning:*, ...). Empty vec == may auto-apply. This is the only existing 'proposal gate' and it is V1-group-shaped."
      },
      {
        "symbol": "llm_suggestion_quote_mismatches",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs:402",
        "signature": "pub(crate) fn llm_suggestion_quote_mismatches(\n    suggestion: &Value,\n    block_texts: &std::collections::BTreeMap<String, String>,\n) -> Vec<String>",
        "notes": "The nearest thing to plan 7.11's find_quote_normalized. Whitespace-normalized lowercase containment plus a hyphen-stripped fallback (normalized_quote_text 382, normalized_quote_text_without_hyphens 393). Keyed by blockId, not page/bbox. Only ever applied to group-repair suggestions - cloud outline quotes are never checked against the source."
      },
      {
        "symbol": "run_auto_pipeline_core_with_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1381",
        "signature": "pub(crate) fn run_auto_pipeline_core_with_gateway<F>(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n    mut llm_gateway: F,\n) -> CommandResult<Value>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value> + Send,",
        "notes": "The injection seam. `mut llm_gateway: F` is moved into `let shared_gateway = Mutex::new(llm_gateway);` at auto_pipeline.rs:1415. 8 test call sites in lib.rs pass a closure."
      },
      {
        "symbol": "run_cloud_review_core_with_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:2217",
        "signature": "pub(crate) fn run_cloud_review_core_with_gateway<F>(\n    root: &Path,\n    job_id: &str,\n    input: Option<RunCloudReviewInput>,\n    mut llm_gateway: F,\n) -> CommandResult<Value>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>,",
        "notes": "No Mutex, no worker, no `+ Send`. Renders the PDF again (main_pdf_vision_extraction, 2243) then calls the outline (2245) and the vision answer key (2258) strictly one after the other on the calling thread. THIS is the function the shipped UI uses for all cloud work."
      },
      {
        "symbol": "run_cloud_conversion_worker",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1226",
        "signature": "fn run_cloud_conversion_worker<F>(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    shared_gateway: &Mutex<F>,\n    extraction_tx: mpsc::Sender<Result<(Value, PathBuf), String>>,\n    worker_tx: mpsc::Sender<CloudWorkerOutcome>,\n) where\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>,",
        "notes": "The exact thing plan 26.3-D attacks. Body: one extraction, then `let mut guard = lock_gateway(shared_gateway); vision_answer_candidate_for_job(...)` (1253-1256), then a SECOND separate `lock_gateway` for `cloud_outline_generate_with_gateway` (1257-1260). Both HTTP calls hold the mutex for their full duration, so they are serial by construction."
      },
      {
        "symbol": "lock_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1214",
        "signature": "fn lock_gateway<F>(shared: &Mutex<F>) -> std::sync::MutexGuard<'_, F>",
        "notes": "Poison-tolerant (unwrap_or_else(|poisoned| poisoned.into_inner())). Called at 1254, 1258 (worker) and 1569, 1717 (main thread)."
      },
      {
        "symbol": "cloud_outline_generate_with_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1072",
        "signature": "fn cloud_outline_generate_with_gateway<F>(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    extraction: &Value,\n    llm_gateway: &mut F,\n) -> Result<Value, String>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>,",
        "notes": "find_profile -> main_pdf_upload_path -> make_cloud_paper_generation_input -> load_llm_api_key -> gateway(\"generate_pdf_reading_outline\"). Returns the raw outline Value; no typed struct, no persistence of its own (raw output only survives in cache/llm/generate_pdf_reading_outline-output-*.json)."
      },
      {
        "symbol": "cloud_outline_check_for_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1052",
        "signature": "fn cloud_outline_check_for_job<F>(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    extraction: &Value,\n    ir: &Value,\n    llm_gateway: &mut F,\n) -> Value",
        "notes": "generate + compare in one call; used only by run_cloud_review_core_with_gateway. The auto-pipeline path splits the two so generation can run concurrently."
      },
      {
        "symbol": "cloud_outline_comparison",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:825",
        "signature": "fn cloud_outline_comparison(ir: &Value, output: &Value) -> Value",
        "notes": "The de-facto DeterministicDiffV1. Returns {passed, issueCount, issues[], observations[], truncatedIssueCount} with issues capped at 20. Alignment key is an EXACT questionRange [start,end] tuple match against local groups (852-875) - no fuzzy/overlap alignment, so a local split of Q8-13 into Q8-10+Q11-13 yields cloud_group_missing_locally rather than a structural diff. Issue kinds: cloud_groups_missing, cloud_group_range_missing, cloud_group_missing_locally, local_group_question_ids_do_not_match_range, cloud_group_question_ids_do_not_match_range, cloud_group_kind_mismatch, cloud_group_layout_mismatch, cloud_answer_missing_locally, cloud_answer_mismatch. Observations (non-blocking): cloud_question_ids_missing, cloud_group_evidence_missing, cloud_completion_kind_normalized, cloud_layout_deprioritized_by_local_inline_notes, cloud_answer_candidate_only."
      },
      {
        "symbol": "cloud_outline_report_from_output",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1102",
        "signature": "fn cloud_outline_report_from_output(\n    profile_id: &str,\n    ir: &Value,\n    output: Result<Value, String>,\n) -> Value",
        "notes": "Wraps the comparison as {attempted, passed, profileId, failure, warningCount, issues, observations, comparison, localSummary, cloudSummary, outputConfidence, outputWarnings}. On Err it sets failure=<raw internal error string> and a single Chinese user issue. That `failure` string lands in pipeline-report.json under quality.cloudComparison and is then a publish blocker (see surprises)."
      },
      {
        "symbol": "vision_transcription_for_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:373",
        "signature": "pub(crate) fn vision_transcription_for_job(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    note: Option<&str>,\n) -> CommandResult<(Value, Value)>",
        "notes": "Public wrapper used by the apply_vision_transcription command; delegates to vision_transcription_for_job_with_gateway(.., &mut run_llm_gateway) (379). Returns (DocumentIRV1 built by parser::vision_transcription_document_ir, raw gateway output)."
      },
      {
        "symbol": "vision_answer_candidate_for_job",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:469",
        "signature": "fn vision_answer_candidate_for_job<F>(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    extraction: &Value,\n    llm_gateway: &mut F,\n) -> CommandResult<(Value, Value)>",
        "notes": "Wraps the raw answer output as schemaVersion \"VisionAnswerCandidateV1\" (487-502) with source/provider/profileId/model/answers/confidence/warnings/evidence/imageCount/extractionWarnings. Persisted to vision-answer-candidates.json by write_vision_answer_candidates_file (1276)."
      },
      {
        "symbol": "select_llm_profile",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:115",
        "signature": "pub(crate) fn select_llm_profile(\n    root: &Path,\n    job: &ImportJob,\n    requested_profile_id: Option<String>,\n) -> Option<String>",
        "notes": "Prefers requested -> job.active_llm_profile_id if still enabled, else scores enabled profiles by (hasApiKey*2, not-placeholder, provider==\"OpenAiCompatible\") - so an Ollama profile ranks BELOW an OpenAiCompatible one even though the gateway routes both (auto_pipeline.rs:156-160)."
      },
      {
        "symbol": "require_openai_compatible_provider",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:135",
        "signature": "fn require_openai_compatible_provider(profile: &Value) -> CommandResult<()>",
        "notes": "The complete provider routing table: `if !matches!(provider, \"OpenAiCompatible\" | \"Ollama\") { return Err(format!(\"llm_provider_unsupported_for_openai_route:{}\", provider)); }` (llm_gateway.rs:146-151). Missing/blank provider -> Err(\"llm_provider_missing\"). There is no LlmProvider enum in Rust at all - provider is a raw string everywhere."
      },
      {
        "symbol": "openai_chat_completions_endpoint",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs:155",
        "signature": "fn openai_chat_completions_endpoint(profile: &Value) -> CommandResult<String>",
        "notes": "Hardcodes format!(\"{}/chat/completions\", base_url). Rejects non-http(s), empty host, embedded credentials, fragments; allows plain http only for localhost / host.docker.internal / *.local / loopback / private / link-local IPs, else llm_profile_base_url_insecure_http. No /responses, /messages or any other route exists."
      },
      {
        "symbol": "SUPPORTED_LLM_PROVIDERS",
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs:79",
        "signature": "const SUPPORTED_LLM_PROVIDERS: [&str; 4] = [\n    \"OpenAiCompatible\",\n    \"AnthropicCompatible\",\n    \"Ollama\",\n    \"Custom\",\n];",
        "notes": "save_llm_profile_core (llm_commands.rs:85-87) accepts all four. So a profile the gateway can never route is still persistable today."
      }
    ],
    "dataFlow": "Two independent entry paths, and the shipped UI uses the second one.\n\nPATH A - run_auto_pipeline (lib.rs:1291-1301, spawn_blocking -> auto_pipeline::run_auto_pipeline_core:1153 -> run_auto_pipeline_core_with_gateway:1381 with the real fn `run_llm_gateway`). Cloud is opt-in: `local_only = execution_mode == Some(\"localOnly\")` and `cloud_diagnostics_opted_in = !local_only && options.profile_id.is_some()` (1394-1395), then `profile_id = cloud_diagnostics_opted_in.then(|| select_llm_profile(..)).flatten()` (1411). The gateway fn is moved into `Mutex::new(llm_gateway)` (1415). `cloud_worker_spawned = selected_profile_id.is_some() && main_source_is_pdf(&job)` (1416). Inside `thread::scope` (1419) one worker thread is spawned (1425-1433) which: renders page images ONCE via main_pdf_vision_extraction (1247, -> parser::extract_pdf_images_for_vision -> python sidecar then pdfium/sips renderer, writes cache/vision/pdf-images.json + cache/vision/assets/*), sends (extraction, asset_dir) down extraction_tx so the main thread can reuse it, then takes lock_gateway TWICE in sequence: extract_pdf_image_answers (1253-1256) and generate_pdf_reading_outline (1257-1260), and finally sends CloudWorkerOutcome{vision_answer, outline} down worker_tx. Meanwhile the main thread parses the document, writes document-ir.json, materializes the DocumentIRV2 physical shadow, and if needs_pdf_vision_transcription it blocks on extraction_rx.recv() (1563) and then takes the SAME lock for transcribe_pdf_images (1569). It then builds split-candidates.json and authoring-ir.json, optionally runs the per-group extract_group repair loop (1698-1723, gated by `!local_only && !main_source_is_pdf(&job)` - mutually exclusive with cloud_worker_spawned), then joins the worker at worker_rx.recv() (1862), writes vision-answer-output.json + vision-answer-candidates.json (1877-1878), and finally calls cloud_outline_report_from_output(profile, &ir, outcome.outline.clone()) (1957) which runs cloud_outline_comparison against the local draft. Everything cloud-derived is written as read-only diagnostics: audit issues via append_authoring_audit_issue plus quality.cloudComparison in pipeline-report.json (2170, written 2185). The cloud outline NEVER mutates authoring-ir.json.\n\nPATH B - run_cloud_review (lib.rs:1303-1312 -> run_cloud_review_core:2209 -> run_cloud_review_core_with_gateway:2217). Reads the existing authoring-ir.json (errors with authoring_ir_missing_for_cloud_review if absent), select_llm_profile, then if main_source_is_pdf: main_pdf_vision_extraction AGAIN (2243, full re-render - extract_pdf_images_for_vision has no cache-hit short circuit), then cloud_outline_check_for_job (2245, generate+compare) followed by vision_answer_candidate_for_job (2258) strictly sequentially on one thread. It then re-reads authoring-ir.json from disk before writing the audit issue back (2350+, \"merge-on-write\" so a concurrent user edit is not clobbered) and rewrites pipeline-report.json (2665).\n\nThe new frontend drives B, not A: src/features/import/useImportFiles.ts:149 calls runAutoPipeline with `executionMode: \"localOnly\"` (so profile_id is None and no worker is ever spawned), then 155-158 pools runCloudReview at `cloudConcurrency` (default 2, src/features/settings/appSettings.ts:30). src/features/editor/ExamWorkspacePage.tsx:115-120 does the same pair.\n\nInside the gateway, one call is: run_llm_gateway writes the redacted input to cache/llm/, dispatches on command_name, the handler builds an OpenAI chat-completions body {model, temperature, messages:[{role:\"system\",content:\"Return valid JSON only.\"},{role:\"user\",content:<prompt or multimodal parts>}]} plus response_format:{\"type\":\"json_object\"} unless profile.forceJson==false (llm_force_json:131 treats anything except explicit false as true), calls openai_post, then openai_chat_content pulls /choices/0/message/content, parse_llm_json_content extracts the single outermost JSON value, and a per-command validator mutates it in place and stamps evidence/metadata with mode/source/model/usage. Attachments: images go in as {\"type\":\"image_url\",\"image_url\":{\"url\":\"data:<mime>;base64,...\"}} (append_pdf_images_to_content:578-618, 32 MiB per image, 128 MiB total measured on data-URL length); the outline sends the PDF itself as {\"type\":\"file\",\"file\":{\"filename\":..,\"file_data\":\"data:application/pdf;base64,..\"}} (data_url_for_pdf:559-577, 128 MiB cap) and only falls back to page images if that whole request fails (run_openai_compatible_cloud_outline_llm:735-761, which pushes a direct_pdf_request_failed:<err> warning into the parsed output). Every attachment path goes through read_llm_file (493-541): must be absolute, canonicalized, and inside the canonicalized app root, else llm_<kind>_path_outside_app_root.\n\nTransport: openai_post (290-318) computes ONE deadline = now + llm_timeout(profile, 60_000) where llm_timeout clamps profile.timeoutMs to [1_000, 300_000] ms, then loops up to MAX_LLM_ATTEMPTS = 3 passing the REMAINING budget as each attempt's per-request timeout. Retry decision is made by string-sniffing the error (is_retryable_llm_http_error:320): retry on llm_http_timeout:/llm_http_connect_failed:/llm_http_body_failed: prefixes, or an llm_http_<status> where status is 408|425|429 or 500-599. Backoff = max(retry_after_ms, 400*(attempt+1)); if the remaining budget is <= that delay it returns llm_timeout_budget_exhausted:<last_error>. Retry-After arrives as a header, is parsed by retry_after_ms_from_header (357, integer seconds or RFC2822 date, capped at 5_000 ms), is appended onto the error string as \";retry_after_ms=<ms>\" (421-425), and is read back out by retry_after_ms_from_error (339). openai_post_once (372) builds a FRESH reqwest::blocking::Client per attempt with redirect::Policy::none(), sets bearer_auth only when api_key is non-blank, pre-checks content_length against MAX_LLM_RESPONSE_BYTES (16 MiB) and re-checks after a .take()-bounded read.",
    "gapsVsPlan": [
      {
        "claim": "7.1: the current cloud contract returns title / groups[].range / kind / layoutHint / questionIds / notesText / answerKey / confidence / warnings.",
        "reality": "Correct but incomplete, and the plan omits which fields are HARD-REQUIRED. validate_cloud_outline_output also requires, per group, a numeric `confidence` and a non-empty `evidence.quotes` array of {pageIndex>=1, text non-empty} (llm_gateway.rs:1273-1300), and optionally accepts group `warnings`. Top level it also injects `metadata{mode,source,model,usage}` (1332-1356) and, on kind normalization, `rawKind` (1315-1317). And `answerKey` is not optional: a missing key is Err(\"cloud_outline_answer_key_missing\") via normalize_answer_map_value_checked (1054-1058, called at 1206), so a question paper with no printed answer key fails the entire outline unless the model emits answerKey:{}.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_gateway.rs:1176-1365; src-tauri/src/llm_suggestions.rs:230-262"
      },
      {
        "claim": "7.14: the UI currently offers AnthropicCompatible and Custom while the gateway routes only OpenAiCompatible and Ollama; phase 1 should delete the unsupported options from the normal UI.",
        "reality": "The gateway half is exactly right (llm_gateway.rs:146). The UI half is already done in the Phase 1-3 surface: src/features/settings/SettingsPage.tsx:29-33 defines SUPPORTED_PROVIDERS as only OpenAiCompatible + Ollama and coerces anything else on load (line 63). What is still open is the BACKEND and the type: llm_commands.rs:79-83 still accepts all four in save_llm_profile_core, src/types/settings.ts:2 still declares all four, and the retired src/pages/Settings.tsx:287-291 still renders all four options.",
        "verdict": "partly-wrong",
        "evidence": "src/features/settings/SettingsPage.tsx:29-33; src-tauri/src/llm_commands.rs:79-87; src/types/settings.ts:2; src/pages/Settings.tsx:287-291"
      },
      {
        "claim": "26.3-D: the existing worker runs in parallel with the local chain, but vision answer and outline execute sequentially under one mutable gateway lock.",
        "reality": "Confirmed literally for run_auto_pipeline_core_with_gateway - two separate lock_gateway acquisitions wrapping two full HTTP calls (auto_pipeline.rs:1253-1260). But this is not what serializes the shipped product, because the shipped UI never spawns that worker: it calls runAutoPipeline with executionMode:\"localOnly\" (so cloud_diagnostics_opted_in is false, profile_id is None, cloud_worker_spawned is false) and then runCloudReview, whose implementation run_cloud_review_core_with_gateway has no Mutex and no thread at all and is sequential by construction. Also, the group-repair loop and the cloud worker can never contend: 1416 gates the worker on main_source_is_pdf while 1698 gates the repair loop on !main_source_is_pdf.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/auto_pipeline.rs:1253-1260, 1415-1416, 1698, 2217-2262; src/features/import/useImportFiles.ts:149-158"
      },
      {
        "claim": "26.3-D: concurrency should be governed by a cloud semaphore rather than a gateway mutex.",
        "reality": "A cloud semaphore already exists - in TypeScript. useImportFiles.ts:155 runs runPool(cloudEligible, cloudConcurrency, runCloudReview) with cloudConcurrency defaulting to 2 (appSettings.ts:30), i.e. up to 2 concurrent run_cloud_review Tauri commands. There is no Rust-side limiter, so the Rust semaphore the plan asks for would be a second, currently absent, layer.",
        "verdict": "partly-wrong",
        "evidence": "src/features/import/useImportFiles.ts:147-158; src/features/settings/appSettings.ts:29-30"
      },
      {
        "claim": "7.4: do not render the same PDF twice, once for answer recognition and once for full-paper recognition.",
        "reality": "Already true WITHIN one auto-pipeline run: run_cloud_conversion_worker renders once (auto_pipeline.rs:1247) and the same `extraction` feeds the answer call, the outline call, and (via extraction_tx/extraction_rx) the main thread's vision transcription. It is NOT true across the two commands: run_cloud_review_core_with_gateway calls main_pdf_vision_extraction again (2243), and parser::extract_pdf_images_for_vision (2454) has no cache-hit short circuit - it re-spawns the python sidecar and re-renders every time even though cache/vision/pdf-images.json already exists. On the live UI path (localOnly pipeline, then cloud review) that is 2 full renders per PDF.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/auto_pipeline.rs:1247, 2243, 362-371; src-tauri/src/parser.rs:2454-2516"
      },
      {
        "claim": "7.12: every page must appear in a page manifest / coverage record so truncation is never mistaken for a complete result.",
        "reality": "The coverage record already exists and is already computed - parser.rs stabilize_pdf_image_extraction_fields writes coverageStatus (\"complete\"|\"partial\"|\"unknown\"), partialCoverage, renderedPageCount and coverage{pageCount, availablePageIndexes, renderedPageIndexes, missingPageIndexes} onto PdfImageExtractionV1. None of it reaches the model or the validator: make_cloud_paper_generation_input forwards only `pages` and `extractionWarnings` (llm_suggestions.rs:220-221), and cloud_outline_prompt serializes only job / sourceFile / outputContract, so even `pages` is invisible to the model on the primary PDF path. There is also no page-count or byte/token precheck anywhere - no 15-page threshold, no sharding, no max_tokens, and finish_reason is never inspected.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/parser.rs:1995-2045; src-tauri/src/llm_suggestions.rs:212-262; src-tauri/src/llm_gateway.rs:484-492, 706-773"
      },
      {
        "claim": "7.6: needs extract_single_json_value + a normalize step + schema validation + one repair + salvage.",
        "reality": "extract_single_json_value already exists and is stricter than the plan's sketch: parse_llm_json_content does a balanced, string-aware scan, keeps only outermost candidates, and explicitly errors with llm_json_parse_failed:ambiguous_wrapped_json when a response contains two independent top-level JSON values. normalize_safe_aliases partly exists as normalize_cloud_outline_kind (1076) + normalize_answer_text/normalize_question_number_key (1000-1030). RepairRequested, Salvaged and the CloudResponseState machine do not exist in any form - there is no second call, and no per-group acceptance.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_gateway.rs:219-288, 1000-1085"
      },
      {
        "claim": "26.3-E: whole-object schema validation throws away 39 good questions because of 1 bad one.",
        "reality": "Confirmed, and the blast radius is larger than described. validate_cloud_outline_output returns Err on the first failing group (1264-1302) AND on any single unparseable answerKey key - normalize_question_number_key only accepts 1..=200 after stripping a leading q/Q, so an answerKey key like \"8-9\", \"8 and 9\" or \"0\" kills the entire outline with cloud_outline_answer_key_question_number_invalid:<key> (1064-1065). Because the Err propagates out of run_llm_gateway, the resulting failure string lands in quality.cloudComparison.failure in pipeline-report.json, which then blocks V2 publish (see surprises).",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/llm_gateway.rs:1000-1007, 1054-1074, 1206, 1264-1302; src-tauri/src/auto_pipeline.rs:1102-1150, 2170"
      },
      {
        "claim": "17.9: llm_gateway.rs should RETAIN request ID / log correlation.",
        "reality": "There is nothing to retain. No request id is generated, sent, or logged; the provider's own response `id` is never read. llm-calls.jsonl records only recordType/commandName/jobId/model/ok/latencyMs/errorClass/recordedAt (llm_gateway.rs:49-68), and errorClass is just error.split(':').next(). Correlation between a cache/llm/*-input-<ms>.json file and its llm-calls.jsonl line is by wall-clock timestamp only.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/llm_gateway.rs:23-81"
      },
      {
        "claim": "17.9: move the CloudReadingOutlineV1 business assembly out of llm_gateway.rs.",
        "reality": "The assembly is spread over three files, so moving llm_gateway.rs alone does not finish the job. The request contract lives in llm_suggestions.rs:212-264 (outputContract.shape + 8 rules), the prompt restating the same shape lives in llm_gateway.rs:484-492, the enforcement lives in llm_gateway.rs:1176-1365, and the business semantics (notes-completion protection, kind families, answer comparison) live in auto_pipeline.rs:754-1012. All four restate the shape independently and can drift.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_suggestions.rs:212-264; src-tauri/src/llm_gateway.rs:484-492, 1176-1365; src-tauri/src/auto_pipeline.rs:754-1012"
      },
      {
        "claim": "7.11: model-supplied page/quote/bbox must be locally resolved to SourceAnchorV2 before entering canonical data; cloud IDs must be reallocated by a local IdAllocator.",
        "reality": "No resolver, no IdAllocator, and no bbox to resolve - the cloud contract never asks for bbox at all (prompt and validator only accept evidence.quotes[{pageIndex,text}]). Cloud outline quotes are never checked against the source document in any way; the only quote verifier in the codebase, llm_suggestion_quote_mismatches, is blockId-keyed and is wired exclusively into the V1 group-suggestion apply path (llm_commands.rs:355, auto_pipeline.rs:1763). Cloud `questionIds` are checked for count and uniqueness but never allocated or rewritten (llm_gateway.rs:1284-1301). The 1-based/0-based hazard the plan flags in 7.7 is real and live: the cloud contract and PdfImageExtractionV1 are 1-based (pageIndex==0 is rejected at 1289-1292), while DocumentIRV2 PageNodeV2.page_index and SourceAnchorV2.page_index are 0-based (schema/mod.rs fixtures use pageIndex 0).",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/llm_gateway.rs:484-492, 1273-1301; src-tauri/src/llm_suggestions.rs:402-449; src-tauri/src/schema/common.rs:67-89; src-tauri/src/schema/document_ir_v2.rs:296"
      },
      {
        "claim": "7.3: CloudRecognitionCandidateV1 needs TaskTypeV2, QuestionNumberExpressionV2, ContentNodeV2, OptionBankV2, ResponseGroupV2.",
        "reality": "All five already exist as typed, deny_unknown_fields, camelCase Rust structs and can be reused verbatim: TaskTypeV2 (ielts_authoring_v2.rs:202), QuestionNumberExpressionV2 (225, internally tagged on \"kind\"), TaskGroupV2 (242-258, which is CloudTaskGroupCandidate minus instruction_signature/source_anchors/quality/review_state plus candidate_id/evidence/confidence), OptionBankV2 (317-325), ResponseGroupV2 (337-353), ContentNodeV2 (content_doc_v2.rs:293-336). The one field that cannot be reused as written is the plan's `bbox: Option<[f64;4]>` - RectV2 (common.rs:19-29) requires unit + origin + pageRotation, so the resolver must construct RectV2, not deserialize into it.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/schema/ielts_authoring_v2.rs:202-353; src-tauri/src/schema/content_doc_v2.rs:293-336; src-tauri/src/schema/common.rs:19-29"
      },
      {
        "claim": "7.13: the second (proofread) call must be conditional on structural / text / answer / unassigned / partial conflict.",
        "reality": "There is no second call at all today, so nothing to make conditional - but the trigger inputs already exist. run_cloud_review_core_with_gateway computes cloud_needs_confirmation from attempted && (!passed || warningCount>0) (2325-2337) and cloud_outline_comparison already separates blocking `issues` from non-blocking `observations` (1005-1011). That boolean is the natural gate. Note that today run_cloud_review IS unconditional per PDF: useImportFiles.ts:155 calls it for every accepted .pdf when a cloud profile exists.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/auto_pipeline.rs:1005-1011, 2325-2337; src/features/import/useImportFiles.ts:151-158"
      },
      {
        "claim": "17.10: the deterministic fallback should no longer be saved as an \"LLM suggestion\" and should be marked source=local_fallback.",
        "reality": "It already self-labels: deterministic_llm_output emits evidence{source:\"rust-local-fallback\", fallback:true} and warnings including \"fallback-output-never-auto-applies\", and llm_suggestion_auto_apply_issues hard-blocks it three separate ways (fallback_evidence_never_auto_applies, non_provider_evidence_source:*, blocking_warning:*). What is still true is that it is written into the same store as real suggestions - save_llm_suggestion writes llm-suggestions/<group>--<id>.json, llm-last-suggestion.json AND appends to llm-calls.jsonl (llm_suggestions.rs:266-295), called at llm_commands.rs:251 and auto_pipeline.rs:1753.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_suggestions.rs:88-106, 266-295, 594-651; src-tauri/src/llm_commands.rs:232-251; src-tauri/src/auto_pipeline.rs:1728-1753"
      },
      {
        "claim": "17.9: keep timeout and Retry-After as-is.",
        "reality": "They work, but the timeout is doing double duty in a way a new transport should not inherit: openai_post derives ONE deadline from profile.timeoutMs (clamped 1s..300s) and uses it as both the per-attempt request timeout and the total 3-attempt budget (llm_gateway.rs:291-296). A first attempt that burns the whole budget leaves attempts 2 and 3 with zero, returning llm_timeout_budget_exhausted. Retry-After is capped at 5_000 ms in both parsers, and retry_after_ms_from_error also handles a \";retry_after=<seconds>\" suffix (349-355) that nothing in the codebase ever emits - only \";retry_after_ms=\" is produced (421-425).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/llm_gateway.rs:121-129, 290-318, 339-370, 415-430"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "shared_gateway / lock_gateway / run_cloud_conversion_worker (1214-1275, 1415-1433, 1569, 1717, 1862)",
        "whatToChange": "This is the 26.3-D change point. Delete `let shared_gateway = Mutex::new(llm_gateway);` (1415), lock_gateway (1214-1219) and its four call sites (1254, 1258, 1569, 1717), and change the injection bound on run_auto_pipeline_core_with_gateway (1387-1388) and run_cloud_review_core_with_gateway (2221-2224) from `F: FnMut(&Path,&str,&str,&Value,Option<&str>) -> CommandResult<Value> [+ Send]` to a shared `&(dyn LlmTransport + Send + Sync)` (or `Arc<T: LlmTransport>`). Once the callable is Sync, run_cloud_conversion_worker can issue the answer-key and outline requests on two scoped threads instead of two sequential lock acquisitions. Keep the mpsc extraction_tx/extraction_rx handshake - it is the thing that stops the PDF being rendered twice.",
        "risk": "high",
        "blockedBy": "9 closure-injection call sites must be rewritten at the same time: lib.rs:4403, 4521, 4772, 6030, 6268, 9737, 9974, 10134 (run_auto_pipeline_core_with_gateway) and lib.rs:10812 (run_cloud_review_core_with_gateway). Each passes a `|_root, _job_id, command_name, _input, _api_key| match command_name {..}` closure."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "symbol": "openai_post (290) + openai_post_once (372)",
        "whatToChange": "Extract these two into the plan's `LlmTransport`. Concretely: build the reqwest client ONCE and store it in the transport (today openai_post_once:379-383 constructs a fresh reqwest::blocking::Client per attempt, so nothing is pooled and TLS is renegotiated on every retry), keep redirect::Policy::none(), the 16 MiB response cap and the take()-bounded read verbatim, and split the single timeout budget (291) into an explicit per-attempt timeout plus a separate total budget so a slow first attempt no longer eats the retries. Replace the error-string sniffing in is_retryable_llm_http_error (320-337) and retry_after_ms_from_error (339-355) with a typed error carrying {status, retry_after, class} - the current design round-trips Retry-After through a \";retry_after_ms=\" suffix appended to a formatted error string (421-425), which is the only reason retry_after_ms_from_error exists. reqwest 0.12 is already present with only [\"blocking\",\"json\",\"rustls-tls\"]; the async Client is available without a Cargo change, but there is no direct tokio dependency, so an `async fn complete_json` needs either tauri::async_runtime or a Cargo addition.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "symbol": "run_llm_gateway command dispatch (36-48)",
        "whatToChange": "Where the new chain plugs in. Add arms for the full recognition call (plan 7.3/7.4, e.g. \"recognize_reading_paper\") and the conditional proofreader (plan 7.10, e.g. \"reconcile_reading_candidates\"), each delegating to a new recognition/cloud module rather than to a local run_openai_compatible_* fn. Keep \"generate_pdf_reading_outline\" wired to the existing handler - plan 17.10 says CloudReadingOutlineV1 stays for diagnostics/migration. Also add a requestId (uuid) here and thread it into both the cache filenames (30-33) and the llm-calls.jsonl record (49-68) so plan 17.9's \"log correlation\" becomes true.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "symbol": "llm_prompt (440), vision_prompt (469), vision_answer_prompt (476), cloud_outline_prompt (484), and the four hardcoded system messages at 630, 661, 692, 725 (+752 in the image fallback)",
        "whatToChange": "Where the versioned skill bundle plugs in. All five prompt strings are `format!` literals compiled into the binary; replace them with a SkillBundle loaded from recognition/skills/ielts-reading-v1/ (system.md, conversion-rules.md, output.schema.json, task-taxonomy.json, examples/*). The single system message \"Return valid JSON only.\" is duplicated verbatim 5 times and is where system.md goes. Note cloud_outline_prompt currently serializes only job / sourceFile / outputContract - it does NOT include `pages`, so the page manifest plan 7.12 wants must be added to the prompt payload, not just to the request struct. Also add manifest.skillVersion + schemaSha256 to the llm-calls.jsonl record so a bad batch can be attributed to a bundle version.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_suggestions.rs",
        "symbol": "make_cloud_paper_generation_input (212-264)",
        "whatToChange": "Where CloudRecognitionRequestV1 plugs in. This builder is the current request contract; split it into recognition/cloud/request.rs per plan 17.10 and add the four fields it lacks: request_id, skill_bundle (SkillBundleDescriptor), page_manifest (a real Vec<PageDescriptor>), and local_hints. The page manifest data is already computed and thrown away - forward extraction's coverage / coverageStatus / partialCoverage / renderedPageCount / coverage.missingPageIndexes (parser.rs:1995-2045) instead of only `pages` + `extractionWarnings` (220-221). Keep `pdfPath` as-is: data_url_for_pdf resolves it under the app root and the 128 MiB cap is enforced by read_llm_file.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "symbol": "validate_cloud_outline_output (1176-1365)",
        "whatToChange": "Where per-group salvage (7.9) plugs in. Change the signature from `-> CommandResult<()>` to something like `-> SalvageResult { valid_groups, rejected_groups, top_level_errors }` and stop returning Err on the first bad group (the loop at 1264-1302 does `?` on every check). Two specific hard-fail-on-good-data bugs to fix while here: (a) answerKey is mandatory (1206 via normalize_answer_map_value_checked) so a paper without a printed key cannot be recognised at all, and any single unparseable key (\"8-9\", \"8 and 9\", 0, >200) rejects the whole outline; (b) layoutHint is checked for presence only - `let _layout_hint = group_obj.get(\"layoutHint\")...` (1219-1223) - so the prompt's inline_completion|table|list constraint is unenforced and an unnormalized value flows straight into the raw string comparison in cloud_outline_comparison (935-964) and produces a spurious cloud_group_layout_mismatch. Also note validate runs the kind-normalization loop twice: 1211-1214 computes `let _normalized_kind` and discards it, then 1304-1330 does it again for real.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/schema/",
        "symbol": "new module cloud_recognition_v1.rs (registered in schema/mod.rs:1-14)",
        "whatToChange": "Where the CloudRecognitionCandidateV1 struct itself goes. Add `pub mod cloud_recognition_v1;` and re-export alongside ContentDocV2/DocumentIRV2/IeltsAuthoringIRV2. Reuse TaskTypeV2, QuestionNumberExpressionV2, ContentNodeV2, OptionBankV2, ResponseGroupV2 verbatim from ielts_authoring_v2.rs - they already exist with the exact field names the plan lists. Follow the house style used by every sibling: #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)] + #[serde(rename_all = \"camelCase\")] + #[serde(deny_unknown_fields)] + an `is_supported_schema_version()` method, and add a round_trip fixture to the schema/mod.rs test module (schema/mod.rs:17-40 has the generic round_trip<T> helper).",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "cloud_outline_comparison (825-1012)",
        "whatToChange": "Where DeterministicDiffV1 (plan 8.2/7.10) plugs in. The alignment key today is an exact [start,end] questionRange tuple equality against local groups (852-875), so any local/cloud split difference degrades to cloud_group_missing_locally instead of a structural diff - this needs overlap-based alignment before it can drive a proofreader. Preserve the two hard-won domain rules when rewriting: local_inline_notes_evidence (801-816) + the layout de-prioritization at 941-953 (local inline notes beat a cloud `list` layout) and semantic_group_kind_family (754-762) which folds summary/sentence/note completion into one \"completion\" family. Also preserve the 20-issue truncation (1005-1011) - `passed` is false whenever truncated>0.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/",
        "symbol": "new recognition/cloud/evidence_resolver.rs (plan 7.11)",
        "whatToChange": "Nothing exists to extend; this is new. The one reusable primitive is the normalization pair in llm_suggestions.rs:382-400 (normalized_quote_text and the hyphen-stripped fallback normalized_quote_text_without_hyphens, which exists because models reproduce line-broken words as \"stencil- ling\"). Two things a resolver must handle that the current code does not: (1) the pageIndex base mismatch - cloud/PdfImageExtractionV1 are 1-based (pageIndex==0 is rejected at llm_gateway.rs:1289-1292 and 1219-1225 in the answer validator) while DocumentIRV2 PageNodeV2.page_index and SourceAnchorV2.page_index are 0-based; (2) RectV2 (schema/common.rs:19-29) requires unit + origin + pageRotation + optional normalized[4], so a bare 4-float model bbox must be interpreted, not deserialized. Note there is currently NO bbox in the cloud contract at all, so the prompt/schema must start asking for it first.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "collect_publish_gate_markers (333-435), called from the publish-proof builder at 287-317",
        "whatToChange": "Must be updated in lockstep with 7.9 salvage or the new chain will silently become unpublishable. It walks the authoring doc plus authoring-ir.json plus pipeline-report.json and pushes into `partial_failures` on (a) any non-empty `failure` value at a path containing \"cloud\"/\"llm\"/\"ai\"/\"vision\"/\"quality\"/\"audit\" (393-406), (b) any `status` of partial|partial_failure|needs_review|auto_apply_blocked under those same paths (377-391), (c) non-empty `failures` arrays under llm/ai paths and any non-empty blockedAutoApplyGroups/lowConfidenceGroups (367-375). Non-empty partial_failures => Err(\"authoring_v2_export_blocked:partial_failures=..\"). A salvaged CloudRecognitionCandidateV1 with rejected_groups, or an outline call that merely failed, therefore blocks publish today.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_gateway.rs",
        "symbol": "openai_chat_content (192-198) and the missing finish_reason / max_tokens handling",
        "whatToChange": "openai_chat_content only reads /choices/0/message/content as a string, so a provider that returns content as an array of parts, or puts the payload in tool_calls, yields Err(\"llm_empty_content\") with no diagnostic. Separately, no request sets max_tokens and nothing ever inspects choices[0].finish_reason - a `\"length\"` truncation is indistinguishable from a malformed response and surfaces as llm_json_parse_failed. Plan 26.3-C (\"truncation must not be treated as a complete result\") cannot be satisfied without reading finish_reason here.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "SUPPORTED_LLM_PROVIDERS (79-83) + save_llm_profile_core (85-87)",
        "whatToChange": "Plan 7.14: reduce to [\"OpenAiCompatible\",\"Ollama\"] so the persisted store cannot contain a profile the gateway will reject at call time with llm_provider_unsupported_for_openai_route. Then narrow `export type LlmProvider` in src/types/settings.ts:2 to match, and decide the fate of src/pages/Settings.tsx:287-291 (the retired page still offers all four). A migration pass over config/llm-profiles.json is needed for users who already saved AnthropicCompatible/Custom. Also worth fixing while here: select_llm_profile (auto_pipeline.rs:156-160) scores provider==\"OpenAiCompatible\" as +1, so an Ollama profile loses a tie to an OpenAiCompatible one despite both being routable.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "test_llm_profile_core (173-211)",
        "whatToChange": "The connectivity test routes through command_name \"test_profile\", which llm_gateway.rs:37-39 dispatches to run_openai_compatible_group_llm and therefore through validate_llm_suggestion_output. A perfectly healthy provider that returns valid JSON but not an IELTS group suggestion (kind in the allowed 13 + numeric confidence + patch/questions/evidence.sourceBlockIds/evidence.quotes arrays) is reported as \"LLM gateway failed\". Give the ping its own minimal validator. Side note: it calls run_llm_gateway with job_id \"profile-test\", which creates jobs/profile-test/cache/llm/* and jobs/profile-test/llm-calls.jsonl - a fake job directory outside any real job's lifecycle and therefore outside cleanup.rs.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/Cargo.toml",
        "symbol": "[profile.release] panic = \"abort\" vs auto_pipeline.rs:1238 catch_unwind",
        "whatToChange": "run_cloud_conversion_worker wraps its body in std::panic::catch_unwind(AssertUnwindSafe(..)) and documents that \"a panic inside the cloud conversions must degrade into an Err outcome (local draft keeps working) instead of hanging recv()\". With panic = \"abort\" in the release profile that catch_unwind can never fire - a panic aborts the whole app. The guarantee only holds in dev/test builds. Either drop panic=\"abort\" or achieve the same by making the worker structurally panic-free and relying on the mpsc sender being dropped (a dropped sender makes recv() return Err, which the code already handles at 1862-1866 via unwrap_or(CloudWorkerOutcome{..\"cloud_conversion_worker_terminated\"..})).",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "run_cloud_review_core_with_gateway (2217-2262)",
        "whatToChange": "Where plan 7.13's conditional proofreader trigger goes, and where the double render must be fixed. It unconditionally re-renders the PDF (2243) then runs outline (2245) + answer key (2258) for every call. Add (a) an extraction cache check so a fresh cache/vision/pdf-images.json from the immediately preceding run_auto_pipeline is reused, and (b) the 7.13 gate around the second (proofread) call using the cloud_needs_confirmation boolean already computed at 2325-2337. Note the existing merge-on-write pattern at 2350-2400 (re-read authoring-ir.json, replay the audit issue onto the fresh copy because append_authoring_audit_issue replaces same-kind issues idempotently) - any new candidate write-back must follow it or it will clobber concurrent user edits.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "symbol": "extract_pdf_images_for_vision (2454-2516)",
        "whatToChange": "Add a cache-hit short circuit: it takes an output_path and writes to it, but never reads it, so every caller re-spawns the python sidecar and re-renders every page. Two callers per PDF on the live UI path (auto_pipeline.rs:369 via the worker, then again at 2243 in cloud review). Gate reuse on the source sha256 so a re-uploaded file still re-renders. Also relevant to the Rust-side semaphore: the pdfium renderer serializes globally anyway through PDFIUM_INSTANCE: OnceLock<Mutex<Result<Pdfium,String>>> (pdf_geometry.rs:31-42), so two concurrent run_cloud_review commands already queue behind each other for rendering.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/validator.rs",
        "symbol": "allowed_question_kind (41-58) and its three copies",
        "whatToChange": "The 13-kind enum is written out four times: validator.rs:41-58 (used by the gateway validators), llm_suggestions.rs:329-346 (is_allowed_llm_group_kind, used by the auto-apply gate), llm_gateway.rs:441-455 (the array joined into llm_prompt), and again as a prose list inside cloud_outline_prompt (llm_gateway.rs:487). They are currently identical; the skill bundle's task-taxonomy.json should become the single source and the prompt lists should be generated from it. TaskTypeV2 (schema/ielts_authoring_v2.rs:202) is the typed enum that already exists - check its variant set against these 13 before unifying.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "cleanup_transient_job_artifacts (99-160) and minimize_process_artifacts_after_authoring (161+)",
        "whatToChange": "Plan 7.11 wants raw cloud output kept only in a transient workspace and deleted on a TTL. The hook exists: both functions already remove the \"cache\" and \"llm-suggestions\" directories plus llm-last-suggestion.json and llm-calls.jsonl, and cache/llm/*-output-*.json is where the raw outline currently lives. Add the new candidate/proposal files to these lists, and note that minimize_process_artifacts_after_authoring already runs at the end of every auto-pipeline (auto_pipeline.rs:2186) - so a raw candidate written under cache/ will already be swept unless it is deliberately placed elsewhere.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/tauri.conf.json",
        "symbol": "bundle.resources + environment.rs sidecar_candidates (33-70) / find_sidecar (71-75)",
        "whatToChange": "How the skill bundle gets shipped and found at runtime. resources is currently [\"../sidecars\"]; add the bundle directory (e.g. \"../recognition\") and resolve it with the existing find_sidecar(\"recognition/skills/ielts-reading-v1/manifest.json\"), which already probes cwd, cwd/.., exe dir, exe/.., exe/resources, exe/../Resources and CARGO_MANIFEST_DIR/.. - the same mechanism parser.rs uses for sidecars/python-parser/parser.py. Alternatively include_str! the bundle into the binary, which removes the file-not-found failure mode but loses the ability to ship a bundle update without a rebuild. Compute manifest.schemaSha256 at load time and fail closed if it does not match, per plan 7.2.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "run_cloud_review (1303-1312), run_auto_pipeline (1291-1301), llm_classify_group (1098-1111), llm_extract_group (1113-1126), apply_llm_suggestion (1128-1147), test_llm_profile (1089-1096)",
        "whatToChange": "Command surface for plan 17.11. Every cloud command is `#[tauri::command] async fn` wrapping tauri::async_runtime::spawn_blocking(move || ..core(&root, ..)) - a new retry_cloud_recognition(item_id) follows the same 10-line shape. llm_classify_group / llm_extract_group / apply_llm_suggestion are the V1 commands 17.11 wants dropped from the new UI; llmExtractGroup is still referenced from the retired src/pages/UnifiedPreview.tsx:805 but not from src/features/*, so removing them from the new surface is already effectively done frontend-side. Note lib.rs is 449 KB but only lines 1-1480 are non-test; `mod tests` starts at 1481.",
        "risk": "low"
      }
    ],
    "testHarness": "All Rust tests for this subsystem live in the single `mod tests` at src-tauri/src/lib.rs:1481 (about 9,300 lines). There is no tests/ directory and no test module inside llm_gateway.rs or llm_suggestions.rs themselves, so anything new must either go into lib.rs's module or bring its own #[cfg(test)] block.\n\nThree harness patterns to copy verbatim:\n\n1. Real HTTP against a one-shot TCP mock. `mock_openai_server(content: Value) -> (String, mpsc::Receiver<String>, thread::JoinHandle<()>)` at lib.rs:1764-1788 binds 127.0.0.1:0, accepts exactly one connection, ships the raw request text back over a channel, and answers with {\"choices\":[{\"message\":{\"content\":<content serialized as a string>}}],\"usage\":{..}}; it returns the base_url as \"http://{addr}/v1\". `mock_openai_server_sequence(responses: Vec<(u16, String)>)` at lib.rs:10296-10319 does the same for N sequential responses and is how retry behaviour is proved. Helpers alongside: `read_mock_http_request`, `mock_profile_input(base_url)` (10321), `mock_group_input(base_url)` (10335), `mock_ok_body()` (10346), `tiny_png_bytes()` (1790), `cached_llm_inputs(root, job_id)` (1798). Exact tests to copy: `rust_llm_gateway_hits_mock_openai_compatible_chat_completion` (lib.rs:2246 - asserts \"POST /v1/chat/completions \", \"authorization: Bearer sk-mock-secret\", \"\\\"response_format\\\":{\\\"type\\\":\\\"json_object\\\"}\" and evidence.source==\"openai-compatible-rust\"), `llm_gateway_retries_429_then_succeeds` (10363), `llm_gateway_call_record_persists_latency_and_error_class` (10394 - points at http://127.0.0.1:9/unreachable and reads jobs/<id>/llm-calls.jsonl), `rust_vision_gateway_sends_image_url_and_parses_transcription` (3187), `llm_json_parse_content_extracts_wrapped_json_and_rejects_garbage` (10419), `llm_suggestion_confidence_out_of_range_never_auto_applies` (10431), `llm_profile_save_rejects_public_http_unknown_provider_and_accepts_localhost` (10517).\n\n2. Closure-injected gateway for pipeline behaviour - no HTTP at all. This is the pattern a new LlmTransport must keep working, and the 9 call sites that will break: `run_auto_pipeline_core_with_gateway(&root, &job.job_id, Some(AutoPipelineInput{ parse_mode, confidence_threshold, profile_id, execution_mode, target, allow_overwrite }), |_root, _job_id, command_name, _input, _api_key| match command_name { \"extract_pdf_image_answers\" => Ok(json!({..})), \"generate_pdf_reading_outline\" => Ok(json!({..})), _ => Err(..) })` at lib.rs:4403, 4521, 4772, 6030, 6268, 9737, 9974, 10134, plus `run_cloud_review_core_with_gateway` at lib.rs:10812. The best worked examples for the outline contract are `cloud_outline_completion_family_diff_does_not_override_local_inline_notes` (lib.rs:5983 - a full note-completion Q8-13 fixture with an answers block, proving the cloud `list` layoutHint must not override local inline_completion) and `cloud_outline_mismatch_is_persisted_as_authoring_audit_summary` (6221). `auto_pipeline_local_only_skips_all_pdf_cloud_calls` (9910) is the one that asserts executionMode:\"localOnly\" issues zero gateway calls - it will catch any change that makes cloud work unconditional. `cloud_review_write_back_preserves_concurrent_user_edit` (10754) guards the merge-on-write path.\n\n3. Live-provider smoke tests, #[ignore]d and env-gated. `live_llm_profile_from_env()` (lib.rs:1811) reads EPIC8_LIVE_LLM_BASE_URL / EPIC8_LIVE_LLM_API_KEY / EPIC8_LIVE_LLM_MODEL; `live_rust_llm_gateway_provider_smoke_text_and_vision` (2315) and `live_llm_repair_contract_on_files_pdf_samples` (2401) run text + vision against a real endpoint and assert the cached inputs never contain the api key.\n\nShared fixtures: `temp_test_root()`, `ensure_app_dirs`, `make_job(CreateJobInput{..})`, `attach_fixture_source(&root, &mut job, \"no-text.pdf\"|\"image-only.pdf\", \"MainQuestion\")`, `save_job`, `ensure_job_dirs(&job_dir(&root, &job.job_id))`, `crate::llm_profiles::save_profiles(&root, &[json!({profileId,name,provider,baseUrl,model,temperature,timeoutMs,forceJson,enabled})])`, and `write_source_review_status(&root, &job_id, Some(&doc), true, None)` (needed or the pipeline stops at DocumentReview). Every test ends with `let _ = fs::remove_dir_all(root);`. Env-mutating tests must take `env_test_lock().lock()` (lib.rs:1858).\n\nSchema round-trip tests for a new CloudRecognitionCandidateV1 belong in src-tauri/src/schema/mod.rs's own `mod tests` (schema/mod.rs:17-40), which has a generic `fn round_trip<T>(value: Value) -> T` asserting serialize/deserialize idempotence - copy `authoring_ir_v2_round_trip_preserves_shared_slot_contract` (schema/mod.rs, ~line 163).\n\nI did not run cargo build or cargo test (instructed not to); everything above is read from source.",
    "surprises": [
      "The concurrency problem plan 26.3-D attacks is not on the live path. The shipped UI never triggers the Mutex<FnMut> worker: src/features/import/useImportFiles.ts:149 calls runAutoPipeline with executionMode:\"localOnly\" (which forces cloud_diagnostics_opted_in=false at auto_pipeline.rs:1395, so profile_id is None and cloud_worker_spawned is false at 1416), then pools runCloudReview separately at 155. run_cloud_review_core_with_gateway (2217) has no Mutex, no thread and no `+ Send` bound - it runs the outline and the answer-key extraction strictly one after the other. src/features/editor/ExamWorkspacePage.tsx:115-120 does the same. So the Mutex is dead weight on the real path and the actual serializer is a plainly sequential function.",
      "A failed cloud call blocks V2 publish. cloud_outline_report_from_output (auto_pipeline.rs:1132-1140) writes the raw internal error string into `failure`, which lands in pipeline-report.json at quality.cloudComparison.failure (2170/2185). authoring_v2_commands.rs collect_publish_gate_markers (393-406) reads pipeline-report.json and pushes any non-empty `failure` under a path containing \"cloud\" into partial_failures, and a non-empty partial_failures returns Err(\"authoring_v2_export_blocked:partial_failures=..\") (311-316). So a provider outage or an over-strict outline validator makes a perfectly good local draft unpublishable until the report is regenerated. Same mechanism catches llm.failures and blockedAutoApplyGroups.",
      "answerKey is mandatory in CloudReadingOutlineV1. validate_cloud_outline_output:1206 calls normalize_answer_map_value_checked(obj.get(\"answerKey\"), ..), which returns Err(\"cloud_outline_answer_key_missing\") when the field is absent. A question paper with no printed answer key can only pass if the model emits answerKey:{}. Worse, normalize_question_number_key (1000-1007) accepts only 1..=200 after stripping a leading q/Q, so a single key like \"8-9\" or \"8 and 9\" rejects the entire outline.",
      "layoutHint is never validated against its own contract. Both the prompt (llm_gateway.rs:487, \"layoutHint must be inline_completion, table, or list when known\") and the outputContract (llm_suggestions.rs:239) constrain it, but the validator only checks presence: `let _layout_hint = group_obj.get(\"layoutHint\").and_then(Value::as_str).ok_or_else(..)?` (1219-1223). The unvalidated string then goes into a raw != comparison in cloud_outline_comparison (935-964), so any synonym the model invents becomes a user-visible cloud_group_layout_mismatch issue.",
      "catch_unwind cannot work in release. run_cloud_conversion_worker (auto_pipeline.rs:1238) wraps its body in std::panic::catch_unwind(AssertUnwindSafe(..)) and the comment promises a panic \"must degrade into an Err outcome (local draft keeps working)\", but Cargo.toml sets panic = \"abort\" under [profile.release]. The guarantee holds only in dev/test builds; in a shipped build a panic in the cloud worker takes the app down.",
      "The cloud outline model never sees the page manifest, or even the page list. cloud_outline_prompt (llm_gateway.rs:484-492) serializes only input.job, input.sourceFile and input.outputContract. `pages` and `extractionWarnings` are built into the request by make_cloud_paper_generation_input (llm_suggestions.rs:220-221) but are used only by append_pdf_images_to_content in the image-fallback branch (740-745). Meanwhile parser.rs already computes exactly the coverage record plan 7.12 asks for - coverageStatus/partialCoverage/renderedPageCount/coverage.missingPageIndexes (1995-2045) - and none of it is forwarded anywhere.",
      "Cloud outline evidence quotes are never verified against the source. The validator requires non-empty evidence.quotes with pageIndex>=1 and non-empty text (1273-1300), but nothing ever checks that the quoted text exists in the PDF. The codebase does have a quote verifier - llm_suggestion_quote_mismatches (llm_suggestions.rs:402) with whitespace and hyphen tolerance - but it is keyed by V1 blockId and is wired only into the group-suggestion apply path (llm_commands.rs:355, auto_pipeline.rs:1763). A model can fabricate every outline quote and cloud_outline_comparison will happily accept the structure.",
      "pageIndex base mismatch across the boundary the resolver must cross. The cloud contract and PdfImageExtractionV1 are 1-based - pageIndex==0 is an explicit error at llm_gateway.rs:1289-1292 (outline quotes) and 1219-1225 (answer evidence). DocumentIRV2 PageNodeV2.page_index (schema/document_ir_v2.rs:296) and SourceAnchorV2.page_index (schema/common.rs:69) are 0-based, as the schema/mod.rs fixtures confirm. Plan 7.7 lists this as a permitted normalization \"only when the request declares it\"; today nothing declares it because no cross-boundary resolution happens.",
      "The connectivity test is held to the full IELTS suggestion contract. test_llm_profile_core (llm_commands.rs:173) sends command_name \"test_profile\", which llm_gateway.rs:37-39 routes to run_openai_compatible_group_llm and therefore through validate_llm_suggestion_output. A reachable, correctly configured provider that returns valid JSON of the wrong shape reports \"LLM gateway failed\". It also writes into a synthetic job directory jobs/profile-test/ that no cleanup path owns.",
      "No max_tokens, no finish_reason. None of the four request builders sets max_tokens, and choices[0].finish_reason is never read anywhere in the gateway. A response truncated at the context limit is indistinguishable from a malformed one and surfaces as llm_json_parse_failed - which is precisely the silent-truncation failure plan 26.3-C says must be impossible.",
      "A whole 128 MiB PDF is base64-inlined into a single blocking JSON request body. data_url_for_pdf (llm_gateway.rs:559-577) reads the file up to MAX_LLM_PDF_BYTES = 128 MiB and embeds it as a data URL, so worst case is roughly 171 MB of base64 held in memory inside the request body, with a 16 MiB cap only on the response. There is no page-count or byte precheck, and no sharding.",
      "openai_post builds a brand-new reqwest::blocking::Client on every attempt (llm_gateway.rs:379-383). Nothing is pooled: three retries mean three TLS handshakes to the same host. Making the client a clonable field of the new LlmTransport fixes this and the Send+Sync requirement at the same time.",
      "Retry-After takes a round trip through a formatted error string. openai_post_once parses the header (401-406), then re-encodes it as a \";retry_after_ms=<ms>\" suffix on the error message (421-425), which retry_after_ms_from_error (339) then re-parses. That function also handles a \";retry_after=<seconds>\" form which nothing in the repo ever produces. Both parsers cap at 5_000 ms.",
      "The single profile timeout is both the per-request timeout and the total retry budget. openai_post computes one deadline (291) and hands the remaining budget to each attempt as its request timeout, so an attempt that consumes the full clamped timeoutMs (max 300 s) leaves nothing for attempts 2 and 3 and the call ends as llm_timeout_budget_exhausted rather than actually retrying.",
      "The 13 allowed question kinds are duplicated four times and can drift silently: validator.rs:41-58 (gateway validators), llm_suggestions.rs:329-346 (auto-apply gate), llm_gateway.rs:441-455 (array joined into llm_prompt), and a fourth prose copy inside cloud_outline_prompt at llm_gateway.rs:487. They happen to agree today.",
      "Everything in this subsystem is serde_json::Value. There is not a single struct or enum in llm_gateway.rs or llm_suggestions.rs - no LlmProvider enum (provider is a raw string compared at llm_gateway.rs:146), no CloudReadingOutlineV1 type (the name appears exactly once in the repo, as a string literal at llm_suggestions.rs:235), no typed error. Meanwhile schema/ already has fully typed, deny_unknown_fields versions of everything plan 7.3 needs, so the typed candidate contract is mostly assembly rather than new modelling.",
      "pdfium serializes globally regardless of any gateway change. pdf_geometry.rs:31-42 keeps a static PDFIUM_INSTANCE: OnceLock<Mutex<Result<Pdfium,String>>> and hands out a guard, so two concurrent run_cloud_review commands queue behind each other for page rendering even after the gateway mutex is gone."
    ]
  },
  {
    "subsystem": "auto_pipeline orchestration (src-tauri/src/auto_pipeline.rs: run_auto_pipeline + run_cloud_review)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "The whole orchestration. Two entry points: run_auto_pipeline_core_with_gateway (1381-2189) and run_cloud_review_core_with_gateway (2217-2668). Also owns the cloud worker thread (1226-1274), the gateway Mutex helper lock_gateway (1214-1218), select_llm_profile (115-168), the V2 shadow write wrapper write_pipeline_authoring_v2_shadow (275-315), and the freshness check current_physical_shadow / physical_shadow_matches_source (249-273).",
        "sizeNote": "2796 lines / 116 KB. Tests are only lines 2670-2796 and cover 4 pure helpers; no test in this file exercises either core function."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "Tauri command wrappers run_auto_pipeline (1291-1301) and run_cloud_review (1303-1313), each a single spawn_blocking. AutoPipelineInput/RunCloudReviewInput structs (286-304). CLI live path run_auto_pipeline_from_path (502-600) — the only caller that actually enables the cloud worker. Command registration list (1409-1476). All pipeline integration tests live in its #[cfg(test)] mod.",
        "sizeNote": "449 KB / ~11k lines. Plan §18 P8-T04 wants this split."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "role": "write_authoring_v2_shadow (60-74) and build_authoring_v2_shadow (76-380). Owns the three shadow filenames (56-58) and the single hard gate: serde_json::from_value::<IeltsAuthoringIRV2> at 377-378.",
        "sizeNote": "112 KB; build_authoring_v2_shadow alone is ~300 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "The consumer. get_authoring_v2_core (54-57) -> build_editor_session (706-719) -> load_current_authoring (725-742), which reads authoring-ir-v2.shadow.json only when revision == 0. apply_authoring_v2_patches_core (123-168) goes through the same loader, so the FIRST save also depends on the shadow.",
        "sizeNote": "97 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "role": "minimize_process_artifacts_after_authoring (158-208), called at the very end of run_auto_pipeline (auto_pipeline.rs:2186). Deletes document-ir.json, split-candidates.json, pipeline-report.json, validation-report.json, cache/, preview/, llm-suggestions/. Does NOT delete authoring-ir-v2.shadow.json or document-ir-v2.shadow.json.",
        "sizeNote": "8.6 KB — small, read it in full"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "role": "authoring_v2_shadow_enabled() (223-225), quality_gate_v2_enabled() (227-229), document_ir_v2_shadow_enabled() (219-221) — all three are `true` with no env lookup. env_flag_enabled (197-209) exists but is only used by cloud_pdf_vision / local_ocr / pdf_renderer."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "evaluate_quality — produces quality.state in {ready, review_required, blocked} (186-199). evaluate_compiler_probes (778-821) re-validates the whole IR and compiles ReadingExamSourceV2 + V1 compatibility on every call, which is what makes each build_authoring_v2_shadow expensive.",
        "sizeNote": "157 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "role": "job.json is the state of record. update_job (46-56) is a non-atomic read-modify-write; save_job (33-44) best-effort dual-writes to the library DB and only eprintln!s on failure.",
        "sizeNote": "3.7 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "role": "Schema DDL 79-190. ingest_jobs (143-155) is the closest thing to a queue table: status CHECK list, current_step, no lease/worker/attempt/stage columns. job_artifact_index (184-190) is declared and never written. WAL + busy_timeout=5000 (55-65)."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/import/useImportFiles.ts",
        "role": "The real product import driver. Stage A creates jobs serially; stage B runs runAutoPipeline through a JS pool (148) then runCloudReview through a second pool (155). This is where today's concurrency actually lives. NOTE: this file was being edited by another agent while I read it — LOCAL_CONCURRENCY constants at 17-18 were replaced by readAppSettings() at 147 between two reads."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx",
        "role": "Workspace. Menu 'rerun local' passes allowOverwrite:true (115); the load-error recovery button (173) does NOT, which makes it unusable. Both pass executionMode:'localOnly', target:'editableDraft'."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "Second V2-shadow writer: build_authoring_ir_core (395-470), shadow block at 417-455. Also parse_document's physical-shadow block (186-230) which — unlike the auto pipeline — handles both pdf and docx. protect_existing_authoring (353-364) is the same overwrite guard as the pipeline's."
      }
    ],
    "publicApi": [
      {
        "symbol": "run_auto_pipeline_core_with_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1381",
        "signature": "pub(crate) fn run_auto_pipeline_core_with_gateway<F>(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n    mut llm_gateway: F,\n) -> CommandResult<Value>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value> + Send,",
        "notes": "The `+ Send` bound exists only because of the scoped cloud worker thread. Returns the pipeline report Value; also the value written to pipeline-report.json at 2185 (which is then deleted at 2186)."
      },
      {
        "symbol": "run_auto_pipeline_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1153",
        "signature": "pub(crate) fn run_auto_pipeline_core(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n) -> CommandResult<Value> {\n    run_auto_pipeline_core_with_gateway(root, job_id, input, run_llm_gateway)\n}",
        "notes": "Production entry. Takes no AppHandle and no channel — already shaped for a queue worker to call directly."
      },
      {
        "symbol": "run_cloud_review_core_with_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:2217",
        "signature": "pub(crate) fn run_cloud_review_core_with_gateway<F>(\n    root: &Path,\n    job_id: &str,\n    input: Option<RunCloudReviewInput>,\n    mut llm_gateway: F,\n) -> CommandResult<Value>\nwhere\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>,",
        "notes": "No `+ Send` — it is entirely single-threaded. Hard-fails at 2231-2232 with \"authoring_ir_missing_for_cloud_review\" if authoring-ir.json is absent."
      },
      {
        "symbol": "AutoPipelineInput",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:286",
        "signature": "pub struct AutoPipelineInput {\n    #[serde(rename = \"profileId\")]\n    pub profile_id: Option<String>,\n    #[serde(rename = \"confidenceThreshold\")]\n    pub confidence_threshold: Option<f64>,\n    #[serde(rename = \"parseMode\")]\n    pub parse_mode: Option<String>,\n    #[serde(rename = \"executionMode\")]\n    pub execution_mode: Option<String>,\n    pub target: Option<String>,\n    #[serde(rename = \"allowOverwrite\")]\n    pub allow_overwrite: Option<bool>,\n}",
        "notes": "executionMode and target are free-form Option<String>, not enums; unknown values are silently accepted."
      },
      {
        "symbol": "RunCloudReviewInput",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:300",
        "signature": "pub struct RunCloudReviewInput {\n    #[serde(rename = \"profileId\")]\n    pub profile_id: Option<String>,\n}",
        "notes": "No executionMode/target. profileId is optional and, unlike the auto pipeline, a None here still falls through to select_llm_profile's auto-pick (2233), so cloud review can fire with no explicit profile."
      },
      {
        "symbol": "run_auto_pipeline (tauri command)",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:1291",
        "signature": "#[tauri::command]\nasync fn run_auto_pipeline(\n    job_id: String,\n    input: Option<AutoPipelineInput>,\n    app: AppHandle,\n) -> CommandResult<Value> {\n    let root = app_root(&app)?;\n    tauri::async_runtime::spawn_blocking(move || run_auto_pipeline_core(&root, &job_id, input))\n        .await\n        .map_err(|error| error.to_string())?\n}",
        "notes": "The entire pipeline is one blocking task. No progress reporting, no cancellation token, no event emit."
      },
      {
        "symbol": "run_cloud_review (tauri command)",
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:1303",
        "signature": "#[tauri::command]\nasync fn run_cloud_review(\n    job_id: String,\n    input: Option<RunCloudReviewInput>,\n    app: AppHandle,\n) -> CommandResult<Value> {\n    let root = app_root(&app)?;\n    tauri::async_runtime::spawn_blocking(move || run_cloud_review_core(&root, &job_id, input))\n        .await\n        .map_err(|error| error.to_string())?\n}"
      },
      {
        "symbol": "write_pipeline_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:275",
        "signature": "fn write_pipeline_authoring_v2_shadow(\n    dir: &Path,\n    job: &ImportJob,\n    authoring: &Value,\n    split: &Value,\n    document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n) -> CommandResult<()>",
        "notes": "THE critical function. Returns Ok(()) even when the shadow could not be produced: the Err arm at 300-312 deletes the shadow and compare files and writes authoring-ir-v2.shadow.error.json instead."
      },
      {
        "symbol": "write_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:60",
        "signature": "pub(crate) fn write_authoring_v2_shadow(\n    job_dir: &Path,\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n    output_path: &Path,\n) -> CommandResult<Value>",
        "notes": "Writes output_path and job_dir/authoring-ir-v2.shadow.compare.json, both via write_canonical_json_atomic (temp file + fsync + replace)."
      },
      {
        "symbol": "build_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:76",
        "signature": "pub(crate) fn build_authoring_v2_shadow(\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n) -> CommandResult<Value>",
        "notes": "Pure. Only three error paths in the whole body: the Option-`?` at 229 (not an error), serde_json::to_value of the instruction signature at 322, and `serde_json::from_value::<IeltsAuthoringIRV2>(authoring.clone()).map_err(|error| format!(\"authoring_ir_v2_shadow_schema_validation_failed:{error}\"))?` at 377-378."
      },
      {
        "symbol": "load_current_authoring",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:725",
        "signature": "fn load_current_authoring(root: &Path, job_id: &str) -> CommandResult<(Value, u64)> {\n    let current = recover_current_revision(root, job_id)?;\n    if current.revision > 0 {\n        let value = read_revision(root, job_id, current.revision)?;\n        validate_authoring(&value)?;\n        return Ok((value, current.revision));\n    }\n\n    let path = job_dir(root, job_id).join(AUTHORING_V2_SHADOW_FILE);\n    let value = read_json_opt(&path)?.ok_or_else(|| {\n        format!(\n            \"AUTHORING_V2_NOT_AVAILABLE:shadow_missing:{}\",\n            path.display()\n        )\n    })?;\n    validate_authoring(&value)?;\n    Ok((value, 0))\n}",
        "notes": "This is the exact seam between the pipeline and the workspace. Note revision > 0 wins permanently — the shadow becomes dead weight after the first save."
      },
      {
        "symbol": "run_cloud_conversion_worker",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1226",
        "signature": "fn run_cloud_conversion_worker<F>(\n    root: &Path,\n    job: &ImportJob,\n    profile_id: &str,\n    shared_gateway: &Mutex<F>,\n    extraction_tx: mpsc::Sender<Result<(Value, PathBuf), String>>,\n    worker_tx: mpsc::Sender<CloudWorkerOutcome>,\n) where\n    F: FnMut(&Path, &str, &str, &Value, Option<&str>) -> CommandResult<Value>,",
        "notes": "Wraps its body in catch_unwind (1239) so a panic degrades to Err rather than hanging recv()."
      },
      {
        "symbol": "lock_gateway",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1214",
        "signature": "fn lock_gateway<F>(shared: &Mutex<F>) -> std::sync::MutexGuard<'_, F> {\n    shared\n        .lock()\n        .unwrap_or_else(|poisoned| poisoned.into_inner())\n}",
        "notes": "The single global serialization point for every LLM call in a pipeline run. Plan 26.3-D targets exactly this."
      },
      {
        "symbol": "minimize_process_artifacts_after_authoring",
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:158",
        "signature": "pub(crate) fn minimize_process_artifacts_after_authoring(\n    root: &Path,\n    job_id: &str,\n    reason: &str,\n) -> CommandResult<Value>",
        "notes": "Called as `let _ = minimize_process_artifacts_after_authoring(root, &job_id, \"run_auto_pipeline\")?;` (auto_pipeline.rs:2186) — the `?` means a cleanup failure fails the whole pipeline even though the draft is already durable."
      },
      {
        "symbol": "quality_gate_requires_review / quality_gate_review_count",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:228",
        "signature": "fn quality_gate_requires_review(quality_gate_enabled: bool, quality: &Value) -> bool {\n    quality_gate_enabled && quality.get(\"state\").and_then(Value::as_str) != Some(\"ready\")\n}",
        "notes": "Since quality_gate_v2_enabled() is hardcoded true, anything other than state==\"ready\" forces JobStatus::NeedsReview. review_count returns max(issues, hardFailures, 1)."
      },
      {
        "symbol": "authoring_v2_shadow_enabled / quality_gate_v2_enabled",
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:223",
        "signature": "pub(crate) fn authoring_v2_shadow_enabled() -> bool {\n    true\n}\n\npub(crate) fn quality_gate_v2_enabled() -> bool {\n    true\n}",
        "notes": "Verbatim. There is no env var, no settings key, no runtime toggle. Any plan text implying a flag gates the V2 shadow is stale."
      }
    ],
    "dataFlow": "run_auto_pipeline (localOnly, the only in-app shape), step by step with line numbers in auto_pipeline.rs:\n\n1. 1391-1397 parse options. parse_mode default \"auto\"; confidence_threshold default 0.85 clamped; local_only = execution_mode == Some(\"localOnly\") exactly (1394); cloud_diagnostics_opted_in = !local_only && profile_id.is_some() (1395); target default \"editableDraft\" (1396); allow_overwrite default false (1397).\n2. 1399-1406 load job.json, ensure the 15 job subdirs (util.rs:223-243), then HARD GUARD: if authoring-ir.json exists and !allow_overwrite -> Err(\"editable_draft_exists; pass allowOverwrite=true before regenerating draft\").\n3. 1411-1418 profile selection (skipped entirely when localOnly), Mutex::new(llm_gateway), cloud_worker_spawned = selected_profile_id.is_some() && main_source_is_pdf, two mpsc channels created unconditionally.\n4. 1420-1435 thread::scope; the worker is spawned only if cloud_worker_spawned. In the in-app path it is not, so the channels are never used and extraction_rx.recv() is never called.\n5. 1438-1479 if document-ir.json is absent: parse_source_document into cache/parser/<job>-document-ir.json, write document-ir.json, write source-review.json, update job.json to NeedsReview|Working + WorkflowStep::DocumentReview.\n6. 1487-1518 physical V2 shadow: only when current_physical_shadow() is None AND main source file_type == \"pdf\" AND the upload exists AND document-ir.json parses -> write_pdf_facts_shadow_with_v1 into document-ir-v2.shadow.json; on error writes document-ir-v2.shadow.error.json. DOCX is silently skipped here (the manual parse_document path does handle docx, authoring_commands.rs:186-230).\n7. 1539-1544 re-read document-ir.json into `doc`; physical_shadow = current_physical_shadow (validated against schemaVersion==DocumentIRV2 + jobId + source file_id + sha256, 249-266); needs_pdf_vision_transcription computed (this internally recomputes make_dynamic_split_candidates, auto_pipeline.rs:186).\n8. 1553-1650 vision transcription — requires profile_id, so dead in-app. Diagnostic only: it never replaces DocumentIRV1 (applied is hardcoded false at 1509-equivalent, 1584-1600).\n9. 1663-1683 source_review_status -> parser warnings + low-confidence blocks; make_dynamic_split_candidates; parse_answer_source_candidates; merge_answer_source_candidates; write split-candidates.json; job -> Working/Split.\n10. 1684-1690 ir = make_dynamic_authoring_ir(job, split, doc); write authoring-ir.json; job -> DraftSaved/Authoring. This is the point after which the guard in step 2 will refuse a re-run.\n11. 1698-1832 group repair loop — gated on `!local_only && !main_source_is_pdf(&job)` (1698). So it runs only for non-PDF sources with a profile, one serial extract_group LLM call per group under the gateway mutex, auto-applying at confidence >= threshold unless llm_suggestion_auto_apply_issues or quote mismatches block it.\n12. 1834-1848 recompute answerKey / questionOrder / questionDisplayMap on the IR.\n13. 1858-1868 join the cloud worker (skipped in-app), then persist vision-answer-output.json + vision-answer-candidates.json (1276-1360) and append the vision-answer audit issue.\n14. 1922-1929 refresh_authoring_review_state; bump audit.revision, set audit.llmUsed/updatedAt.\n15. 1931-2024 cloud comparison report + cloud_comparison_summary audit issue (skipped in-app).\n16. 2026 write authoring-ir.json (final V1).\n17. 2027-2034 write_pipeline_authoring_v2_shadow -> authoring-ir-v2.shadow.json + .compare.json, atomically. UNCONDITIONAL apart from the hardcoded-true flag.\n18. 2036-2046 validate_for_runtime_gate -> report; static_runtime_passed = passed && runtime.mode == \"static-rust\".\n19. 2048-2070 v2 quality gate: build_authoring_v2_shadow is called AGAIN, purely to read .quality out of it. On Err it reports hardFailures:[\"QUALITY_GATE_EVALUATION_FAILED\"].\n20. 2072-2110 status/step/route decision. has_review_blocks ORs low-confidence groups, blocked auto-apply, parser review, cloud confirmation, remaining authoring review, and v2_quality_requires_review. next_status: NeedsReview if blocked, else DraftSaved when target==\"editableDraft\".\n21. 2119-2143 update_job with the summed issue counts.\n22. 2145-2185 build and write pipeline-report.json.\n23. 2186 minimize_process_artifacts_after_authoring — writes authoring-project.json then DELETES document-ir.json, split-candidates.json, pipeline-report.json (the file just written at 2185), validation-report.json, publish-readiness-report.json, cache/, preview/, llm-suggestions/, llm-calls.jsonl and the root parser cache entries. Gated only on config/diagnostics-settings.json keepFullProcessArtifacts, which defaults to false (diagnostics.rs:16-22).\n\nThen the workspace: getAuthoringV2 -> get_authoring_v2_core (authoring_v2_commands.rs:54) -> build_editor_session -> load_current_authoring -> revision 0 so it reads authoring-ir-v2.shadow.json, revalidates it with validate_authoring (830-837), and returns {authoring, revision:0, source:\"shadow\"}. The first apply_authoring_v2_patches goes through the same loader, then append_revision writes authoring/revisions/, after which the shadow is never read again for that job.\n\nrun_cloud_review (auto_pipeline.rs:2217-2668), which the import flow fires per PDF after the local pass:\n- 2227-2232 load job, ensure dirs, read authoring-ir.json or hard-fail.\n- 2233 select_llm_profile with the requested id or an auto-pick.\n- 2242-2322 if a profile resolved and the source is a PDF: main_pdf_vision_extraction RE-RENDERS the PDF page images (cache/vision was deleted by the pipeline's cleanup), then cloud_outline_check_for_job and vision_answer_candidate_for_job run sequentially on this one thread.\n- 2338-2420 merge-on-write: re-read authoring-ir.json and replay only the cloud_comparison_summary issue onto the fresh content so a concurrent user save is not clobbered; append_authoring_audit_issue replaces same-kind issues so the replay is idempotent (1014-1049).\n- 2427 write authoring-ir.json.\n- 2430-2456 source_doc = read_json_opt(\"document-ir.json\") — which the pipeline already DELETED, so this is None; split is then rebuilt from a None document; build_authoring_v2_shadow is called on that degraded input purely to compute quality.v2Gate. The result is NOT written to disk: run_cloud_review never calls write_pipeline_authoring_v2_shadow.\n- 2478-2665 recompute status/step from a pipeline-report.json that no longer exists (so the fallback literal at 2501-2536 is used, meaning lowConfidenceGroups/blockedAutoApplyGroups always read as 0), update_job, then write pipeline-report.json back.",
    "gapsVsPlan": [
      {
        "claim": "§17.8: \"逐步删除现有超大同步编排… 不再由一个 blocking Tauri command 等待整条链完成\"",
        "reality": "Still exactly one blocking command per chain. lib.rs:1291-1301 and 1303-1313 are single spawn_blocking calls that await the whole pipeline. There is no adapter, no processing module, no enqueue_import.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:1291-1313"
      },
      {
        "claim": "§17.8: \"本地 candidate 生成后立即持久化，不等待 cloud join 才返回\"",
        "reality": "Already true for the V1 draft: authoring-ir.json is written at 1685, well before the worker join at 1858-1868. But NOT true for the artifact the workspace actually loads: authoring-ir-v2.shadow.json is written at 2027, i.e. after the cloud join. With a cloud profile enabled, the workspace cannot open the item until the cloud calls finish.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1685 vs 1858-1868 vs 2027"
      },
      {
        "claim": "§5.5: local and cloud chains run concurrently via tokio::join!, each behind its own semaphore, with CPU work on spawn_blocking",
        "reality": "No tokio::join!, no Semaphore, no tokio::spawn anywhere in src-tauri. The only in-pipeline concurrency is std::thread::scope with a single scope.spawn (1420/1425) plus two mpsc channels. spawn_blocking appears only at command boundaries.",
        "verdict": "confirmed",
        "evidence": "grep over F:/workspace/PDF2Test/src-tauri/src/ for tokio::join|join!|Semaphore|tokio::spawn returns nothing; auto_pipeline.rs:1420,1425; lib.rs:1298,1310"
      },
      {
        "claim": "§5.5 / §26.3-D: \"现有 worker 虽然与本地链并行\" — the cloud worker does run in parallel with the local chain today",
        "reality": "Only in the CLI live path and unit tests. cloud_worker_spawned requires selected_profile_id.is_some() (1416), which requires cloud_diagnostics_opted_in = !local_only && options.profile_id.is_some() (1395). Every in-app caller sends executionMode:'localOnly' and no profileId, so profile_id is None and the thread is never spawned. lib.rs:9909 auto_pipeline_local_only_skips_all_pdf_cloud_calls asserts gateway_calls == 0 for exactly this input shape.",
        "verdict": "partly-wrong",
        "evidence": "auto_pipeline.rs:1394-1416; src/features/import/useImportFiles.ts:149; src/features/editor/ExamWorkspacePage.tsx:115,173; src/pages/ImportWizard.tsx:181; lib.rs:9909-9990; only lib.rs:585-596 (CLI) passes execution_mode:None + profile_id:Some"
      },
      {
        "claim": "§26.3-D: vision answer and outline execute sequentially under one mutable gateway lock",
        "verdict": "confirmed",
        "reality": "Exactly right, and it is worse than stated: lock_gateway is also taken by the main thread for vision transcription (1568) and for every extract_group call (1717), so all LLM traffic in a run is globally serialized. run_llm_gateway itself is a plain fn needing no exclusive state — the Mutex exists only because F is FnMut so tests can capture counters.",
        "evidence": "auto_pipeline.rs:1214-1218, 1256-1264, 1568, 1717; llm_gateway.rs:23-29"
      },
      {
        "claim": "§12.2/§12.3/§12.5: batch import writes rows first, enqueues durable jobs, reports stage-weighted progress, recovers Running jobs on startup",
        "reality": "None of the queue exists. db.rs has ingest_jobs (143-155) with status/current_step but no lease, worker_id, attempt_count, stage or enqueued_at; job_artifact_index (184-190) is declared and never written. Job state of record is the job.json file. No startup recovery. Batch fan-out and both concurrency limits live in the frontend pool (useImportFiles.ts:148,155).",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/db.rs:143-190; job_store.rs:29-57; src/features/import/useImportFiles.ts:142-162"
      },
      {
        "claim": "§12.6: frontend subscribes to a processing://item-updated Tauri event",
        "reality": "The backend emits nothing. lib.rs:88 imports only `use tauri::{AppHandle, Manager};` — no Emitter, and a grep for `emit` across src-tauri matches only source comments. The UI refreshes by calling changed.current() after each awaited command.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:88; grep -rn emit over src-tauri/src yields comment hits only"
      },
      {
        "claim": "§12.4: cancellation and retry of running jobs",
        "reality": "No cancellation token, no retry bookkeeping. The only re-run affordances are run_auto_pipeline with allowOverwrite (which destroys the draft) and run_cloud_review.",
        "verdict": "confirmed",
        "evidence": "auto_pipeline.rs:1402-1406; no cancel/token/abort symbols in auto_pipeline.rs"
      },
      {
        "claim": "Implied by §5.5 and §17.8: the V2 authoring artifact is a normal pipeline output",
        "reality": "It is produced by a helper that swallows its own failure. write_pipeline_authoring_v2_shadow returns Ok(()) after DELETING the shadow and compare files and writing an error file (auto_pipeline.rs:300-312). The pipeline then reports success while get_authoring_v2 will fail with AUTHORING_V2_NOT_AVAILABLE:shadow_missing.",
        "verdict": "confirmed",
        "evidence": "auto_pipeline.rs:275-315 (esp. 300-312); authoring_v2_commands.rs:732-737"
      },
      {
        "claim": "§17.17 cleanup: process artifacts can be minimized after authoring without losing anything the product needs",
        "reality": "cleanup deletes document-ir.json and split-candidates.json, which are the only inputs from which the V2 shadow can be regenerated. After a fresh import, run_cloud_review's build_authoring_v2_shadow (2434) runs on source_doc == None, and build_authoring_ir_core (authoring_commands.rs:403-410) would rebuild from an empty split. A test locks this deletion in.",
        "verdict": "confirmed",
        "evidence": "cleanup.rs:176-192; auto_pipeline.rs:2430-2440; authoring_commands.rs:403-410; lib.rs:10161-10175 asserts document-ir.json/split-candidates.json/pipeline-report.json must NOT persist"
      },
      {
        "claim": "Plan-implied: a feature flag gates the V2 shadow / quality gate",
        "reality": "Both are hardcoded `true` functions with no env lookup. environment.rs:223-229. quality_gate_v2_enabled() being unconditionally true also means quality.state != \"ready\" always forces JobStatus::NeedsReview.",
        "verdict": "already-done",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/environment.rs:219-229; auto_pipeline.rs:228-230, 2073-2079"
      },
      {
        "claim": "§11.1: dual sources of truth must end",
        "reality": "Still fully dual and now one-directional. update_authoring_ir_core (authoring_commands.rs:475-515) and apply_vision_answer_candidates (llm_commands.rs, no shadow write at all) modify authoring-ir.json without touching the V2 shadow. Conversely apply_authoring_v2_patches_core writes only the V2 revision chain and never authoring-ir.json (\"The legacy authoring-ir.json file is never rewritten here\", authoring_v2_commands.rs:7).",
        "verdict": "confirmed",
        "evidence": "authoring_commands.rs:475-515; authoring_v2_commands.rs:1-7,123-168; grep shows no shadow write in llm_commands.rs"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "write_pipeline_authoring_v2_shadow (275-315), Err arm 300-312",
        "whatToChange": "Stop swallowing the failure. Today the Err arm deletes authoring-ir-v2.shadow.json AND authoring-ir-v2.shadow.compare.json and returns Ok(()), so the pipeline reports success while the workspace has nothing to open. Either propagate the error so the job goes to a Failed state with a user-visible reason, or write a minimal-but-valid V2 skeleton (exam + empty taskGroups + empty answerSlots + a blocked QualityReportV2 carrying the schema error as a hardFailure) so get_authoring_v2 always has something loadable. Also surface the error file's content in the pipeline report — right now the only breadcrumb is quality.v2Gate.hardFailures == [\"QUALITY_GATE_EVALUATION_FAILED\"] produced by an independent second build at 2050.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "run_auto_pipeline_core_with_gateway, overwrite guard 1402-1406",
        "whatToChange": "The guard keys on authoring-ir.json existing, which is unrelated to whether the V2 artifact is usable. Add a repair path that regenerates the shadow from the existing authoring-ir.json without requiring allowOverwrite (a new command, e.g. rebuild_authoring_v2_shadow, is cleaner than relaxing the guard). Without this there is NO way to recover a job whose shadow failed to build: this guard and the identical protect_existing_authoring (authoring_commands.rs:357-362) are the only two doors, and both refuse.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "run_auto_pipeline_core_with_gateway, 2027-2034 and 2048-2070",
        "whatToChange": "build_authoring_v2_shadow is executed twice per run: once inside write_pipeline_authoring_v2_shadow and again to read .quality for the report. Each call runs quality::evaluate_quality, which compiles ReadingExamSourceV2 and the V1 compatibility source and round-trips both schemas (quality.rs:778-821). Capture the Value returned by write_authoring_v2_shadow (it already returns CommandResult<Value>) and read quality out of it. A third build happens at 207 via has_reliable_question_groups when vision transcription runs.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "run_cloud_review_core_with_gateway, 2430-2456",
        "whatToChange": "This computes a V2 quality gate from build_authoring_v2_shadow but never persists the shadow, and it feeds that build source_doc = read_json_opt(\"document-ir.json\") which the auto pipeline already deleted, plus a split rebuilt from that None. So the v2Gate reported by cloud review is computed from degraded input and the on-disk shadow is left stale relative to the cloud-updated authoring-ir.json. Either call write_pipeline_authoring_v2_shadow here too (with real inputs), or stop reporting a v2Gate from this command.",
        "risk": "high",
        "blockedBy": "Requires document-ir.json and split-candidates.json to survive cleanup, i.e. the cleanup change below, which is locked by a test at lib.rs:10161-10175."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "minimize_process_artifacts_after_authoring (158-208), removal list 176-192",
        "whatToChange": "Keep document-ir.json and split-candidates.json (or relocate them under extraction/ and keep them) — they are the sole regeneration inputs for both the V2 shadow and the V1 draft. Also stop deleting pipeline-report.json, which auto_pipeline.rs:2185 writes one line before this call and run_cloud_review then tries to read at 2501. Note the call site uses `let _ = ...?`, so any IO failure in cleanup fails a pipeline whose draft is already durable.",
        "risk": "medium",
        "blockedBy": "lib.rs:10161-10175 asserts these files must NOT persist (test is #[ignore]d), and lib.rs:4438 auto_pipeline_retains_process_artifacts_only_when_diagnostics_enabled is active and will need updating."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "physical shadow block 1487-1518",
        "whatToChange": "The filter is `main_source_file(&job).filter(|source| source.file_type == \"pdf\")`, so a DOCX import through the auto pipeline gets no DocumentIRV2 at all, hence physical_shadow == None into build_authoring_v2_shadow, hence coverageStatus.physicalShadow == \"missing\" and a permanently non-ready quality state. The manual parse_document path already handles both (authoring_commands.rs:186-230 dispatches to write_docx_facts_shadow_with_v1). Mirror that dispatch here.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "lock_gateway (1214-1218) + Mutex::new(llm_gateway) (1415) + call sites 1256, 1260, 1568, 1717",
        "whatToChange": "This is plan 26.3-D. Replace the `F: FnMut` + Mutex abstraction with a Send+Sync transport trait (an &dyn LlmTransport or a cloneable client), so vision answer, outline and per-group repair can be in flight simultaneously, with a cloud semaphore rather than a mutex providing the concurrency limit. Every generic signature in this file threads that same FnMut bound, so this is a wide but mechanical change: run_auto_pipeline_core_with_gateway, run_cloud_review_core_with_gateway, run_cloud_conversion_worker, vision_transcription_for_job_with_gateway, vision_transcription_with_extraction, vision_answer_candidate_for_job, cloud_outline_check_for_job, cloud_outline_generate_with_gateway.",
        "risk": "high",
        "blockedBy": "~14 tests in lib.rs pass closures that mutate captured counters (e.g. lib.rs:9974 `gateway_calls += 1`); they must move to interior mutability (AtomicUsize / Mutex<Vec<_>>) before the FnMut bound can be dropped."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "run_auto_pipeline (1291-1301) and run_cloud_review (1303-1313)",
        "whatToChange": "This is the durable-queue seam. Both cores are already pure (root, job_id, input) -> CommandResult<Value> with no AppHandle, so a queue worker can call them unchanged. Turn each command into an enqueue that returns a job/queue id immediately, and have a Rust-side worker pool drive the cores. Keep the direct-call form behind a flag until the queue is trusted — the CLI (lib.rs:585) and every test call the cores directly.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/db.rs",
        "symbol": "ingest_jobs DDL (143-155), job_artifact_index DDL (184-190)",
        "whatToChange": "ingest_jobs has status + current_step but nothing a queue needs: no worker_id, lease_expires_at, attempt_count, stage, stage_progress, enqueued_at, claimed_at, last_error. Add them (or a separate processing_jobs table) plus a claim_job UPDATE ... WHERE lease expired. job_artifact_index is declared and never written by any code — decide whether it becomes the artifact registry or gets dropped. Connection strategy is transient-connection + WAL + busy_timeout=5000 (db.rs:55-65), adequate for a small worker pool.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs",
        "symbol": "update_job (46-56) and save_job (33-44)",
        "whatToChange": "update_job is an unlocked read-modify-write of job.json via the non-atomic write_json (util.rs:259-265, plain fs::write). With a Rust worker pool (or today's two-wide frontend pool plus a user save) two writers can lose each other's updates, and a reader can observe a truncated file — read_json_opt returns Err(\"parse_json:...\"), not None, and run_cloud_review's `?` at 2340 turns that into a whole-command failure. Move job state into the DB as the record of truth, or at minimum use the existing write_canonical_json_atomic (artifact_store.rs:504-540) plus an fs2 file lock like append_revision already does.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "main_pdf_vision_extraction (362-371)",
        "whatToChange": "Renders PDF page images into <job_dir>/cache/vision/, which the pipeline's own cleanup then deletes (cleanup.rs:174-175 removes cache/). The very next thing the import flow does is run_cloud_review, which re-renders from scratch (2243). Move the render output under extraction/ (already created by ensure_job_dirs) and key it by source sha256 so it is a reusable stage output — this is also the \"render_or_extract 20%\" stage the plan's progress model needs.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "AutoPipelineInput (286-298)",
        "whatToChange": "execution_mode and target are Option<String> compared by string literal, with no validation: only the exact string \"localOnly\" is recognized (auto_pipeline.rs:1394) and any other value — \"local_only\", \"LocalOnly\", \"full\", a typo — silently means \"cloud allowed\". target is read at only two places (2080, 2090), never gates an artifact, and \"editableDraft\" is both the default and the only value any of the 4 callers ever sends. Make execution_mode a #[serde] enum and delete target, or keep it and give it a second real value.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx",
        "symbol": "load-error recovery button, line 173",
        "whatToChange": "`runAutoPipeline(itemId, { executionMode: \"localOnly\", target: \"editableDraft\" })` omits allowOverwrite. This button only appears when get_authoring_v2 failed, which in practice means authoring-ir.json exists but the shadow does not — precisely the case where the 1402 guard returns \"editable_draft_exists\". The button can therefore never succeed. Either pass allowOverwrite:true (matching line 115) or point it at a new shadow-repair command.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "load_current_authoring (725-742)",
        "whatToChange": "Once revision > 0 the shadow is never consulted again, so re-running local recognition (ExamWorkspacePage.tsx:115, allowOverwrite:true) rewrites authoring-ir-v2.shadow.json while the editor keeps serving the old revision — the rerun appears to do nothing. Decide the contract: either a rerun opens a new revision seeded from the fresh shadow, or the UI must state that a rerun cannot apply after the first save. Also note the error string AUTHORING_V2_NOT_AVAILABLE:shadow_missing:<absolute path> leaks a filesystem path straight into the UI (ExamWorkspacePage.tsx:170).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "symbol": "update_authoring_ir_core (475-515); also llm_commands.rs apply_vision_answer_candidates",
        "whatToChange": "Both write authoring-ir.json without refreshing the V2 shadow, so a legacy edit or an accepted vision answer candidate silently diverges from what the new workspace loads. vision-answer-candidates.json is read only by the legacy UnifiedPreview.tsx:977, job_commands.rs:112 and llm_commands.rs:459 — the new features/editor never sees it, so cloud answer candidates currently cannot reach the converged workspace at all.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "symbol": "evaluate_quality tail, 226-227",
        "whatToChange": "`\"evaluatedAt\": Utc::now().to_rfc3339()` makes the shadow non-deterministic byte-for-byte across runs even for identical input, despite being written through write_canonical_json_atomic. A durable queue that wants idempotent retries, content hashing, or \"did this rerun change anything\" comparison needs an injected timestamp or an exclusion from the hash.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs",
        "symbol": "inspect_job_artifacts (384-405)",
        "whatToChange": "get_job_artifact_status reports current revision, revision records and directory paths but says nothing about whether authoring-ir-v2.shadow.json exists or whether authoring-ir-v2.shadow.error.json is present. The UI has no way to distinguish \"shadow never built\" from other load failures except by string-matching the error. Add shadow presence + last shadow error to this payload; get_job_detail (job_commands.rs:99-114) also omits the V2 shadow entirely.",
        "risk": "low"
      }
    ],
    "testHarness": "Where the tests live. auto_pipeline.rs has its own `mod tests` at 2670-2796 but it only covers four pure helpers: v2_quality_gate_off_preserves_legacy_reliability, v2_quality_gate_on_requires_ready_report_and_slots, v2_quality_review_count_never_hides_a_non_ready_state, physical_shadow_freshness_requires_schema_job_source_and_hash. Neither core function is tested from this file. Everything end-to-end lives in lib.rs's #[cfg(test)] mod (starts ~1500).\n\nThe pattern to copy (verbatim shape from lib.rs:4320-4327):\n    let root = temp_test_root();                       // lib.rs:1576, env::temp_dir()/epic8-test-<uuid>\n    ensure_app_dirs(&root).unwrap();\n    let mut job = test_job();                          // lib.rs:1540, make_job(CreateJobInput{..})\n    attach_fixture_source(&root, &mut job, \"complex-reading.txt\", \"MainQuestion\");  // lib.rs:1829\n    save_job(&root, &job).unwrap();\n    let report = run_auto_pipeline_core(&root, &job.job_id, None).unwrap();\nFixtures resolve through parser_fixture (lib.rs:1580) = <CARGO_MANIFEST_DIR>/../fixtures/parser/<name>; useful names in existing tests: complex-reading.txt, no-text.pdf. The 8-PDF golden corpus is reached through phase4_pdf_fixtures (lib.rs:1588) reading fixtures/golden/phase4-eight-pdf-acceptance.json and fixtures/golden/manifest.json.\n\nTo exercise the cloud path, use run_auto_pipeline_core_with_gateway with a closure as the 4th argument and pass AutoPipelineInput { execution_mode: None, profile_id: Some(id), .. } — profile_id alone is not enough only in the sense that execution_mode must not be \"localOnly\". You must first write an enabled profile with crate::llm_profiles::save_profiles(&root, &[json!({...,\"enabled\":true})]). Closest existing models: lib.rs:9909 auto_pipeline_local_only_skips_all_pdf_cloud_calls (asserts gateway_calls == 0), lib.rs:6221 cloud_outline_mismatch_is_persisted_as_authoring_audit_summary, lib.rs:10754 cloud_review_write_back_preserves_concurrent_user_edit (uses a gateway closure that writes authoring-ir.json mid-call to prove the merge-on-write at 2338-2420). Note every one of these closures mutates a captured local, which is why the F: FnMut + Mutex bound exists.\n\nTo keep intermediates for assertions, write diagnostics settings first — write_diagnostics_settings(&root, &DiagnosticsSettings { keep_full_process_artifacts: true }) (used at lib.rs:4443, 8090, 9679, 9915) — otherwise minimize_process_artifacts_after_authoring deletes document-ir.json, split-candidates.json and pipeline-report.json before your assertions run.\n\nThe gap this subsystem most needs closed: there is NO test anywhere that runs run_auto_pipeline_core and then get_authoring_v2_core (or asserts authoring-ir-v2.shadow.json exists). grep for authoring-ir-v2.shadow.json across src-tauri/src outside ielts_grammar returns only the two constant declarations and nas_package_v2/authoring_v2_commands consumers. The real-PDF pipeline test that would have caught it, lib.rs:10060 files_pdf_samples_auto_pipeline_minimizes_artifacts_and_preserves_review_gate, is #[ignore]d (\"superseded by the registered Phase 4 eight-PDF corpus; requires removed legacy Files/*.pdf inputs\"), and the active 8-PDF suite (ielts_grammar/real_pdf_acceptance.rs:1282 process_fixture, build at 1369) calls build_authoring_v2_shadow directly with a freshly computed physical shadow — it never goes through run_auto_pipeline, never through write_pipeline_authoring_v2_shadow, and never through load_current_authoring. Add a test that does exactly: attach a golden PDF, run_auto_pipeline_core, assert job_dir/authoring-ir-v2.shadow.json exists, then get_authoring_v2_core(&root, &job_id).unwrap() and assert source == \"shadow\" and revision == 0.\n\nI did not run cargo build or cargo test (instructed not to), so every claim here is from reading source; I have not observed a live failure of the schema validation at ielts_grammar/mod.rs:377.",
    "surprises": [
      "CRITICAL ANSWER — a fresh import does attempt the V2 artifact unconditionally, but nothing guarantees it lands, and the failure is silent. write_pipeline_authoring_v2_shadow is called at auto_pipeline.rs:2027 on every successful run with no executionMode / target / profile / source-type gate. The only flag is authoring_v2_shadow_enabled(), which is `pub(crate) fn authoring_v2_shadow_enabled() -> bool { true }` (environment.rs:223-225) — hardcoded, no env var. The single real gate is content: build_authoring_v2_shadow's final `serde_json::from_value::<IeltsAuthoringIRV2>(authoring.clone()).map_err(|error| format!(\"authoring_ir_v2_shadow_schema_validation_failed:{error}\"))?` at ielts_grammar/mod.rs:377-378 (IeltsAuthoringIRV2 and every nested struct are #[serde(deny_unknown_fields)] with all fields required). If that fails, auto_pipeline.rs:301-302 DELETES authoring-ir-v2.shadow.json and .compare.json, writes authoring-ir-v2.shadow.error.json, and returns Ok(()) — the pipeline reports success and the workspace then fails with AUTHORING_V2_NOT_AVAILABLE:shadow_missing:<path> from authoring_v2_commands.rs:732-737. So: gated by a schema-validation outcome, not a flag; not covered by any test at that seam; and unrecoverable, because both regeneration doors (auto_pipeline.rs:1402 and authoring_commands.rs:357) refuse while authoring-ir.json exists.",
      "The in-app product path never makes a cloud call from run_auto_pipeline and never spawns the worker thread. cloud_diagnostics_opted_in = !local_only && options.profile_id.is_some() (1395); all four callers pass executionMode:'localOnly' and none passes profileId. The scoped thread, the Mutex<F>, both mpsc channels and the entire vision-transcription branch are dead code in production. The only caller that activates them is the CLI live-API path (lib.rs:585-596). Cloud work reaches the product exclusively through the separate, entirely single-threaded run_cloud_review.",
      "Note the asymmetry: run_auto_pipeline requires an explicitly passed profileId before it will even consider cloud (1395), while run_cloud_review passes options.profile_id straight into select_llm_profile (2233), which falls back to job.active_llm_profile_id and then auto-picks any enabled profile (115-168). So ExamWorkspacePage.tsx:120's runCloudReview(itemId) with no arguments can fire real LLM calls against an auto-selected profile.",
      "run_auto_pipeline writes pipeline-report.json at 2185 and then deletes it one line later at 2186 via minimize_process_artifacts_after_authoring (cleanup.rs:178). The Value is still returned to the caller so nothing visibly breaks, but run_cloud_review's read_json_opt of that same file at 2500 always misses, silently falling back to a literal whose lowConfidenceGroups/blockedAutoApplyGroups are empty arrays — so cloud review's has_review_blocks can never see LLM review debt the local pass recorded.",
      "target:'editableDraft' is effectively inert. It is read at exactly two places — 2080 (next_status becomes DraftSaved rather than ExportReady) and 2090 (next_step forced to WorkflowStep::Authoring) — never gates an artifact, is the default at 1396, and is the only value any caller sends (the TS type is literally `target?: \"editableDraft\"`, tauriCommands.ts:250). Similarly executionMode is matched against the bare string \"localOnly\" with no validation, so a casing typo would silently enable cloud.",
      "The V2 shadow's task groups derive from split.questionGroupCandidates, not from the LLM-repaired IR. In run_auto_pipeline the LLM group-repair loop (1698-1832) mutates `ir` but never `split`, and `split` is what is handed to write_pipeline_authoring_v2_shadow at 2030. LLM-applied kind/layout/question changes reach the shadow only indirectly through find_v1_group lookups against v1_authoring.",
      "Once the user saves once, the shadow is dead. load_current_authoring returns the revision whenever revision > 0 (authoring_v2_commands.rs:727-731), and auto_pipeline.rs imports nothing from artifact_store — it never touches the revision chain. So 'rerun local recognition' rewrites the shadow and the editor keeps showing the old revision, with no warning.",
      "The V1 and V2 write paths use different durability. The shadow goes through write_canonical_json_atomic -> write_bytes_atomic (temp file, create_new, fsync, replace; artifact_store.rs:504-540). authoring-ir.json and every other V1 artifact go through util.rs:259-265 write_json, a plain non-atomic fs::write. Since read_json_opt returns Err on a parse failure rather than None (util.rs:252-257), a torn read of authoring-ir.json propagates through the `?` at auto_pipeline.rs:2340 and fails the entire cloud review.",
      "run_cloud_review re-renders every PDF page image. The pipeline's cleanup deletes <job_dir>/cache (cleanup.rs:174-175) including cache/vision/pdf-images.json and its assets, and run_cloud_review's first act for a PDF is main_pdf_vision_extraction (2243) which rebuilds them. In the import flow the two commands run back to back per file, so every scanned PDF is rendered twice.",
      "There is no event channel at all. lib.rs:88 is `use tauri::{AppHandle, Manager};` — no Emitter import, and grep for `emit` over src-tauri/src matches only prose in comments. Plan §5.4 and §12.6's processing://item-updated does not exist in any form, so 'progress' today is whatever the frontend infers between awaited commands.",
      "Both quality_gate_v2_enabled() and authoring_v2_shadow_enabled() being hardcoded true has a product consequence beyond the shadow: quality_gate_requires_review returns true for any state other than \"ready\" (228-230), and evaluate_quality only returns \"ready\" when documentScore >= 0.95, no task score < 0.92, sourceCoverage >= 0.995 and issues is empty (quality.rs:186-199). So essentially every real import lands in JobStatus::NeedsReview.",
      "A DOCX import through run_auto_pipeline gets no physical evidence at all: the physical-shadow block filters file_type == \"pdf\" (1488-1490), so physical_shadow is None, coverageStatus.physicalShadow is \"missing\", and the quality gate can never reach \"ready\". The manual parse_document command does handle docx (authoring_commands.rs:186-230). The two entry points into the same artifact disagree.",
      "ingest_jobs and job_artifact_index exist in db.rs (143-190) but are not a queue and, for job_artifact_index, are never written by any code. The real state of record is the job.json file, dual-written to SQLite on a best-effort basis by save_job (job_store.rs:33-44) which only eprintln!s on DB failure."
    ]
  },
  {
    "subsystem": "Runtime compile + NAS publish: src-tauri/src/reading_source_v2.rs (IeltsAuthoringIRV2 -> ReadingExamSourceV2 compiler + semantic validator) and src-tauri/src/nas_package_v2.rs (publish_nas_package_v2 lock/CAS/staging/probe/manifest-last/atomic-commit/journal-recovery)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "role": "The only runtime compiler. Types RuntimeExamMetaV2/RuntimeAssetManifestRefV2/RuntimePassageV2/RuntimeAuditV2/ReadingExamSourceV2/CompilerIssueV2/ScoreResultV2, compile_reading_source_v2 (L79), validate_reading_source_v2 (L135), validate_response_group (L278), score_response_group (L482). No I/O, no app root, pure function.",
        "sizeNote": "1056 lines; L1-610 production, L611-1056 tests (~42%)"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "role": "Single-item NAS V2 publisher. validate_v2_export_binding (L56), publish_nas_package_v2_core (L245), stage_and_commit (L352), commit_package (L582), make_paths (L857), recover_incomplete_transactions (L1257), write_journal (L1385) plus lock/CAS/backup/atomic-replace helpers.",
        "sizeNote": "2039 lines; L1-1422 production, L1424-2039 tests (~30%)"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs",
        "role": "Probe + asset path policy consumed by the publisher: ExamAssetManifestV2, StudentProbeReportV2, run_student_loader_probe (L48), safe_join_asset_path (L141), verify_asset_descriptor (L182), referenced_asset_ids (L271). Carries #![allow(dead_code)] at L1.",
        "sizeNote": "462 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/runtime_compiler.rs",
        "role": "ExamCompiler trait + ReadingExamCompilerV2 wrapper over compile/validate. Pure indirection, no other impl, no callers outside its own test.",
        "sizeNote": "34 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "Upstream publish gate and export materializer. validate_authoring_v2_publish_readiness (L174) is the source of every authoring_v2_export_blocked:* string and is re-run INSIDE the NAS publisher; export_authoring_v2_core (L449) writes the export bundle the publisher consumes; collect_publish_gate_markers (L333) is the 'scan all JSON for fallback/partial' scanner plan 13.3 wants removed.",
        "sizeNote": "97587 bytes; the two relevant fns span L174-615"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/export_artifacts.rs",
        "role": "build_wrapper (L33) produces the {examId}.js registry wrapper the publisher stages; safe_exam_id (L24) is the exam_id path-segment gate.",
        "sizeNote": "~150 lines production"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/export_nas_library.rs",
        "role": "Path helpers the publisher reuses: normalize_nas_library_root (L145) strips a trailing publish/ component, nas_reading_exams_dir (L174) returns the normalized root ITSELF (not a subdirectory).",
        "sizeNote": "1500+ lines, only L145-180 relevant"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "role": "Command surface: async fn publish_nas_package_v2 at L1209-1212, registered in generate_handler at L1460. type CommandResult<T> = Result<T, String> at L90.",
        "sizeNote": "449181 bytes; only L1209 and L1460 matter here"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "Where the business-completeness checks plan 17.20 wants in the compiler live TODAY, untyped over serde_json::Value: option_has_renderable_content (L2781), group_has_ambiguous_prompt (L2770), is_prompt_placeholder (L2798), validate_visual_task (L2324), validate_completion_host (L1888), collect_asset_references (L2729). evaluate_compiler_probes (L778) already calls compile_reading_source_v2 and folds issue codes into quality.compilerProbes.v2Runtime.",
        "sizeNote": "4468 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src/api/publishClient.ts",
        "role": "The ONLY new-frontend publish path. describePublishError (L29) does the string parsing; publishItem (L46) chains exportAuthoringV2 -> exportNasPackageV2; publishItems (L71) is the serial 'batch'. Its header comment already admits the 13.6 and 13.4 gaps.",
        "sizeNote": "85 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src/pages/ExportPage.tsx",
        "role": "Legacy publish page that parses authoring_v2_export_blocked with a regex (L60) and authoring_v2_export_compile_blocked (L63). Still referenced by scripts/verify-phase6-runtime.mjs, so retiring it breaks that gate.",
        "sizeNote": "~600 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src/types/reading-source.ts",
        "role": "ExportNasPackageV2Input (L141) and NasPackageV2PublishResult (L151). The declared probe shape ({passed, checkedAssetIds, referencedAssetIds, errors, warnings}) does not match the Rust StudentProbeReportV2."
      },
      {
        "path": "F:/workspace/PDF2Test/contracts/reading-exam-source-v2.schema.json",
        "role": "JSON-Schema mirror of ReadingExamSourceV2 with additionalProperties:false and questionOrder minItems:1. Its sha256 is pinned in contracts/contract-manifest.json under schemas.ReadingExamSourceV2 and asserted by scripts/verify-phase6-runtime.mjs L103."
      },
      {
        "path": "F:/workspace/PDF2Test/scripts/verify-phase6-runtime.mjs",
        "role": "The CI gate for this subsystem. Requires the files at L86-97 to exist (including src/pages/ExportPage.tsx and src/pages/StructuredAuthoringEditorV2.tsx), asserts token presence in lib.rs/tauriCommands.ts/ExportPage.tsx, and runs `cargo test --lib reading_runtime_v2` and `cargo test --lib nas_package_v2` (L257-258).",
        "sizeNote": "268 lines"
      }
    ],
    "publicApi": [
      {
        "symbol": "compile_reading_source_v2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "signature": "pub(crate) fn compile_reading_source_v2(\n    source: &IeltsAuthoringIRV2,\n) -> Result<ReadingExamSourceV2, Vec<CompilerIssueV2>>",
        "notes": "Total function, no I/O. Returns Err only from validate_reading_source_v2 on the COMPILED output, except the one early return at L80-86 (RUNTIME_PASSAGE_MISSING, target_id = source.job_id) when source.passage is None. Ordering: answer_slots sorted by (question_number, slot_id) -> question_order + question_display_map (L86-96). Drops source.modality and source.listening silently and never checks source.schema_version == \"IeltsAuthoringIRV2\"."
      },
      {
        "symbol": "validate_reading_source_v2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "signature": "pub(crate) fn validate_reading_source_v2(source: &ReadingExamSourceV2) -> Vec<CompilerIssueV2>",
        "notes": "Emits 24 distinct codes. Empty Vec == valid. Called from three places: compile (L124), publish_nas_package_v2_core L270, and run_student_loader_probe L56."
      },
      {
        "symbol": "CompilerIssueV2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "signature": "#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]\n#[serde(rename_all = \"camelCase\", deny_unknown_fields)]\npub(crate) struct CompilerIssueV2 {\n    pub code: String,\n    pub message: String,\n    pub target_id: String,\n}",
        "notes": "This is the only typed issue the compiler produces. No severity, no fix action, no source anchor - so plan 13.4's PublishBlocker { code, target_id, user_message, action } needs a new code->action map plus English->Chinese message mapping (all 24 messages are English)."
      },
      {
        "symbol": "ReadingExamSourceV2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "signature": "#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\n#[serde(rename_all = \"camelCase\", deny_unknown_fields)]\npub(crate) struct ReadingExamSourceV2 {\n    pub schema_version: String,\n    pub exam_id: String,\n    pub meta: RuntimeExamMetaV2,\n    pub assets: RuntimeAssetManifestRefV2,\n    pub passage: RuntimePassageV2,\n    pub task_groups: Vec<TaskGroupV2>,\n    pub answer_slots: BTreeMap<String, AnswerSlotV2>,\n    pub answer_key: BTreeMap<String, AnswerValueV2>,\n    pub question_order: Vec<String>,\n    pub question_display_map: BTreeMap<String, String>,\n    pub audit: RuntimeAuditV2,\n}",
        "notes": "deny_unknown_fields + a hash-pinned JSON schema with additionalProperties:false. Adding any field is a cross-repo contract break; compile_with_report() must therefore return issues ALONGSIDE the struct, never inside it."
      },
      {
        "symbol": "score_response_group",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "signature": "pub(crate) fn score_response_group(\n    group: &ResponseGroupV2,\n    answers: &BTreeMap<String, AnswerValueV2>,\n    answer_key: &BTreeMap<String, AnswerValueV2>,\n) -> ScoreResultV2",
        "notes": "Referenced only from the test module in this same file (L701, L732, L773). No production caller anywhere in the crate - it is a scoring oracle for the JS student runtime, not shipped logic."
      },
      {
        "symbol": "ExamCompiler / ReadingExamCompilerV2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/runtime_compiler.rs",
        "signature": "pub(crate) trait ExamCompiler {\n    type Output;\n\n    fn compile(&self, source: &IeltsAuthoringIRV2) -> Result<Self::Output, Vec<CompilerIssueV2>>;\n    fn validate(&self, compiled: &Self::Output) -> Vec<CompilerIssueV2>;\n}",
        "notes": "ReadingExamCompilerV2 is the only impl and has no caller besides its own #[test] at L30-33. If compile_with_report() is added, this trait must gain a third method or be deleted."
      },
      {
        "symbol": "publish_nas_package_v2_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "pub(crate) fn publish_nas_package_v2_core(root: &Path, input: Value) -> CommandResult<Value>",
        "notes": "Synchronous. `root` is the APP data dir (app.path().app_data_dir(), lib.rs L357-359) and is used only to locate the job's persisted authoring revision for the binding check - it is NOT the NAS root. Returns Ok(json!({schemaVersion, status:\"committed\", examId, manifestPath, reportPath, probe, assetCount, exportId})) at L546-554 of stage_and_commit."
      },
      {
        "symbol": "NasPackagePublishInput",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "#[derive(Debug, Deserialize)]\n#[serde(rename_all = \"camelCase\")]\npub(crate) struct NasPackagePublishInput {\n    #[serde(alias = \"exportDir\")]\n    pub library_root: String,\n    #[serde(alias = \"runtimePath\", alias = \"sourceFile\")]\n    pub source_path: String,\n    pub asset_root: Option<String>,\n    pub exam_id: Option<String>,\n    pub minimum_runtime_version: Option<String>,\n    pub expected_manifest_sha256: Option<String>,\n    pub fault: Option<String>,\n    pub job_id: Option<String>,\n    pub revision: Option<u64>,\n}",
        "notes": "One item per call - no batch shape exists. `fault` is a live fault-injection hook on the real Tauri command, not #[cfg(test)]-gated, and is exposed in the TS input type. Accepted values: after_assets, after_source, before_manifest, report_write, committed_journal, backup_resources, manifest_rename."
      },
      {
        "symbol": "validate_v2_export_binding",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "fn validate_v2_export_binding(\n    root: &Path,\n    input: &NasPackagePublishInput,\n    source_path: &Path,\n    source_bytes: &[u8],\n) -> CommandResult<()>",
        "notes": "Hard-requires source_path.file_name() == \"reading-source-v2.json\" (L62) and siblings manifest-v2.json + authoring-ir-v2.json. Re-compiles the bound authoring IR and demands exported_source == bound_source (L196-200), then re-runs validate_authoring_v2_publish_readiness and demands manifest[\"publishProof\"] == recomputed proof by full Value equality (L202-205). This is the wall a batch publisher must get through for every item."
      },
      {
        "symbol": "stage_and_commit",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "fn stage_and_commit(\n    _app_root: &Path,\n    input: &NasPackagePublishInput,\n    source: &ReadingExamSourceV2,\n    source_value: &Value,\n    source_path: &Path,\n    paths: &PackagePaths,\n    export_id: &str,\n) -> CommandResult<Value>",
        "notes": "Single-exam by construction: one staging_exam_path, one staging_resource_path, one manifest merge, one journal entry, one report. Order is asset copy -> asset-manifest.json -> wrapper .js -> heartbeat -> probe -> CAS -> manifest merge -> staged manifest.js -> journal \"committing\" -> commit_package -> report -> journal \"committed\" -> cleanup."
      },
      {
        "symbol": "commit_package",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "fn commit_package(\n    paths: &PackagePaths,\n    source: &ReadingExamSourceV2,\n    probe: &StudentProbeReportV2,\n    runtime_sha256: &str,\n    export_id: &str,\n    fault: Option<&str>,\n) -> CommandResult<PackageReceipt>",
        "notes": "Captures had_exam/had_resources/had_manifest/had_report as four scalar bools into backup_root/state.json (L613-622) and closes over them in the `rollback` closure (L710-796). A batch needs those four bools PER exam, so this signature and the state file schema (NasBackupStateV1) both have to change."
      },
      {
        "symbol": "PackagePaths",
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "signature": "#[derive(Debug, Clone)]\nstruct PackagePaths {\n    library_root: PathBuf,\n    reading_root: PathBuf,\n    manifest_path: PathBuf,\n    exam_path: PathBuf,\n    resource_path: PathBuf,\n    staging_root: PathBuf,\n    staging_exam_path: PathBuf,\n    staging_resource_path: PathBuf,\n    staging_manifest_path: PathBuf,\n    backup_root: PathBuf,\n    journal_path: PathBuf,\n    report_path: PathBuf,\n    lock_path: PathBuf,\n    lock_metadata_path: PathBuf,\n    base_manifest_sha256: String,\n}",
        "notes": "lock_path/lock_metadata_path/manifest_path/library_root/reading_root are per-LIBRARY (shared, batch-friendly). exam_path/resource_path/staging_exam_path/staging_resource_path/backup_root/journal_path/report_path are per-EXAM (must become per-item collections)."
      },
      {
        "symbol": "run_student_loader_probe",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs",
        "signature": "pub(crate) fn run_student_loader_probe(\n    source: &ReadingExamSourceV2,\n    manifest: &ExamAssetManifestV2,\n    resource_root: &Path,\n) -> StudentProbeReportV2",
        "notes": "Never returns Err; failure is probe.passed == false + probe.issues. Runs against the STAGED resource dir before commit. Re-runs validate_reading_source_v2 (L56) and verifies every asset's sha256/byteLength/mime on disk."
      },
      {
        "symbol": "StudentProbeReportV2",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs",
        "signature": "#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\n#[serde(rename_all = \"camelCase\", deny_unknown_fields)]\npub(crate) struct StudentProbeReportV2 {\n    pub schema_version: String,\n    pub exam_id: String,\n    pub passed: bool,\n    pub checked_asset_ids: Vec<String>,\n    pub referenced_asset_ids: Vec<String>,\n    pub issues: Vec<RuntimeAssetIssueV2>,\n}",
        "notes": "Serialized verbatim into the publish result and the report file. Plan 17.21 (\"do not return hashes or internal path lists\") requires this to stop being echoed to the UI - the UI at src/pages/ExportPage.tsx L512 only reads probe.passed today."
      },
      {
        "symbol": "safe_join_asset_path",
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs",
        "signature": "pub(crate) fn safe_join_asset_path(root: &Path, relative_path: &str) -> Result<PathBuf, String>",
        "notes": "Calls fs::canonicalize on BOTH root and candidate, so the asset must already exist on disk; returns Err(\"asset_path_canonicalize:...\") for a missing file. Used both to read source assets (nas_package_v2.rs L381) and to verify staged ones."
      },
      {
        "symbol": "validate_authoring_v2_publish_readiness",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "signature": "pub(crate) fn validate_authoring_v2_publish_readiness(\n    root: &Path,\n    job_id: &str,\n    revision: u64,\n    authoring_value: &Value,\n) -> CommandResult<Value>",
        "notes": "Ok => json!({schemaVersion:\"AuthoringV2PublishProofV1\", jobId, revision, qualityState:\"ready\", humanVerified:true, sourceReview, unresolvedAnswers:[], unresolvedBlockingIssues:[], aiFallbacks:[], partialFailures:[]}). Err => every authoring_v2_export_blocked:* string. Deterministic (no timestamps generated at call time), which is what makes the export-time vs publish-time proof equality comparison work at all."
      }
    ],
    "dataFlow": "END-TO-END, as actually wired today (src/api/publishClient.ts publishItem, L46-68):\n\n1. `getAuthoringV2(itemId)` -> read session, take `session.revision`.\n2. `exportAuthoringV2({ jobId: itemId, exportDir: destination, revision })` -> `export_authoring_v2_core` (authoring_v2_commands.rs L449). It takes a per-job flock at `<job>/export-history/phase5-export.lock`, loads the current revision (revision 0 = the `authoring-ir-v2.shadow.json` shadow, >0 = `authoring/revisions/{n}.json`), calls `refresh_quality_report` which REPLACES `authoring.quality` in memory (not persisted), rejects non-`ready` quality, computes `publish_proof = validate_authoring_v2_publish_readiness(...)`, then `compile_reading_source_v2(&authoring)`. It stages `authoring-ir-v2.json` + `reading-source-v2.json` + `manifest-v2.json` (the AuthoringV2ExportReceiptV1 carrying `authoringSha256`, `runtimeSha256`, and the embedded `publishProof`) plus every asset copied at its job-relative path, then `fs::rename(staging_dir, output_dir)` where output_dir = `<exportDir>/{safeExamId}-r{revision}-{uuid}`.\n3. `exportNasPackageV2({ libraryRoot: destination, sourcePath: receipt.runtimePath, assetRoot: receipt.outputDir, examId, minimumRuntimeVersion: \"0.2.0\" })` -> `publish_nas_package_v2` (lib.rs L1209) -> `publish_nas_package_v2_core`.\n\nINSIDE publish_nas_package_v2_core (nas_package_v2.rs L245-350), in order:\n- `normalize_nas_library_root(absolute_path(\"library_root\", ...))` strips a trailing `publish/` component so `<X>` and `<X>/publish` share one transaction root; `absolute_path` rejects relative paths with `nas_package_v2_library_root_must_be_absolute`.\n- Read `source_path` bytes (`nas_package_v2_source_read:...`).\n- `validate_v2_export_binding` (L56-206): filename must be `reading-source-v2.json`; sibling `manifest-v2.json` must deserialize as `AuthoringV2ExportReceiptV1` with exactly the 3-file `files` list, matching `jobId`/`revision` against the optional input, `examId`/`sourceDocumentId` matching the sibling authoring IR, `reviewRequired == false`, and `sha256(authoring bytes)`/`sha256(source bytes)` matching the receipt. Then it re-reads the job's persisted authoring (`<app_root>/jobs/<jobId>/authoring-ir-v2.shadow.json` for revision 0, else `authoring/revisions/{n}.json`) and compares it to the exported authoring with `quality` stripped (`authoring_binding_value`). Then it re-compiles the bound authoring and requires `exported_source == bound_source`. Finally it re-runs `validate_authoring_v2_publish_readiness` against the LIVE job dir and requires exact `Value` equality with the receipt's `publishProof`.\n- Deserialize into `ReadingExamSourceV2` (`nas_package_v2_source_contract:...`), run `validate_reading_source_v2` (`nas_package_v2_source_invalid:<json array of CompilerIssueV2>`), derive `safe_exam_id` from the raw JSON and cross-check the optional `examId` input.\n- `reading_root = nas_reading_exams_dir(library_root)` — which is the normalized library root ITSELF, so `manifest.js`, `{examId}.js` and `resources/{examId}/` all live directly in the destination the user picked. `make_paths` then puts the lock at `<root>/.publish-control/export.lock`, backups under `<root>/.publish-control/backups/{examId}-{exportId}`, the journal at `<root>/.publish-control/{examId}-{exportId}.journal.json`, and the report at `<root>/publish/reports/{examId}-{exportId}.json`.\n- `OpenOptions::new().create(true).read(true).write(true).open(lock_path)` then `try_lock_exclusive()` (fs2 flock). Non-blocking: a concurrent publisher fails fast with `nas_package_v2_lock_busy:...`. This is also the de-facto \"destination writable\" probe, but `fs::create_dir_all(&reading_root)` at L287 runs FIRST and surfaces a BARE, unprefixed OS error on a read-only share.\n- `write_lock_metadata` (pid + heartbeat), `recover_incomplete_transactions` (replays every stale journal in `.publish-control` before doing anything new), capture `base_manifest_sha256`, optionally CAS against `expected_manifest_sha256`, write journal status `staging`.\n- `stage_and_commit`, then on Err write journal `failed` + `fs::remove_dir_all(staging_root)`; always drop lock metadata and unlock.\n\nINSIDE stage_and_commit (L352-568):\n- For each `source.assets.assets` descriptor: `validate_package_relative_path` (rejects `\\`, `:`, `://`, leading `/`, empty/`.`/`..` components, and the unicode slash lookalikes `⁄ ∕ ╱ ⧸`), `safe_join_asset_path(asset_root, relative_path)` to read, case-insensitive destination-collision check, then a plain `fs::copy` into `staging_root/resources/{examId}/{relativePath}`. No hash check here — the probe does that.\n- Write `resources/{examId}/asset-manifest.json` (canonical JSON of `ExamAssetManifestV2`, keyed by assetId) and `staging_root/{examId}.js` = `build_wrapper(source_value)` (a `(function registerReadingExamData(global){...})` IIFE with pretty-printed JSON inline). `runtime_bytes`/`runtime_sha256` are computed from the canonicalized re-serialized struct but the raw runtime JSON is NEVER written into the package — only the wrapper is.\n- `update_lock_heartbeat`, then `run_student_loader_probe(source, &asset_manifest, staging_resource_path)`. `!probe.passed` => `nas_package_v2_probe_failed:<json StudentProbeReportV2>`.\n- `verify_manifest_compare_and_swap(manifest_path, base_manifest_sha256)` (second CAS, this time against the baseline taken after the lock), `load_existing_manifest` (a brace-balanced, string-aware JS scanner that accepts both the V2 `window.__READING_EXAM_MANIFEST__ = {...}` form and the legacy closure form), `validate_minimum_runtime_version` (must parse and be <= [0,2,0]), insert/overwrite this exam's manifest entry, rewrite `_meta` to `ReadingExamManifestV2`, write `staging_root/manifest.js`.\n- Journal `committing` -> `commit_package` -> report JSON -> journal `committed` -> `cleanup_transaction_artifacts`.\n\nINSIDE commit_package (L582-855):\n- Type guards on the four live paths, then `state.json` (NasBackupStateV1) recording had_exam/had_resources/had_manifest/had_report.\n- Backup order matters and is deliberate: the manifest is COPY-verified (`copy_file_verified`, stays live so a student loader never sees a missing manifest), while exam.js / resources/ / report.json are RENAMED into the backup.\n- Commit: `rename(staging_exam)` -> `rename(staging_resources)` -> `atomic_replace_file(staging_manifest, manifest_path)` (plain rename on Unix; remove-then-rename fallback on Windows) -> journal `manifest_committed`. Manifest is genuinely last.\n- Any failure runs the `rollback(manifest_committed)` closure, which first calls `validate_backup_contents` and REFUSES to delete any visible file if a declared backup is missing (it only restores already-moved paths and returns `backup_incomplete:missing=...`, leaving the journal+backup for the next recovery pass). Errors are joined with `|` and appended as `;rollback=<...>`; a clean rollback is `;rollback=complete`.\n- Post-commit metadata failures (report dir/encode/write, committed journal) never return a plain error: `recover_post_commit_metadata_failure` (L573) replays `recover_incomplete_transactions` and appends `;rollback=complete` or `;rollback=incomplete:<err>`.\n\nRECOVERY (recover_incomplete_transactions, L1257-1369) runs at the START of every publish while holding the lock. It scans `.publish-control/*.journal.json`, skips `committed`/`recovered`, validates `examId`/`exportId` with `safe_transaction_component` (<=128 chars, ASCII alnum + `-_.`, not leading `.`), re-derives all paths from the journal, and per item calls `restore_backup_file_or_keep`/`restore_backup_dir_or_keep` (idempotent: restore if the backup exists, else keep the live file, else fail with `nas_package_v2_recovery_backup_incomplete:{label}_missing`). Then it deletes staging + backup and rewrites the journal as `recovered`.\n\nFRONTEND BATCH TODAY: `publishItems` (publishClient.ts L71-85) is a plain `for` loop over `publishItem`, i.e. N independent export+publish transactions, N manifest rewrites, N journals, N reports. Nothing rolls back item 1 when item 3 fails; the loop just partitions into `succeeded`/`failed`. LibraryPage.tsx L94 drives it and renders \"正在发布 done/total\"; ExamWorkspacePage.tsx L72 calls `publishItem` for the single-item button.",
    "gapsVsPlan": [
      {
        "claim": "13.3: the publish gate keeps only business closure and drops 'requires every group manually Confirmed', 'permanently blocked because a fallback appeared in history', 'requires a complete SourceReviewV1 file', and 'scan the whole authoring/pipeline JSON for any partial/fallback string'.",
        "reality": "All four are still live and all four are enforced INSIDE the NAS publisher, not just at export. authoring_v2_commands.rs L253-259 blocks on quality.hard_failures; L280-283 requires audit.humanVerified == true; L286-307 requires a SourceReviewV1 file that is present, non-stale, resolved and issue-free; L309-336 runs collect_publish_gate_markers over the authoring IR plus authoring-ir.json and pipeline-report.json, matching key names (aifallback/llmfallback/fallbackused/partialfailure/...), non-empty arrays (failures/blockedautoapplygroups/lowconfidencegroups), status strings (partial/needs_review/auto_apply_blocked) and even free-text substrings ('ai fallback', 'rust-local-fallback', 'partial failure') in note/audit paths. nas_package_v2.rs L202 calls this whole thing again, so relaxing it only at export changes nothing.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:253-336 and F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:202-205"
      },
      {
        "claim": "13.3: '引用资源存在' (referenced resources exist) is part of the gate.",
        "reality": "Partly. The probe verifies every asset in the manifest exists with the right sha256/byteLength/mime, and flags ASSET_CLOSURE_MISSING for content-referenced ids absent from the manifest - but the closure walker only matches the JSON key \"assetId\" (reading_runtime_v2.rs L316). TableContentNodeV2.visual_fallback_asset_id serializes as \"visualFallbackAssetId\" and is therefore invisible to it. quality.rs:2729-2742 already scans BOTH keys, so the two closure computations disagree and a table whose visual fallback image is missing from assets[] can pass the probe.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs:309-325 vs F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:2739-2746 and F:/workspace/PDF2Test/src-tauri/src/schema/content_doc_v2.rs:149"
      },
      {
        "claim": "13.3: '必需题干非空' and '选择题 option 完整' are enforced by the retained gate.",
        "reality": "Not by the compiler. validate_reading_source_v2 never inspects ResponseGroupV2.prompt, OptionV2.content, or TaskGroupV2.instructions at all - it only counts options and checks label uniqueness/non-emptiness (reading_source_v2.rs L376-390). Emptiness and placeholder detection live only in the untyped quality pass (option_has_renderable_content L2781, is_prompt_placeholder L2798 matching '[prompt pending review]' / '[shared prompt pending review]', group_has_ambiguous_prompt L2770), and reach the gate only indirectly as quality.hard_failures / blocking issues. Also: response kinds DiagramHotspot and Composite fall into the `_ => true` arm at reading_source_v2.rs L367, so their answer kinds are not validated at all.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs:367-390 and F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:2781-2818"
      },
      {
        "claim": "13.3: 'answer key 完整（若产品要求带答案）' is enforced.",
        "reality": "Only against the literal kind \"unresolved\". validate_authoring_v2_publish_readiness L211-224 filters answerKey entries whose kind == \"unresolved\". An entry of {\"kind\":\"text\",\"values\":[]} or {\"kind\":\"option\",\"labels\":[]} passes: the compiler's kind_matches accepts any Text/Option variant (L358-368), answer_labels/normalized_labels filter empties away rather than flagging them, and quality.rs validate_answer_value L2174-2253 only checks option-label membership and word/number limits. Only the unordered_set branch (L459-475) demands exactly one non-empty unique label per slot.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:211-224, F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs:358-368, F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:2210-2253"
      },
      {
        "claim": "13.3: '目标目录可写' is a distinct preflight check.",
        "reality": "No explicit writability check exists. It is implicit and split: fs::create_dir_all(&reading_root) at L287 fails first with a BARE unprefixed OS error string, and only after that does the lock open produce the recognisable nas_package_v2_lock_open:{error}. publishClient.ts compensates with a /permission|denied|readonly|read-only/i regex over the raw message (L38-40), which is locale-dependent and will miss non-English Windows messages.",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:287-325 and F:/workspace/PDF2Test/src/api/publishClient.ts:38-40"
      },
      {
        "claim": "13.4: the frontend no longer parses long authoring_v2_export_blocked:... strings.",
        "reality": "Both frontends still do. src/pages/ExportPage.tsx L60 regex-matches ^authoring_v2_export_blocked:(quality_state|unresolved_answers|hard_failures|issues)=(.*)$ and L63 slices the authoring_v2_export_compile_blocked: prefix. src/api/publishClient.ts L31 does a substring test and collapses EVERY blocker kind into one sentence about missing content - so revision_not_current, job_id_mismatch, source_review_stale, human_verification_required, ai_fallback and partial_failures are all mis-described. Nothing named PublishCheckResultV1 / PublishBlocker / PublishFixAction exists anywhere in the repo.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src/pages/ExportPage.tsx:60-63, F:/workspace/PDF2Test/src/api/publishClient.ts:29-42; repo-wide grep for PublishCheckResult finds only the TODO comment at publishClient.ts:9"
      },
      {
        "claim": "13.5: staging / asset copy / runtime compile / manifest last / atomic commit / failure cleanup are already in place and only need hiding.",
        "reality": "Correct, and better than the plan implies: there is also a per-library flock, a two-point manifest CAS, a durable journal, a verified backup, replay-on-next-publish recovery, and a rule that refuses to delete visible files when a backup is incomplete. The 'runtime compile' step is NOT inside the publisher though - the publisher only VALIDATES a pre-compiled ReadingExamSourceV2 (and re-compiles the bound authoring purely to prove the two agree). Compilation happens in export_authoring_v2_core.",
        "verdict": "already-done",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:320-350, 428-487, 582-841, 1257-1369"
      },
      {
        "claim": "13.6: 'the current V2 UI path is more mature for a single item; add a batch publisher' with create_batch_stage / compile_and_stage / update_library_manifest_in_stage / probe_stage / commit_stage.",
        "reality": "Accurate, and none of those five functions exist. The publisher is single-exam in its types, not just its loop: NasPackagePublishInput has one source_path; PackagePaths has one exam_path/resource_path/backup_root/journal_path/report_path; commit_package closes over four scalar had_* bools; write_journal writes one examId. Today's 'batch' is publishItems' serial for-loop, i.e. N transactions with N manifest rewrites and no cross-item rollback - exactly the behaviour 13.6 forbids. The plan's per-item error reporting is however already met in shape (PublishItemOutcome[]).",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:42-54, 217-243, 582-622, 1385-1412; F:/workspace/PDF2Test/src/api/publishClient.ts:71-85"
      },
      {
        "claim": "17.20: remove the 'Phase 4 runtime slice' wording and the partial-scope historical limits.",
        "reality": "Exactly two such sites, and both are real functional restrictions, not just wording. L211-212 'The Phase 4 runtime slice accepts task_group option banks only.' rejects OptionBankScopeV2::Document. L267 'The Phase 4 runtime slice accepts scoring slots only.' rejects AnswerSlotParticipationV2::Example and ::NonScoring, so an IELTS 'Example' row cannot exist in a publishable exam. Lifting the second one is not cosmetic: question_order/answer_key/question_display_map key-set equality (L144-188) and the assigned_slots == answer_slot_ids rule (L237-243) all assume every slot scores, and score_response_group divides by slot_ids.len().",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs:205-215, 262-269, 144-188, 237-243"
      },
      {
        "claim": "17.20: provide compile_with_report() returning runtime + typed issues.",
        "reality": "Does not exist. compile_reading_source_v2 returns Result<_, Vec<CompilerIssueV2>> - you get the runtime OR the issues, never both, so today's callers cannot show warnings on a successful compile. Two callers already work around this: ielts_grammar/quality.rs:782-820 compiles and then folds issue codes into quality.compilerProbes.v2Runtime, and nas_package_v2.rs:190-195 compiles only to compare structs. Note the runtime struct is deny_unknown_fields AND mirrored by a sha256-pinned JSON schema with additionalProperties:false, so the report must be a sibling return value, never a new field.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs:79-133, F:/workspace/PDF2Test/contracts/reading-exam-source-v2.schema.json:6, F:/workspace/PDF2Test/contracts/contract-manifest.json (schemas.ReadingExamSourceV2.sha256)"
      },
      {
        "claim": "17.21: 'the interface only accepts canonical DS lists that already passed preflight'.",
        "reality": "Inverted today. The publisher accepts a FILE PATH, not a DS, and then re-derives the entire preflight itself: it re-reads the receipt, re-reads the persisted authoring revision from the app data dir, re-compiles, re-validates, and re-runs the full publish-readiness gate demanding byte-identical proof equality. Moving to 'already passed preflight' means deleting validate_v2_export_binding's trust chain, which is currently the ONLY thing preventing an arbitrary JSON file that happens to deserialize as ReadingExamSourceV2 from being published (the test publisher_rejects_valid_json_without_v2_export_binding pins this).",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:56-206, 245-284, 1567-1593"
      },
      {
        "claim": "17.21: 'user result returns only item count, success/failure and destination - no hashes or internal path lists'.",
        "reality": "The Ok payload currently returns manifestPath, reportPath, exportId and the full probe object including checkedAssetIds and referencedAssetIds (L546-554), and the on-disk report adds runtimeSha256/assetManifestSha256/manifestSha256. Error strings are worse: they embed absolute paths (nas_package_v2_export_receipt_missing:<path>:<err>), serialized CompilerIssueV2 arrays, serialized probe reports, and pipe-joined rollback traces.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:496-554, 72-77, 271-276, 431-437"
      },
      {
        "claim": "Publishing marks the library item as published.",
        "reality": "It does not. publish_nas_package_v2_core touches no DB and no job store. The legacy V1 exporters do (export_nas_library.rs:1513 job.status = JobStatus::Exported; export_pack.rs:181,278) and also run cleanup_transient_job_artifacts afterwards; the V2 path does neither, so library_items.status never reaches 'published' via this route.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:245-350 (no db/job_store import at L8-32) vs F:/workspace/PDF2Test/src-tauri/src/export_nas_library.rs:1511-1515"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "NasPackagePublishInput (L42-54), PackagePaths (L217-235), publish_nas_package_v2_core (L245), stage_and_commit (L352), commit_package (L582), write_journal (L1385), make_paths (L857)",
        "whatToChange": "Introduce the batch transaction. Concretely: (a) a new input carrying Vec<item> plus one library_root/overwrite policy, keeping the single-item shape as a one-element adapter so the existing command keeps working; (b) split PackagePaths into a shared part (library_root, reading_root, manifest_path, lock_path, lock_metadata_path, base_manifest_sha256, staging_root, backup_root, journal_path, report_path) and a per-item part (exam_path, resource_path, staging_exam_path, staging_resource_path) held in a Vec; (c) hoist the lock + recover_incomplete_transactions + base_manifest_sha256 capture + CAS to run ONCE for the whole batch (they are already per-library, so no path derivation changes); (d) loop asset-copy + wrapper + asset-manifest + probe per item into one staging root; (e) merge ALL items into the manifest map in a single load_existing_manifest -> insert*N -> write staging manifest.js; (f) rewrite commit_package to take Vec<PerItemCommitState> with per-item had_exam/had_resources bools and one shared had_manifest/had_report, and rewrite the rollback closure to unwind every item before restoring the manifest.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "backup_root/state.json schema \"NasBackupStateV1\" (L613-622) and recover_incomplete_transactions (L1257-1369)",
        "whatToChange": "state.json currently holds four scalar bools for one exam; a batch needs a per-exam array. recover_incomplete_transactions reads exactly those four keys (L1319-1341) and derives exam_path/resource_path from a single journal examId (L1298-1300), so both the state file and the journal must gain an items array and the recovery loop must iterate it. Keep the read path backward-compatible with V1 state files or old journals left on a NAS by a previous version will make recovery panic-free but wrong. safe_transaction_component (L1028) must be applied to every exam id in the batch, not one.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "validate_v2_export_binding (L56-206)",
        "whatToChange": "This is the plan-17.21 'accept only preflighted canonical DS' decision point, and it is the highest-risk deletion in the subsystem. It is currently the only barrier against publishing an arbitrary ReadingExamSourceV2-shaped file (pinned by publisher_rejects_valid_json_without_v2_export_binding at L1567). If the batch publisher receives in-memory DS values instead of file paths, the equivalent guarantee has to be re-established inside the same process/transaction (e.g. compile from the persisted revision under the same lock and record the revision in the journal) rather than dropped. Also note the proof comparison at L202-205 is exact Value equality against the LIVE job dir, so anything that mutates source-review or appends a revision between export and publish turns into a publish failure.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "stage_and_commit return payload (L546-554), report json (L496-513), and every Err(...) string",
        "whatToChange": "For plan 13.4/17.21, stop returning strings and internal detail. Replace CommandResult<Value> with a typed result carrying { itemCount, succeeded, failed: Vec<PublishBlocker>, destination } and move manifestPath/reportPath/exportId/probe/hashes into the on-disk report only. There are ~95 distinct error string sites in this file; the ones the UI must be able to branch on are: nas_package_v2_lock_busy, nas_package_v2_lock_open, nas_package_v2_manifest_conflict, nas_package_v2_probe_failed, nas_package_v2_source_invalid, nas_package_v2_asset_source / _asset_copy / _asset_path_unsafe / _asset_path_collision, nas_package_v2_runtime_incompatible, nas_package_v2_export_binding_detached, nas_package_v2_export_receipt_proof_mismatch, and anything containing ';rollback=incomplete' (which means a human must intervene).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "NasPackagePublishInput.fault (L51) and the seven fault checks (L423,426,485,514,543,650,824)",
        "whatToChange": "`fault` is a live fault-injection switch on the shipped Tauri command and is echoed in the TS input type. Gate it behind #[cfg(test)] or a debug-only field before the converged product ships, or a caller can force a rollback path on a real NAS. Note that six of the eight rollback/recovery tests drive their scenarios exclusively through this field, so gating it requires giving those tests another injection seam (e.g. a test-only fn parameter, which commit_package already has as `fault: Option<&str>`).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "compile_reading_source_v2 (L79) + a new compile_with_report",
        "whatToChange": "Add `pub(crate) fn compile_with_report(source: &IeltsAuthoringIRV2) -> (Option<ReadingExamSourceV2>, Vec<CompilerIssueV2>)` (or a struct with runtime + blockers + warnings) and re-express compile_reading_source_v2 on top of it so existing callers keep their signature. Do NOT add fields to ReadingExamSourceV2: it is deny_unknown_fields and its JSON schema has additionalProperties:false with a sha256 pinned in contracts/contract-manifest.json and asserted by scripts/verify-phase6-runtime.mjs:103. Two callers benefit immediately: ielts_grammar/quality.rs:782-820 (which currently discards warnings on success) and the future PublishCheckResultV1.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "validate_reading_source_v2 (L135) / validate_response_group (L278)",
        "whatToChange": "Add the plan-17.20 business checks, typed, over the structs already in hand: (1) ResponseGroupV2.prompt non-empty and free of the '[prompt pending review]' / '[shared prompt pending review]' placeholders when the task type requires a stem; (2) OptionV2.content renderable (non-empty and not '[missing option text]') for every option in inline options and referenced banks; (3) visual fallback host closure - TableContentNodeV2.visual_fallback_asset_id and FigureNodeV2/DiagramNodeV2/ImageNodeV2.asset_id must resolve into source.assets.assets, and FigureHotspot-hosted slots must sit inside a figure/diagram that has hotspots covering them. Port the predicates from ielts_grammar/quality.rs (option_has_renderable_content L2781, is_prompt_placeholder L2798, node_contains_prompt_placeholder L2805, validate_visual_task L2324, validate_completion_host L1888, collect_asset_references L2729) rather than re-inventing them, so the typed compiler and the untyped quality pass cannot drift. Every new code must be added to fixtures/golden/synthetic/ielts/reading-v2-invalid-fixture-matrix.json with a mutation branch in invalid_fixture_matrix_has_stable_runtime_issue_codes.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "RUNTIME_OPTION_BANK_SCOPE_UNSUPPORTED (L205-215) and RUNTIME_SLOT_PARTICIPATION_UNSUPPORTED (L262-269)",
        "whatToChange": "These are the only two 'Phase 4 runtime slice' sites. Dropping the option-bank-scope restriction means OptionBankScopeV2::Document banks must actually resolve - today resolved_bank_options (L307-310) only ever looks at task.option_bank, so a document-scoped bank has nowhere to be looked up and would need a document-level registry on ReadingExamSourceV2 (a contract change). Dropping the scoring-only restriction means Example/NonScoring slots become legal, which then contradicts the four key-set equality rules at L144-188 and the assigned_slots==answer_slot_ids rule at L237-243, and requires score_response_group's possible_points = slot_ids.len() to exclude non-scoring slots. Treat these as two separate, individually-tested changes.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "validate_reading_source_v2 empty-exam hole (L144-158) and RUNTIME_SCHEMA_UNSUPPORTED target_id (L138-142)",
        "whatToChange": "An exam with zero answer_slots passes every check (0 == 0 on the length compare, empty == empty on all the set compares, assigned_slots empty == answer_slot_ids empty), while the JSON schema declares questionOrder minItems:1 - so the Rust compiler accepts a package the contract rejects. Add an explicit non-empty guard for answer_slots/task_groups. Separately, RUNTIME_SCHEMA_UNSUPPORTED passes &source.schema_version as target_id, i.e. the version string sits in an id field; fix before target_id becomes PublishBlocker.target_id and the UI tries to focus on it.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "RUNTIME_SLOT_ASSIGNED_TWICE (L228-234 and L300-306) and RUNTIME_DISPLAY_MAP_MISMATCH (L170-176 and L255-261)",
        "whatToChange": "Each of these two codes is emitted from two sites with different target_id KINDS (slot_id vs response_group_id; exam_id vs slot_id) and different meanings. A typed PublishBlocker that maps code -> fix action cannot disambiguate them. Split into four codes, or add a target_kind field to CompilerIssueV2, before building the code->PublishFixAction table.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_source_v2.rs",
        "symbol": "compile_reading_source_v2 modality/schema handling (L79-124)",
        "whatToChange": "The compiler ignores source.modality (a Listening IR that happens to carry a passage compiles into a 'reading' runtime) and never checks source.schema_version == IELTS_AUTHORING_IR_V2_SCHEMA_VERSION - it only copies it into audit.source_schema_version at L119. Both guards exist elsewhere (typed_authoring_for_probe in quality.rs L823-835, validate_authoring in authoring_v2_commands.rs L830-837) but not in the compiler that 17.20 designates as the single authority. Add both.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/reading_runtime_v2.rs",
        "symbol": "collect_asset_ids_from_value (L309-325)",
        "whatToChange": "Add \"visualFallbackAssetId\" alongside \"assetId\" so the probe's asset closure matches ielts_grammar/quality.rs:2739 (which already checks both). Without this a table's visual fallback image can be referenced by content, absent from the manifest, and still pass ASSET_CLOSURE_MISSING. One-line fix, but it will start failing existing fixtures that relied on the looser closure - verify against fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json first.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "validate_authoring_v2_publish_readiness (L174-329) and collect_publish_gate_markers (L333-437)",
        "whatToChange": "This is where plan 13.3's five removals actually have to happen, because nas_package_v2.rs:202 re-runs it. Concretely: (a) delete or demote collect_publish_gate_markers entirely - it is the 'scan the whole JSON for any partial/fallback string' behaviour, and it reads authoring-ir.json and pipeline-report.json, i.e. HISTORICAL process files, exactly what 13.3 forbids; (b) decide whether audit.humanVerified (L280-283) stays a hard blocker; (c) decide whether a missing/stale SourceReviewV1 file (L286-307) stays a hard blocker. Note the return value is the publishProof compared byte-for-byte at nas_package_v2.rs:203, so ANY change to the returned json! shape invalidates every export bundle already sitting on a NAS and must be versioned (AuthoringV2PublishProofV1 -> V2) rather than edited in place.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "symbol": "export_authoring_v2_core output_dir (L505-512)",
        "whatToChange": "publishClient.ts passes the SAME directory as exportDir and libraryRoot, so every publish leaves <NAS>/{examId}-r{rev}-{uuid}/ containing authoring-ir-v2.json (full authoring IR including the answer key), reading-source-v2.json, manifest-v2.json and copies of every asset, permanently, next to the student-facing manifest.js. Nothing deletes it - output_dir is never removed on success. Either give the export a scratch directory under the app data dir and pass only the resulting paths to the publisher, or make the batch publisher clean up its own bundle after commit. Treat the existing NAS folders as data to be cleaned, not just a future bug.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src/api/publishClient.ts",
        "symbol": "publishItem (L46-68), publishItems (L71-85), describePublishError (L29-42)",
        "whatToChange": "Replace the serial loop with one batch call once the Rust side exists, and delete describePublishError's string matching in favour of the typed blockers. Until then, at minimum split the authoring_v2_export_blocked branch: the current single message ('还有未完成的内容需要先补齐') is actively misleading for revision_not_current (the item was edited after export - retry), job_id_mismatch, source_review_stale, human_verification_required, ai_fallback and partial_failures. Also surface ';rollback=incomplete' distinctly - that is the one outcome where the NAS is left in a state needing a retry or manual repair.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/types/reading-source.ts",
        "symbol": "NasPackageV2PublishResult.probe (L157-163) and ExportNasPackageV2Input.fault (L148)",
        "whatToChange": "probe declares `errors: string[]; warnings: string[]` which the Rust StudentProbeReportV2 does not have (it has schemaVersion, examId, issues: RuntimeAssetIssueV2[]). Only src/pages/ExportPage.tsx:512 reads probe.passed, so nothing breaks today, but the type is a lie and src/services/devFallbackBackend.ts:3581 fabricates those fields to satisfy it. Fix both when introducing the typed result. `fault` enumerates only 4 of the 7 Rust fault values and should not be in a product type at all.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/scripts/verify-phase6-runtime.mjs",
        "symbol": "requiredFiles (L86-97) and the token assertions (L233-243)",
        "whatToChange": "This gate hard-requires src/pages/ExportPage.tsx and src/pages/StructuredAuthoringEditorV2.tsx to exist and to contain NAS_PACKAGE_V2_ENABLED / exportNasPackageV2 / nas-package-v2-export-result / 'const NAS_PACKAGE_V2_ENABLED = true'. Plan 16.9/16.10/16.12 retires both pages, so the gate must be repointed at src/features/editor/ExamWorkspacePage.tsx + src/api/publishClient.ts in the same commit that deletes them, or verify:phase6:runtime goes red. It also runs `cargo test --lib nas_package_v2` and `cargo test --lib reading_runtime_v2` - a batch refactor must keep those filters matching.",
        "risk": "medium",
        "blockedBy": "Frontend page retirement (plan 16.9/16.10) must land in the same change as this script update."
      },
      {
        "file": "F:/workspace/PDF2Test/contracts/reading-exam-source-v2.schema.json",
        "symbol": "root schema + contracts/contract-manifest.json schemas.ReadingExamSourceV2.sha256",
        "whatToChange": "Only touch if ReadingExamSourceV2 gains or loses a field (e.g. if the Document-scope option bank registry becomes necessary for 17.20). Changing the file requires recomputing the sha256 in contract-manifest.json or scripts/verify-phase6-runtime.mjs:103 fails, and requires mirroring the change into src/types/reading-runtime-v2.ts and src/services/readingRuntimeV2.ts (the JS runtime has its own copy of the semantic validator, asserted at L233-230 of the verify script).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "asset-manifest.json collision (L404-417) and _meta.assetCount (L470-476)",
        "whatToChange": "Two small correctness items to fix while in the file. (1) The seen_destinations collision check only compares descriptor destinations, so a descriptor with relative_path == \"asset-manifest.json\" is silently overwritten by the manifest write and only surfaces later as a probe hash mismatch - add the manifest filename to the reserved set. (2) _meta.assetCount is set to manifest.len() AFTER _meta was removed, i.e. it is the exam-entry count, not an asset count (export_artifacts.rs:52-55 has the same mislabel). Either rename it or compute the real asset total; the student loader must be checked before renaming.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "copy_dir_recursive (L1186-1207)",
        "whatToChange": "Dead code - the only reference to it is its own recursive call. Delete it, or use it: a batch backup of multiple resource directories is exactly the case it was presumably written for.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/runtime_compiler.rs",
        "symbol": "ExamCompiler trait",
        "whatToChange": "34-line abstraction with one impl and no production caller. Either extend it with compile_with_report so it becomes the real seam plan 17.20 describes, or delete it and let reading_source_v2 be the interface directly. Leaving it as-is guarantees the new report API and the trait drift apart.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "publish_nas_package_v2 (L1209-1212) and generate_handler (L1460)",
        "whatToChange": "Register the batch command here and keep publish_nas_package_v2 as a thin single-item wrapper so scripts/verify-phase6-runtime.mjs:236 (which greps for 'async fn publish_nas_package_v2' and 'publish_nas_package_v2,') keeps passing. Note `root` here is app_data_dir, and the publisher needs it only for the job-artifact binding check - if 17.21 removes that check, the parameter becomes unused.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs",
        "symbol": "post-publish status write (absent)",
        "whatToChange": "Add the library-item status transition the V1 exporters perform. export_nas_library.rs:1511-1515 does update_job(... job.status = JobStatus::Exported; job.current_step = WorkflowStep::Export) plus cleanup_transient_job_artifacts per job; the V2 publisher does neither, so library_items.status never becomes 'published' through this path (mapping lives at library_commands.rs:160-167). Do this inside the batch commit's success path, after the manifest commit, and decide whether a partial batch leaves items unpublished.",
        "risk": "medium"
      }
    ],
    "testHarness": "All Rust tests are in-file #[cfg(test)] modules; there is no src-tauri/tests/ directory and no e2e spec files.\n\nRUN COMMANDS (the CI gate uses exactly these, scripts/verify-phase6-runtime.mjs:257-258):\n  cargo test --manifest-path src-tauri/Cargo.toml --lib nas_package_v2 -- --nocapture\n  cargo test --manifest-path src-tauri/Cargo.toml --lib reading_runtime_v2 -- --nocapture\n  cargo test --manifest-path src-tauri/Cargo.toml --lib reading_source_v2\n  npm run verify:phase6:runtime      (does npm run check + both cargo filters + JSON-schema and token assertions)\nI did not run any of these - the task forbade cargo builds.\n\nreading_source_v2.rs tests (L611-1056), 8 tests. Fixture helpers to copy: fixture() at L619 loads fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json via env!(\"CARGO_MANIFEST_DIR\"); invalid_fixture_matrix() at L627 loads fixtures/golden/synthetic/ielts/reading-v2-invalid-fixture-matrix.json; option_answer(&[\"A\"]) at L633 builds an unordered_set option answer.\n  early_approaches_compiles_to_one_shared_runtime_response (L641) - the happy path; asserts question_order == [\"q14\",\"q15\"], 5 bank options, one shared response.\n  shared_response_scoring_is_order_independent (L681), shared_response_awards_one_point_for_one_correct_unique_label (L712), shared_response_rejects_duplicate_and_extra_labels_and_scores_zero_matches (L743) - score_response_group semantics.\n  text_entry_response_and_text_answers_do_not_require_an_option_bank (L781).\n  invalid_fixture_matrix_has_stable_runtime_issue_codes (L815) - THE pattern to extend for any new validation. It iterates the 33 cases in the JSON matrix, applies a named `mutation` to a freshly compiled runtime via a big match, and asserts the expected code appears in validate_reading_source_v2's output. Adding a check means adding a {id, mutation, expectedCode} row to the fixture AND a match arm here; the match ends in `unknown => panic!(\"unknown invalid fixture mutation: {unknown}\")` so a fixture row without an arm fails loudly.\n  invalid_fixture_matrix_unknown_enums_are_rejected_by_typed_deserialization (L1008) - the four unknown_* mutations are asserted at the serde layer, not the validator.\n  writes_pr06_reading_source_v2_fixture (L1046) - #[ignore]d, writes the cross-repo fixture to $IELTS_READING_V2_FIXTURE_PATH.\n\nnas_package_v2.rs tests (L1424-2039), 13 tests. Two helpers are the whole harness:\n  temp_root() (L1431) - std::env::temp_dir()/ielts-phase6-package-{nanos}. Every test ends with `let _ = fs::remove_dir_all(root);` and there is no Drop guard, so a panicking test leaks a temp dir.\n  test_export_bundle(root) (L1441) - THE fixture you must reuse for any batch test. It loads the golden authoring fixture, force-sets quality.state=\"ready\" and quality.issues=[], compiles, writes authoring-ir-v2.json + reading-source-v2.json into <root>/export, writes the same authoring bytes to <root>/jobs/<jobId>/authoring-ir-v2.shadow.json (so the persisted-binding check passes at revision 0), calls validate_authoring_v2_publish_readiness to get a real proof, and hand-builds manifest-v2.json with the correct authoringSha256/runtimeSha256/publishProof. Returns (source_path, ReadingExamSourceV2). A batch test needs an N-item variant with distinct exam ids and job ids.\n  Transaction/rollback tests, all driven by the `fault` input: package_publish_probes_before_commit_and_fault_rolls_back_old_manifest (L1604, fault=manifest_rename), partial_resource_backup_fault_restores_old_visible_package (L1641, fault=backup_resources), post_commit_report_failure_rolls_back_package_and_report (L1686, fault=report_write), v2_commit_fault_preserves_loadable_legacy_v1_manifest (L1730).\n  Concurrency/CAS: package_publish_rejects_busy_lock_before_commit (L1784) - note it builds the lock path via make_paths(&library_root, &library_root, exam_id).lock_path, which is the clearest evidence that reading_root == library_root. commit_cas_rejects_manifest_changed_after_staging_baseline (L1819), manifest_cas_rejects_stale_writer (L1512).\n  Recovery: recovery_restores_persistent_backup_after_manifest_commit_interruption (L1834), recovery_keeps_visible_package_when_no_backup_moves_completed (L1911), recovery_restores_partial_exam_backup_and_keeps_other_live_items (L1979). These hand-construct PackagePaths + journal + backup on disk and then call recover_incomplete_transactions directly - the pattern to copy for multi-item recovery.\n  Policy units: package_path_policy_rejects_unsafe_relative_paths (L1498), runtime_version_policy_blocks_invalid_or_future_student_runtime (L1596), existing_manifest_parser_accepts_legacy_closure_wrapper (L1521), publish_normalizes_publish_child_to_one_transaction_root (L1541), publisher_rejects_valid_json_without_v2_export_binding (L1567).\n\nreading_runtime_v2.rs tests (L344-462) cover safe_join_asset_path traversal rejection and the probe's asset verification; its temp_root() pre-creates an images/ subdir.\n\nWHAT IS NOT COVERED (write these first for a batch change): no test publishes two different exams into one library root; no test asserts that a mid-batch failure leaves earlier items unpublished; no test covers two concurrent processes past the lock-busy check; no test covers a read-only destination; no test asserts the compiler rejects an empty prompt, an empty option, an empty answer value list, or a zero-slot exam.",
    "surprises": [
      "nas_reading_exams_dir(library_root) returns the normalized library root ITSELF (export_nas_library.rs:174-176), so reading_root == library_root: manifest.js, {examId}.js and resources/{examId}/ all land directly in the folder the user picked, and .publish-control/ + publish/reports/ sit beside them. The sibling nas_publish_dir() which appends 'publish' is never used by this module, and normalize_nas_library_root actively STRIPS a trailing publish/ component - so passing <X>/publish writes to <X>.",
      "publishClient.ts passes the same directory as both exportDir and libraryRoot (L48-52), so every publish permanently leaves <NAS>/{examId}-r{rev}-{uuid}/authoring-ir-v2.json on the NAS - the full authoring IR including the answer key - next to the student-facing manifest.js. Nothing ever deletes it and each publish creates a new uuid-suffixed directory. The legacy ExportPage.tsx:236-243 does exactly the same, so this is pre-existing, not new.",
      "The publisher re-runs the ENTIRE authoring publish gate (nas_package_v2.rs:202 -> validate_authoring_v2_publish_readiness) and demands byte-exact Value equality with the publishProof baked into the export receipt. Relaxing the gate only in export_authoring_v2_core changes nothing, and any change to the proof's json! shape silently invalidates every export bundle already staged on a NAS. It also means an edit between export and publish surfaces as authoring_v2_export_blocked:revision_not_current:current=N:requested=M from the PUBLISH command - which publishClient.ts:31 mislabels as 'content still needs to be filled in'.",
      "validate_reading_source_v2 accepts an exam with ZERO answer slots: the length compare is 0==0 and every set comparison is empty==empty. The pinned JSON schema declares questionOrder minItems:1, so the Rust compiler will happily produce and the publisher will happily publish a package the contract schema rejects.",
      "An answer key entry of {\"kind\":\"text\",\"values\":[]} or {\"kind\":\"option\",\"labels\":[]} passes all three gates: the compiler's kind_matches only checks the variant (L358-368), quality.rs validate_answer_value only checks label membership and word limits (L2210-2253), and the publish gate only filters kind == \"unresolved\" (authoring_v2_commands.rs:211-224). Only the unordered_set branch demands one non-empty unique label per slot.",
      "Response kinds DiagramHotspot and Composite hit the `_ => true` arm at reading_source_v2.rs:367, so their answer-key kinds are never validated at all - including Unresolved.",
      "The probe's asset-closure walker matches only the JSON key \"assetId\" (reading_runtime_v2.rs:316), while quality.rs:2739 matches both \"assetId\" and \"visualFallbackAssetId\". A table with a visual fallback image missing from assets[] passes the publish probe but is flagged by the quality pass - two closure computations that disagree.",
      "The `fault` field is a live fault-injection hook on the shipped Tauri command (not #[cfg(test)]-gated) and is exposed in the TypeScript input type. Rust honours seven values (after_assets, after_source, before_manifest, report_write, committed_journal, backup_resources, manifest_rename); the TS type enumerates only four.",
      "The runtime JSON is never written into the NAS package. Only the {examId}.js IIFE wrapper (build_wrapper, export_artifacts.rs:33) and resources/{examId}/asset-manifest.json land there. runtime_bytes/runtime_sha256 are computed solely to populate the manifest checksum block, and scriptSha256 hashes the pretty-printed wrapper while runtimeSha256 hashes the canonicalized re-serialized struct - two different encodings of the same data.",
      "_meta.assetCount in the published manifest is the EXAM count, not an asset count: manifest.len() is read at L473 after _meta was removed at L470, so it counts exam entries. export_artifacts.rs:52-55 has the identical mislabel, so the V1 and V2 manifests are consistently wrong together.",
      "The rollback closure deliberately refuses to delete any visible file when validate_backup_contents reports a missing backup (L710-748). It restores only already-moved paths, returns backup_incomplete:missing=..., and LEAVES the journal and backup on disk for the next publish or app start to replay. This is why several error strings end in ';rollback=incomplete:...' - that suffix means the NAS is in a state that still needs a retry, and it is the one outcome a user must be told about.",
      "The manifest is COPY-verified into the backup (copy_file_verified, L1089) rather than renamed, specifically so a student loader never observes a missing manifest.js mid-publish - while exam.js, resources/ and report.json are renamed. That asymmetry is load-bearing and easy to break in a batch rewrite.",
      "load_existing_manifest uses a hand-written brace-balanced, quote-and-escape-aware JS scanner (extract_manifest_json_object, L965-1003) so it can read both the V2 assignment form and the legacy closure-wrapped V1 form. A batch publisher must keep this - the target NAS may still hold a V1 manifest.",
      "serde_json is pulled in without the preserve_order feature (src-tauri/Cargo.toml), so Map is a BTreeMap and to_string_pretty output is key-sorted and deterministic. The manifest CAS depends on that determinism.",
      "score_response_group (reading_source_v2.rs:482) has no production caller anywhere in the crate - it is referenced only from the test module in its own file. It is a scoring oracle for the JS student runtime, not shipped logic. copy_dir_recursive (nas_package_v2.rs:1186) is outright dead code, referenced only by its own recursive call.",
      "publish_nas_package_v2_core writes nothing to the DB or job store. The V1 exporters set job.status = JobStatus::Exported and run cleanup_transient_job_artifacts (export_nas_library.rs:1511-1526, export_pack.rs:181,278); the V2 path does neither, so library_items.status never reaches 'published' via the V2 publisher.",
      "NasPackageV2PublishResult.probe in TypeScript declares errors: string[] and warnings: string[], which the Rust StudentProbeReportV2 (deny_unknown_fields: schemaVersion, examId, passed, checkedAssetIds, referencedAssetIds, issues) does not have. devFallbackBackend.ts:3581 fabricates them to satisfy the type. Only ExportPage.tsx:512 reads probe.passed, so nothing breaks today.",
      "scripts/verify-phase6-runtime.mjs hard-requires src/pages/ExportPage.tsx and src/pages/StructuredAuthoringEditorV2.tsx to exist and to contain specific tokens including the literal 'const NAS_PACKAGE_V2_ENABLED = true'. Plan 16.9/16.10 retires both pages, so that gate breaks unless updated in the same commit.",
      "All 24 compiler messages are English sentences ('Response group must contain at least one slot.'), while every user-facing string in the new frontend is Chinese. A typed PublishBlocker.user_message needs a code -> Chinese-message table; the compiler messages cannot be surfaced directly.",
      "Two issue codes are emitted from two sites each with different target_id kinds: RUNTIME_SLOT_ASSIGNED_TWICE (slot_id at L231 vs response_group_id at L303) and RUNTIME_DISPLAY_MAP_MISMATCH (exam_id at L173 vs slot_id at L258). RUNTIME_SCHEMA_UNSUPPORTED puts the version STRING in target_id (L141). All three will confuse a UI that focuses on target_id."
    ]
  },
  {
    "changePoints": [
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "mark_user_audit (839-846) + validate_authoring_v2_publish_readiness humanVerified gate (255-261)",
        "whatToChange": "HIGHEST PRIORITY — these two are mutually exclusive and make V2 publish unreachable. mark_user_audit unconditionally does `audit.insert(\"humanVerified\".to_string(), json!(false));` on EVERY successful patch save; the publish gate requires `/audit/humanVerified == true`. No other code path in the repo writes true: the shadow builder hardcodes `\"humanVerified\": false` (ielts_grammar/mod.rs:369), no patch op can reach the `audit` object (find_object_mut_by_id matches only objects with an `id` field; is_safe_node_attribute (1894-1910) does not list humanVerified; find_object_by_any_id_mut matches only id/taskId/responseGroupId/slotId). So revision 0 (shadow) and every revision >0 are both blocked. Either delete the humanVerified gate (plan 13.3 says drop manual per-group confirmation) or add an explicit confirm command/patch op that sets it and that mark_user_audit does not clobber.",
        "risk": "high"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "collect_publish_gate_markers (333-437) and its call sites (287-317)",
        "whatToChange": "Delete the historical-file scan. Lines 295-305 loop over exactly `[\"authoring-ir.json\", \"pipeline-report.json\"]` under jobs/<id>/ and recurse the whole document. Three rules have NO path gate and therefore fire on pure history: `requiresmanualquestionimport == true` (a serialized non-optional bool on V1 QuestionDraftV1/QuestionGroupDraftV1, authoring_pipeline.rs:197 and 221), `blockedautoapplygroups`/`lowconfidencegroups` non-empty arrays (written into pipeline-report.json by auto_pipeline.rs:2156 and re-written without cleanup by run_cloud_review_core_with_gateway at auto_pipeline.rs:2665), and `code == \"PARTIAL_RECOVERY_FAILURE\"` (V1 audit issue code, authoring_review.rs:56). Keep at most the current-DS string scan of audit.notes if anything.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\quality.rs",
        "symbol": "evaluate_quality state computation (187-199)",
        "whatToChange": "`state = ready` requires `issues.is_empty()` literally — `else if document_score < 0.95 || has_low_task_score || source_coverage < 0.995 || !issues.is_empty()` → review_required. Nothing filters issues whose `details.resolution` is `resolved`/`ignored`, so the `resolveIssue` patch op can never move the document to ready, while validate_authoring_v2_publish_readiness (233-254) DOES honour those resolutions for its own blocker list. Result: publish always dies earlier at `authoring_v2_export_blocked:quality_state=review_required` (export_authoring_v2_core:482-486). Make the state computation resolution-aware and severity-aware, or drop `state` from the publish gate and rely on typed blockers only.",
        "risk": "high"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "validate_authoring_v2_publish_readiness (174-331) → replace with typed PublishCheckResultV1",
        "whatToChange": "Plan 13.4's `PublishCheckResultV1 { passed, blockers, warnings }` does not exist anywhere (grep for PublishCheckResultV1/PublishBlocker returns nothing). The function returns Err(String) with 11 distinct shapes: job_id_mismatch, revision_not_current:current=..:requested=.., quality_state=.., unresolved_answers=.., hard_failures=.., issues=.., human_verification_required, source_review_invalid, source_review_stale, source_review_unresolved, source_review_issues=<json>, ai_fallback=<dot.paths>, partial_failures=<dot.paths>. It also returns raw `read_json:<path>:<err>` if authoring-ir.json or pipeline-report.json exist but are malformed (read_json_opt → read_json, util.rs:246-257). Convert to a typed result and keep the Err path only for genuine internal failures.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src\\pages\\ExportPage.tsx",
        "symbol": "parseV2ExportBlock (56-68) regex",
        "whatToChange": "The regex only recognises 4 kinds: `/^authoring_v2_export_blocked:(quality_state|unresolved_answers|hard_failures|issues)=(.*)$/`. Commit bb978be added 7 more blocker spellings (human_verification_required, source_review_stale/unresolved/invalid, source_review_issues=, ai_fallback=, partial_failures=, revision_not_current:, job_id_mismatch) — all fall through to `guidance: [message]` and are shown raw. src/api/publishClient.ts:31-33 is worse: every `authoring_v2_export_blocked` collapses to one sentence about missing stems/options/answers, which is actively wrong for the marker and humanVerified blockers. Both must be rewritten against the typed result.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "symbol": "append_revision (240-335), list_revision_records (345-380), JobArtifactPaths::revision_path / revision_meta_path / patch_path (77-87)",
        "whatToChange": "Plan 4.4's bounded recovery. Today one debounced save (450ms, useCanonicalEditor.ts:13) writes exactly 3 immutable files — `authoring/revisions/N.json`, `authoring/revisions/N.meta.json`, `authoring/patches/N.jsonl` (patches are never empty because apply_authoring_v2_patches_core:127 rejects empty batches) — plus an atomic rewrite of `authoring/current-revision.json`. Nothing ever prunes them (cleanup.rs touches only top-level files and cache/preview/llm-suggestions dirs). Worse, cost is O(N) per operation: list_revision_records reads and JSON-parses every `*.meta.json`, and it runs twice per save (once inside append_revision→recover_current_revision:215, once at apply_authoring_v2_patches_core:164) and twice per session open (recover_current_revision, then build_editor_session:720). Introduce last_good_snapshot + capped journal and stop returning the full record list in the session payload.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "refresh_quality_report (744-755)",
        "whatToChange": "Plan 9.5 asks for `validate_changed_targets(&ds, commands.targets())`. Reality: every save calls evaluate_quality over the entire document, including both compiler probes (evaluate_compiler_probes → compile_reading_source_v2 plus the V1 compatibility compiler, quality.rs:164-180). There is no incremental path and no target list is threaded from the patches. Also note it does NOT call refresh_group_quality (private to ielts_grammar/mod.rs, only called at shadow build, mod.rs:377), so `taskGroups[i].quality` and `taskGroups[i].reviewState` go stale immediately after the first user save.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "validate_authoring_v2_publish_readiness source-review block (263-285)",
        "whatToChange": "Plan 13.3 wants SourceReviewV1 off the normal publish line. Four hard blockers live here and they are order-dependent on cleanup: when jobs/<id>/document-ir.json is present the review is recomputed from parser warnings + low-confidence blocks; when it is absent (cleanup.rs:124-139 and 184-198 delete it) source_review_status returns the SAVED source-review.json verbatim (source_review.rs:126-130), stale flag and all. So deleting document-ir.json can freeze a stale=true review permanently. If both files are absent the synthesized review passes (required=false → resolved=true). Decide explicitly which of these three states the new gate wants.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "module split per plan 17.16",
        "whatToChange": "Single 2551-line file. Natural seams already exist: session load (711-755), patch dispatch table (848-875) + 19 op implementations (877-1520), traversal helpers (1522-1867), validation/expression helpers (1869-2001), publish preflight (174-447), export + assets (449-709). Note apply_patch's dispatch (853-873) is the only enumeration of the vocabulary — keep it as the single registry when splitting.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "symbol": "JobArtifactPaths fields asset_blobs_dir / asset_metadata_dir / asset_previews_dir / preview_runtime_dir / legacy_dir (29-36), created in ensure_job_artifact_layout (147-163)",
        "whatToChange": "Plan 17.15 says 'asset blob logic 迁到全局 content-addressed asset store'. There is no asset blob logic: these five directories are created on every ensure_job_artifact_layout call and are never written to or read anywhere in src-tauri (grep across the crate finds no use outside artifact_store.rs). Only sources/extraction/authoring/assets/preview/export-history/legacy are accepted namespaces by validate_v2_namespace (469-496), and only authoring/, assets/ (via materialize_authoring_assets) and export-history/ are actually used. Delete the dead scaffolding rather than migrating it.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "symbol": "read_revision (337-343)",
        "whatToChange": "Loads `revisions/{N}.json` with no integrity check against `RevisionRecordV2.artifact_sha256`, which was written next to it. Only validate_authoring (schema) runs afterwards (load_current_authoring:729). If revision integrity matters to the converged design, verify the hash here; if it does not, drop artifact_sha256/patch_sha256 from the record.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "symbol": "recover_current_revision (210-238)",
        "whatToChange": "get_authoring_v2 is NOT read-only: when current-revision.json is missing or behind the highest meta record, this writes a repaired pointer via write_canonical_json_atomic (233-236). It errors with `current_revision_missing_record` if the pointer is AHEAD. Any move of the pointer into SQLite must preserve both behaviours (tests at 702-754 and 756-792 lock them in).",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "symbol": "apply_patch dispatch (848-875) — missing replaceAsset op",
        "whatToChange": "SuggestedActionV2::ReplaceAsset exists in schema/quality_report_v2.rs:46 and can be emitted as a suggested action, but there is no `replaceAsset` op. The 19 ops are replaceText, setNodeAttrs, replaceContent, insertNode, deleteNode, moveNode, cropAsset, setHotspot, removeHotspot, setTaskType, setQuestionExpression, setResponseCardinality, setResponseGroup, setOptionBank, insertAnswerSlot, deleteAnswerSlot, setAnswer, bindSource, resolveIssue. Also nothing can write TaskGroupV2.review_state (Unreviewed/Confirmed/Edited), which is emitted as \"unreviewed\" at mod.rs:331 and never updated or read.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src\\features\\editor\\useCanonicalEditor.ts",
        "symbol": "RECOVERY_KEY_PREFIX write path (124-134)",
        "whatToChange": "The crash-recovery snapshot is write-only: it is written to localStorage only on save FAILURE and removed on success (line 111); nothing ever reads it back (grep for 'workspace-recovery' finds only the constant declaration at line 14). Plan 4.4's bounded recovery needs a restore path, or this should be deleted rather than left as dead state.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\nas_package_v2.rs",
        "symbol": "validate_v2_export_binding (56-206), proof comparison at 201-204",
        "whatToChange": "NAS publish re-runs validate_authoring_v2_publish_readiness on the exported authoring bytes and requires `manifest.publishProof == proof` byte-for-byte. The proof embeds the whole recomputed `sourceReview` object (authoring_v2_commands.rs:325), so anything that changes source-review.json or deletes document-ir.json between export and publish yields `nas_package_v2_export_receipt_proof_mismatch`. Note the deliberate carve-out: quality is stripped before the persisted-vs-exported comparison (authoring_binding_value, 208-214) because export refreshes quality in memory without persisting it. Any change to the proof shape must be made in both places at once.",
        "risk": "medium"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src\\services\\devFallbackBackend.ts",
        "symbol": "phase5Session (2479-2497) and the export_authoring_v2 branch (2578-2600)",
        "whatToChange": "This is the only producer of `source: \"fixture\"` (line 2492) — the Rust backend emits only \"shadow\" (revision 0) or \"revision\" (>0) at authoring_v2_commands.rs:713. The dev fallback's export gate is also a different, softer policy than Rust's: it whitelists away ANSWER_KEY_MISSING_SLOT/RUNTIME_COMPILER_FAILED, emits `blocking_issues=` (Rust emits `issues=`), and has no humanVerified, source-review or marker checks at all. Plan 20.2/P8-T02 wants this deleted; until then treat any green run through it as meaningless for publish readiness.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\lib.rs",
        "symbol": "get_job_artifact_status (1069-1073) → inspect_job_artifacts",
        "whatToChange": "Plan 17.15 wants inspect_job_artifacts off the normal product surface. It is still a registered Tauri command (registered in the invoke_handler list around lib.rs:1435-1460) and still wrapped in src/api/tauriCommands.ts:103-107, but no page calls getJobArtifactStatus — it is already orphaned on the frontend, so removal is cheap.",
        "risk": "low"
      },
      {
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\real_pdf_acceptance.rs",
        "symbol": "run_phase5_real_pdf_edit_export (1584-1690) / phase5_real_pdf_edit_and_v2_export_round_trip (1692-2046)",
        "whatToChange": "This is the only end-to-end test of shadow → 2 saves → export. It was NOT updated by commit bb978be (which added the humanVerified gate, source-review gate and marker scan to the export path — the commit touched authoring_v2_commands.rs, auto_pipeline.rs, llm_commands.rs, llm_gateway.rs, nas_package_v2.rs, parser.rs, source_review.rs, but not real_pdf_acceptance.rs). Its `.expect(\"real PDF V2 editor/export round trip must complete\")` at line 2035 cannot pass with the current gates: mark_user_audit sets humanVerified=false on both saves, and refresh_quality_report recomputes state to review_required. I could not confirm which assertion fires first because the corpus is absent (fixtures/golden/private-real contains only README.md and a .gitignore for *.pdf) and I was instructed not to run cargo test. Fix this test in the same change as the gate.",
        "risk": "medium"
      }
    ],
    "dataFlow": "SESSION LOAD (get_authoring_v2). lib.rs:1008-1012 → get_authoring_v2_core (authoring_v2_commands.rs:54-57) → safe_job_dir (util.rs:44-47, validates job_id as a single safe path segment) → build_editor_session (711-723) → load_current_authoring (725-742). load_current_authoring is the whole story: recover_current_revision(root, job_id) reads jobs/<id>/authoring/current-revision.json; if `current.revision > 0` it reads jobs/<id>/authoring/revisions/<N>.json, validates it as IeltsAuthoringIRV2, and returns (value, N); otherwise it reads jobs/<id>/authoring-ir-v2.shadow.json (const AUTHORING_V2_SHADOW_FILE, line 34) and returns (value, 0), erroring `AUTHORING_V2_NOT_AVAILABLE:shadow_missing:<path>` if absent. build_editor_session then sets `source = if revision > 0 { \"revision\" } else { \"shadow\" }` (713) and attaches the full list_revision_records vector. There is NO fixture path in Rust: \"fixture\" is produced only by the browser dev fallback (src/services/devFallbackBackend.ts:2492, seeded from createPhase5Fixture for the literal jobId \"phase5-editor-fixture\"); the union type lives at src/types/authoring-editor-v2.ts:15. get_authoring_v2 does not create the artifact layout, but it is not strictly read-only — recover_current_revision rewrites current-revision.json when the pointer lags the newest meta record (artifact_store.rs:226-237).\\n\\nSAVE (apply_authoring_v2_patches). lib.rs:1024-1028 → apply_authoring_v2_patches_core (123-168). Deserializes ApplyAuthoringV2PatchesInput {jobId, baseRevision, patches}; rejects empty batches and batches >200; load_current_authoring; pre-checks `current_revision != input.base_revision` → `revision_conflict:current={}:base={}`; applies each patch in order through apply_patch's 19-op dispatch (848-875); mark_user_audit (839-846) sets audit.revision = base+1, source=\"user\", humanVerified=false, updatedAt=now; refresh_quality_report (744-755) recomputes the whole quality report; validate_authoring (830-837) round-trips the document through IeltsAuthoringIRV2; append_revision persists. Returns the same session shape plus savedPatchCount. Note patches are applied to an in-memory clone and only the FINAL document is persisted — the .jsonl patch log is a side record, never replayed by any code.\\n\\nREVISION PERSISTENCE (artifact_store.rs:240-335). append_revision ensures the 12-directory layout, opens jobs/<id>/authoring/revision.lock and takes an exclusive fs2 advisory lock, then re-reads recover_current_revision UNDER the lock and re-checks `current.revision != base_revision` → `revision_conflict:current={}:base={}`. That second check is what actually makes concurrency safe; the pre-check in apply_authoring_v2_patches_core is only a fast path. Next revision = max(highest number found in revisions/ by filename scan, current.revision) + 1 — monotonic allocation quarantines a half-written N.json rather than overwriting it (test at 829-859). It then writes with create_new (fails if the file exists): revisions/<N>.json (canonical JSON), patches/<N>.jsonl (one canonical-JSON line per patch, only when patches is non-empty), revisions/<N>.meta.json (RevisionRecordV2 with parent_revision, source, artifact_sha256, patch_sha256), and finally atomically replaces authoring/current-revision.json. The lock releases on File drop, including on the early-return conflict path.\\n\\nREVISION_CONFLICT — three distinct emitters, same string prefix: (1) authoring_v2_commands.rs:135-140, save pre-check; (2) artifact_store.rs:261-266, authoritative check under the file lock; (3) authoring_v2_commands.rs:467-473, export when the caller passes an explicit `revision` that is not current. A fourth, differently-spelled conflict lives in the publish gate: `authoring_v2_export_blocked:revision_not_current:current=..:requested=..` (186-191). The frontend matches on the substring \"revision_conflict\" only (src/features/editor/useCanonicalEditor.ts:117).\\n\\nEXPORT (export_authoring_v2). lib.rs:1030-1034 → export_authoring_v2_core (449-614). Takes an exclusive lock on export-history/phase5-export.lock; load_current_authoring; optional requested-revision check; refresh_quality_report (mutates in memory, never persisted back to the revision file); typed deserialize; early `authoring_v2_export_blocked:quality_state=<state>` if not ready; validate_authoring_v2_publish_readiness → publish_proof; compile_reading_source_v2 → `authoring_v2_export_compile_blocked:<json issues>`; then a journalled staging commit into the caller-supplied absolute export_dir: writes .phase5-staging-<uuid>/{authoring-ir-v2.json, reading-source-v2.json, manifest-v2.json} plus hash-and-size-verified asset copies (materialize_authoring_assets, 640-691), and commits by renaming staging → <safeExamId>-r<revision>-<uuid>. A journal file export-history/phase5-v2-<uuid>.journal.json tracks staging/committed/failed, and a receipt is written to export-history/phase5-v2-<revision>-<uuid>.json via write_artifact_json. manifest-v2.json embeds the publishProof verbatim.\\n\\nASSET PREVIEW (resolve_authoring_asset_preview). lib.rs:1014-1022 → resolve_authoring_asset_preview_core (59-121). Canonicalizes the job dir, loads the current authoring via the same load_current_authoring, finds the AssetDescriptorV2 by asset_id, requires mime to start with \"image/\", resolves descriptor.relative_path through safe_asset_relative_path (616-638) and fs::canonicalize, re-checks starts_with(job_root) after canonicalization, then verifies byte_length AND sha256 against the manifest before returning a `data:<mime>;base64,<...>` URI. Fully manifest-closed: five distinct error codes for missing/unsupported/escaped/size-mismatch/hash-mismatch. Consumed by src/exam-canvas/ExamCanvas.tsx:36-43 through a module-level Map cache keyed `jobId:assetId` that is never invalidated.\\n\\nPUBLISH CHAIN. src/features/editor/ExamWorkspacePage.tsx:59-72 → publishItem (src/api/publishClient.ts:46-69) → getAuthoringV2, then exportAuthoringV2({jobId: itemId, exportDir: destination, revision: session.revision}), then exportNasPackageV2 → nas_package_v2::publish_nas_package_v2_core (245+) → validate_v2_export_binding (56-206), which re-reads the persisted shadow (revision 0) or revisions/<N>.json (revision >0), compares it to the exported authoring with `quality` stripped, recompiles the runtime and compares it to the exported reading-source-v2.json, then RE-RUNS validate_authoring_v2_publish_readiness and requires manifest.publishProof to equal the fresh proof exactly.",
    "gapsVsPlan": [
      {
        "claim": "§17.16 / §13.3: validate_authoring_v2_publish_readiness 递归扫描历史 JSON 中 fallback/partial 字符串 / 扫描整个 authoring/pipeline JSON 寻找任意字符串 partial/fallback",
        "reality": "Half right, and the wrong half matters. It IS a recursive walk (collect_publish_gate_markers, 333-437) and it DOES read historical files — but exactly two named ones, `jobs/<id>/authoring-ir.json` and `jobs/<id>/pipeline-report.json` (line 295), plus the current DS labelled \"authoring\". It is not a directory crawl and not an arbitrary substring grep. It is key-name driven: boolean keys aifallback/llmfallback/fallbackused/partialfailure/aipartialfailure/requiresmanualquestionimport; bare `fallback` only when the parent path contains audit/llm/ai/suggestion/evidence; non-empty arrays named failures (under llm/ai paths) or blockedautoapplygroups/lowconfidencegroups (ungated); `status` in {partial, partial_failure, needs_review, auto_apply_blocked} under llm/ai/vision/cloud/quality/audit paths; `code == PARTIAL_RECOVERY_FAILURE` (ungated); `failure` non-empty under those same paths. Only the String arm does substring matching, and only for four literals (\"ai fallback\", \"llm fallback\", \"rust-local-fallback\", \"deterministic-local-fallback\") under paths containing note/audit, plus \"partial failure\" under audit. Note the path gates are substring tests on an accumulated dotted path, so an innocent segment can open a gate — e.g. pipeline-report.json.authoring.remainingReviewItems contains \"ai\" via \"remaining\".",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:287-317 (call sites), 333-437 (the walker), 439-447 (json_value_is_nonempty)"
      },
      {
        "claim": "§13.3: 因历史日志中出现过 fallback 就永久禁止发布",
        "reality": "Confirmed, and reachable through three ungated rules. (a) `requiresManualQuestionImport: true` is a non-optional serialized bool on the V1 QuestionDraftV1 and QuestionGroupDraftV1 written into authoring-ir.json — one manual-import group permanently yields `partial_failures=authoring-ir.json.groups[0].requiresManualQuestionImport`. (b) `blockedAutoApplyGroups` / `lowConfidenceGroups` non-empty in pipeline-report.json.llm. (c) `code == PARTIAL_RECOVERY_FAILURE` in the V1 audit issues. None of these can be cleared by editing the V2 DS. Mitigating detail the plan does not mention: minimize_process_artifacts_after_authoring deletes pipeline-report.json right after it is written (auto_pipeline.rs:2185-2186), so vector (b) only survives when developer retention is on or when run_cloud_review_core_with_gateway re-writes it at auto_pipeline.rs:2665 without a following minimize.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:295, 367-376, 393-397; src-tauri/src/authoring_pipeline.rs:197,221; src-tauri/src/authoring_review.rs:56; src-tauri/src/auto_pipeline.rs:2156,2185-2186,2665; src-tauri/src/cleanup.rs:124-139"
      },
      {
        "claim": "§13.3: 从普通发布主线移除『要求所有题组手工逐个 Confirmed』",
        "reality": "There is no per-group confirmation gate in the V2 publish path — TaskGroupV2.review_state exists (schema/ielts_authoring_v2.rs:262-266) but is written once as \"unreviewed\" at shadow build (ielts_grammar/mod.rs:331) and is never read by the gate, never updated by any patch op. What actually exists is a single document-level `/audit/humanVerified == true` requirement that NO code path can satisfy: the shadow hardcodes false (mod.rs:369) and mark_user_audit forces false on every save (839-846). So the real defect is stricter and different from the plan's description: publish is unreachable, not merely tedious. (I could not execute cargo test to demonstrate this; the reasoning is from the write/read sites listed.)",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:255-261 and 839-846; src-tauri/src/ielts_grammar/mod.rs:331,369; src-tauri/src/schema/ielts_authoring_v2.rs:262-266"
      },
      {
        "claim": "§13.3: 从普通发布主线移除『要求保留完整 SourceReviewV1 文件』",
        "reality": "It does not require the file to exist. With no document-ir.json and no source-review.json, source_review_status synthesizes required=false → resolved=true and the gate passes. What it does require is that IF a saved source-review.json exists it must not say stale or unresolved, and that source_review_issues() is empty. The nasty part is order dependence: with document-ir.json present the review is recomputed from parser warnings and low-confidence blocks; once cleanup deletes document-ir.json, source_review_status returns the saved file verbatim, so a stale=true review is frozen permanently.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:263-285; src-tauri/src/source_review.rs:120-162, 206-311; src-tauri/src/cleanup.rs:124-139,184-198"
      },
      {
        "claim": "§4.4: 当前 artifact_store.rs 会为每次保存建立不可变 artifact、meta 和 patch 文件；长期编辑会生成大量文件",
        "reality": "Exactly right, and quantifiable: 3 create_new files per debounced save (revisions/N.json, revisions/N.meta.json, patches/N.jsonl) plus an atomic rewrite of current-revision.json, at a 450ms debounce, with no pruning anywhere. The plan understates the cost though — it is not just file count. list_revision_records reads and parses every meta file, and runs twice per save and twice per session open, so save latency grows linearly with edit history.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/artifact_store.rs:268-321, 345-380; src-tauri/src/authoring_v2_commands.rs:127-129,149-167,712,720; src/features/editor/useCanonicalEditor.ts:13"
      },
      {
        "claim": "§4.4: 数据库保存 canonical_ds_json + current_edit_version；新表 library_item_recovery_v1 / editor_journal_v1",
        "reality": "None of it exists. grep for library_item_recovery_v1, editor_journal_v1, canonical_ds_json, current_edit_version, edit_version across src-tauri/src returns nothing. The V2 canonical DS lives only on disk under jobs/<id>/; the DB's library_item_revisions.payload_json is documented as holding the V1 ReadingAuthoringIR / WritingJob body, and the eight tables are library_meta, exams, library_items, library_item_revisions, ingest_jobs, source_assets, publish_records, job_artifact_index. Nothing writes the V2 DS into SQLite.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/db.rs:79-190 (schema listing; library_item_revisions at 129-141); grep across src-tauri/src for the five identifiers returns no hits"
      },
      {
        "claim": "§9.5: apply_editor_commands(itemId, baseVersion, commands) 在 db.transaction 中加载当前 DS、验证 baseVersion",
        "reality": "No such command. The real surface is apply_authoring_v2_patches(input: {jobId, baseRevision, patches: Vec<Value>}) and there is no DB transaction — atomicity comes from an fs2 exclusive lock on jobs/<id>/authoring/revision.lock plus create_new file writes. The optimistic-concurrency semantics the plan wants are however already correct: baseRevision is re-verified under the lock, and the debounce/base-version/\"已保存\" frontend half is fully built (useCanonicalEditor.ts, which even cites §9.5 in its header comment) and already handles revision_conflict.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:38-44,123-168; src-tauri/src/artifact_store.rs:248-266; src/features/editor/useCanonicalEditor.ts:7-13,93-141"
      },
      {
        "claim": "§9.5: 运行轻量增量校验 (validate_changed_targets(&ds, commands.targets()))",
        "reality": "Wrong. refresh_quality_report re-evaluates the entire document every save via evaluate_quality, which runs all seven document validators, every task-group evaluation, coverage-ledger computation, AND both compiler probes (the V2 runtime compiler and the V1-compatibility compiler). No patch-target list is threaded anywhere. It also skips refresh_group_quality, so per-task quality drifts stale after the first save.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/authoring_v2_commands.rs:744-755; src-tauri/src/ielts_grammar/quality.rs:27-56,164-180; src-tauri/src/ielts_grammar/mod.rs:377 and 2058 (refresh_group_quality is private and called only at shadow build)"
      },
      {
        "claim": "§13.4: Typed PublishCheckResultV1 { passed, blockers, warnings } — 前端不再解析 authoring_v2_export_blocked:... 长字符串",
        "reality": "Not started, and the string surface got worse since the plan was written. No PublishCheckResultV1/PublishBlocker type exists in Rust. Commit bb978be (Sep 4) added 7 new blocker spellings to a frontend parser that only recognises 4, and src/api/publishClient.ts:31-33 maps every blocked error to one incorrect sentence about missing stems/options/answers. src/api/publishClient.ts:9 already admits this in a comment.",
        "verdict": "confirmed",
        "evidence": "grep for PublishCheckResultV1/PublishBlocker in src-tauri/src: no hits; src/pages/ExportPage.tsx:56-68; src/api/publishClient.ts:9,29-44"
      },
      {
        "claim": "§17.15: asset blob 逻辑迁到全局 content-addressed asset store；atomic write helper 下沉到 util/atomic.rs",
        "reality": "There is no asset blob logic to migrate. assets/blobs, assets/metadata, assets/previews and preview/runtime and legacy/ are created by ensure_job_artifact_layout and then never touched by any code in the crate — the only asset I/O is materialize_authoring_assets copying descriptor.relative_path (typically assets/<name>) from the job dir into the export staging dir, and resolve_authoring_asset_preview reading it back. The atomic-write helper (write_bytes_atomic, with a backup-and-restore replace_file) is still private inside artifact_store.rs; util/atomic.rs does not exist.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/artifact_store.rs:29-36,147-163,504-542,598-641; src-tauri/src/authoring_v2_commands.rs:640-691; grep for the five dir fields outside artifact_store.rs returns no hits"
      },
      {
        "claim": "§17.15: 保留读取现有 revision 的迁移能力（短期）",
        "reality": "Already true. read_revision, list_revision_records, read_current_revision and recover_current_revision are self-contained readers over the on-disk layout, and inspect_job_artifacts (384-405) is an explicitly read-only diagnostic that never creates directories — its test at 871-887 asserts a legacy V1 job is not mutated.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/artifact_store.rs:197-238,337-405,871-887"
      },
      {
        "claim": "§17.16: 拆分为 editor/{commands,apply,session}.rs、publish/preflight.rs、library/assets.rs",
        "reality": "Not started. One 2551-line / 97,587-byte file holds session load, the 19-op patch vocabulary, all traversal helpers, the publish preflight, the export transaction and asset materialization, plus a 549-line test module.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/authoring_v2_commands.rs (2551 lines; tests begin at 2003)"
      },
      {
        "claim": "Orientation fact given to me: the workspace saves through the EXISTING commands get_authoring_v2 / apply_authoring_v2_patches, and library_items.id == job.job_id",
        "reality": "Confirmed on both counts. useCanonicalEditor calls getAuthoringV2(itemId) and applyAuthoringV2Patches({jobId: itemId, ...}) and the code comments the id equivalence explicitly at line 106 and at publishClient.ts:49. The backend accepts it because safe_job_dir only requires job_id to be a safe single path segment.",
        "verdict": "confirmed",
        "evidence": "src/features/editor/useCanonicalEditor.ts:67,106-107; src/api/publishClient.ts:48-50; src-tauri/src/util.rs:14-47"
      }
    ],
    "keyFiles": [
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "role": "The whole editor-session/patch/publish-preflight/export subsystem in one file: get_authoring_v2_core, resolve_authoring_asset_preview_core, apply_authoring_v2_patches_core, validate_authoring_v2_publish_readiness, export_authoring_v2_core, the 19-op patch vocabulary, the publish marker scanner, and asset materialization.",
        "sizeNote": "2551 lines / 97,587 bytes; production code ends at 2001, #[cfg(test)] mod tests spans 2003-2551. Last touched Sep 4 by commit bb978be, which added 404 lines here (the entire publish-readiness function)."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "role": "On-disk V2 job layout and the revision store: JobArtifactPaths, append_revision (the lock + optimistic-concurrency core), recover_current_revision, read_revision, list_revision_records, canonical/atomic write helpers, inspect_job_artifacts.",
        "sizeNote": "888 lines / 30,470 bytes; 6 concurrency/recovery tests at 643-888 that pin the exact revision_conflict string."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\quality.rs",
        "role": "evaluate_quality — the sole producer of the QualityReportV2 that gates publish. Contains the readiness-state formula (187-199) that makes `ready` require a literally empty issues list.",
        "sizeNote": "~3650 lines of production code plus a very large test module; issue codes live in the sibling issue_codes.rs (53 codes)."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\mod.rs",
        "role": "build_authoring_v2_shadow / write_authoring_v2_shadow — creates jobs/<id>/authoring-ir-v2.shadow.json, the revision-0 session source. Hardcodes audit.humanVerified=false (369) and taskGroups[].reviewState=\"unreviewed\" (331). Owns the private refresh_group_quality (2058).",
        "sizeNote": "SHADOW_ARTIFACT_FILE/SHADOW_COMPARE_FILE/SHADOW_ERROR_FILE consts at 56-58; real_pdf_acceptance is #[cfg(test)]-gated at 23-24."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\source_review.rs",
        "role": "SourceReviewV1 status/fingerprint/issues — four of the publish blockers come from here. Its doc-absent branch returns the saved file verbatim (126-130), which is what freezes a stale review after cleanup.",
        "sizeNote": "312 lines; modified by bb978be (+36/-?)."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\nas_package_v2.rs",
        "role": "Second consumer of validate_authoring_v2_publish_readiness. validate_v2_export_binding (56-206) re-verifies the export receipt, rebinds the exported authoring to the persisted shadow/revision with `quality` stripped, and requires an exact publishProof match.",
        "sizeNote": "79,984 bytes; the revision-0-vs-N persisted-path switch is at 154-159."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\schema\\quality_report_v2.rs",
        "role": "Typed QualityReportV2 / ReviewIssueV2 / ReadinessStateV2 / ReviewSeverityV2 / SuggestedActionV2 that the publish gate deserializes. ReviewIssueV2.details is BTreeMap<String,Value>, which is how `resolution: resolved|ignored` is stored.",
        "sizeNote": "153 lines; every struct is #[serde(deny_unknown_fields)]."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\schema\\ielts_authoring_v2.rs",
        "role": "IeltsAuthoringIRV2 and AuthoringAuditV2 (deny_unknown_fields, exactly revision/source/humanVerified/llmUsed/updatedAt/notes) — the canonical DS contract every patch must keep valid.",
        "sizeNote": "546 lines; IeltsAuthoringIRV2 at 521-540, AuthoringAuditV2 at 509-519."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\cleanup.rs",
        "role": "Deletes document-ir.json and pipeline-report.json (124-139, 184-198), which silently changes what the publish gate scans and how source review resolves. Never prunes authoring/revisions or authoring/patches.",
        "sizeNote": "213 lines; write_authoring_project hard-requires the V1 authoring-ir.json via read_json (45), so it errors on a V2-only job."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\lib.rs",
        "role": "Tauri command wrappers for all four commands (1008-1034) plus get_job_artifact_status (1069-1073) and the invoke_handler registration list (~1435-1460).",
        "sizeNote": "449,181 bytes — plan P8-T04 wants it split; only the ~60 lines above are relevant here."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src\\features\\editor\\useCanonicalEditor.ts",
        "role": "The already-built §9.5 frontend save chain: 450ms debounce, optimistic baseRevision, revision_conflict handling, undo/redo via inverse patches, write-only localStorage recovery snapshot.",
        "sizeNote": "245 lines."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src\\pages\\ExportPage.tsx",
        "role": "parseV2ExportBlock — the 4-kind regex that must become a typed-result consumer.",
        "sizeNote": "regex at line 60; buildV2ExportDiagnostics at 71-110."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src\\api\\publishClient.ts",
        "role": "publishItem/describePublishError — the workspace publish button's path; collapses all publish-gate errors into one wrong sentence and already documents the §13.4 gap in its header comment.",
        "sizeNote": "~70 lines for the relevant part."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src\\services\\devFallbackBackend.ts",
        "role": "The only source of session source==\"fixture\" and a softer, divergent export gate. Any green run through this proves nothing about publish readiness.",
        "sizeNote": "phase5Session 2479-2497; the three command branches 2548-2600."
      },
      {
        "path": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\real_pdf_acceptance.rs",
        "role": "The only end-to-end shadow→save→save→export test (run_phase5_real_pdf_edit_export, 1584-1690). Not updated by the commit that added the new publish gates.",
        "sizeNote": "#[cfg(test)] only; requires fixtures/golden/private-real/*.pdf, which are gitignored and absent in this checkout."
      }
    ],
    "publicApi": [
      {
        "symbol": "get_authoring_v2_core",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) fn get_authoring_v2_core(root: &Path, job_id: &str) -> CommandResult<Value>",
        "notes": "Line 54. Body is only `safe_job_dir(root, job_id)?; build_editor_session(root, job_id)`. CommandResult<T> = Result<T, String> (lib.rs:90)."
      },
      {
        "symbol": "resolve_authoring_asset_preview_core",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) fn resolve_authoring_asset_preview_core(\n    root: &Path,\n    job_id: &str,\n    asset_id: &str,\n) -> CommandResult<Value>",
        "notes": "Lines 59-63. Returns {assetId, mime, widthPx, heightPx, resourceUri: \"data:<mime>;base64,...\"}. Image-only; verifies byte_length and sha256 against the manifest and re-checks path containment after canonicalize."
      },
      {
        "symbol": "apply_authoring_v2_patches_core",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) fn apply_authoring_v2_patches_core(root: &Path, input: Value) -> CommandResult<Value>",
        "notes": "Line 123. Returns the session object plus `savedPatchCount`. Max 200 patches; empty batch rejected."
      },
      {
        "symbol": "validate_authoring_v2_publish_readiness",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) fn validate_authoring_v2_publish_readiness(\n    root: &Path,\n    job_id: &str,\n    revision: u64,\n    authoring_value: &Value,\n) -> CommandResult<Value>",
        "notes": "Lines 174-179. Ok returns the AuthoringV2PublishProofV1 object {schemaVersion, jobId, revision, qualityState:\"ready\", humanVerified:true, sourceReview, unresolvedAnswers:[], unresolvedBlockingIssues:[], aiFallbacks:[], partialFailures:[]} (319-330). Called from export_authoring_v2_core:488 and nas_package_v2.rs:201."
      },
      {
        "symbol": "export_authoring_v2_core",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) fn export_authoring_v2_core(root: &Path, input: Value) -> CommandResult<Value>",
        "notes": "Line 449. Returns {receipt, history, outputDir, revision, examId}."
      },
      {
        "symbol": "ApplyAuthoringV2PatchesInput",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "#[derive(Debug, Deserialize)]\n#[serde(rename_all = \"camelCase\")]\npub(crate) struct ApplyAuthoringV2PatchesInput {\n    pub job_id: String,\n    pub base_revision: u64,\n    pub patches: Vec<Value>,\n}",
        "notes": "Lines 38-44. Patches are untyped Value and validated only by apply_patch's per-op code."
      },
      {
        "symbol": "ExportAuthoringV2Input",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "#[derive(Debug, Deserialize)]\n#[serde(rename_all = \"camelCase\")]\npub(crate) struct ExportAuthoringV2Input {\n    pub job_id: String,\n    pub export_dir: String,\n    pub revision: Option<u64>,\n}",
        "notes": "Lines 46-52. export_dir must be absolute (497-499)."
      },
      {
        "symbol": "AUTHORING_V2_SHADOW_FILE / DOCUMENT_V2_SHADOW_FILE / SESSION_SCHEMA_VERSION",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "pub(crate) const AUTHORING_V2_SHADOW_FILE: &str = \"authoring-ir-v2.shadow.json\";\nconst DOCUMENT_V2_SHADOW_FILE: &str = \"document-ir-v2.shadow.json\";\nconst SESSION_SCHEMA_VERSION: &str = \"AuthoringEditorSessionV1\";",
        "notes": "Lines 34-36. AUTHORING_V2_SHADOW_FILE duplicates ielts_grammar::SHADOW_ARTIFACT_FILE (mod.rs:56) — two consts, same literal."
      },
      {
        "symbol": "build_editor_session (private)",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn build_editor_session(root: &Path, job_id: &str) -> CommandResult<Value>",
        "notes": "Lines 711-723. Emits {schemaVersion:\"AuthoringEditorSessionV1\", jobId, authoring, revision, source, revisions, v1FilesRemainReadable:true}; `source` is `if revision > 0 { \"revision\" } else { \"shadow\" }` — never \"fixture\"."
      },
      {
        "symbol": "load_current_authoring (private)",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn load_current_authoring(root: &Path, job_id: &str) -> CommandResult<(Value, u64)>",
        "notes": "Lines 725-742. The single shadow-vs-revision decision point, shared by all four commands."
      },
      {
        "symbol": "refresh_quality_report (private)",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn refresh_quality_report(root: &Path, job_id: &str, authoring: &mut Value) -> CommandResult<()>",
        "notes": "Lines 744-755. Gates the physical shadow on physical_shadow_matches_authoring (757-778: schemaVersion==DocumentIRV2, jobId match, documentId==sourceDocumentId, at least one shared sourceFileId) and re-applies user resolutions via preserve_issue_resolutions (780-828)."
      },
      {
        "symbol": "collect_publish_gate_markers (private)",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn collect_publish_gate_markers(\n    value: &Value,\n    path: &str,\n    ai_fallbacks: &mut Vec<String>,\n    partial_failures: &mut Vec<String>,\n)",
        "notes": "Lines 333-437. `let lower_path = path.to_ascii_lowercase();` at 339 is the PARENT path used for all gate tests on that object's children."
      },
      {
        "symbol": "mark_user_audit (private)",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn mark_user_audit(document: &mut Value, revision: u64)",
        "notes": "Lines 839-846. Sets revision, source=\"user\", humanVerified=false, updatedAt. Silently no-ops if `audit` is not an object."
      },
      {
        "symbol": "apply_patch (private) — the 19-op vocabulary",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\authoring_v2_commands.rs",
        "signature": "fn apply_patch(document: &mut Value, patch: &Value) -> CommandResult<()>",
        "notes": "Lines 848-875. Ops: replaceText, setNodeAttrs, replaceContent, insertNode, deleteNode, moveNode, cropAsset, setHotspot, removeHotspot, setTaskType, setQuestionExpression, setResponseCardinality, setResponseGroup, setOptionBank, insertAnswerSlot, deleteAnswerSlot, setAnswer, bindSource, resolveIssue; anything else → AUTHORING_PATCH_UNSUPPORTED:<op>. Content targets are kind ∈ {node, passage, taskInstructions, taskStimulus, responsePrompt, option} mapping to keys children/content/instructions/stimulus/prompt (1576-1583)."
      },
      {
        "symbol": "append_revision",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "pub(crate) fn append_revision(\n    root: &Path,\n    job_id: &str,\n    base_revision: u64,\n    source: RevisionSourceV2,\n    artifact: &Value,\n    patches: &[Value],\n) -> CommandResult<RevisionWriteResultV2>",
        "notes": "Lines 240-247. The only caller in the crate is apply_authoring_v2_patches_core:149. Emits `revision_conflict:current={}:base={}` at 261-266 under the exclusive lock."
      },
      {
        "symbol": "recover_current_revision",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "pub(crate) fn recover_current_revision(\n    root: &Path,\n    job_id: &str,\n) -> CommandResult<CurrentRevisionV2>",
        "notes": "Lines 210-213. Repairs a lagging pointer by writing current-revision.json; errors `current_revision_missing_record:current=..:latest=..` if the pointer is ahead of the newest meta record."
      },
      {
        "symbol": "read_current_revision / read_revision / list_revision_records",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "pub(crate) fn read_current_revision(root: &Path, job_id: &str) -> CommandResult<CurrentRevisionV2>\npub(crate) fn read_revision(root: &Path, job_id: &str, revision: u64) -> CommandResult<Value>\npub(crate) fn list_revision_records(\n    root: &Path,\n    job_id: &str,\n) -> CommandResult<Vec<RevisionRecordV2>>",
        "notes": "Lines 197, 337, 345-348. read_revision rejects revision 0 with `revision_zero_has_no_artifact` and performs no hash verification. list_revision_records skips any N.json without a sibling N.meta.json and sorts by revision."
      },
      {
        "symbol": "JobArtifactPaths",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "#[derive(Debug, Clone)]\npub(crate) struct JobArtifactPaths {\n    pub(crate) job_dir: PathBuf,\n    pub(crate) sources_dir: PathBuf,\n    pub(crate) extraction_dir: PathBuf,\n    pub(crate) authoring_dir: PathBuf,\n    pub(crate) revisions_dir: PathBuf,\n    pub(crate) patches_dir: PathBuf,\n    pub(crate) assets_dir: PathBuf,\n    pub(crate) asset_blobs_dir: PathBuf,\n    pub(crate) asset_metadata_dir: PathBuf,\n    pub(crate) asset_previews_dir: PathBuf,\n    pub(crate) preview_dir: PathBuf,\n    pub(crate) preview_runtime_dir: PathBuf,\n    pub(crate) export_history_dir: PathBuf,\n    pub(crate) legacy_dir: PathBuf,\n}",
        "notes": "Lines 21-37; for_job at 39-71 maps to jobs/<id>/{sources,extraction,authoring,authoring/revisions,authoring/patches,assets,assets/blobs,assets/metadata,assets/previews,preview,preview/runtime,export-history,legacy}. Path helpers: current_revision_path()=authoring/current-revision.json, revision_path(n)=authoring/revisions/{n}.json, revision_meta_path(n)=authoring/revisions/{n}.meta.json, patch_path(n)=authoring/patches/{n}.jsonl (73-87)."
      },
      {
        "symbol": "CurrentRevisionV2 / RevisionRecordV2 / RevisionWriteResultV2 / ArtifactReceiptV2 / RevisionSourceV2",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "pub(crate) struct CurrentRevisionV2 { pub schema_version: String, pub layout_version: String, pub job_id: String, pub revision: u64, pub updated_at: String }\npub(crate) struct RevisionRecordV2 { pub schema_version: String, pub layout_version: String, pub job_id: String, pub revision: u64, pub parent_revision: u64, pub source: RevisionSourceV2, pub created_at: String, pub artifact_path: String, pub artifact_sha256: String, pub patch_path: Option<String>, pub patch_sha256: Option<String> }\npub(crate) struct RevisionWriteResultV2 { pub current: CurrentRevisionV2, pub record: RevisionRecordV2, pub artifact: ArtifactReceiptV2, pub patch: Option<ArtifactReceiptV2> }\npub(crate) struct ArtifactReceiptV2 { pub relative_path: String, pub byte_length: u64, pub sha256: String, pub canonical_json: bool }\npub(crate) enum RevisionSourceV2 { AutoExtract, User, Migration }",
        "notes": "Lines 90-140, all #[serde(rename_all=\"camelCase\")] (RevisionSourceV2 is snake_case). Consts: JOB_ARTIFACT_LAYOUT_VERSION=\"JobArtifactLayoutV1\", CURRENT_REVISION_SCHEMA_VERSION=\"JobCurrentRevisionV1\", REVISION_RECORD_SCHEMA_VERSION=\"AuthoringRevisionRecordV1\" (17-19); validated on read at 417-443."
      },
      {
        "symbol": "ensure_job_artifact_layout / write_artifact_json / write_canonical_json_atomic / canonical_json_hash / inspect_job_artifacts",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\artifact_store.rs",
        "signature": "pub(crate) fn ensure_job_artifact_layout(\n    root: &Path,\n    job_id: &str,\n) -> CommandResult<JobArtifactPaths>\npub(crate) fn write_artifact_json(\n    root: &Path,\n    job_id: &str,\n    relative_path: &str,\n    value: &Value,\n) -> CommandResult<ArtifactReceiptV2>\npub(crate) fn write_canonical_json_atomic(\n    path: &Path,\n    value: &Value,\n) -> CommandResult<ArtifactReceiptV2>\npub(crate) fn canonical_json_hash(value: &Value) -> CommandResult<String>\npub(crate) fn inspect_job_artifacts(root: &Path, job_id: &str) -> CommandResult<Value>",
        "notes": "Lines 142-145, 180-185, 172-175, 167, 384. write_artifact_json enforces validate_v2_namespace: first path segment must be one of sources|extraction|authoring|assets|preview|export-history|legacy (469-496)."
      },
      {
        "symbol": "Tauri command wrappers",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\lib.rs",
        "signature": "#[tauri::command]\nasync fn get_authoring_v2(job_id: String, app: AppHandle) -> CommandResult<Value>\n\n#[tauri::command]\nasync fn resolve_authoring_asset_preview(\n    job_id: String,\n    asset_id: String,\n    app: AppHandle,\n) -> CommandResult<Value>\n\n#[tauri::command]\nasync fn apply_authoring_v2_patches(input: Value, app: AppHandle) -> CommandResult<Value>\n\n#[tauri::command]\nasync fn export_authoring_v2(input: Value, app: AppHandle) -> CommandResult<Value>",
        "notes": "Lines 1008-1034; each is a two-line wrapper that resolves `app_root(&app)?` and delegates. Registered in the invoke_handler at 1435-1438. Frontend wrappers at src/api/tauriCommands.ts:142-165 (note apply/export pass a nested `{ input }`)."
      },
      {
        "symbol": "compile_reading_source_v2 / CompilerIssueV2",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\reading_source_v2.rs",
        "signature": "pub(crate) fn compile_reading_source_v2(\n    source: &IeltsAuthoringIRV2,\n) -> Result<ReadingExamSourceV2, Vec<CompilerIssueV2>>\n\npub(crate) struct CompilerIssueV2 {\n    pub code: String,\n    pub message: String,\n    pub target_id: String,\n}",
        "notes": "Lines 79-81 and 64-68. Called by export (489) and, indirectly, twice per quality evaluation via evaluate_compiler_probes. Already a typed issue list — the natural donor shape for PublishBlocker."
      },
      {
        "symbol": "evaluate_quality",
        "file": "F:\\workspace\\PDF2Test\\src-tauri\\src\\ielts_grammar\\quality.rs",
        "signature": "pub(crate) fn evaluate_quality(authoring: &Value, physical_shadow: Option<&Value>) -> Value",
        "notes": "Line 27, re-exported as crate::ielts_grammar::evaluate_quality (mod.rs:26). Returns an untyped Value that is later deserialized into QualityReportV2 by the publish gate — a schema drift risk since deny_unknown_fields applies on the way back in."
      }
    ],
    "subsystem": "authoring V2 editor session, patches, revisions, publish readiness (src-tauri/src/authoring_v2_commands.rs + src-tauri/src/artifact_store.rs)",
    "surprises": [
      "V2 publish is currently unreachable, not merely strict. validate_authoring_v2_publish_readiness requires /audit/humanVerified == true (authoring_v2_commands.rs:255-261), but mark_user_audit forces it to false on every successful save (843) and the shadow builder hardcodes false (ielts_grammar/mod.rs:369). No patch op can reach the audit object and is_safe_node_attribute does not list humanVerified. The only document in the repo with humanVerified:true is the hand-authored golden fixture fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json, which is why the readiness unit test passes while the real pipeline cannot.",
      "Even without the humanVerified gate, publish would still be blocked earlier: quality.state == \"ready\" requires `issues.is_empty()` literally (ielts_grammar/quality.rs:187-199), and refresh_quality_report re-runs evaluate_quality on every save. The resolveIssue patch op writes details.resolution, and preserve_issue_resolutions carries it forward, but nothing removes the issue from the list — so resolving every issue still yields review_required. The publish gate's own blocker list (233-254) honours resolutions; the state check does not. Two contradictory notions of 'resolved' in one function.",
      "The plan's 'recursively greps historical JSON for fallback/partial strings' is directionally right but mechanically wrong, and the correction changes the fix. It is key-name driven with substring path gates, and it reads exactly two historical files. The genuinely broken rules are the three with NO path gate: requiresManualQuestionImport (a plain non-optional bool on every V1 question and group), blockedAutoApplyGroups/lowConfidenceGroups, and code == PARTIAL_RECOVERY_FAILURE. Deleting a string grep would miss all three.",
      "The path gates are substring tests on an accumulated dotted path, so unrelated field names open them. `lower_path.contains(\"ai\")` is satisfied by \"remaining\" — pipeline-report.json.authoring.remainingReviewItems (written at auto_pipeline.rs:2657) unlocks the status/failure rules for everything beneath it. Same class of hazard for any future field containing 'ai', 'note', 'llm' or 'quality' as a substring.",
      "Publish-gate behaviour depends on whether cleanup has run. cleanup.rs:124-139 deletes both scanned files. Removing pipeline-report.json silently disarms the llm marker rules; removing document-ir.json flips source_review_status into its 'return the saved file verbatim' branch (source_review.rs:126-130), which can freeze stale=true forever. The same publish attempt can therefore pass or fail depending on artifact-retention settings.",
      "Revision cost is O(N) per keystroke-batch, not O(1). list_revision_records reads and JSON-parses every *.meta.json and is called twice per save (append_revision→recover_current_revision, then apply_authoring_v2_patches_core:164) and twice per session open. The full record vector is also returned in every session payload. At a 450ms debounce this means save latency and response size both grow linearly with edit history.",
      "Five of the fourteen JobArtifactPaths directories are pure dead scaffolding: assets/blobs, assets/metadata, assets/previews, preview/runtime and legacy/ are created on every ensure_job_artifact_layout call and referenced nowhere else in the crate. Plan 17.15 asks to 'migrate the asset blob logic' — there is none to migrate.",
      "Two unrelated directories are both called 'revisions'. authoring_commands.rs:113-121 archives V1 drafts into jobs/<id>/revisions/<timestamp>-source-replaced/, while the V2 store uses jobs/<id>/authoring/revisions/<N>.json. The publish scan reads only top-level authoring-ir.json and pipeline-report.json, so the V1 archive copies are not scanned — but the naming collision will mislead anyone writing cleanup or migration code.",
      "get_authoring_v2 is not read-only. recover_current_revision writes a repaired current-revision.json when the pointer lags the newest meta record (artifact_store.rs:226-237). Any plan to move the pointer into SQLite must keep both the repair and the opposite case, which hard-errors with current_revision_missing_record when the pointer is ahead.",
      "The session `source: \"fixture\"` variant in the shared TypeScript union (src/types/authoring-editor-v2.ts:15) is produced only by the browser dev fallback (devFallbackBackend.ts:2492). The Rust backend emits only \"shadow\" or \"revision\". The dev fallback's export gate is also materially softer than Rust's — it whitelists away ANSWER_KEY_MISSING_SLOT and RUNTIME_COMPILER_FAILED, spells the blocker `blocking_issues=` instead of `issues=`, and has no humanVerified, source-review or marker checks — so any publish-readiness confidence gained in dev mode is false.",
      "The new workspace never shows the backend quality report. src/features/editor/actionableIssues.ts derives its own six-code issue list from the current DS and ignores session.authoring.quality entirely; ExamWorkspacePage imports only deriveActionableIssues and blockerCount. So a user can clear every issue the UI shows and still be blocked by quality.state, and publishClient.ts:31-33 will explain the failure as 'missing stems, options or answers' regardless of the real cause.",
      "TaskGroupV2.review_state (Unreviewed/Confirmed/Edited) is written once as \"unreviewed\" at shadow build and is never read or updated by anything — no patch op touches it and the publish gate ignores it. Likewise SuggestedActionV2::ReplaceAsset can be emitted as a suggested action but there is no replaceAsset patch op, so that suggestion is unactionable.",
      "refresh_quality_report updates the document-level quality but never calls refresh_group_quality, which is private to ielts_grammar/mod.rs and runs only at shadow build (mod.rs:377). Per-task taskGroups[i].quality.score and .hardFailures therefore go stale the moment a user saves once.",
      "The frontend's crash-recovery snapshot is write-only: useCanonicalEditor writes it to localStorage only when a save fails and deletes it on success, and nothing ever reads the key back (only the constant declaration at line 14 matches a repo-wide grep).",
      "read_revision loads revisions/<N>.json with no verification against the artifact_sha256 recorded in the sibling meta file, so a hand-edited or corrupted revision is accepted as long as it still deserializes as IeltsAuthoringIRV2."
    ],
    "testHarness": "RUST UNIT TESTS (I did not execute them — instructed not to run cargo).\\n\\nauthoring_v2_commands.rs `#[cfg(test)] mod tests` (2003-2551), 14 tests. Two shared builders: text_document() (2029-2041) and structured_document() (2043-2075) — hand-rolled minimal JSON, not full IeltsAuthoringIRV2, used for pure apply_patch tests. temp_asset_dirs() (2017-2027) makes a nanosecond-suffixed dir under std::env::temp_dir(). Patch-vocabulary tests: replace_text_uses_character_offsets_and_marks_provenance, expression_expansion_is_bounded_and_deterministic, unsupported_node_attributes_are_rejected, structural_content_patches_support_insert_move_delete_crop_hotspot_and_cardinality, invalid_crop_and_hotspot_rects_are_rejected, replace_content_cannot_drop_an_existing_answer_slot, inserted_answer_slot_can_be_removed_only_by_an_explicit_inverse_patch, semantic_option_bank_and_answer_slot_patches_keep_registries_in_sync, undo_patch_can_restore_provenance_without_marking_user_edited. Command-level tests that need a fake app root: export_materializes_and_verifies_non_empty_assets (2343), authoring_asset_preview_resolves_only_manifest_backed_image_bytes (2376), export_rejects_non_ready_quality_state_before_materialization (2422), publish_readiness_rejects_human_unverified_stale_review_and_ai_fallback (2461), quality_refresh_rejects_stale_physical_shadow_and_preserves_resolution (2519).\\n\\nTHE PATTERN TO COPY for any new session/publish test (from 2376-2420 and 2461-2517): build `root = std::env::temp_dir().join(format!(\\\"ielts-authoring-v2-<name>-{suffix}\\\"))` with a nanos suffix; create `root.join(\\\"jobs\\\").join(job_id)`; `serde_json::from_str(include_str!(\\\"../../fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json\\\"))` as the base DS; override `authoring[\\\"jobId\\\"] = json!(job_id)` and `authoring[\\\"quality\\\"][\\\"state\\\"] = json!(\\\"ready\\\")`; write it to `job_dir.join(AUTHORING_V2_SHADOW_FILE)`; call the *_core function with `&root`; `let _ = fs::remove_dir_all(root)` at the end. The fixture is the load-bearing input — it ships `audit.humanVerified: true`, `quality.state: \\\"review_required\\\"`, 1 issue and 0 hardFailures, so tests that call validate_authoring_v2_publish_readiness DIRECTLY pass (no quality refresh) while tests that go through export_authoring_v2_core get quality recomputed to review_required and assert exactly `\\\"authoring_v2_export_blocked:quality_state=review_required\\\"` (2453-2456). Two tests deliberately assert the marker and stale-review blockers: 2487-2506 writes a source-review.json with stale:true and expects `authoring_v2_export_blocked:source_review_stale`; 2509-2515 sets `authoring[\\\"audit\\\"][\\\"notes\\\"] = json!([\\\"AI fallback was used\\\"])` and expects a prefix match on `authoring_v2_export_blocked:ai_fallback=`. Any change to the marker scanner or the gate ordering must update these two.\\n\\nartifact_store.rs tests (643-888), 7 tests, temp_root() = temp_dir().join(format!(\\\"phase1-artifact-{}\\\", Uuid::new_v4().simple())). These are the concurrency contract and are the most valuable to keep: revisions_are_optimistic_and_recoverable (702) exercises base-mismatch rejection and pointer recovery; append_with_missing_pointer_never_overwrites_revision_one (756) asserts the exact string `\\\"revision_conflict:current=1:base=0\\\"`; concurrent_base_zero_writers_cannot_replace_each_other (794) spawns two threads on a Barrier and asserts exactly one Ok and one Err with a single surviving record; incomplete_revision_number_is_quarantined_by_monotonic_allocation (829) plants a bare revisions/1.json and asserts the next allocation is 2; artifact_relative_paths_cannot_escape_job (861) asserts \\\"../outside.json\\\", \\\"C:/outside.json\\\", \\\"document-ir.json\\\" and \\\"uploads/source.pdf\\\" are all rejected; status_is_read_only_for_a_legacy_job (871) asserts inspect_job_artifacts creates no authoring/ dir.\\n\\nEND-TO-END. src-tauri/src/ielts_grammar/real_pdf_acceptance.rs (#[cfg(test)] via mod.rs:23-24) holds the only full loop: run_phase5_real_pdf_edit_export (1584-1690) writes the shadow, applies phase5_answer_resolution_patches at baseRevision 0, applies one replaceText at baseRevision 1, then calls export_authoring_v2_core with the resulting revision and asserts manifest/authoring/runtime schemaVersions. Driven by `#[test] fn phase5_real_pdf_edit_and_v2_export_round_trip` (1692). It cannot run here: it needs fixtures/golden/private-real/chili-peppers.pdf, and that directory contains only README.md plus a .gitignore for *.pdf. It also was not updated by commit bb978be, which added the humanVerified/source-review/marker gates to the export path, so it needs fixing in the same change.\\n\\nGAPS. There is no test for get_authoring_v2_core / build_editor_session at all (no test asserts source==\\\"shadow\\\" vs \\\"revision\\\", nor the AUTHORING_V2_NOT_AVAILABLE:shadow_missing error). There is no test for the two ungated marker vectors that matter in production (requiresManualQuestionImport in authoring-ir.json, blockedAutoApplyGroups/lowConfidenceGroups in pipeline-report.json) — the only marker test uses audit.notes. There is no test that a fully resolved issue set reaches quality.state == ready, which is precisely the contradiction to fix. Frontend: no vitest/jest file covers useCanonicalEditor or actionableIssues (src/features/editor contains only the three .tsx/.ts sources)."
  },
  {
    "subsystem": "DocumentIRV2 physical facts (schema/document_ir_v2.rs + pdf_facts_shadow.rs + pdf_ingest/* + docx_facts_shadow.rs + docx_ingest/*)",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/document_ir_v2.rs",
        "role": "THE typed contract. 476 lines, 20 structs/enums, every struct `#[serde(rename_all=\"camelCase\")] #[serde(deny_unknown_fields)]`. This is the recognizer's input type.",
        "sizeNote": "476 lines / 14.7 KB — small, safe to read whole"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/schema/common.rs",
        "role": "RectV2, QuadV2, CoordinateUnitV2/OriginV2, SourceAnchorV2, SourceVariantV2, TextStyleV2, AssetDescriptorV2, DiagramQuestionRegionV2, ExtractionModeV2, canonical_json_bytes. Also deny_unknown_fields throughout.",
        "sizeNote": "256 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "role": "PDF producer. Implements pdf_extract::OutputDev to emit glyphs+vectorPaths as raw serde_json::Value, then collects images/vector renders/annotations/image placements/marked content, calls pdf_ingest::enrich_page per page, builds the coverage ledger, and finally validates the whole thing through serde_json::from_value::<DocumentIRV2>. Also owns the 3-component atomic commit transaction and the debug overlay command.",
        "sizeNote": "~4050 lines / 157 KB — read by region, not whole"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/mod.rs",
        "role": "`enrich_page(&mut Value) -> PageLayerSummary` — the single orchestrator that turns glyphs+vectorPaths into spans/lines/regions/tables/readingOrder/readingOrderGraph. Also Bounds geometry helpers, `rect()` (note: NO `normalized`), visual/annotation region synthesis, OCR plan summary, SVG overlay renderer.",
        "sizeNote": "~880 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/line_builder.rs",
        "role": "Glyph→span→line. Baseline clustering with font-relative tolerance, horizontal/vertical orientation split, gap-based span splitting, inlineGapsPt, indentationPt, lineHeightPt.",
        "sizeNote": "586 lines incl. 2 tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/region_builder.rs",
        "role": "Column/gutter detection (projection occupancy bins) + section banding + paragraph-gap region splitting + region_kind heuristic. Emits columnIndex/sectionIndex.",
        "sizeNote": "379 lines, no tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/reading_order.rs",
        "role": "DAG over regions (same_column_above / column_transition / full_width_heading_to_column / source_line_sequence), topological sort with cycle fallback, primary + one alternative, writes readingOrderRank onto regions.",
        "sizeNote": "343 lines incl. 1 test"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/table_detector.rs",
        "role": "Ruled-line grid detection from vectorPaths (x/y clustering + rowSpan/colSpan by missing boundary) and a borderless text-alignment fallback. Also re-labels overlapping regions to kind=\"table\".",
        "sizeNote": "596 lines incl. 1 test"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/coordinates.rs",
        "role": "PdfPageGeometry: MediaBox/CropBox/Rotate/UserUnit → pdf_to_display 6-tuple, display_point/display_bounds/native_rect/display_rect (the ONLY place `normalized` is emitted) and page_transform_value.",
        "sizeNote": "314 lines incl. 1 rotation test"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/ocr_router.rs",
        "role": "Duplicate-glyph / hidden-text detection → quality.duplicateTextRatio + requiresOcrRegions + warnings. O(n²) over page glyphs, re-parsing bbox from JSON each comparison.",
        "sizeNote": "95 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/ocr_merge.rs",
        "role": "native/OCR glyph merge with variant preservation. DEAD in production: keyed on `_ocrProviderOutput`, which no producer in the repo ever writes.",
        "sizeNote": "278 lines, mostly tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/compare_report.rs",
        "role": "V1-vs-V2 text diff written to document-ir-v2.shadow.compare.json. Required by the commit transaction (a missing compare file aborts the commit).",
        "sizeNote": "~230 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs",
        "role": "DOCX producer with TWO mutually exclusive geometry paths: (a) render-assisted — render to PDF, run extract_pdf_facts_shadow, then rewrite anchors back to OOXML paths; (b) default — DocxPageBuilder synthesizes geometry from estimated character advance widths.",
        "sizeNote": "~2900 lines / 112 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_ingest/model.rs",
        "role": "The OOXML fact model actually available: DocxRunFormatting (bold/italic/underline/strike/fontSize/fontName/color/lang/vertAlign), DocxParagraphFormatting (indents incl. hanging, spacing, alignment, pageBreakBefore), DocxTable/Row/Cell (gridSpan, vMerge, rowSpan, widthTwips, padding, borderEvidence), DocxDrawing (EMU x/y/w/h, crop, rotation, altText), DocxTextBox, DocxSection.",
        "sizeNote": "252 lines — read whole"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_ingest/render_fallback.rs",
        "role": "`requested_from_environment()` — the EPIC8_DOCX_RENDER_ASSIST gate that decides which of the two DOCX geometry paths runs. Defaults OFF.",
        "sizeNote": "~330 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_ingest/tables.rs",
        "role": "OOXML table parse; sets row_span=0 on vMerge=\"continue\" cells and accumulates row_span onto the anchor cell.",
        "sizeNote": "~300 lines incl. tests"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_ingest/sections.rs",
        "role": "DocxSection: pgSz/pgMar/cols → the page box and margins the synthetic DOCX layout uses. Note `columns: Vec<DocxColumn>` is parsed but the layout builder ignores it.",
        "sizeNote": "~120 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_geometry.rs",
        "role": "The V1 pdfium parser — a SECOND, independent scan of the same PDF. Produces DocumentIRV1 with real per-line bboxes and the diagram question-region PNG crops that the V2 artifact later imports. Collects only (char, origin_x, origin_y): no font size, no font name, no char box.",
        "sizeNote": "~2900 lines / 175 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs",
        "role": "`semantic_lines_from_v2_shadow` — the ONLY thing that currently reads DocumentIRV2 for recognition, and it keeps only line.text + line.bbox + a squashed anchor.",
        "sizeNote": "~600 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "`significant_physical_nodes()` (line 3311+) — the existing region/line/span/glyph de-duplication + significance rule, and `source_coverage_summary`. This is the working implementation the plan's §6.11 unassigned-evidence ledger must build on, not replace.",
        "sizeNote": "~3600 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "`parse_document_core` — the only write site that builds the V2 artifact for BOTH pdf and docx (lines 184-229).",
        "sizeNote": "~620 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "Auto-import write site (lines 1486-1521) — PDF ONLY, plus `current_physical_shadow()` (line 268) and `physical_shadow_matches_source()` (line 249), the freshness guard.",
        "sizeNote": "~3000 lines / 116 KB"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "role": "`document_ir_v2_shadow_enabled() -> bool { true }` (line 219) — hardcoded on. `local_ocr_enabled()` defaults false (line 215).",
        "sizeNote": "~700 lines"
      },
      {
        "path": "F:/workspace/PDF2Test/contracts/document-ir-v2.schema.json",
        "role": "A JSON-Schema 2020-12 mirror of the Rust contract. 18 KB. NOT referenced by any Rust, TS, or script file in the repo — it is documentation, not a gate.",
        "sizeNote": "18061 bytes"
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "role": "The end-to-end golden harness: builds real physical facts via write_pdf_facts_shadow (line 1368) for the corpus in fixtures/golden/manifest.json + fixtures/golden/phase4-eight-pdf-acceptance.json.",
        "sizeNote": "~2200 lines"
      }
    ],
    "publicApi": [
      {
        "symbol": "DocumentIRV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:455",
        "signature": "#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\n#[serde(rename_all = \"camelCase\")]\n#[serde(deny_unknown_fields)]\npub struct DocumentIRV2 {\n    pub schema_version: String,\n    pub document_id: String,\n    pub job_id: String,\n    pub source_files: Vec<SourceFileRecordV2>,\n    pub pages: Vec<PageNodeV2>,\n    pub assets: Vec<AssetDescriptorV2>,\n    pub coverage_ledger: Vec<CoverageEntryV2>,\n    pub parser: ParserMetadataV2,\n}",
        "notes": "pub const DOCUMENT_IR_V2_SCHEMA_VERSION: &str = \"DocumentIRV2\" (line 9). Only method: `pub fn is_supported_schema_version(&self) -> bool` (line 470). document_id = format!(\"document-{}\", &sha256[..16]) — derived from the SOURCE FILE hash, so two jobs importing the same PDF share a documentId. job_id is the per-job key and (per your verified fact) equals library_items.id."
      },
      {
        "symbol": "PageNodeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:292",
        "signature": "pub struct PageNodeV2 {\n    pub page_index: u32,\n    pub width_pt: f64,\n    pub height_pt: f64,\n    pub rotation: u16,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub media_box: Option<RectV2>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub crop_box: Option<RectV2>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub page_transform: Option<PageTransformV2>,\n    pub glyphs: Vec<GlyphNodeV2>,\n    pub spans: Vec<SpanNodeV2>,\n    pub lines: Vec<LineNodeV2>,\n    pub regions: Vec<RegionNodeV2>,\n    pub vector_paths: Vec<VectorPathV2>,\n    pub tables: Vec<TableNodeV2>,\n    pub asset_ids: Vec<String>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub image_placements: Vec<PdfImagePlacementV2>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub marked_content: Vec<PdfMarkedContentV2>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub annotations: Vec<PdfAnnotationV2>,\n    pub reading_order: Vec<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub reading_order_graph: Option<ReadingOrderGraphV2>,\n    pub quality: PageQualityV2,\n}",
        "notes": "page_index is 0-based. width_pt/height_pt are DISPLAY dims (CropBox × UserUnit, swapped for 90/270). The layer vecs are flat per-page lists; parent→child is by id list (line.span_ids → span.glyph_ids, region.child_line_ids), NOT by nesting. reading_order holds REGION ids on the PDF path (test-enforced) but mixed region+line+table ids on the synthetic DOCX path."
      },
      {
        "symbol": "GlyphNodeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:19",
        "signature": "pub struct GlyphNodeV2 {\n    pub id: String,\n    pub text: String,\n    pub bbox: RectV2,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub quad: Option<QuadV2>,\n    pub origin: PointV2,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub baseline: Option<f64>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub angle_rad: Option<f64>,\n    pub style: TextStyleV2,\n    pub unicode_map_error: bool,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub hidden: Option<bool>,\n    pub visibility_observed: bool,\n    pub unicode_map_error_observed: bool,\n    pub geometry_basis: GlyphGeometryBasisV2,\n    pub confidence: f64,\n    pub source: GlyphSourceV2,\n    pub source_anchor: SourceAnchorV2,\n}",
        "notes": "One node PER CHARACTER (not per word). PDF id format `p{page1based:03}-g{n:05}`; DOCX `docx-p{page:03}-glyph{n:05}`. GlyphGeometryBasisV2 = PdfiumCharBox | TextMatrixDerived | OcrObserved | OoxmlLayoutDerived (line 47); PDF always emits `text_matrix_derived`, DOCX `ooxml_layout_derived` — `pdfium_char_box` and `ocr_observed` are never produced. GlyphSourceV2 = Native | Ocr (line 56); `ocr` never produced."
      },
      {
        "symbol": "SpanNodeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:61",
        "signature": "pub struct SpanNodeV2 {\n    pub id: String,\n    pub glyph_ids: Vec<String>,\n    pub text: String,\n    pub bbox: RectV2,\n    pub style: TextStyleV2,\n    pub whitespace_before: WhitespaceOriginV2,\n    pub whitespace_after: WhitespaceOriginV2,\n    pub confidence: f64,\n    pub source_anchors: Vec<SourceAnchorV2>,\n}",
        "notes": "WhitespaceOriginV2 = None | Source | Synthetic (line 78). PDF span id is `{line_id}-s{n:03}` so span→line is recoverable from the id prefix; DOCX span id is `docx-p{page:03}-span{n:04}` and is NOT line-prefixed. PDF `whitespace_after` is hardcoded \"none\" (line_builder.rs:388) — only whitespace_before carries information. PDF span.style is copied from the FIRST glyph only (line_builder.rs:386), so a span that mixes sizes silently reports one."
      },
      {
        "symbol": "LineNodeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:84",
        "signature": "pub struct LineNodeV2 {\n    pub id: String,\n    pub span_ids: Vec<String>,\n    pub text: String,\n    pub bbox: RectV2,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub baseline: Option<f64>,\n    pub writing_mode: WritingModeV2,\n    pub indentation_pt: f64,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub hanging_indent_pt: Option<f64>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub line_height_pt: Option<f64>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub hard_break_after: Option<bool>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub break_basis: Option<String>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub inline_gaps_pt: Vec<f64>,\n    pub source_order: u32,\n    pub confidence: f64,\n    pub source_anchors: Vec<SourceAnchorV2>,\n}",
        "notes": "WritingModeV2 = HorizontalTb | VerticalRl | VerticalLr (kebab-case) — `vertical-lr` never produced. PDF id `p{page1based:03}-l{n:04}`, DOCX `docx-p{page:03}-line{n:04}`. `inline_gaps_pt` is the load-bearing field for plan §6.6/§6.7 tab-column detection: it is the ordered list of every POSITIVE inter-glyph gap in reading direction (line_builder.rs:328-339, 441) — big entries mark tab stops / column boundaries inside a single line."
      },
      {
        "symbol": "RegionNodeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:136",
        "signature": "pub struct RegionNodeV2 {\n    pub id: String,\n    pub kind: PhysicalRegionKindV2,\n    pub bbox: RectV2,\n    pub child_line_ids: Vec<String>,\n    pub child_object_ids: Vec<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub column_index: Option<u32>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub section_index: Option<u32>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub z_index: Option<i32>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub reading_order_rank: Option<u32>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub reading_order_alternatives: Option<Vec<Vec<String>>>,\n    pub confidence: f64,\n    pub source_anchors: Vec<SourceAnchorV2>,\n}",
        "notes": "PhysicalRegionKindV2 (line 121, snake_case) = Text | Title | List | Table | Figure | Diagram | Form | Header | Footer | PageNumber | Rule | Unknown. `column_index == None` means full-width/banner, NOT column 0 (region_builder.rs:94-96). `z_index` is never set by any producer. On the PDF path only text|title|header|page_number|table|figure|diagram|form|unknown are ever emitted — `list`, `footer`, `rule` never are."
      },
      {
        "symbol": "TableNodeV2 / TableCellV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:238 and :187",
        "signature": "pub struct TableNodeV2 {\n    pub id: String,\n    pub bbox: RectV2,\n    pub rows: u32,\n    pub cols: u32,\n    pub cells: Vec<TableCellV2>,\n    pub detection_mode: TableDetectionModeV2,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub caption_region_id: Option<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub visual_fallback_asset_id: Option<String>,\n    pub topology_confidence: f64,\n    pub content_confidence: f64,\n    pub source_anchors: Vec<SourceAnchorV2>,\n}\n\npub struct TableCellV2 {\n    pub cell_id: String,\n    pub row: u32,\n    pub col: u32,\n    pub row_span: u32,\n    pub col_span: u32,\n    pub bbox: RectV2,\n    pub content_region_ids: Vec<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub width_pt: Option<f64>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub row_height_pt: Option<f64>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub row_height_rule: Option<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub vertical_alignment: Option<String>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub padding_pt: Option<TableCellPaddingV2>,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub header_scope: Option<HeaderScopeV2>,\n    pub border_evidence: Vec<String>,\n    pub confidence: f64,\n    pub source_anchors: Vec<SourceAnchorV2>,\n}",
        "notes": "TableDetectionModeV2 (line 259) = Ooxml | RulingLines | TextAlignment | VisionModel | Manual — `vision_model` and `manual` never produced. `caption_region_id` never set by any producer. `content_region_ids` always holds REGION ids (the `line_ids_for_cell` fallback name is a misnomer — table_detector.rs:35-59 maps lines back to their owning regions). width_pt/row_height_pt/row_height_rule/vertical_alignment/padding_pt are OOXML-only and are all None on the PDF path."
      },
      {
        "symbol": "ReadingOrderGraphV2 / ReadingOrderEdgeV2",
        "file": "src-tauri/src/schema/document_ir_v2.rs:325 and :338",
        "signature": "pub struct ReadingOrderGraphV2 {\n    pub primary: Vec<String>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub alternatives: Vec<Vec<String>>,\n    pub edges: Vec<ReadingOrderEdgeV2>,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub cycle_edges_removed: Vec<ReadingOrderEdgeV2>,\n    pub confidence: f64,\n}\n\npub struct ReadingOrderEdgeV2 {\n    pub from: String,\n    pub to: String,\n    pub relation: String,\n    pub confidence: f64,\n}",
        "notes": "`relation` is a free String; the only four values any producer emits are \"same_column_above\", \"column_transition\", \"full_width_heading_to_column\", \"source_line_sequence\" (reading_order.rs:120, 140, 162, 188). `alternatives` holds at most ONE alternative — the pure top-to-bottom-then-left-to-right order, and only when it differs from primary (reading_order.rs:252-256). `confidence` == the page gutter confidence, in [0.55, 1.0]."
      },
      {
        "symbol": "RectV2",
        "file": "src-tauri/src/schema/common.rs:16",
        "signature": "#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\n#[serde(rename_all = \"camelCase\")]\n#[serde(deny_unknown_fields)]\npub struct RectV2 {\n    pub x: f64,\n    pub y: f64,\n    pub width: f64,\n    pub height: f64,\n    pub unit: CoordinateUnitV2,\n    pub origin: CoordinateOriginV2,\n    pub page_rotation: u16,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub normalized: Option<[f64; 4]>,\n}",
        "notes": "x/y/width/height — NOT x0/y0/x1/y1. CoordinateUnitV2 = Pt | Emu | Px (lowercase, line 33); every producer emits `pt`. CoordinateOriginV2 = \"top-left\" | \"bottom-left\" (line 40). `normalized` = [x/W, y/H, w/W, h/H] clamped to 0..1, and ONLY glyph bboxes, vectorPath bboxes and image-placement bboxes carry it (coordinates.rs:137-155). Line, span, region and table bboxes never do (pdf_ingest/mod.rs:130-140 and docx_facts_shadow.rs:1823 both omit it)."
      },
      {
        "symbol": "TextStyleV2",
        "file": "src-tauri/src/schema/common.rs:134",
        "signature": "pub struct TextStyleV2 {\n    pub font_name: Option<String>,\n    pub font_size_pt: Option<f64>,\n    pub weight: Option<u16>,\n    pub bold: Option<bool>,\n    pub italic: Option<bool>,\n    pub underline: Option<bool>,\n    pub strike: Option<bool>,\n    pub color: Option<String>,\n    pub background_color: Option<String>,\n    pub superscript: Option<bool>,\n    pub subscript: Option<bool>,\n    pub language: Option<String>,\n}",
        "notes": "(all fields carry #[serde(skip_serializing_if = \"Option::is_none\")]). On the PDF path ONLY `font_size_pt` is ever populated (pdf_facts_shadow.rs:256) — font_name, bold, italic, underline, color are permanently None. On the synthetic DOCX path all of them except background_color are populated (docx_facts_shadow.rs:1799-1813)."
      },
      {
        "symbol": "SourceAnchorV2",
        "file": "src-tauri/src/schema/common.rs:64",
        "signature": "pub struct SourceAnchorV2 {\n    pub source_file_id: String,\n    pub page_index: i32,\n    pub node_ids: Vec<String>,\n    pub bbox: Option<RectV2>,\n    #[serde(rename = \"nativeBBox\")]\n    pub native_bbox: Option<RectV2>,\n    #[serde(rename = \"displayBBox\")]\n    pub display_bbox: Option<RectV2>,\n    pub pdf_to_display: Option<[f64; 6]>,\n    pub char_range: Option<SourceCharRangeV2>,\n    pub ooxml_path: Option<String>,\n    pub relationship_id: Option<String>,\n    pub extraction_mode: ExtractionModeV2,\n    pub source_hash: String,\n    #[serde(default, skip_serializing_if = \"Vec::is_empty\")]\n    pub variants: Vec<SourceVariantV2>,\n}",
        "notes": "ExtractionModeV2 (common.rs:7) = PdfNative | PdfOcr | PdfRenderedCrop | DocxOoxml | DocxRenderedFallback | Manual. `bbox` == `display_bbox` on the PDF path (both top-left); `native_bbox` is bottom-left PDF user space. `char_range` scope differs by producer: per PAGE for PDF, per PARAGRAPH for DOCX (declared in parser.options.charRangeScope, pdf_facts_shadow.rs:983 / docx_facts_shadow.rs:379)."
      },
      {
        "symbol": "AssetDescriptorV2 / DiagramQuestionRegionV2",
        "file": "src-tauri/src/schema/common.rs:186 and :213",
        "signature": "pub struct AssetDescriptorV2 {\n    pub asset_id: String,\n    pub kind: AssetKindV2,\n    pub mime: String,\n    pub relative_path: String,\n    pub sha256: String,\n    pub byte_length: u64,\n    pub width_px: Option<u32>,\n    pub height_px: Option<u32>,\n    pub duration_ms: Option<u64>,\n    pub extraction_mode: AssetExtractionModeV2,\n    pub alt_text: Option<String>,\n    pub decorative: Option<bool>,\n    pub source_anchor: Option<SourceAnchorV2>,\n    pub diagram_question_region: Option<DiagramQuestionRegionV2>,\n}\n\npub struct DiagramQuestionRegionV2 {\n    pub question_range: [u32; 2],\n    pub expected_numbers: Vec<u32>,\n    pub recovery_status: DiagramQuestionRecoveryStatusV2,\n    pub number_closure: bool,\n    pub source_backed: bool,\n}",
        "notes": "AssetKindV2 (line 166) = RasterImage | VectorRender | PageCrop | Diagram | Chart | Audio | Thumbnail. AssetExtractionModeV2 (line 178) = Embedded | PageCrop | RenderedVector | DocxMedia | UserUpload. `relative_path` is job-dir-relative: \"assets/shadow/pdf/<id>.<ext>\" or \"assets/shadow/docx/<id>.<ext>\". DiagramQuestionRegionV2 is exactly the hybrid diagram+slot-overlay evidence plan §6.10 wants, and it is imported from the V1 pdfium parser, not derived in V2."
      },
      {
        "symbol": "extract_pdf_facts_shadow / write_pdf_facts_shadow_with_v1",
        "file": "src-tauri/src/pdf_facts_shadow.rs:830 and :474",
        "signature": "pub(crate) fn extract_pdf_facts_shadow(\n    job: &ImportJob,\n    source: &SourceFile,\n    input_path: &Path,\n) -> CommandResult<Value>\n\npub(crate) fn write_pdf_facts_shadow_with_v1(\n    job: &ImportJob,\n    source: &SourceFile,\n    input_path: &Path,\n    output_path: &Path,\n    v1_document: Option<&Value>,\n) -> CommandResult<Value>",
        "notes": "Both return serde_json::Value, not DocumentIRV2 — the typed struct is used only as a mid-flight validation gate (pdf_facts_shadow.rs:998-1003) and immediately re-serialized. `CommandResult<T> = Result<T, String>`. There is no `build_document_ir_v2()` and no `ingest_source_to_document_v2()` anywhere in the repo (plan §17.2's target signature does not exist yet)."
      },
      {
        "symbol": "enrich_page",
        "file": "src-tauri/src/pdf_ingest/mod.rs:270",
        "signature": "pub(crate) fn enrich_page(page: &mut Value) -> PageLayerSummary\n\npub(crate) struct PageLayerSummary {\n    pub line_count: usize,\n    pub region_count: usize,\n    pub table_count: usize,\n    pub vector_path_count: usize,\n    pub visual_object_count: usize,\n    pub annotation_count: usize,\n    pub column_count: u32,\n    pub reading_order_confidence: f64,\n    pub ocr_region_count: usize,\n    pub duplicate_text_ratio: f64,\n    pub warnings: Vec<String>,\n}",
        "notes": "The single seam where all physical enrichment happens, and the natural insertion point for a new geometry pass. Mutates the page Value in place. Fixed internal order: ocr_merge → build_lines → build_regions → detect_tables → visual regions → annotation regions → apply_reading_order → ocr_router. Every step consumes and produces untyped `Value`."
      },
      {
        "symbol": "PdfPageGeometry",
        "file": "src-tauri/src/pdf_ingest/coordinates.rs:6",
        "signature": "pub(crate) struct PdfPageGeometry {\n    pub page_index: u32,\n    pub media_box: Bounds,\n    pub crop_box: Bounds,\n    pub rotation: u16,\n    pub user_unit: f64,\n    pub display_width: f64,\n    pub display_height: f64,\n    pub pdf_to_display: [f64; 6],\n}\n\npub(crate) fn collect_page_geometries(document: &Document) -> BTreeMap<u32, PdfPageGeometry>\npub(crate) fn bounds_for_points(points: &[(f64, f64)]) -> Option<Bounds>",
        "notes": "collect_page_geometries is keyed by pdf_extract's 1-BASED page_num; PageNodeV2.page_index is 0-based (page_num - 1). Rotation is normalized to {0,90,180,270} (coordinates.rs:223-231). `pdf_to_display` folds CropBox translation, y-flip and UserUnit into one affine — it is the exact matrix a WYSIWYG overlay needs, and it is also published per page as PageTransformV2.pdf_to_display."
      },
      {
        "symbol": "build_lines / build_regions / detect_tables / apply_reading_order",
        "file": "src-tauri/src/pdf_ingest/{line_builder.rs:445, region_builder.rs:205, table_detector.rs:464, reading_order.rs:86}",
        "signature": "pub(crate) fn build_lines(page: &Value) -> Vec<Value>\n\npub(crate) fn build_regions(page_width: f64, page_height: f64, lines: &[Value]) -> RegionBuild\npub(crate) struct RegionBuild {\n    pub regions: Vec<Value>,\n    pub line_to_region: BTreeMap<String, String>,\n    pub column_count: u32,\n    pub gutter_confidence: f64,\n}\n\npub(crate) fn detect_tables(\n    page: &Value,\n    lines: &[Value],\n    regions: &mut [Value],\n    page_width: f64,\n    page_height: f64,\n    page_rotation: u16,\n) -> Vec<Value>\n\npub(crate) fn apply_reading_order(\n    regions: &mut [Value],\n    line_to_region: &BTreeMap<String, String>,\n    column_count: u32,\n    gutter_confidence: f64,\n) -> OrderResult",
        "notes": "build_lines returns lines that still carry a transient nested \"spans\" array; enrich_page drains it into page.spans (pdf_ingest/mod.rs:284-288). `RegionBuild.line_to_region` is the line→region index the recognizer will want and it is currently thrown away after apply_reading_order — it is not persisted in the artifact."
      },
      {
        "symbol": "extract_docx_facts_shadow_with_renderer",
        "file": "src-tauri/src/docx_facts_shadow.rs:264",
        "signature": "fn extract_docx_facts_shadow_with_renderer(\n    job: &ImportJob,\n    source: &SourceFile,\n    input_path: &Path,\n    artifact_root: Option<&Path>,\n    render_requested: bool,\n    renderer: &dyn Fn(&Path, bool) -> DocxRenderAssistResult,\n) -> CommandResult<Value>",
        "notes": "The renderer is already dependency-injected (that is how the render-assist test works), so a convergence refactor can drive both DOCX geometry paths without touching env vars. `pub(crate) fn extract_docx_facts_shadow(job, source, input_path) -> CommandResult<Value>` (line 240) is the production entry and hardwires `requested_from_environment()` + `render_docx`."
      },
      {
        "symbol": "semantic_lines_from_v2_shadow",
        "file": "src-tauri/src/ielts_grammar/instruction_zone.rs:161",
        "signature": "pub(crate) fn semantic_lines_from_v2_shadow(shadow: &Value) -> Vec<SemanticLine>\n\npub(crate) struct SemanticLine {\n    pub id: String,\n    pub text: String,\n    pub source_anchor: Value,\n    pub page_index: i32,\n    pub order: usize,\n    pub role: String,\n    pub bbox: Option<[f64; 4]>,\n}",
        "notes": "This is the entire current bridge from DocumentIRV2 into recognition, and it is the flattening plan §6.1 describes. It keeps line.text (whitespace-normalized), line.bbox as [x, y, width, height] (parse_bbox, instruction_zone.rs:315-333) and one squashed anchor. It iterates `page.lines` in ARRAY order, ignoring page.reading_order entirely — so multi-column reading order is already discarded before any grammar rule runs. `role` is always initialized to \"\"."
      },
      {
        "symbol": "significant_physical_nodes",
        "file": "src-tauri/src/ielts_grammar/quality.rs:3311",
        "signature": "fn significant_physical_nodes(physical: &Value) -> BTreeMap<String, BTreeSet<String>>",
        "notes": "Returns node_id → alias set, where aliases let an anchor on a child (span/glyph/line) satisfy coverage for its parent region. Already implements: skip regions of kind header|footer|page_number|rule (line 3443); skip regions with no alphanumeric child text unless they have child objects or a structural kind (3459-3466); skip lines already claimed by a region, spans already claimed by a line, glyphs already claimed by a span (3471-3510); and a `__text:<normalized>` alias for regions with ≥12 alphanumeric chars (3431-3434). Reuse this rather than reimplementing §6.11."
      }
    ],
    "dataFlow": "PDF (the main path). `parse_document` → authoring_commands.rs:154 `parse_document_core` → (a) `parser::parse_source_document` → `parse_pdf_with_rust_text_extractor` (parser.rs:492) → `pdf_geometry::parse_pdf_with_pdfium` builds the authoritative **DocumentIRV1** to `<job>/document-ir.json`; then (b) if `document_ir_v2_shadow_enabled()` (environment.rs:219, hardcoded `true`) → `write_pdf_facts_shadow_with_v1(job, source, upload, <job>/document-ir-v2.shadow.json, Some(&v1_ir))`. The same PDF is therefore opened and scanned TWICE by two different libraries: pdfium for V1, the pure-Rust `pdf-extract` 0.10 crate for V2.\n\nInside `write_pdf_facts_shadow_with_v1` (pdf_facts_shadow.rs:474): a staging dir `.pdf-shadow-txn-<uuid>/` is created with `assets/shadow/pdf/` → `extract_pdf_facts_shadow_internal` → `merge_v1_diagram_question_region_assets` → typed re-validation → `write_canonical_json_atomic` (sorted object keys) → `build_compare_report` → `commit_shadow_bundle`, which atomically installs exactly three components (artifact json, compare json, assets/shadow/pdf dir) with backup+rollback (pdf_facts_shadow.rs:714-826). A missing compare file or missing asset dir aborts the whole commit.\n\n`extract_pdf_facts_shadow_internal` (pdf_facts_shadow.rs:838-1003), in order:\n1. `load_document_with_xref_repair` (with a classic-xref/stream-Length repair pass), then `enforce_document_resource_limits` (≤1000 pages, ≤1e6 objects).\n2. `collect_page_geometries` → per-page `PdfPageGeometry` (MediaBox/CropBox/Rotate/UserUnit → `pdf_to_display`).\n3. `pdf_extract::output_doc(&document, &mut PdfFactsCollector)`. `OutputDev::output_character` (line 166) is called once per character with `(trm, width, spacing, font_size, char)` where `trm = Tsm × Tm × CTM` and does NOT include the font size. The collector synthesizes a parallelogram from origin, origin+advance·x̂ (advance = width·font_size) and origin+font_size·ŷ, maps all four corners through `display_point`, and emits `bbox` (axis-aligned hull), `quad` (all 8 display coords — the real rotated shape), `origin`, `baseline`, `angleRad`, `style.fontSizePt`, and a full `sourceAnchor` with bbox+nativeBBox+displayBBox+pdfToDisplay+charRange. `OutputDev::end_line` sets a transient `_sourceLineBreakAfter` on the last glyph. `stroke`/`fill` push vectorPaths with full command lists and an `isAxisAlignedRule` flag ((w≤2.6 ∧ h≥12) ∨ (h≤2.6 ∧ w≥12), line 428).\n4. `page_value` (line 2336) emits the page shell with **glyphs and vectorPaths only** — spans/lines/regions/tables/assetIds/readingOrder are all literal `[]` (lines 2423-2428) — plus first-pass `quality` (classification born_digital|garbled|scanned by glyph count and unicodeErrorRatio>0.1) and a transient `_coverage`.\n5. `extract_pdf_visual_assets` — embedded Image XObjects → PNG/JPEG bytes under `assets/shadow/pdf/`, asset_id `pdf-image-p{page:03}-{objnum}-{gen}`. The asset's own sourceAnchor has **no bbox**.\n6. `collect_page_object_facts` (line 1894) — walks the page content stream tracking q/Q/cm, and on each `Do` computes the unit-square-through-CTM bbox → `page.imagePlacements[]` (this is where image geometry actually lives). `BMC`/`BDC` → `page.markedContent[]` with mcid/tag/ActualText/Alt; `structurePath` is always `[]` (no PDF structure-tree traversal anywhere).\n7. `extract_pdf_vector_assets` — **one** `pdf-vector-p{page:03}` SVG per page, bbox = union of ALL non-rule vector paths on that page.\n8. `append_page_asset_ids` + `append_page_visual_objects` → transient `_visualObjects` (kind figure|diagram) and a raster-driven re-classification of `quality` to mixed|scanned plus `requiresOcrRegions` / imageCoverageRatio.\n9. `collect_pdf_annotations` → `page.annotations[]` (Widget/other, fieldName/fieldType/value/flags, plus a rendered SVG appearance fallback asset when needed).\n10. `page_contains_hidden_text` per page (Tr 3/7 or ≥0.98 white fill before a text-showing op) → transient `_hiddenTextDetected`.\n11. **`enrich_page` per page** — the only place the semantic-neutral layers are built: `ocr_merge::merge_provider_output` (no-op, see gaps) → `line_builder::build_lines` (baseline clustering, tolerance `(font_size.max(median_font) * 0.22).clamp(0.8, 4.5)`, vertical-overlap ≥0.35, angle delta <0.18; then intra-cluster split on gap > `max(font*2.4, typical_gap*3.5, 12.0)`) → drain nested spans into `page.spans` → `region_builder::build_regions` (occupancy-bin gutter detection with ≤1 tolerated crossing and a parallel-text requirement, full-width banner sectioning, then split a column's line run when the vertical gap exceeds 14.0 pt single-column / 10.0 pt multi-column) → `table_detector::detect_tables` → visual + annotation regions appended → `reading_order::apply_reading_order` (constraint DAG + topo sort, writes readingOrderRank/readingOrderAlternatives and OVERWRITES region.confidence with the page gutter confidence, reading_order.rs:257-265) → `ocr_router::analyze` (pairwise glyph IoU ≥0.82 → duplicateTextRatio, PDF_HIDDEN_TEXT_MISALIGNED / PDF_NATIVE_OCR_CONFLICT).\n12. `coverage_ledger_from_pages` (line 2703) — one `{sourceNodeId, disposition:\"unassigned\", targetIds:[], reason}` entry for EVERY glyph, span, line, region, vectorPath, table, table cell, annotation, imagePlacement and markedContent node in the document, de-duplicated by id in a BTreeMap.\n13. transient keys (`_coverage`, `_hiddenTextDetected`, `_visualObjects`, `_annotations`) stripped, `parser.options` assembled (preflight, physicalLayers.summary, ocrPlan), and the whole document round-tripped through `serde_json::from_value::<DocumentIRV2>` — a hard error if any field is unknown or missing.\n\nDOCX. `open_docx` → `parse_document_model` (+ collect_drawings / collect_text_boxes / collect_composite_fallbacks / assign_numbering_labels). Then one of two paths (docx_facts_shadow.rs:302-326):\n- **render-assisted** (only when `EPIC8_DOCX_RENDER_ASSIST` is truthy AND a soffice-class provider answers `--headless --version`): render to PDF, run `extract_pdf_facts_shadow` on it, keep those pages verbatim, then `bind_rendered_pages_to_ooxml` greedily matches each rendered line's whitespace-stripped text against the ordered OOXML paragraph list and `rewrite_rendered_page_anchors` stamps every glyph/span/line/region anchor with `extractionMode: \"docx_rendered_fallback\"` + `ooxmlPath` + a `docx_ooxml` text variant. Node ids in this path are PDF-shaped (`p001-l0001`), and tables come from the rendered PDF's geometric detector.\n- **default (semantic-only)**: `build_pages` + `DocxPageBuilder`. One LINE per paragraph segment (no wrapping), glyph x advanced by `char_width` = font_size × 0.52 (ASCII) / 0.9 (non-ASCII) / 0.32 (space) / 2.5 (tab), y advanced by resolved line spacing + spacingAfter. One REGION per paragraph. Pages break only on `pageBreakBefore`, a `w:br type=\"page\"` run, or a non-continuous section change. Tables come from OOXML with real gridWidths/rowHeights/padding/vMerge.\n\nPersistence and read-back. The artifact is a single canonical-JSON file on disk at `<app_root>/jobs/<job_id>/document-ir-v2.shadow.json` (`util::job_dir`, util.rs:33), assets at `<app_root>/jobs/<job_id>/assets/shadow/{pdf,docx}/`, sidecars `document-ir-v2.shadow.compare.json`, `document-ir-v2.shadow.error.json`, `debug/document-ir-v2.shadow.overlay.svg`. It is **never written to SQLite** (no reference to it in db.rs, library_commands.rs, nas_package_v2.rs) and `cleanup.rs` does not delete it. Read-back is always `read_json_opt(...) -> Option<Value>` — untyped, whole-file, no size cap (util.rs:246-257) — gated by a freshness check (`physical_shadow_matches_source`, auto_pipeline.rs:249 / authoring_commands.rs:97 / `physical_shadow_matches_authoring`, authoring_v2_commands.rs:757). Three consumers exist: `ielts_grammar::build_authoring_v2_shadow` (via `semantic_lines_from_v2_shadow` + `assets`), `ielts_grammar::quality::evaluate_quality` (via `significant_physical_nodes` / `source_coverage_summary`), and the `debug_document_ir_v2_overlay` Tauri command (lib.rs:917, registered at lib.rs:1426). Since `library_items.id == job.job_id`, a recognizer given a library item id resolves its physical facts at `<app_root>/jobs/<id>/document-ir-v2.shadow.json` with no DB lookup.",
    "gapsVsPlan": [
      {
        "claim": "§6.1: \"现有 DocumentIRV2 已经有足够丰富的物理事实\" — the physical layer is rich enough, the problem is only that ielts_grammar flattens it.",
        "reality": "Half right. Geometry (bbox/quad/baseline/angle/columns/reading-order DAG/inline gaps) is genuinely rich. But typography is nearly absent on the PDF path: TextStyleV2 gets ONLY fontSizePt; font_name, bold, italic, underline, color are permanently None because the collector emits `\"style\": {\"fontSizePt\": ...}` and nothing else. The plan's own scoring features in §6.4 (\"font size / bold / spacing\") and §6.5 (\"字号与其他题号一致\") are therefore only half-implementable on PDF today.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_facts_shadow.rs:256 vs src-tauri/src/schema/common.rs:137-162; src-tauri/src/docx_facts_shadow.rs:1799-1813 (DOCX populates the same struct fully)"
      },
      {
        "claim": "§6.5: `detect_question_number_tokens(page: &DocumentPageV2, ...)` iterating `page.spans` with `parse_small_numeric_token`.",
        "reality": "The type is `PageNodeV2`, not `DocumentPageV2`, and there is no `DocumentPageV2` anywhere in the repo. `page.spans` is a flat per-page Vec whose parent line is recoverable only from the id prefix (`{line_id}-s{nnn}`) on the PDF path — and NOT recoverable at all on the synthetic DOCX path, where span ids are `docx-p{page:03}-span{n:04}` with no line prefix. Spans are also word-like, not token-like: a span breaks only on a source space glyph or a gap > `max(median_font*0.28, median_gap*1.75, 1.0)`, so \"5\" in \"5 Which...\" is a separate span but \"(a)\" or \"27.\" is not necessarily split from what follows.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/schema/document_ir_v2.rs:295; src-tauri/src/pdf_ingest/line_builder.rs:348, 380, 407-421; src-tauri/src/docx_facts_shadow.rs:800-804"
      },
      {
        "claim": "§6.5: \"-0.30 位于页脚/页码区域\" — header/footer/page-number exclusion can key off region role.",
        "reality": "`region_kind` can only produce header|page_number under absurdly narrow conditions: \"header\" requires bbox.y < 50.0 AND the text to contain the literal \"ielts\"; \"page_number\" requires bbox.y > 0.86·pageHeight AND the text to be digits/whitespace only. `PhysicalRegionKindV2::Footer` and `::Rule` are never emitted by any producer. Real running heads/feet land in kind=\"text\" and are counted as significant source nodes.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_ingest/region_builder.rs:146-162; src-tauri/src/ielts_grammar/quality.rs:3443 (the header|footer|page_number|rule skip that consequently almost never fires)"
      },
      {
        "claim": "§6.10 表格: \"优先使用 DocumentIRV2.pages[].tables[]\" with a `PhysicalTableV2` type and `table.rows.map(...)`.",
        "reality": "The type is `TableNodeV2`; `rows` is a `u32` count, not an iterable — cells are a flat `Vec<TableCellV2>` addressed by (row, col). More importantly the PDF detector emits AT MOST ONE table per page: `detect_tables` tries the ruled candidate, and only `if tables.is_empty()` tries the borderless one, and neither loops. Worse, the ruled detector clusters ALL rule x/y coordinates on the page into ONE grid, so two separate tables on one page collapse into a single spurious grid (and bail out entirely if xs·ys > 400). The borderless detector additionally refuses to run if the page has ANY vector path at all.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/schema/document_ir_v2.rs:241-255; src-tauri/src/pdf_ingest/table_detector.rs:472-488, 347-357, 396-402"
      },
      {
        "claim": "§6.10 表格 / §17.5: \"Word 原生 table 优先产生 physical table\".",
        "reality": "Inverted whenever render assist is on. `extract_docx_facts_shadow_with_renderer` uses the rendered PDF's pages *instead of* `build_pages`, so the OOXML tables — the only ones with real gridWidths, rowHeights, padding, verticalAlignment and vMerge-derived rowSpan — are discarded and replaced by the PDF geometric detector's single-table-per-page guess. `detectionMode: \"ooxml\"` can only appear on the semantic-only path.",
        "verdict": "wrong",
        "evidence": "src-tauri/src/docx_facts_shadow.rs:302-326 (rendered_pages wins over build_pages) vs :955-1105 (add_table, the only producer of detectionMode ooxml + widthPt/rowHeightPt/paddingPt)"
      },
      {
        "claim": "§6.10 diagram: source-crop + normalized hotspot fallback needs to be built.",
        "reality": "The evidence half already exists and is typed: `DiagramQuestionRegionV2 { question_range, expected_numbers, recovery_status, number_closure, source_backed }` on a `kind: \"page_crop\"` asset with display+native bboxes. But it is *imported from the V1 pdfium parser*, not derived in V2 — and it is merged AFTER enrich_page, so the crop is added to `page.assetIds` and `document.assets` but NEVER becomes a region. A recognizer walking `page.regions` cannot see it.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/schema/common.rs:213-229; src-tauri/src/pdf_facts_shadow.rs:518-660 (called at :551, after extract_pdf_facts_shadow_internal returns)"
      },
      {
        "claim": "§6.2/§6.3: a Question Layout Graph intermediate layer must be added.",
        "reality": "Confirmed, and nothing pre-exists: no `QuestionLayoutGraph`, `question_blocks`, `LayoutGraph`, `recognize_local` or `LocalRecognitionOutcome` symbol appears anywhere in src-tauri. ielts_grammar/ has 16 files and none of them is a graph builder.",
        "verdict": "confirmed",
        "evidence": "repo-wide grep over src-tauri/src for QuestionLayoutGraph|question_layout|LayoutGraph|recognize_local|LocalRecognitionOutcome returns zero hits; ls src-tauri/src/ielts_grammar/"
      },
      {
        "claim": "§6.3: reading order should be recovered before task classification, i.e. downstream should consume a reading order.",
        "reality": "Confirmed and worse than stated: reading order IS computed well (a real constraint DAG with 4 relation types, cycle removal, primary + alternative, per-page confidence) and then completely ignored. `semantic_lines_from_v2_shadow` iterates `page.lines` in array order — which `build_lines` sorted by raw (min y, min x) across the WHOLE page, i.e. it interleaves columns. Multi-column question papers are already scrambled before any grammar rule runs.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/pdf_ingest/reading_order.rs:86-283 produces it; src-tauri/src/pdf_ingest/line_builder.rs:448-470 sorts lines page-globally by y-then-x; src-tauri/src/ielts_grammar/instruction_zone.rs:176-181 iterates page.lines and never reads readingOrder"
      },
      {
        "claim": "§17.4 ocr_router/ocr_merge: \"保留 selective OCR 决策\" / \"保留 native/OCR 冲突；把冲突作为 field evidence\".",
        "reality": "There is no OCR at all. `ocr_merge::merge_provider_output` keys on a transient `page[\"_ocrProviderOutput\"]` that NO producer in the repo ever writes (grep finds it only inside ocr_merge.rs itself and its tests), so it returns `OcrMergeResult::default()` on every real page. `build_ocr_plan_summary` self-declares `\"engine\": \"not_configured\"`. `local_ocr_enabled()` defaults false. GlyphSourceV2::Ocr, GlyphGeometryBasisV2::OcrObserved, ExtractionModeV2::PdfOcr and SourceAnchorV2.variants are unreachable on the PDF path.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_ingest/ocr_merge.rs:94-100; repo-wide grep for _ocrProviderOutput; src-tauri/src/pdf_ingest/mod.rs:645; src-tauri/src/environment.rs:215"
      },
      {
        "claim": "§17.4 line_builder: \"输出明确 lineId/spanId/glyphId，禁止提前压平重要 gap\".",
        "reality": "Already done, and the gap preservation is real: `inlineGapsPt` records every positive inter-glyph gap, `hardBreakAfter`/`breakBasis: \"pdf_output_device_end_line\"` are set only when the output device actually reported end_line, and there is a regression test asserting breaks are not fabricated. Ids are explicit and hierarchical for PDF. The remaining gap is DOCX span ids not being line-prefixed.",
        "verdict": "already-done",
        "evidence": "src-tauri/src/pdf_ingest/line_builder.rs:504, 510-518, and test `unobserved_line_breaks_are_not_fabricated_and_gaps_are_recorded` at :571"
      },
      {
        "claim": "§17.4 reading_order: \"输出 primary + alternatives；低置信度时不给下游伪确定顺序\".",
        "reality": "primary + alternatives + edges + cycleEdgesRemoved + confidence are all already emitted, and `PAGE_READING_ORDER_AMBIGUOUS` is pushed when columnCount>1 ∧ gutterConfidence<0.72. But the \"don't hand downstream a false certainty\" half is violated: `page.readingOrder` is unconditionally set to `graph.primary`, and every region in a multi-column page gets the SAME full page-level alternatives array copied onto it.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_ingest/mod.rs:318-336, 366-371; src-tauri/src/pdf_ingest/reading_order.rs:260-262"
      },
      {
        "claim": "§17.3: \"统一与 pdf_geometry.rs 的 PDFium/文本提取入口，避免同一 PDF 多次扫描\".",
        "reality": "Confirmed, and the two scanners are not interchangeable. pdf_geometry uses pdfium but collects only `(unicode_char, origin_x, origin_y)` per char — no font size, no font name, no char bbox — so V1 line bboxes are inferred from char origins. pdf_facts_shadow uses the `pdf-extract` crate and gets font_size + advance width + full text matrix. Unifying on pdfium would need `loose_bounds()`/`scaled_font_size()`/`font_name()` wired in; unifying on pdf-extract would lose the diagram page-crop rendering that only pdfium can do.",
        "verdict": "confirmed",
        "evidence": "src-tauri/src/pdf_geometry.rs:121-125 (CharWithOrigin), :351-372 (collect_chars_with_origin), and zero font-related identifiers in the file; src-tauri/src/parser.rs:502; src-tauri/src/pdf_facts_shadow.rs:166-267"
      },
      {
        "claim": "§17.3: \"将 shadow artifact 写入逻辑与 facts collector 分离\" and \"只做 PDF 物理事实，不推断 IELTS\".",
        "reality": "The second half already holds — pdf_facts_shadow and pdf_ingest contain no IELTS vocabulary except one incidental \"ielts\"/\"reading passage\" string test in region_kind. The first half does not: `write_pdf_facts_shadow_with_v1` mixes the 3-component staging/backup/rollback transaction, the V1 diagram-asset bridge and the compare-report write into one 60-line closure, and `extract_pdf_facts_shadow_internal` both collects facts and writes asset bytes (via the `asset_root: Option<&Path>` parameter that silently degrades to \"no assets persisted\" when None).",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_facts_shadow.rs:474-514, 838-1003, 1466-1471; src-tauri/src/pdf_ingest/region_builder.rs:150-152"
      },
      {
        "claim": "§17.5: \"与 PDF 输出同一 DocumentIRV2\".",
        "reality": "Same schema, materially different semantics. DOCX (default path) has: no vectorPaths, no imagePlacements, no markedContent, no annotations, no pageTransform, no mediaBox/cropBox, no readingOrderGraph, no `normalized` on any bbox, `hardBreakAfter: true` on every line, `readingOrder` containing region AND line AND table ids interleaved, one line per paragraph regardless of wrapping, and glyph x positions from a 0.52·fontSize monospace-ish estimate. Also: DOCX never gets a V2 artifact at all on the auto-import path, which filters `source.file_type == \"pdf\"`.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/docx_facts_shadow.rs:1106-1145 (finish), :919-921, :944-945, :1111-1114, :1784-1797; src-tauri/src/auto_pipeline.rs:1487"
      },
      {
        "claim": "§17.5: \"DrawingML/SmartArt/浮动文本框难以结构化时生成 visual fallback asset + contained text regions\".",
        "reality": "Floating text boxes DO become regions (positioned by EMU x/y when both are present, docx_facts_shadow.rs:1690-1700). SmartArt and Chart do NOT: `collect_composite_fallbacks` extracts their accessible text into `DocxCompositeDrawing.text`, and that text is published only inside `parser.options.ooxml.compositeDrawings[].accessibleText`. It never becomes a glyph, span, line, region or asset, so a geometry-first recognizer sees nothing for SmartArt content.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/docx_ingest/smartart.rs:7-39; src-tauri/src/docx_facts_shadow.rs:573-581, :1690-1700"
      },
      {
        "claim": "§18 P4-T01: \"DocumentIRV2 producer schema gate\".",
        "reality": "Partly there and partly missing. The Rust gate is strong: every producer round-trips through `serde_json::from_value::<DocumentIRV2>` and every struct is `deny_unknown_fields`, so an unknown key fails the whole extraction. But `contracts/document-ir-v2.schema.json` (18 KB, JSON-Schema 2020-12) is referenced by NOTHING — no Rust, no TS, no script — so the Rust structs and the published contract can drift silently.",
        "verdict": "partly-wrong",
        "evidence": "src-tauri/src/pdf_facts_shadow.rs:998-1003 and src-tauri/src/docx_facts_shadow.rs:403-408 (the Rust gate); repo-wide grep for \"document-ir-v2.schema.json\" outside contracts/ returns zero hits"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/table_detector.rs",
        "symbol": "detect_tables (line 464), ruled_candidate (line 303), borderless_candidate (line 387), make_table (line 200)",
        "whatToChange": "Highest-value single fix for plan §6.10 and §17.4. (1) Return MULTIPLE tables per page: replace the `if tables.is_empty()` gate at :476 with connected-component clustering of rules before gridding, so two tables on one page do not merge into one grid (:347-357 currently clusters every rule x and y on the page into a single xs/ys pair). (2) Let the borderless detector run on pages that have vector paths — the `if page.vectorPaths is non-empty { return None }` guard at :396-402 means any single underline or header rule anywhere on the page kills borderless detection entirely, which is exactly the IELTS completion-table case. (3) Make table and cell ids page-unique: `format!(\"p-table-{:04}\", index + 1)` at :291 and `format!(\"p-table-{}-r{}-c{}\", ...)` at :276 produce \"p-table-0001\" / \"p-table-1-r0-c0\" on EVERY page, so ids collide document-wide; the coverage ledger's BTreeMap silently de-duplicates them (pdf_facts_shadow.rs:2761) and hides the collision. (4) `headerScope` is `if row == 0 {\"row\"} else {\"none\"}` at :283 — replace with real header inference or emit None rather than a guess.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "PdfFactsCollector::output_character (line 166), specifically the style literal at line 256",
        "whatToChange": "Two separate problems in one line. (a) `\"style\": {\"fontSizePt\": clean_number(safe_font_size * page.geometry.user_unit)}` uses the raw Tf operand and applies only UserUnit — it does NOT apply the CTM scale, while the bbox on the very next lines DOES (all four corners go through `trm` then `display_point`). So under a scaling CTM, `style.fontSizePt` and `bbox.height` disagree. pdf-extract's own reference output computes `sqrt(|trm.transform_vector(font_size, font_size)|)` (crate lib.rs:2160-2162). Until fixed, any geometry-first scorer should derive effective size from `bbox.height` or the `quad`, not from `style.fontSizePt`. (b) Nothing but fontSizePt is emitted, so fontName/bold/italic/color are unavailable on the PDF path — pdf-extract's OutputDev signature does not expose the font object, so populating them requires either a fork/upgrade of pdf-extract or a parallel pdfium pass (see pdf_geometry unification below).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/mod.rs",
        "symbol": "enrich_page (line 270)",
        "whatToChange": "The single insertion seam for the new geometry-first pass, and the place to stop discarding indexes. `RegionBuild.line_to_region` (region_builder.rs:9) is the line→region map the recognizer needs and it is dropped after apply_reading_order — either persist it or return it from enrich_page. Also add a table→region and region→column index. Note the fixed pipeline order at :275-311: tables are detected AFTER regions and then RETRO-LABEL overlapping regions to kind=\"table\" (table_detector.rs:508-533), so a region's kind depends on detection order; visual and annotation regions are appended after that and carry no columnIndex/sectionIndex, which makes them full-width in `base_compare`.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/region_builder.rs",
        "symbol": "region_kind (line 114), build_regions paragraph split at line 307-309, detect_gutters (line 29)",
        "whatToChange": "Plan §6.4 needs region roles this function cannot supply. `region_kind` can only return title|header|page_number|text; \"header\" requires y<50 AND the literal \"ielts\" in the text, \"page_number\" requires y>0.86·H AND digits-only. Add repeated-position header/footer detection ACROSS pages (nothing in the codebase compares pages to each other) and emit `Footer`/`List`/`Rule`. Separately, the region split threshold is an absolute `gap > 14.0` (single-column) / `gap > 10.0` (multi-column) in points, not font-relative — a 9 pt body font and a 14 pt body font get the same paragraph boundary, which will over- or under-split. `detect_gutters` bins at pageWidth/4 clamped to 32..512, tolerates ≤1 crossing, and requires a gap ≥ 3.5% of page width with parallel text on both sides — reasonable, but returns a confidence of only `(0.55 + meanGapRatio).clamp(0.55, 1.0)`, so a perfect 2-column page reports ~0.60 and trips the `<0.72` PAGE_READING_ORDER_AMBIGUOUS warning at pdf_ingest/mod.rs:366. Recalibrate before you gate anything on region confidence.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/reading_order.rs",
        "symbol": "apply_reading_order (line 86), specifically lines 257-265",
        "whatToChange": "`object.insert(\"confidence\", json!(clean(gutter_confidence)))` OVERWRITES each region's own confidence (0.86 for a columned region, 0.94 for full-width, set in region_builder.rs:200) with the page-level gutter confidence. Any recognizer that reads `region.confidence` expecting a region-quality signal gets a page-layout signal instead. Also `readingOrderAlternatives` copies the entire page-level alternatives array onto EVERY region in a multi-column page (:261) — pure duplication that multiplies artifact size by the region count. Move both to the page-level ReadingOrderGraphV2 and leave region.confidence alone.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/line_builder.rs",
        "symbol": "build_lines (line 445), build_spans (line 306), lines 502-507",
        "whatToChange": "(1) `indentationPt` is the ABSOLUTE `bbox.x` (:502), not an indent relative to the column's left edge, so it is useless for §6.6's `same_column_or_hanging_indent` / §6.7's `hanging_indent_matches` until you subtract the region's or column's left edge. (2) `lineHeightPt` is `bbox.height` (:503) — the glyph box height, i.e. roughly the font size — NOT the inter-baseline distance, so it cannot be used for §6.6's `vertical_gap_below_threshold`; compute leading from consecutive `baseline` deltas instead. (3) `hangingIndentPt` is NEVER set on the PDF path (only DOCX sets it, from `w:ind/@hanging`); the geometric equivalent has to be derived. (4) `sourceAnchors` at :507 pushes ONE anchor PER GLYPH, each carrying bbox + nativeBBox + displayBBox + a 6-number pdfToDisplay + charRange + a 64-char sourceHash. Region anchors then re-concatenate all of them (mod.rs:150-169), and table/cell anchors concatenate them a third and fourth time (table_detector.rs:62-74, 156-198). Collapsing a line's anchors to one merged anchor with a merged nodeIds list is the single largest artifact-size win available. (5) The page-global line sort at :448-470 is (min y, min x) across the whole page, which interleaves columns before regions are built.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "coverage_ledger_from_pages (line 2703)",
        "whatToChange": "Emits an \"unassigned\" ledger entry for EVERY node at EVERY granularity — glyphs, spans, lines, regions, vectorPaths, tables, table cells, annotations, imagePlacements, markedContent. For a per-character glyph layer this is the dominant cost in the file and it carries no information a `page.glyphs[].id` scan could not derive. Plan §6.11 needs a ledger over SIGNIFICANT nodes only, and that predicate already exists in ielts_grammar/quality.rs:3311 `significant_physical_nodes` (region-first with span/glyph aliasing, skipping header/footer/page_number/rule and text-free regions). Reuse it and drop glyph/span-level entries. Note this function is also where the table-id collision is silently masked: `entries_by_id.entry(id).or_insert_with(...)` keeps only the first \"p-table-0001\".",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs",
        "symbol": "extract_docx_facts_shadow_with_renderer (line 264), lines 302-326",
        "whatToChange": "Plan §17.5's \"Word 原生 table 优先\" requires MERGING the two paths instead of choosing one. Today `rendered_pages` wins wholesale and the OOXML table facts (gridWidths, rowHeights, rowHeightRule, padding, verticalAlignment, vMerge-derived rowSpan, per-border evidence — all produced only by `add_table` at :955) are thrown away. Target: take geometry from the rendered PDF, take table TOPOLOGY from OOXML, and reconcile them by bbox. Also `bind_rendered_pages_to_ooxml` (:1296) matches lines to paragraphs by whitespace-stripped substring containment with a forward-only cursor and a single `active_split_match` — it will mis-bind on repeated boilerplate (e.g. identical \"TRUE / FALSE / NOT GIVEN\" option runs), and the first `.or_else` fallback at :1381 scans from index 0 and can bind a late line to an early paragraph.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs",
        "symbol": "DocxPageBuilder::add_paragraph_at (line 748), char_width (line 1784), finish (line 1106)",
        "whatToChange": "The semantic-only DOCX path — the DEFAULT, because EPIC8_DOCX_RENDER_ASSIST is off — produces geometry that a geometry-first recognizer must not trust: x advances by `font_size * 0.52` for ASCII / `0.9` for non-ASCII / `0.32` for spaces / `2.5` for tabs, with no font metrics and no line wrapping, so ONE line node can be several times wider than the content box (only `line_bbox` is clamped by `.min(self.content_width_pt())` at :1097 — the glyph and span x values are not). Either (a) declare DOCX geometry advisory and drive DOCX recognition off `ooxmlPath` + `indentationPt`/`hangingIndentPt` + numbering labels rather than bboxes, or (b) make render assist mandatory for DOCX. Also `readingOrder` mixes region ids, line ids and table ids (:944-945, :1111-1114) whereas the PDF path holds region ids only — a test at pdf_facts_shadow.rs:3175-3186 asserts `set(readingOrder) == set(region ids)` for PDF, so any shared consumer must handle both shapes.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "the shadow-materialization block at line 1486-1521",
        "whatToChange": "`main_source_file(&job).filter(|source| source.file_type == \"pdf\")` at :1487 means an auto-imported DOCX gets NO DocumentIRV2 at all — only the manual `parse_document` command handles docx (authoring_commands.rs:185-190). If the new recognizer is DocumentIRV2-only, every DOCX auto-import silently has nothing to recognize. Add the docx branch here, or better: move both write sites behind one `ingest_source_to_document_v2`-shaped function (plan §17.2) so there is exactly one producer.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "merge_v1_diagram_question_region_assets (line 518), called from write_pdf_facts_shadow_with_v1 at line 551",
        "whatToChange": "Runs AFTER extract_pdf_facts_shadow_internal (and therefore after enrich_page), so the diagram page-crop asset is appended to `document.assets` and `page.assetIds` but never becomes a region and never appears in `page.readingOrder`. Plan §6.10's hybrid diagram wants exactly this asset with hotspots; the recognizer must therefore join `document.assets[] where diagramQuestionRegion != null` against `page.assetIds`, not walk regions. Either move the merge before enrich_page or synthesize a `region-visual-*` region for it (the machinery is already there: pdf_ingest/mod.rs:171-210 append_visual_regions). Note the crop only exists when the V1 pdfium parser produced it — it reads `asset.path` off the V1 document and hard-errors if that file is gone (:543-547).",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_ingest/ocr_router.rs",
        "symbol": "analyze (line 15)",
        "whatToChange": "O(n²) over page glyphs with a `super::bounds_of` JSON re-parse on BOTH sides of every comparison (:25, :29). At ~3000 glyphs/page that is ~4.5M comparisons and ~9M serde_json Map lookups per page, before the new recognizer adds any work of its own. Replace with a spatial bucket (the Bounds helpers at pdf_ingest/mod.rs:30-95 already give you iou/intersection) or hoist bounds parsing out of the inner loop. This is the one place likely to dominate wall time on a real paper.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/instruction_zone.rs",
        "symbol": "semantic_lines_from_v2_shadow (line 161), SemanticLine (line 8), aggregate_line_source_anchor (line 206)",
        "whatToChange": "The chokepoint plan §6.1 identifies. It reduces the whole physical layer to {id, normalized text, one anchor, page_index, order, bbox as [x,y,w,h]} and iterates `page.lines` in array order — so spans, glyphs, regions, columnIndex, readingOrder, readingOrderGraph, tables, vectorPaths, imagePlacements, annotations and markedContent are ALL discarded before recognition. `aggregate_line_source_anchor` also silently drops the per-glyph charRange when it merges nodeIds (:206-238). This is the function to delete, not extend — but keep it alive as the V1-compat path (`semantic_lines_from_v1_document`, same file) since `build_authoring_v2_shadow` falls back to it when the shadow is missing (ielts_grammar/mod.rs:105-112).",
        "risk": "medium",
        "blockedBy": "Needs the new recognizer entry point (plan §17.7 `recognize_local(&DocumentIRV2) -> LocalRecognitionOutcome`) to exist first; build_authoring_v2_shadow currently requires V1 `split.questionGroupCandidates` as its iteration driver (ielts_grammar/mod.rs:131-140)."
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/schema/document_ir_v2.rs",
        "symbol": "every struct — the #[serde(deny_unknown_fields)] attribute",
        "whatToChange": "Hard constraint on every change above, and the most common way this refactor will break at runtime rather than at compile time: producers build untyped `serde_json::Value` and are validated once at the end (pdf_facts_shadow.rs:998, docx_facts_shadow.rs:403). Because every struct denies unknown fields, adding ANY key to a json! literal without adding the field to the struct fails the entire extraction with `pdf_facts_shadow_schema_validation_failed:...` — and that error is written to `document-ir-v2.shadow.error.json` and swallowed (authoring_commands.rs:216-227, auto_pipeline.rs:1509-1519), so the job continues with no physical facts. Fields to add for the new recognizer (all must be `Option`/`#[serde(default)]` so old artifacts still parse): a semantic role on RegionNodeV2 for §6.4, a column-relative indent on LineNodeV2, and any per-token annotation for §6.5. Also update contracts/document-ir-v2.schema.json in the same commit — nothing enforces that it matches (no code references it), so add that gate as part of P4-T01.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/util.rs",
        "symbol": "read_json (line 246) / read_json_opt (line 252)",
        "whatToChange": "`fs::read_to_string` then `serde_json::from_str` into an untyped `Value` — whole file, no size cap, full DOM. Combined with the per-character glyph layer and the anchor duplication described above (glyph anchor appears in glyph.sourceAnchor, again in line.sourceAnchors, again in region.sourceAnchors, and again in table/cell.sourceAnchors when the region overlaps a table — 3 to 5 copies of a ~700-byte anchor per character), the artifact for a real multi-page paper will be large. I could not measure it: no generated document-ir-v2.shadow.json exists anywhere in the working tree, and I was instructed not to run cargo, so treat the byte figure as unquantified — but the duplication FACTOR is verified from the code. If the new recognizer loads this file per job, per keystroke-adjacent save, or across a batch import, measure first and consider a typed streaming read plus the anchor collapse in line_builder.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "page_value (line 2336) — quality fields",
        "whatToChange": "Two quality metrics a recognizer should not trust as-is: `textCoverageRatio` is `sum(glyph.bbox.area) / (W*H)` (:2374) and double-counts every overlap, so a page with duplicated hidden text can exceed the real ink coverage; `unicodeErrorRatio` is per-GLYPH-COUNT, not per-character-weight. Also `visibilityObserved: false` and no `hidden` field are emitted for every PDF glyph (:255) — invisible text (Tr 3/7) is detected only as a PAGE-level boolean by `page_contains_hidden_text` (:1146) and surfaced as a warning plus a full-page OCR region, never per glyph. A geometry-first recognizer that needs to drop invisible glyphs cannot do so from this artifact today.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_geometry.rs",
        "symbol": "collect_chars_with_origin (line 351), parse_pdf_with_pdfium (line 177)",
        "whatToChange": "Plan §17.3's unification target. This is the second full scan of the same PDF and it discards almost everything pdfium offers: it takes only `unicode_char()`, `origin_x()`, `origin_y()` and drops CR/LF/TAB. pdfium-render also exposes per-char loose bounds, scaled font size and font name — which is exactly the typography that DocumentIRV2's TextStyleV2 is missing on the PDF path. The cheapest route to a genuinely geometry-first recognizer is probably to keep pdfium as the single reader (it already ships and is already probed via `pdfium_library_path()`), extend collect_chars_with_origin to carry size/box/font, and emit DocumentIRV2 from it — which also removes the double scan and keeps the diagram page-crop rendering that only pdfium can do (recover_diagram_question_region_assets at :2559).",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "commit_shadow_bundle_with_hook (line 718) and the docx twin commit_docx_shadow_bundle_with_hook (docx_facts_shadow.rs:121)",
        "whatToChange": "Both hardcode a 3-element staged/target/backup array and abort if any component is missing (`pdf_shadow_staged_component_missing`). If the new recognizer adds a fourth artifact next to the physical facts (a layout graph, a recognition outcome, a per-page index), it must be threaded through both of these transactions or it will not be committed atomically with the facts it describes — and the two implementations are near-duplicates that should be unified while you are in there.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "symbol": "process_fixture (line 1282), the write_pdf_facts_shadow call at line 1368",
        "whatToChange": "This is where Phase 4's acceptance metrics (empty prompt = 0, option label recall ≥99.5%, statement completeness ≥99%, matching structure ≥98%) have to land, and it already wires real PDFs → physical facts → authoring → quality → runtime with a JSON report at tmp/phase4-real-pdf-acceptance/report.json. Extend `push_check`/`quality_acceptance_policy` here rather than building a new harness. Corpus is declared in fixtures/golden/manifest.json + fixtures/golden/phase4-eight-pdf-acceptance.json, with the real PDFs under fixtures/golden/private-real/ (gated on availability — the tests skip when the private corpus is absent).",
        "risk": "low"
      }
    ],
    "testHarness": "Everything is `#[cfg(test)] mod tests` in-crate; there is no tests/ directory for this subsystem. Run with `cargo test -p <crate> <name>` from src-tauri/ (I did not run anything — instructed not to).\n\nUnit tests to copy for a new geometry pass, all small and self-contained:\n- `src-tauri/src/pdf_ingest/line_builder.rs:553 rotated_glyphs_are_retained_as_vertical_lines` and `:571 unobserved_line_breaks_are_not_fabricated_and_gaps_are_recorded` — the pattern is a `glyph(id, text, x, y, angle) -> Value` helper (:533) feeding a `json!({\"pageIndex\",\"rotation\",\"glyphs\":[...]})` page straight into `build_lines`. This is the cheapest way to test any new line/token logic; note the fixture omits `unit` in the bbox, which works because build_lines reads raw Values and never goes through RectV2.\n- `src-tauri/src/pdf_ingest/reading_order.rs:312 graph_uses_line_membership_and_column_constraints` — `region(id, x, y, column)` helper (:299) + a `line_to_region` BTreeMap, asserts exact primary order AND that all four relation strings appear.\n- `src-tauri/src/pdf_ingest/table_detector.rs:565 missing_internal_rule_creates_a_merged_cell_with_cell_specific_evidence` — `rule(id, start, end)` helper (:541) building vectorPath Values, calls `make_table` directly; asserts colSpan merging plus non-empty borderEvidence and per-cell sourceAnchors.\n- `src-tauri/src/pdf_ingest/coordinates.rs:285 rotation_matrices_map_crop_box_to_upright_display` — loops rotation ∈ {0,90,180,270} with UserUnit 2.0 and asserts display dims and that the CropBox maps to (0,0). Extend this for any coordinate change; it is the only rotation test.\n- `src-tauri/src/pdf_ingest/ocr_merge.rs:211/:228/:258` — three merge tests, the only coverage of the (dead) OCR path.\n\nEnd-to-end PDF tests in `src-tauri/src/pdf_facts_shadow.rs` (mod tests starts ~:2900):\n- `:2984 extracts_native_glyph_boxes_font_size_angle_and_zero_based_anchors` — the baseline shape assertion, uses `fixtures/parser/complex-reading.pdf`.\n- `:3120 all_synthetic_pdf_fixtures_round_trip_physical_layers_without_schema_loss` — THE invariant test to extend. Iterates 15 synthetic fixtures in `fixtures/golden/synthetic/pdf/` (pdf-two-column, pdf-three-column, pdf-ruled-table, pdf-borderless-table, pdf-flowchart, pdf-vector-diagram, pdf-map-hotspot, pdf-header-footer, pdf-hidden-ocr, pdf-image-only, pdf-mixed-text-image, pdf-native-ocr-conflict, pdf-broken-font, pdf-rotated-page, pdf-question-before-passage) and asserts: typed DocumentIRV2 parse, glyphs ⇒ non-empty lines AND spans, physicalLayers.summary.lineCount == sum of page line counts, and `set(page.readingOrder) == set(region ids)` — that last one is the contract that makes PDF readingOrder region-only.\n- `:3196 available_private_real_pdf_corpus_preserves_physical_layer_invariants` — driven by `fixtures/golden/manifest.json`, skips when the private corpus is absent.\n- `:3288 assert_column_fixture` / `:3317 synthetic_two_and_three_column_reading_order_is_geometry_aware` — the column-count harness to extend for §6.4.\n- `:3323 ruled_and_borderless_table_candidates_are_shadow_only_and_anchored`, `:3361 hidden_ocr_conflicts_are_reported_without_native_text_overwrite`, `:3383 embedded_images_are_written_to_shadow_asset_store_with_integrity_metadata`, `:3439 indirect_page_resources_preserve_real_chili_passage_and_answer_images`, `:3480 real_organisational_image_only_answer_pages_route_to_full_page_ocr_plan`, `:3544 mixed_native_text_and_raster_routes_only_image_regions_without_performing_ocr`, `:3576 vector_figure_fallback_and_widget_regions_are_shadow_only_and_anchored`.\n- Transaction tests: `:3727 shadow_bundle_commit_replaces_artifacts_and_assets_as_one_staged_set`, `:3788 ..._mid_commit_failure_restores_previous_bytes`, `:3846 ..._rollback_failure_is_fail_closed_and_preserves_backup_root`, `:3916 pdf_preflight_limits_fail_with_stable_codes`, `:3961 repaired_classic_structure_has_canonical_xref_and_stream_length`. Helpers: `fixture_source()` (:2917), `temporary_pdf_source()` (:2955), `widget_pdf_bytes()` (:2923).\n\nDOCX tests in `src-tauri/src/docx_facts_shadow.rs` (mod tests ~:2150): `:2239 rich_docx_shadow_preserves_structure_assets_and_strict_schema`, `:2437 paragraph_line_and_page_breaks_create_distinct_segments_without_repeating_numbering`, `:2479 external_image_sets_publish_block_and_explicit_issue_code`, `:2512 checked_in_phase3_docx_packages_reach_typed_shadow_ir` (docx-external-image / -floating-text-box / -section-columns / -smartart / -table-merged-cells), `:2630 adversarial_phase3_docx_fixtures_enforce_the_checked_in_matrix`, plus the docx commit/rollback trio at :2288/:2315/:2370. The one to copy for any render-path work is **`:2521 render_provider_pdf_geometry_binds_split_option_columns_to_ooxml`**: it injects a closure renderer returning a checked-in `render-assisted-two-column-options.provider-output.pdf` into `extract_docx_facts_shadow_with_renderer`, then asserts `regions[].columnIndex == {0,1}`, that each option line's owning region is in readingOrder, and that anchors carry `extractionMode == \"docx_rendered_fallback\"` with a non-null `ooxmlPath`. Helpers: `package_bytes()` (:2177), `job_and_source()` (:2208), `phase3_fixture_root()`, `assert_render_binding()` (:2831). OOXML table topology has its own test at `src-tauri/src/docx_ingest/tables.rs:278-298` (gridSpan 2 / rowSpan 2 on the anchor cell, rowSpan 0 on the vMerge continuation).\n\nSchema-level round-trip lives in `src-tauri/src/schema/mod.rs:86 document_ir_v2_round_trip_preserves_schema_version_and_empty_physical_layers` — the minimal valid DocumentIRV2 literal (mod.rs:88-135) is the fastest fixture to clone when adding fields, and `:210 canonical_json_is_deterministic_and_keeps_array_order` guards the on-disk encoding.\n\nFull-chain acceptance: `src-tauri/src/ielts_grammar/real_pdf_acceptance.rs` — `:1894 chili_real_pdf_reaches_all_declared_acceptance_truth`, `:1921 petri_real_pdf_reaches_all_declared_acceptance_truth`, plus policy unit tests at `:1788`, `:1820`, `:1860`. Spec files: `fixtures/golden/phase4-eight-pdf-acceptance.json`, `fixtures/golden/manifest.json`; reports to `tmp/phase4-real-pdf-acceptance/report.json` and `tmp/phase5-real-pdf-acceptance/report.json`.\n\nNot covered anywhere, and worth adding before you change them: `region_builder::build_regions` has NO tests at all (no `#[cfg(test)]` block in the file), `ocr_router::analyze` has none, and `coverage_ledger_from_pages` has none.",
    "surprises": [
      "A PDF glyph bbox is NOT a real glyph box. `geometry_basis` is always `text_matrix_derived`: the collector synthesizes a parallelogram from the text-matrix origin, an advance vector of length `width × font_size`, and a height vector of length `font_size` (pdf_facts_shadow.rs:183-196). Consequences a geometry-first recognizer must internalize: for unrotated text `glyph.baseline == glyph.bbox.y + glyph.bbox.height` EXACTLY, descenders fall entirely outside the box, there is no cap-height or x-height information, and `bbox.height ≈ font_size` rather than the inked extent. `quad` (8 display coords) is the only faithful shape for rotated text; `bbox` is its axis-aligned hull.",
      "Table and cell ids collide across pages. `format!(\"p-table-{:04}\", index + 1)` where index is always 0 (one table per page max) yields the literal id \"p-table-0001\" on every page of the document, and cells are \"p-table-1-r0-c0\" everywhere (table_detector.rs:291, :276). Every other node id is page-prefixed. The coverage ledger's BTreeMap-by-id de-duplication (pdf_facts_shadow.rs:2761) silently swallows the collision, so it does not surface as an error — it surfaces as missing tables. Any global id→node index built by the new recognizer will be wrong on multi-page PDFs.",
      "OCR does not exist in this pipeline. `ocr_merge::merge_provider_output` is keyed on a transient `page[\"_ocrProviderOutput\"]` that no code in the repo ever writes (grep finds it only inside ocr_merge.rs and its own tests), so it returns default on every real page. `build_ocr_plan_summary` self-declares `\"engine\": \"not_configured\"` and `local_ocr_enabled()` defaults false. Four schema variants — GlyphSourceV2::Ocr, GlyphGeometryBasisV2::OcrObserved, ExtractionModeV2::PdfOcr, and SourceAnchorV2.variants on the PDF path — are unreachable. The `requiresOcrRegions` / classification=scanned machinery is a plan, not a result.",
      "DOCX geometry is estimated, and it is the DEFAULT. `EPIC8_DOCX_RENDER_ASSIST` is off unless explicitly set (docx_ingest/render_fallback.rs:103-112), so real DOCX imports go through DocxPageBuilder, which advances x by `font_size × 0.52` per ASCII char, emits one line per paragraph with no wrapping, and lets glyph x run past the content box (only the line bbox is clamped, docx_facts_shadow.rs:1097). Nothing in the artifact marks these coordinates as synthetic except `parser.options.renderAssist.geometryAuthority == \"ooxml-semantic-only\"` and `glyph.geometryBasis == \"ooxml_layout_derived\"`.",
      "`page.readingOrder` has two incompatible shapes. PDF: region ids only, asserted by test (pdf_facts_shadow.rs:3175-3186 `assert_eq!(order_ids, region_ids)`). Synthetic DOCX: region id AND line id pushed per paragraph (docx_facts_shadow.rs:944-945) plus table ids (:1111-1114), all interleaved. The schema is just `Vec<String>` so both validate.",
      "Region `confidence` is not about the region. `apply_reading_order` overwrites whatever region_builder computed (0.86 columned / 0.94 full-width) with the page-level gutter confidence for every region (reading_order.rs:263). And because gutter confidence is `(0.55 + meanGapRatio).clamp(0.55, 1.0)`, a clean two-column page scores around 0.60 and trips the `<0.72` PAGE_READING_ORDER_AMBIGUOUS warning — the metric is miscalibrated for its own threshold.",
      "`style.fontSizePt` and `bbox` can disagree. fontSizePt is the raw Tf operand × UserUnit (pdf_facts_shadow.rs:256) and never picks up the CTM scale, while the bbox corners are all pushed through `trm` (which includes CTM) and then `pdf_to_display`. pdf-extract's own reference consumer computes effective size as `sqrt(|trm.transform_vector(font_size, font_size)|)` (crate lib.rs:2160-2162). Under a scaling CTM the reported font size is wrong while the geometry is right — so font-size-equality scoring (plan §6.5) should use bbox height, not style.fontSizePt.",
      "`normalized` is present on exactly three node kinds. Only glyph bboxes, vectorPath bboxes and image-placement bboxes carry `normalized: [x/W, y/H, w/W, h/H]`, because only `PdfPageGeometry::display_rect` emits it (coordinates.rs:137-155). Lines, spans, regions and tables go through `pdf_ingest::mod::rect` (:130-140) or `docx_facts_shadow::rect` (:1823), which both omit it. Plan §6.10's `normalizedRect` hotspots must be computed from `page.widthPt`/`heightPt` (or `pageTransform.displayToNormalized`), not read off the region.",
      "The V1 pdfium parser — nominally \"authoritative\" per its own comment at parser.rs:499 — throws away more than the V2 shadow keeps. `collect_chars_with_origin` (pdf_geometry.rs:351) takes only `(unicode_char, origin_x, origin_y)`: no font size, no font name, no char bbox. There is not a single font-related identifier in the whole 175 KB file. So the two PDF readers are not redundant — pdf-extract has the typography, pdfium has the rendering (page crops) — and neither alone can produce a font-aware DocumentIRV2.",
      "SmartArt and Chart text is invisible to any layer-based recognizer. `collect_composite_fallbacks` (docx_ingest/smartart.rs:7) harvests `<a:t>`/`<v>` text from diagramData/chart parts into `DocxCompositeDrawing.text`, and it is published ONLY at `parser.options.ooxml.compositeDrawings[].accessibleText` (docx_facts_shadow.rs:573-581). It never becomes a glyph, span, line, region or asset. `ParserMetadataV2.options` being a free `BTreeMap<String, Value>` is what makes this side channel possible — it is also the escape hatch for adding new producer metadata without touching the deny_unknown_fields structs.",
      "`deny_unknown_fields` on every struct turns a typo into silent data loss, not a compile error. Producers build untyped `Value` and are validated once at the very end; a failure writes `document-ir-v2.shadow.error.json` and is then swallowed by both call sites (authoring_commands.rs:216-227, auto_pipeline.rs:1509-1519), so the job proceeds with zero physical facts and no user-visible error. Add a field to a `json!` literal without adding it to the Rust struct and the whole document disappears.",
      "One vector asset per page, bbox = union of ALL non-rule paths on that page (pdf_facts_shadow.rs:1601-1680, asset_id `pdf-vector-p{page:03}`). Two separate diagrams on one page produce one merged asset with a bbox spanning both. Same collapse as the table detector, different code path.",
      "Source anchors are duplicated 3-5× per character. A glyph's anchor (bbox + nativeBBox + displayBBox + 6-number pdfToDisplay + charRange + 64-char sourceHash) appears in glyph.sourceAnchor, again once-per-glyph in its line's `sourceAnchors` (line_builder.rs:507), again in its region's `sourceAnchors` (pdf_ingest/mod.rs:150-169), and again in the overlapping table's and each cell's anchors (table_detector.rs:62-74, :156-198). The coverage ledger then adds a separate entry per glyph, span, line, region, cell and path. I could not measure the resulting file — no generated shadow artifact exists in the working tree and I did not run cargo — but the duplication factor is verified from the code, and `read_json` loads the whole file into a String and then a full untyped Value DOM with no size cap (util.rs:246).",
      "`contracts/document-ir-v2.schema.json` (18 KB, JSON-Schema 2020-12, listed in contracts/contract-manifest.json) is referenced by no Rust, TypeScript, or script file in the repo. It can drift from the Rust structs indefinitely without any test noticing — which is precisely the \"DocumentIRV2 producer schema gate\" that plan P4-T01 asks for.",
      "`document_id` is derived from the SOURCE FILE sha256 (`format!(\"document-{}\", &sha256[..16])`, pdf_facts_shadow.rs:975), not from the job. Two jobs importing the same PDF get the same documentId with different jobIds — and `physical_shadow_matches_authoring` (authoring_v2_commands.rs:757) matches on documentId AND jobId, so this is currently benign, but it means documentId is not a usable primary key for per-job recognition output."
    ]
  },
  {
    "subsystem": "V1 authoring pipeline and parser (src-tauri/src/authoring_pipeline.rs + parser.rs), and the V1→V2 bridge that currently makes every \"V2\" artifact a derivative of V1",
    "keyFiles": [
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs",
        "role": "The whole V1 recognizer. Produces SplitCandidatesV1 and ReadingAuthoringIrV1 from a DocumentIRV1 JSON blob. Zero `pub` items — everything is `pub(crate)` and untyped `serde_json::Value` at the boundary.",
        "sizeNote": "13,848 lines / 547 KB. Production code = lines 1-8,862 (219 top-level `fn`, of which only 9 are `pub(crate) fn`). `#[cfg(test)] mod tests` starts at line 8,864 and runs to EOF: 4,985 lines, 99 `#[test]`. So ~36% of the file is its own test corpus."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "role": "File-type detection + DocumentIRV1 production. `parse_source_document` dispatches txt/md → rust text parser, pdf → pdf_geometry (pdfium) with pdf_extract text-layer fallback, docx → OOXML with Python sidecar fallback. Also owns the V1 block HTML/blockType/roleHint helpers that pdf_geometry re-uses.",
        "sizeNote": "2,840 lines / 104 KB. 13 `pub(crate) fn`. Only 4 `#[test]` (two `#[cfg(test)]` blocks at 2,733 and 2,808) — almost untested relative to its blast radius."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "role": "THE BRIDGE, and the single most important file for this refactor. `build_authoring_v2_shadow` builds IeltsAuthoringIRV2 by iterating `split[\"questionGroupCandidates\"]` (V1) and calling `answer_key_from_v1(v1_authoring)`. DocumentIRV2 only supplies `physical_lines` used to upgrade line geometry. Declared `#[cfg(test)] mod real_pdf_acceptance;` at line 23-24.",
        "sizeNote": "V1 inputs are non-optional parameters (`v1_authoring: &Value`, `split: &Value`); `physical_shadow` is `Option<&Value>`."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "role": "The one production driver of the V1 pipeline for new imports. `run_auto_pipeline_core_with_gateway` parses, writes document-ir.json, then split-candidates.json + authoring-ir.json, then the V2 shadow, then minimizes artifacts. This is the function the new frontend calls.",
        "sizeNote": "116 KB. `#[cfg(test)]` at line 2,669."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "role": "The legacy step-by-step commands: parse_document_core, run_rule_split_core, build_authoring_ir_core, update_authoring_ir_core, render_group_html_core. Only reachable from the legacy compat pages now."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "role": "The REAL DocumentIRV2 producer for PDF (pdf_extract + lopdf). `extract_pdf_facts_shadow` is already V1-free; only the `_with_v1` wrapper takes a V1 doc, and solely to bridge `diagramQuestionRegion` assets.",
        "sizeNote": "157 KB. Constants: SHADOW_ARTIFACT_FILE = \"document-ir-v2.shadow.json\" (line 19)."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs",
        "role": "Same for DOCX. `extract_docx_facts_shadow` V1-free; `write_docx_facts_shadow_with_v1` takes v1_document only for the compare report.",
        "sizeNote": "112 KB."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/pdf_geometry.rs",
        "role": "The real V1 PDF parser (pdfium-render). Emits DocumentIRV1 blocks with REAL bboxes via `document_block_with_bbox` (line 2,304) — but still calls parser::block_type_for_text_pub / role_hint_for_text_pub / markdownish_to_html_pub for each block.",
        "sizeNote": "179 KB. `#[cfg(test)]` at 2,883 — so the make_dynamic_* calls at 3,156/3,162 are tests, not production."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs",
        "role": "The V2 read/write/publish surface the new workspace uses: get_authoring_v2_core, apply_authoring_v2_patches_core, export_authoring_v2_core, validate_authoring_v2_publish_readiness. Reads V1 files only via `read_json_opt` (optional).",
        "sizeNote": "97 KB. AUTHORING_V2_SHADOW_FILE = \"authoring-ir-v2.shadow.json\" (line 34)."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "role": "Quality gate. Contains `probe_v1_compatibility` (line 952) which compiles V2 down to ReadingExamSourceV1 and, on failure, emits a `severity: \"blocking\"` issue → hardFailures → quality.state = \"blocked\" → export refused. V1 is a live publish gate."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "role": "`write_authoring_project` (line 38) HARD-reads authoring-ir.json. `minimize_process_artifacts_after_authoring` (line 160) deletes document-ir.json + split-candidates.json but KEEPS both V2 shadows and authoring-ir.json."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "role": "`reading_payload` (line 71) uses authoring-ir.json as the library payload; `upsert_library_item_from_exam(root, &record, \"ReadingAuthoringIRV1\")` (line 106) hardcodes the schema version. Called from job_store::save_job, i.e. on every update_job."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/source_review.rs",
        "role": "Derives the publish-blocking source review purely from DocumentIRV1 shape: `/parser/warnings` plus per-block `confidence` via `dynamic_document_blocks`. Imports three authoring_pipeline accessors at line 1."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/reading_source.rs",
        "role": "V1 ReadingExamSourceV1 compiler (`reading_source(authoring)` line 522, `render_group_body_html` line 345). Feeds the V1 exporters, runtime_validation, and — critically — the still-blocking V1 compatibility probe."
      },
      {
        "path": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "role": "The real-PDF acceptance harness that exercises the whole chain V1 → V2 shadow → edit → V2 export. `#[cfg(test)]`-only.",
        "sizeNote": "2,000+ lines; spec at F:/workspace/PDF2Test/fixtures/golden/phase4-eight-pdf-acceptance.json"
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/import/useImportFiles.ts",
        "role": "New import feature. Line 149 calls `runAutoPipeline(jobId, { confidenceThreshold: 0.85, executionMode: \"localOnly\", target: \"editableDraft\" })` — i.e. the new import path still runs the V1 pipeline."
      },
      {
        "path": "F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx",
        "role": "New workspace. Lines 115 and 173 call `runAutoPipeline` (with and without allowOverwrite); line 120 `runCloudReview`; line 123 links to the legacy compat pages. Reads/saves via useCanonicalEditor → get_authoring_v2 / apply_authoring_v2_patches."
      }
    ],
    "publicApi": [
      {
        "symbol": "make_dynamic_split_candidates",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:4193",
        "signature": "pub(crate) fn make_dynamic_split_candidates(\n    job_id: &str,\n    job: &ImportJob,\n    doc: Option<&Value>,\n) -> Value",
        "notes": "V1 entry point #1. Returns a `SplitCandidatesV1{...}.to_value()` (constructed at line 4,721, serialized at 4,744). Falls back to a hardcoded demo blob `split_candidates(job_id)` (line 255) when `dynamic_document_blocks(doc)` is empty — silently produces a fake 'Detective Stories' exam. 6 production call sites."
      },
      {
        "symbol": "make_dynamic_authoring_ir",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:8345",
        "signature": "pub(crate) fn make_dynamic_authoring_ir(\n    job: &ImportJob,\n    split: &Value,\n    doc: Option<&Value>,\n) -> Value",
        "notes": "V1 entry point #2. Returns `ReadingAuthoringIrV1{...}.to_value()` (constructed at 8,809, serialized at 8,861), schemaVersion \"ReadingAuthoringIRV1\". Requires BOTH the split value and the document; passage HTML is the concatenation of `dynamic_block_html` over the split's passage range."
      },
      {
        "symbol": "merge_answer_source_candidates",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:8225",
        "signature": "pub(crate) fn merge_answer_source_candidates(split: &mut Value, answer_candidates: Vec<Value>)",
        "notes": "Mutates split in place to fold in answer-key sidecar files. Always paired with make_dynamic_split_candidates."
      },
      {
        "symbol": "dynamic_document_blocks",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:289",
        "signature": "pub(crate) fn dynamic_document_blocks(doc: Option<&Value>) -> Vec<Value>",
        "notes": "Generic DocumentIRV1 flattener: pages[].blocks[] → sorted reading order, injecting private `_epic8PageWidth/_epic8PageHeight/_epic8PageRotation/_epic8OriginalOrder` keys. Used well outside the V1 pipeline: source_review.rs:79+103, llm_commands.rs:348, auto_pipeline.rs:1373. This one cannot simply be frozen."
      },
      {
        "symbol": "dynamic_block_text",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:341",
        "signature": "pub(crate) fn dynamic_block_text(block: &Value) -> String",
        "notes": "Prefers `text`, falls back to strip_html(`html`). Used by source_review.rs, llm_commands.rs:352, auto_pipeline.rs:98/1375."
      },
      {
        "symbol": "dynamic_block_id",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:357",
        "signature": "pub(crate) fn dynamic_block_id(block: &Value) -> String"
      },
      {
        "symbol": "dynamic_block_role",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:365",
        "signature": "pub(crate) fn dynamic_block_role(block: &Value) -> &str"
      },
      {
        "symbol": "collapse_whitespace",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:268",
        "signature": "pub(crate) fn collapse_whitespace(input: &str) -> String",
        "notes": "Plan §17.2 says to delete the new chain's dependency on this. Still used by parser.rs at lines 275, 1326, 1540, 1632 — including inside the DOCX table/paragraph builders, not only the V1 chain."
      },
      {
        "symbol": "has_dynamic_ielts_question_instruction_evidence",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:3885",
        "signature": "pub(crate) fn has_dynamic_ielts_question_instruction_evidence(text: &str) -> bool",
        "notes": "Inverted dependency: parser.rs:69 (`role_hint_for_text`) calls INTO authoring_pipeline. The parser cannot be separated from the V1 recognizer until this moves."
      },
      {
        "symbol": "parse_dynamic_answer_text",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:2734",
        "signature": "pub(crate) fn parse_dynamic_answer_text(text: &str) -> serde_json::Map<String, Value>",
        "notes": "Answer-key text → {displayNumber: answer}. Used by auto_pipeline.rs:98 inside `parse_answer_source_candidates`, which serves answer-key sidecar SOURCE FILES, not the main question document. A V2-direct path still needs this capability."
      },
      {
        "symbol": "dynamic_completion_foreign_slots",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:6204",
        "signature": "pub(crate) fn dynamic_completion_foreign_slots(\n    prompt: &str,\n    number: u32,\n    range_start: u32,\n    range_end: u32,\n) -> Vec<u32>",
        "notes": "Sole consumer is authoring_review.rs:503 (V1 review defect COMPLETION_PROMPT_FOREIGN_SLOTS). Dies with V1 review."
      },
      {
        "symbol": "dynamic_interaction_for_kind",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:4747",
        "signature": "pub(crate) fn dynamic_interaction_for_kind(kind: &str) -> Value",
        "notes": "Sole consumer llm_suggestions.rs:99 (V1 patch synthesis)."
      },
      {
        "symbol": "dynamic_template_for_kind",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:4767",
        "signature": "pub(crate) fn dynamic_template_for_kind(kind: &str) -> &'static str",
        "notes": "Sole consumer llm_suggestions.rs:95."
      },
      {
        "symbol": "is_dynamic_umbrella_question_range",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:731",
        "signature": "pub(crate) fn is_dynamic_umbrella_question_range(text: &str) -> bool",
        "notes": "Only consumers are lib.rs tests (5151-5163). Safe to make private."
      },
      {
        "symbol": "SplitCandidatesV1",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:130",
        "signature": "pub(crate) struct SplitCandidatesV1 {\n    pub job_id: String,\n    pub passage_candidates: Vec<PassageCandidateV1>,\n    pub question_group_candidates: Vec<SplitGroupCandidateV1>,\n    pub answer_key_candidates: Vec<AnswerKeyCandidateV1>,\n    pub umbrella_question_ranges: Vec<UmbrellaQuestionRangeV1>,\n    pub issues: Vec<String>,\n}",
        "notes": "Typed but NOT part of any public signature — `to_value()` (line 139) is private and every caller sees only `Value`. Same for the 8 nested V1 structs (PassageCandidateV1 17, GroupInteractionClassificationV1 25, GroupClassificationV1 37, SplitSectionEvidenceV1 47, SplitContinuationEdgeV1 81, SplitGroupCandidateV1 90, UmbrellaQuestionRangeV1 114, AnswerKeyCandidateV1 123)."
      },
      {
        "symbol": "ReadingAuthoringIrV1",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:236",
        "signature": "pub(crate) struct ReadingAuthoringIrV1 {\n    pub schema_version: String,\n    pub job_id: String,\n    pub exam: ExamMetaDraftV1,\n    pub passage: PassageDraftV1,\n    pub groups: Vec<QuestionGroupDraftV1>,\n    pub answer_key: serde_json::Map<String, Value>,\n    pub question_order: Vec<String>,\n    pub question_display_map: serde_json::Map<String, Value>,\n    pub audit: AuthoringAuditV1,\n}",
        "notes": "Nested types: AuthoringSourceFileV1 148, ExamMetaDraftV1 161, PassageHtmlBlockV1 172, PassageDraftV1 179, QuestionDraftV1 188, QuestionGroupDraftV1 202, AuthoringAuditV1 226. Note PassageDraftV1 collapses the whole passage to ONE html_blocks entry with block_id \"passage-main\" (line 8,838) — all per-block geometry is lost at this step."
      },
      {
        "symbol": "parse_source_document",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:2519",
        "signature": "pub(crate) fn parse_source_document(\n    job: &ImportJob,\n    source: &SourceFile,\n    upload_path: &Path,\n    output_path: &Path,\n    mode: &str,\n) -> CommandResult<Value>",
        "notes": "THE DocumentIRV1 entry point. Never returns Err for a bad source — degrades to `parser_failure_document_ir`. PDF path: pdf_geometry::parse_pdf_with_pdfium → on error parse_pdf_with_text_layer (fabricated bboxes) → on error Python sidecar → on error failure stub."
      },
      {
        "symbol": "manual_transcription_document_ir",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:392",
        "signature": "pub(crate) fn manual_transcription_document_ir(\n    job: &ImportJob,\n    content: &str,\n    note: Option<&str>,\n) -> Value"
      },
      {
        "symbol": "vision_transcription_document_ir",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:425",
        "signature": "pub(crate) fn vision_transcription_document_ir(\n    job: &ImportJob,\n    content: &str,\n    confidence: f64,\n    warnings: Vec<String>,\n    evidence: Value,\n    note: Option<&str>,\n) -> Value"
      },
      {
        "symbol": "parser_failure_document_ir",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:2618",
        "signature": "pub(crate) fn parser_failure_document_ir(\n    job: &ImportJob,\n    source: &SourceFile,\n    mode: &str,\n    error: &str,\n) -> Value"
      },
      {
        "symbol": "missing_source_document_ir",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:2657",
        "signature": "pub(crate) fn missing_source_document_ir(job: &ImportJob, mode: &str, reason: &str) -> Value"
      },
      {
        "symbol": "extract_pdf_images_for_vision",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:2454",
        "signature": "pub(crate) fn extract_pdf_images_for_vision(\n    job_id: &str,\n    input_path: &Path,\n    output_path: &Path,\n    asset_dir: &Path,\n) -> CommandResult<Value>",
        "notes": "Cloud-vision rescue path; V1-independent. Keep."
      },
      {
        "symbol": "render_pdf_pages_with_adapter",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:2161",
        "signature": "pub(crate) fn render_pdf_pages_with_adapter(\n    job_id: &str,\n    input_path: &Path,\n    output_path: &Path,\n    asset_dir: &Path,\n    prior_warnings: Vec<String>,\n) -> CommandResult<Value>"
      },
      {
        "symbol": "block_type_for_text_pub / role_hint_for_text_pub / markdownish_to_html_pub",
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs:180,184,188",
        "signature": "pub(crate) fn block_type_for_text_pub(text: &str) -> &'static str\npub(crate) fn role_hint_for_text_pub(text: &str) -> Option<&'static str>\npub(crate) fn markdownish_to_html_pub(text: &str, block_type: &str) -> String",
        "notes": "Thin `_pub` re-exports of private fns, existing only so pdf_geometry.rs:2312/2313/2318 can build V1 blocks. Plan §17.2 wants markdownish_to_html gone from the new chain; today it is on the GOOD (pdfium) path, not just the fallback."
      },
      {
        "symbol": "write_pdf_facts_shadow_with_v1",
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs:474",
        "signature": "pub(crate) fn write_pdf_facts_shadow_with_v1(\n    job: &ImportJob,\n    source: &SourceFile,\n    input_path: &Path,\n    output_path: &Path,\n    v1_document: Option<&Value>,\n) -> CommandResult<Value>",
        "notes": "The real DocumentIRV2 producer. Only V1 use is `merge_v1_diagram_question_region_assets` (line 518), which copies V1 assets carrying `diagramQuestionRegion` into the V2 asset store — and RETURNS Err if the V1 asset file is missing (line 543), so a stale V1 doc can hard-fail V2 production."
      },
      {
        "symbol": "extract_pdf_facts_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs:830",
        "signature": "pub(crate) fn extract_pdf_facts_shadow(\n    job: &ImportJob,\n    source: &SourceFile,\n    input_path: &Path,\n) -> CommandResult<Value>",
        "notes": "Already fully V1-free. This is the closest thing that exists to the plan's `ingest_source_to_document_v2`."
      },
      {
        "symbol": "build_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:76",
        "signature": "pub(crate) fn build_authoring_v2_shadow(\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n) -> CommandResult<Value>",
        "notes": "The load-bearing bridge. Group loop is `split[\"questionGroupCandidates\"]` (line 131). `answer_key_from_v1(v1_authoring)` at 130. `build_passage(job, v1_authoring, split, ...)` at 119. physical_shadow contributes only `physical_lines` (line 105) used as geometry fallback. THERE IS NO V2-ONLY VARIANT."
      },
      {
        "symbol": "write_authoring_v2_shadow",
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:60",
        "signature": "pub(crate) fn write_authoring_v2_shadow(\n    job_dir: &Path,\n    job: &ImportJob,\n    v1_authoring: &Value,\n    split: &Value,\n    v1_document: Option<&Value>,\n    physical_shadow: Option<&Value>,\n    output_path: &Path,\n) -> CommandResult<Value>",
        "notes": "Writes authoring-ir-v2.shadow.json — the file `get_authoring_v2` serves to the new workspace when revision == 0."
      },
      {
        "symbol": "get_authoring_v2_core / load_current_authoring",
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:54,725",
        "signature": "pub(crate) fn get_authoring_v2_core(root: &Path, job_id: &str) -> CommandResult<Value>\nfn load_current_authoring(root: &Path, job_id: &str) -> CommandResult<(Value, u64)>",
        "notes": "THE freeze-boundary fact: load_current_authoring reads `authoring/revisions/` when revision > 0, else `authoring-ir-v2.shadow.json`. It never touches authoring_pipeline. Opening an old job in the new workspace executes ZERO V1 recognizer code."
      },
      {
        "symbol": "run_auto_pipeline_core",
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1153",
        "signature": "pub(crate) fn run_auto_pipeline_core(\n    root: &Path,\n    job_id: &str,\n    input: Option<AutoPipelineInput>,\n) -> CommandResult<Value>",
        "notes": "Delegates to run_auto_pipeline_core_with_gateway (line 1381). This is the single Tauri-facing entry point for new-import recognition — the whole convergence hinges on one function."
      }
    ],
    "dataFlow": "Today, for a new import (verified end to end):\n\n1. Frontend stage A: `createImportJob` + `importSourceFile` (useImportFiles.ts:126-127) writes job.json and uploads/<stored_name>. `job_store::save_job` immediately mirrors into SQLite (see step 9).\n2. Frontend stage B: `runAutoPipeline(jobId, {executionMode:\"localOnly\", target:\"editableDraft\"})` (useImportFiles.ts:149) → Tauri `run_auto_pipeline` (lib.rs:1292) → `auto_pipeline::run_auto_pipeline_core_with_gateway` (auto_pipeline.rs:1381), inside `spawn_blocking`.\n3. Guard: `if dir.join(\"authoring-ir.json\").exists() && !allow_overwrite { return Err(\"editable_draft_exists...\") }` (auto_pipeline.rs:1402). The re-entry guard is keyed on the V1 artifact.\n4. Parse: `parser::parse_source_document` (auto_pipeline.rs:1449) → for PDF, `pdf_geometry::parse_pdf_with_pdfium` (pdf_geometry.rs:177) using pdfium-render, real per-glyph geometry, blocks built by `document_block_with_bbox` (pdf_geometry.rs:2304) whose blockType/roleHint/html come from parser::*_pub. Result = DocumentIRV1, written to `document-ir.json` (auto_pipeline.rs:1464).\n5. Second, independent scan: `write_pdf_facts_shadow_with_v1` (auto_pipeline.rs:1496) using pdf_extract + lopdf → `document-ir-v2.shadow.json`. This is a *separate* engine reading the same PDF; it is guarded by `if current_physical_shadow(&dir, &job).is_none()` and PDF-only (DOCX gets no V2 shadow on the auto path — only `parse_document_core` does DOCX, authoring_commands.rs:203).\n6. Recognize: `make_dynamic_split_candidates` → `merge_answer_source_candidates` → `split-candidates.json` (auto_pipeline.rs:1675-1678); then `make_dynamic_authoring_ir` → `authoring-ir.json` (1684-1685). All structure decisions — passage range, question groups, kinds, option banks, answer slots — happen here, from DocumentIRV1 only.\n7. Optional cloud diagnostics (vision transcription, outline compare) mutate `ir` but explicitly never replace it (auto_pipeline.rs:1619-1623, 2404-2415).\n8. Derive V2: `write_pipeline_authoring_v2_shadow` (auto_pipeline.rs:2027) → `ielts_grammar::build_authoring_v2_shadow(job, &ir, &split, doc, physical_shadow)`. The group loop is over `split[\"questionGroupCandidates\"]`; `physical_shadow` only upgrades line geometry. Output `authoring-ir-v2.shadow.json` + `.compare.json`. `evaluate_quality` runs inside (ielts_grammar/mod.rs:375) and includes the blocking V1-compatibility probe.\n9. Every `update_job` in that flow calls `job_store::save_job` → `library_commands::upsert_reading_job` → payload = the whole authoring-ir.json → `upsert_exam` + `upsert_library_item_from_exam(..., \"ReadingAuthoringIRV1\")` (library_commands.rs:106). So library_items.revision_payload_json is a V1 blob and a new revision row is appended on each status transition.\n10. Cleanup: `minimize_process_artifacts_after_authoring` (auto_pipeline.rs:2186) first calls `write_authoring_project` — which HARD-reads authoring-ir.json (cleanup.rs:44) — then deletes document-ir.json, split-candidates.json, pipeline-report.json, cache/, preview/. Both V2 shadows survive.\n\nThen, for editing and publishing (the already-converged Phase 1-3 path):\n\n11. `getAuthoringV2(itemId)` (useCanonicalEditor.ts:67) → `get_authoring_v2_core` → `load_current_authoring`: revision > 0 → read `authoring/revisions/<n>`; revision == 0 → read `authoring-ir-v2.shadow.json`. No authoring_pipeline involvement.\n12. `applyAuthoringV2Patches` (useCanonicalEditor.ts:107) → `apply_authoring_v2_patches_core` → apply patches → `mark_user_audit` → `refresh_quality_report` (reads document-ir-v2.shadow.json for source coverage) → `validate_authoring` → `append_revision`. From revision 1 onward the exam is genuinely V1-free on disk.\n13. Publish: `publishItem` → `getAuthoringV2` → `exportAuthoringV2` (publishClient.ts:48-50) → `export_authoring_v2_core`: quality.state must be \"ready\", then `validate_authoring_v2_publish_readiness` (which requires source-review.json resolved — a DocumentIRV1-derived artifact), then `compile_reading_source_v2` → files → `exportNasPackageV2` → `publish_nas_package_v2_core`, which is entirely ReadingExamSourceV2 and has no V1 dependency.\n\nNet: the ONLY thing V1 still decides is question structure at step 6, but that decision is laundered into every downstream V2 artifact at step 8. DocumentIRV2 is currently a decoration layer, not a structure source.",
    "gapsVsPlan": [
      {
        "claim": "§17.2 target interface: `pub async fn ingest_source_to_document_v2(source: &StoredSourceAsset, options: IngestOptions) -> Result<DocumentIRV2>`",
        "reality": "None of `ingest_source_to_document_v2`, `build_document_ir_v2`, `IngestOptions`, `StoredSourceAsset` exists anywhere in src-tauri (grep over all .rs returns zero hits). The actual V2 producers are synchronous and V1-shaped: `write_pdf_facts_shadow_with_v1(job, source, input_path, output_path, v1_document) -> CommandResult<Value>` and its DOCX twin. There is no typed `DocumentIRV2` return anywhere — `serde_json::from_value::<DocumentIRV2>` is used only as a validation round-trip (pdf_facts_shadow.rs:497-500).",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs:474; F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs:37; grep for the four plan symbols returns nothing"
      },
      {
        "claim": "§17.2 «新 PDF/DOCX 主线调用 build_document_ir_v2()»",
        "reality": "The main line calls `parser::parse_source_document`, which for PDF uses pdfium-render (pdf_geometry.rs:177) and produces DocumentIRV1. DocumentIRV2 is produced by a SECOND, independent scan using pdf_extract + lopdf (pdf_facts_shadow.rs:11-13, 843-846). Every PDF is therefore parsed twice by two different engines, and only the V1 result drives structure. On the auto-import path the V2 shadow is PDF-only — DOCX gets no V2 shadow at all unless the legacy `parse_document` command is used.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1449 vs 1496; F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs:195-209"
      },
      {
        "claim": "§17.2 «删除新链路对 collapse_whitespace、fake bbox 和 markdownish_to_html 的依赖»",
        "reality": "Fake bbox is already off the good path: `document_block_with_bbox` emits real coordinates and its doc comment says so explicitly; the fabricated `[72, y0, 520, y0+36]` envelope now only appears in `parse_pdf_with_text_layer`, the degraded fallback used when pdfium is unavailable (documented at parser.rs:524-527). But `markdownish_to_html_pub` IS still called on the good path (pdf_geometry.rs:2318), and `collapse_whitespace` is used inside the DOCX paragraph/table builders (parser.rs:1540, 1632), not only by the V1 recognizer.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/pdf_geometry.rs:2301-2327; F:/workspace/PDF2Test/src-tauri/src/parser.rs:524-534, 1540, 1632"
      },
      {
        "claim": "§17.2 «DocumentIRV1 只用于打开旧题和回归对照»",
        "reality": "The exact opposite is true today. DocumentIRV1 is the only structure source for every artifact, including the V2 ones. `build_authoring_v2_shadow` takes `v1_authoring: &Value` and `split: &Value` as NON-optional parameters, iterates `split[\"questionGroupCandidates\"]` to emit taskGroups, and calls `answer_key_from_v1(v1_authoring)`. `physical_shadow` (DocumentIRV2) is `Option<&Value>` and contributes only `physical_lines`, used as a geometry fallback. Deleting DocumentIRV1 writes today would produce empty V2 authoring.",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:76-82, 105-137"
      },
      {
        "claim": "§17.6 «移入 legacy/v1_authoring_pipeline.rs»",
        "reality": "No `legacy/` directory exists under src-tauri/src (the only `legacy` is a per-job output subdirectory, JobArtifactPaths.legacy_dir, artifact_store.rs:36/52). `mod authoring_pipeline;` is still a flat top-level module (lib.rs:46).",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:46; F:/workspace/PDF2Test/src-tauri/src/artifact_store.rs:36,52; directory listing of src-tauri/src"
      },
      {
        "claim": "§17.6 «新 processing worker 不再调用它生成权威稿»",
        "reality": "There is no new processing worker. The already-converged frontend calls the V1 pipeline: useImportFiles.ts:149 and ExamWorkspacePage.tsx:115/173 both call `runAutoPipeline`, which is `auto_pipeline::run_auto_pipeline_core` → make_dynamic_split_candidates + make_dynamic_authoring_ir. The plan's own §12.2 note in useImportFiles.ts:13-16 admits stage B is still in-page scheduling, not a Rust/SQLite durable queue.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src/features/import/useImportFiles.ts:149; F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx:115,173; F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1675,1684"
      },
      {
        "claim": "§17.6 «只用于：旧任务打开、V1 fixture regression、迁移工具»",
        "reality": "Old-task OPENING already needs zero V1 code — `get_authoring_v2` → `load_current_authoring` reads `authoring/revisions/` or `authoring-ir-v2.shadow.json` and never touches authoring_pipeline. What actually still needs the V1 entry points is (a) re-running local recognition, (b) the legacy compat pages DocumentReview.tsx / UnifiedPreview.tsx / ExportPage.tsx which the workspace still links to (ExamWorkspacePage.tsx:123), (c) the acceptance harness. So the plan's freeze list is misdirected: it names the one use case that is already free.",
        "verdict": "partly-wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:711-741; F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx:123"
      },
      {
        "claim": "§20.4 «V1 authoring pipeline 新任务入口» to be deleted after one compat release",
        "reality": "Confirmed and easier than the plan implies: there is exactly ONE Tauri command that runs the V1 pipeline for new work — `run_auto_pipeline` (lib.rs:1292, registered at lib.rs:1455). `run_rule_split` and `build_authoring_ir` are only reachable from src/pages/DocumentReview.tsx. So the new-task entry is a single choke point.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/lib.rs:1292,1431-1433,1455; F:/workspace/PDF2Test/src/pages/DocumentReview.tsx"
      },
      {
        "claim": "§20.4 «DocumentIRV1 新写入» to be deleted",
        "reality": "There are exactly three production write sites of document-ir.json: authoring_commands.rs:231 (parse_document), auto_pipeline.rs:1464 (auto pipeline), plus the parser's own scratch write to root/cache/parser/<jobId>-document-ir.json. It is already ephemeral — `minimize_process_artifacts_after_authoring` deletes document-ir.json and split-candidates.json right after authoring while keeping authoring-ir.json and both V2 shadows.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs:231; F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1464; F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:181-199"
      },
      {
        "claim": "§20.4 «legacy exams 双写» to be deleted",
        "reality": "Confirmed and broader than 'exams': `job_store::save_job` unconditionally mirrors into BOTH the legacy `exams` table and the Phase-1 `library_items` + revisions tables, with `schema_version` hardcoded to \"ReadingAuthoringIRV1\" and `revision_payload_json` = the entire authoring-ir.json. Since every `update_job` calls save_job, one import appends several library revision rows carrying V1 payloads.",
        "verdict": "confirmed",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/job_store.rs:33-44; F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:71-108, 172-206"
      },
      {
        "claim": "§20.4 «V1 Reading JS exporter（确认 NAS 全量支持 V2 后）» to be deleted",
        "reality": "Already off the product surface: no frontend file references exportReadingJs or exportReadingAssets outside tauriCommands.ts/devFallbackBackend.ts. `exportNasLibrary` survives only in src/pages/ExportPage.tsx. The new publish path is publishClient.ts → exportAuthoringV2 → exportNasPackageV2, and `publish_nas_package_v2_core` has zero V1 references.",
        "verdict": "already-done",
        "evidence": "F:/workspace/PDF2Test/src/api/publishClient.ts:1,48-50; F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:245-280 (only V2 filenames appear)"
      },
      {
        "claim": "Implicit plan assumption that a V2-direct path can bypass V1 without touching the publish gate",
        "reality": "Wrong. `ielts_grammar/quality.rs:952 probe_v1_compatibility` compiles the V2 authoring DOWN to ReadingExamSourceV1 and validates it; on failure `append_compiler_probe_issue` pushes an issue with `severity: \"blocking\"` (line 913), which `push_issue` promotes to hardFailures (line 3635-3639), which forces `state = \"blocked\"` (line 190-192), which makes `export_authoring_v2_core` return `authoring_v2_export_blocked:quality_state=blocked`. V1 is a live publish gate on the pure-V2 path, and `reading_source::ReadingExamSourceV1` + `validator::validate_reading_source_contract` cannot be deleted without a deliberate decision here.",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:169-180, 897-927, 952-988, 3629-3641; F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:481-487"
      },
      {
        "claim": "Implicit plan assumption that dropping authoring-ir.json is safe once V2 is authoritative",
        "reality": "Wrong in three places. (a) `cleanup::write_authoring_project` does `read_json(&dir.join(\"authoring-ir.json\"))?` — a hard error — and it is called unconditionally by `minimize_process_artifacts_after_authoring`, which both build_authoring_ir_core and run_auto_pipeline call. (b) `library_commands::reading_payload` falls back to serializing job.json, so the library payload silently degrades to metadata-only. (c) `export_pack::export_reading_assets_with_options_core` and `preview_commands` hard-read it. Only `validate_authoring_v2_publish_readiness` reads it optionally (read_json_opt, authoring_v2_commands.rs:295).",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:44, 160-166; F:/workspace/PDF2Test/src-tauri/src/library_commands.rs:71-81; F:/workspace/PDF2Test/src-tauri/src/export_pack.rs:137; F:/workspace/PDF2Test/src-tauri/src/preview_commands.rs:21,37"
      },
      {
        "claim": "Implicit plan assumption that the V2 publish gate is V1-schema-free",
        "reality": "`validate_authoring_v2_publish_readiness` hard-requires source-review.json to be valid, non-stale and resolved with zero issues (authoring_v2_commands.rs:265-285). source-review.json is computed entirely from DocumentIRV1 shape: `/parser/warnings` plus per-block `confidence` read through `dynamic_document_blocks`. A V2-direct import that never produces a DocumentIRV1 will produce an empty/absent source review and either auto-pass for the wrong reason or block publishing.",
        "verdict": "wrong",
        "evidence": "F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:265-285; F:/workspace/PDF2Test/src-tauri/src/source_review.rs:1, 68-116, 118-135"
      },
      {
        "claim": "§17.6 «不在此文件继续增加题型规则» — implies the file is a stable frozen blob",
        "reality": "authoring_pipeline.rs was last touched 2025-09-01 but the four most recent commits on main are all about tightening V1-derived geometry and completion recovery (\"fix: harden AI import review and V2 publishing\", \"feat: tighten geometry-backed completion recovery\", \"feat: harden v2 geometry and completion structure\", \"feat: close geometry-backed reading structure recovery\"). The V1 recognizer's behavior is the moving baseline the V2 recognizer must match, and its 99 colocated tests plus ~60 lib.rs integration tests encode that baseline.",
        "verdict": "confirmed",
        "evidence": "git log on F:/workspace/PDF2Test main: bb978be, 5248c3d, 04b47a9, 7b2fcc3; 99 `#[test]` in authoring_pipeline.rs"
      }
    ],
    "changePoints": [
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "run_auto_pipeline_core_with_gateway (lines 1675-1685, 2027)",
        "whatToChange": "THE single production site to fork. Lines 1675-1678 write split-candidates.json; 1684-1685 write authoring-ir.json; 2027 derives the V2 shadow from them. Introduce a V2-direct branch that goes uploads/ -> DocumentIRV2 -> IeltsAuthoringIRV2 and writes authoring-ir-v2.shadow.json (or revision 0) directly, leaving the V1 three lines unreachable for new imports. Note the cloud paths in between (vision transcription 1539-1660, outline compare) all read and mutate the V1 `ir`, so the fork has to span roughly lines 1440-2190, not just the three write lines.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs",
        "symbol": "build_authoring_v2_shadow (line 76) and build_passage (383) / group_lines (555)",
        "whatToChange": "Needs a genuinely V1-free sibling. Today the taskGroups loop source is `split[\"questionGroupCandidates\"]` (line 131), answers come from `answer_key_from_v1(v1_authoring)` (130), and the passage comes from `build_passage(job, v1_authoring, split, ...)` (119). A V2-direct entry has to derive groups from DocumentIRV2 regions/lines instead, then reuse the existing per-group machinery (anchors, prompt_assembler, option_bank, completion, quality). This is the largest single piece of new work in the whole convergence and the plan does not name it.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "parse step, lines 1437-1520",
        "whatToChange": "Invert the priority: today DocumentIRV1 is produced unconditionally (1449, written 1464) and DocumentIRV2 only opportunistically, PDF-only, behind `if current_physical_shadow(&dir, &job).is_none()` (1485-1520). Make the V2 producer primary and unconditional, cover DOCX (today the auto path never produces a DOCX V2 shadow — only authoring_commands.rs:203 does), and stop feeding `Some(&document)` into it.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/pdf_facts_shadow.rs",
        "symbol": "write_pdf_facts_shadow_with_v1 (474) / merge_v1_diagram_question_region_assets (518)",
        "whatToChange": "The only V1 coupling inside the V2 producer: it copies V1 assets carrying `diagramQuestionRegion` into the V2 asset store, and RETURNS Err when the referenced V1 asset file is missing (line 543). A V2-direct path must either detect diagram question regions natively from V2 vector/raster assets, or pass v1_document = None and accept the loss. Leaving it as-is means a stale V1 document can hard-fail V2 production.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/cleanup.rs",
        "symbol": "write_authoring_project (line 38, hard read at 44) and minimize_process_artifacts_after_authoring (160)",
        "whatToChange": "`read_json(&dir.join(\"authoring-ir.json\"))?` is a hard error. Both build_authoring_ir_core (authoring_commands.rs:458) and run_auto_pipeline (auto_pipeline.rs:2186) call it unconditionally, and export_pack.rs:147/254 and export_nas_library.rs:1118 too. Either make the V1 read optional, or write an AuthoringProjectV2 from the V2 authoring + revision metadata. Also re-check the delete lists (181-193): they must NOT start deleting document-ir-v2.shadow.json / authoring-ir-v2.shadow.json, which are now the durable source.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/library_commands.rs",
        "symbol": "reading_payload (71), upsert_reading_job (92), upsert_library_item_from_exam call (106)",
        "whatToChange": "Payload must come from the V2 authoring (revision or shadow) and `schema_version` must stop being the hardcoded string \"ReadingAuthoringIRV1\". Note the verified fact library_items.id == job.job_id holds here: `LibraryItemRecord.id = exam.id = job.job_id`, with linked_ingest_job_id and created_from_job_id also set to it (lines 187, 202-203). Also note the row is written on EVERY update_job via job_store::save_job:37, so each status transition appends a revision — worth bounding while you are here.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/source_review.rs",
        "symbol": "low_confidence_block_ids (79), source_review_fingerprint (95), source_review_status (118)",
        "whatToChange": "Entirely DocumentIRV1-shaped (`/parser/warnings`, per-block `confidence` via dynamic_document_blocks) yet it gates V2 publishing through validate_authoring_v2_publish_readiness. Reshape onto DocumentIRV2 (glyph/line confidence, OCR plan summary, unicodeMapError) or the V2-direct path will either auto-pass for the wrong reason or be unpublishable. Line 1 is one of the three files importing authoring_pipeline accessors from outside the V1 chain.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs",
        "symbol": "probe_v1_compatibility (952), compile_v1_compatibility_shadow (990), append_compiler_probe_issue call for \"v1Compatibility\" (178-185)",
        "whatToChange": "Explicit decision required: keep the probe (then reading_source::ReadingExamSourceV1 and validator::validate_reading_source_contract must survive the whole refactor, contradicting §20.2's spirit) or downgrade its severity from \"blocking\" so V1_COMPATIBILITY_COMPILER_FAILED stops forcing quality.state = \"blocked\" and blocking export_authoring_v2_core. Do not discover this at publish time.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "main_pdf_needs_vision_transcription (174), has_reliable_question_groups (195), legacy_has_reliable_question_groups (317)",
        "whatToChange": "All three decide whether to spend money on cloud vision by running `make_dynamic_split_candidates` and inspecting V1 `questionGroupCandidates` — and has_reliable_question_groups additionally builds a full V1 authoring IR + V2 shadow just to read `quality.state` (lines 203-207). Rewire onto the V2 quality report directly. These are easy to miss because they are not in the write path.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs",
        "symbol": "draft-exists guard, line 1402",
        "whatToChange": "`if dir.join(\"authoring-ir.json\").exists() && !allow_overwrite` must become a check on the V2 revision/shadow state. Note the asymmetry in the frontend: ExamWorkspacePage.tsx:115 (\"重新运行本地识别\") passes allowOverwrite:true, while :173 (the \"no draft yet\" recovery button) and useImportFiles.ts:149 do not. If the V2-direct path writes a V2 artifact but no authoring-ir.json, the guard silently stops firing and re-runs will clobber user edits.",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "symbol": "run_rule_split_core (366), build_authoring_ir_core (395), protect_existing_authoring (353)",
        "whatToChange": "The freeze targets. Keep them byte-compatible for the legacy compat pages, but strip the V2 side-effects: build_authoring_ir_core lines 418-457 write the V2 shadow from V1, which means an operator opening the legacy page on a V2-direct job would OVERWRITE the good V2 authoring with a V1-derived one. Gate that block on \"this job has no V2 revision\".",
        "risk": "high"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs",
        "symbol": "parse_document_core (154)",
        "whatToChange": "Same hazard: line 231 writes document-ir.json and lines 187-227 write the V2 physical shadow from it, and line 229 `archive_current_draft_for_source_replacement` archives the current draft. Re-parsing an old job from the legacy page must not silently reset a V2-direct job's physical shadow.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "symbol": "parse_source_document (2519)",
        "whatToChange": "Keep as-is for txt/md and for opening/re-parsing old jobs. For the new PDF/DOCX main line it should not be called at all. Do not try to make it return DocumentIRV2 — the plan's async signature is a different function; add a new one alongside instead of changing this.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/parser.rs",
        "symbol": "role_hint_for_text (57) calling has_dynamic_ielts_question_instruction_evidence, plus block_type_for_text_pub / role_hint_for_text_pub / markdownish_to_html_pub (180/184/188)",
        "whatToChange": "Inverted dependency to break first: parser.rs:69 calls INTO authoring_pipeline, so parser and the V1 recognizer cannot be separated until that predicate moves (to a shared `ielts_text_signals` module, or into the new recognition/local). The three `_pub` shims exist only for pdf_geometry.rs:2312/2313/2318; they are the reason §17.2's «delete markdownish_to_html from the new chain» is not yet true on the pdfium path.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs",
        "symbol": "dynamic_document_blocks (289), dynamic_block_text (341), dynamic_block_id (357), dynamic_block_role (365), collapse_whitespace (268)",
        "whatToChange": "Do NOT freeze these inside the 547 KB blob. They are generic DocumentIRV1 accessors consumed by source_review.rs:1, llm_commands.rs:348-352, auto_pipeline.rs:1373-1375 and parser.rs:2 — code that must keep working regardless of V1's fate. Extract them into a small `document_ir_v1_access.rs` first; that single move is what actually makes the rest of authoring_pipeline.rs isolatable.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs",
        "symbol": "parse_dynamic_answer_text (2734)",
        "whatToChange": "Must survive the freeze in usable form. Its consumer, auto_pipeline::parse_answer_source_candidates (line 61), processes separate ANSWER-KEY source files — a capability the V2-direct path still needs. Either lift it out of authoring_pipeline or reimplement it on the V2 side before freezing.",
        "risk": "medium"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/llm_commands.rs",
        "symbol": "apply_llm_suggestion evidence check, lines 347-355",
        "whatToChange": "Quote verification reads document-ir.json and uses dynamic_document_blocks/dynamic_block_text to build the block-text map. Since cleanup deletes document-ir.json after authoring, this check already silently skips on cleaned jobs (the comment at 497-500 admits it). Rewire onto DocumentIRV2 lines, which survive cleanup.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/features/import/useImportFiles.ts",
        "symbol": "stage B, line 149",
        "whatToChange": "`runAutoPipeline(jobId, {confidenceThreshold: 0.85, executionMode: \"localOnly\", target: \"editableDraft\"})` is the new product's recognition entry. Switch to the new V2-direct command (or to run_auto_pipeline with a v2Direct flag). Also note the honest comment at lines 13-16: stage B is still in-page scheduling, so §5's durable Rust queue is still outstanding.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src/features/editor/ExamWorkspacePage.tsx",
        "symbol": "lines 115, 173 (runAutoPipeline), 120 (runCloudReview), 123 (legacy link)",
        "whatToChange": "Three call sites to move with the backend, plus the «打开旧版确认与编辑页（兼容）» link at 123, which is what keeps DocumentReview.tsx / UnifiedPreview.tsx (and therefore run_rule_split / build_authoring_ir / update_authoring_ir / validate_authoring_ir / generate_preview_assets) alive on the product surface.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/environment.rs",
        "symbol": "document_ir_v2_shadow_enabled (219), authoring_v2_shadow_enabled (223), quality_gate_v2_enabled (227)",
        "whatToChange": "All three are hardcoded `-> true` with no env override. There is no flag to stage a V2-direct rollout behind, and no way to roll back a bad build without a recompile. Add a real switch (a `v2_direct_recognition_enabled()` reading an env var or the diagnostics settings) before landing the fork.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/lib.rs",
        "symbol": "generate_reading_source_from_path, lines 474-479",
        "whatToChange": "A CLI-only regression fixture generator (not a Tauri command) that calls parse_source_document + both V1 entry points + reading_source. Easy to overlook because it sits far above the #[cfg(test)] boundary at 1480. Keep it as the V1 regression fixture producer, or port it to emit V1-vs-V2-direct comparison fixtures.",
        "risk": "low"
      },
      {
        "file": "F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs",
        "symbol": "phase4_eight_real_pdfs_reach_physical_authoring_quality_truth (1945), phase5_real_pdf_edit_and_v2_export_round_trip (2016), spec F:/workspace/PDF2Test/fixtures/golden/phase4-eight-pdf-acceptance.json",
        "whatToChange": "The acceptance spec's `policy` block literally asserts `v1Authoritative: true`, `v2ShadowOnly: true`, `productionExposure: false`. Those three flags must flip, and the two tests must gain a V2-direct variant that reaches the same 11 requiredProofs from DocumentIRV2 alone. This is the only harness that proves the convergence on real PDFs, so it is the gate, not an afterthought.",
        "risk": "medium"
      }
    ],
    "testHarness": "Three layers, all colocated `#[cfg(test)]` — there is no `src-tauri/tests/` integration directory.\n\n1. Unit / behavior corpus — `F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs`, `#[cfg(test)] mod tests` at line 8,864 to EOF (4,985 lines, 99 `#[test]`). Helpers to copy verbatim: `test_job()` (8,871), `layout_block(...)` (8,888), `two_column_option_table(rows: &[(&str,&str)])` (8,907), `layout_block_on_page(...)` (8,934). Every test builds `json!({\"schemaVersion\":\"DocumentIRV1\",\"pages\":[{\"pageIndex\":N,\"width\":..,\"height\":..,\"blocks\":[...]}],\"assets\":[]})` then asserts on `make_dynamic_split_candidates` and/or `make_dynamic_authoring_ir`. Representative names showing exactly what the V2-direct recognizer must reproduce: `detect_dynamic_group_kind_covers_form_map_and_letter_matching_variants` (8,963), `tea_style_roman_table_populates_all_heading_options` (8,997), `heading_matching_uses_roman_bank_not_paragraph_letters` (9,031), `declared_a_j_bank_keeps_the_j_option_across_detection_and_filtering` (9,213), `explicit_letter_bank_turns_completion_interactions_into_selectable_matching` (9,254), `completion_without_an_explicit_letter_bank_remains_free_text` (9,336), `bare_question_number_with_split_option_lines_recovers_prompt_and_abc` (9,444), `matching_bank_closes_across_interleaved_next_heading_by_geometry` (9,625), `matching_bank_recovers_cross_column_final_label_after_next_heading` (9,669), `rotated_text_matrix_recovers_number_without_visual_space` (9,729). Copy the INPUT FIXTURES (they are real IELTS layouts distilled to block JSON), not the assertions — the assertions are shaped around SplitCandidatesV1/ReadingAuthoringIrV1 field names.\n\n2. Command-level integration — `F:/workspace/PDF2Test/src-tauri/src/lib.rs`, `#[cfg(test)] mod tests` at line 1,480 (136 `#[test]`). Roughly 60 of them call `make_dynamic_split_candidates` + `make_dynamic_authoring_ir` against a temp app root; imports at lib.rs:1,483-1,494 pull in the authoring_pipeline entry points, `run_auto_pipeline_core_with_gateway`, `cleanup_transient_job_artifacts` and the authoring_commands `_core` fns. Load-bearing examples: `run_auto_pipeline_core(&root, &job.job_id, None)` at 4,327 / 4,464 / 4,683 / 4,869 / 4,932 / 9,864; `run_auto_pipeline_core_with_gateway(...)` with a stub LLM closure at 4,403 / 4,521 / 4,772 / 6,030 / 6,268 / 9,737 / 9,974 / 10,134; the draft-guard pair at 6,389-6,410 (`run_rule_split_core(...).unwrap_err()` then with allowOverwrite); `run_cloud_review_core_with_gateway` at 10,812 with a comment about the read-compute-write window. These will break loudest and are the honest signal that a V2-direct path changed observable behavior.\n\n3. Real-PDF acceptance (the actual gate) — `F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs`, declared `#[cfg(test)] mod real_pdf_acceptance;` at `ielts_grammar/mod.rs:23-24`. Spec: `F:/workspace/PDF2Test/fixtures/golden/phase4-eight-pdf-acceptance.json` — 8 fixtureIds (chili-peppers, conformity, fishbourne-roman-palace, listening-to-the-ocean, organisational-design, petri-dish, sleep-study, western-celebrity), 11 requiredProofs (including `real_pdf_physical_document_ir_v2`, `physical_to_authoring_v2_to_quality`, `organisational_shared_prompt_two_slots`, `petri_single_list_of_people_bank`, `western_questions_before_passage`, `ocean_q9_q13_inline_a_d`, `answer_raster_excluded_from_passage`), and a `policy` block asserting `v1Authoritative: true`, `v2ShadowOnly: true`, `productionExposure: false`, `falseReadyForbidden: true`. Manifest at `fixtures/golden/manifest.json`; PDFs under `fixtures/golden/private-real/`. Key tests: `phase4_eight_real_pdfs_reach_physical_authoring_quality_truth` (1,945) and `phase5_real_pdf_edit_and_v2_export_round_trip` (2,016) — together these run the full chain `parse_source_document` → `write_pdf_facts_shadow` → `make_dynamic_split_candidates` → `make_dynamic_authoring_ir` → `build_authoring_v2_shadow` → `apply_authoring_v2_patches_core` → `export_authoring_v2_core`. Reports land at `tmp/phase4-real-pdf-acceptance/report.json` and `tmp/phase5-real-pdf-acceptance/report.json` (constants at lines 16-19). Two `#[ignore]`d private-PDF probes exist for targeted repair: `chili_real_pdf_reaches_all_declared_acceptance_truth` (1,893) and `petri_real_pdf_reaches_all_declared_acceptance_truth` (1,920) — copy that `#[ignore]` pattern for a per-fixture V2-direct probe. Policy-shaped unit tests in the same file that will need updating: `quality_acceptance_policy_rejects_unexplained_blockers_and_requires_live_ocr_evidence` (1,820), `quality_acceptance_policy_limits_runtime_exception_to_organisational_unresolved_set` (1,860), `compiler_probe_chain_requires_honest_failure_semantics` (1,788).\n\nCoverage asymmetry worth knowing: `parser.rs` has only 4 `#[test]` across two blocks (2,733 and 2,808) despite being the sole DocumentIRV1 producer, and `pdf_geometry.rs`'s tests (boundary at 2,883) are the only place the pdfium path is exercised — including the make_dynamic_* calls at 3,156/3,162, which are tests, not production. Per the assignment I read at interface level and did not run cargo build or cargo test, so these are static observations.",
    "surprises": [
      "The headline finding: DocumentIRV2 is decoration, not structure. `ielts_grammar::build_authoring_v2_shadow` (mod.rs:76) takes `v1_authoring: &Value` and `split: &Value` as REQUIRED parameters and iterates `split[\"questionGroupCandidates\"]` (line 131) to emit taskGroups; `physical_shadow` is `Option<&Value>` and feeds only `physical_lines` used as a geometry fallback (105-115). Every V2 artifact the new workspace shows is therefore a V1 decision wearing a V2 schema. 'Freeze and isolate' per §17.6 is necessary but nowhere near sufficient — someone has to write a DocumentIRV2 structure recognizer that does not exist in any form today.",
      "Every PDF is parsed twice by two different engines. DocumentIRV1 comes from pdfium-render (pdf_geometry.rs, `use pdfium_render::prelude::*`); DocumentIRV2 comes from pdf_extract + lopdf (pdf_facts_shadow.rs, `use pdf_extract::{ColorSpace, Document, MediaBox, OutputDev, ...}`). Plan §17.3 mentions unifying them in one line; in practice these are two independent extraction stacks with independent bugs, and the V2 one is not the one with real glyph geometry from pdfium.",
      "V1 is still a live publish gate on the pure-V2 path. `probe_v1_compatibility` (quality.rs:952) compiles V2 authoring down to ReadingExamSourceV1; failure becomes `severity: \"blocking\"` (913) → hardFailures (3,635) → `state = \"blocked\"` (190) → `export_authoring_v2_core` returns `authoring_v2_export_blocked:quality_state=blocked` (481-487). You cannot delete reading_source.rs's V1 compiler as §20.2 implies without first deciding what happens to this probe.",
      "authoring_pipeline.rs has no typed public API at all. Zero `pub` items; 9 `pub(crate) fn`; the two entry points return bare `serde_json::Value`. The V1 structs exist (SplitCandidatesV1, ReadingAuthoringIrV1) but are serialized through private `to_value()` methods and never appear in a signature — so the compiler will not help you find breakage when the artifact shape changes. And 4,985 of its 13,848 lines (36%) are its own test module.",
      "All three V2 feature flags are hardcoded `-> true` with no env override: `document_ir_v2_shadow_enabled` (environment.rs:219), `authoring_v2_shadow_enabled` (223), `quality_gate_v2_enabled` (227). There is nothing to stage a V2-direct rollout behind and no way to roll back without a recompile — unusual given that every other knob in environment.rs reads an EPIC8_* env var.",
      "Opening an old job needs zero V1 code. `get_authoring_v2` → `build_editor_session` → `load_current_authoring` (authoring_v2_commands.rs:725) reads `authoring/revisions/<n>` when revision > 0, else `authoring-ir-v2.shadow.json`, and never touches authoring_pipeline. §17.6's stated justification for keeping the V1 pipeline ('旧任务打开') is the one use case that is already free of it.",
      "V1 artifacts are already ephemeral, but the wrong one is kept. `minimize_process_artifacts_after_authoring` (cleanup.rs:160) deletes document-ir.json and split-candidates.json immediately after authoring while keeping authoring-ir.json — and `write_authoring_project` (cleanup.rs:44) HARD-reads that kept file with `read_json(...)?`. So the recognizer's inputs are thrown away and only its output is load-bearing, which also means re-running run_rule_split on an old job silently re-parses from uploads/ with whatever the current code does.",
      "The re-run guard is keyed on the V1 artifact, and the frontend is already inconsistent about it. `if dir.join(\"authoring-ir.json\").exists() && !allow_overwrite` (auto_pipeline.rs:1402); ExamWorkspacePage.tsx:115 passes allowOverwrite:true but :173 and useImportFiles.ts:149 do not. A V2-direct path that stops writing authoring-ir.json inherits a guard that never fires — i.e. silent clobbering of user edits.",
      "Every `update_job` appends a library revision row carrying the full V1 IR. `job_store::save_job` (33-44) unconditionally calls `upsert_reading_job`, which reads authoring-ir.json as payload and writes `library_items` + a new revision with `schema_version: \"ReadingAuthoringIRV1\"` hardcoded (library_commands.rs:106). One import produces several such rows. The verified fact library_items.id == job.job_id is confirmed here: id, linked_ingest_job_id and created_from_job_id are all exam.id == job.job_id.",
      "`make_dynamic_split_candidates` silently fabricates a demo exam. When `dynamic_document_blocks(doc)` is empty it returns the hardcoded `split_candidates(job_id)` blob (line 255) — a fake 'The Rise and Fall of Detective Stories' passage with invented TRUE/FALSE/NOT GIVEN answers. Any V2-direct migration comparing old jobs against V1 output will hit these phantom exams and should treat them as a data-quality finding, not a regression.",
      "PassageDraftV1 destroys per-block geometry. `make_dynamic_authoring_ir` collapses the entire passage into ONE html_blocks entry with block_id \"passage-main\" (authoring_pipeline.rs:8,836-8,846) built by concatenating `dynamic_block_html` with newlines. Anything downstream that wants per-paragraph source anchors has to recover them from `source_block_ids`, which is why `group_lines` (ielts_grammar/mod.rs:555) has a three-tier fallback (sectionEvidence → v1_lines by blockId → all fallback_lines).",
      "parser.rs calls INTO authoring_pipeline. `role_hint_for_text` (parser.rs:57-74) uses `has_dynamic_ielts_question_instruction_evidence` at line 69. The parser cannot be separated from the V1 recognizer until that predicate moves, so §17.2 ('keep parser, freeze pipeline') is not independently actionable in the stated order.",
      "`merge_v1_diagram_question_region_assets` can hard-fail V2 production from V1 state: if a V1 asset carrying `diagramQuestionRegion` points at a file that no longer exists, it returns Err `diagram_question_region_source_asset_missing` (pdf_facts_shadow.rs:543), aborting the whole DocumentIRV2 write. Since cleanup removes cache/ and preview/, this is a real staleness hazard on re-parse.",
      "On the auto-import path, DOCX never gets a DocumentIRV2 shadow at all. The shadow block at auto_pipeline.rs:1485-1520 filters `source.file_type == \"pdf\"`; only the legacy `parse_document` command handles DOCX (authoring_commands.rs:203). So today DOCX imports are 100% V1 with no V2 physical evidence, and the quality report's source-coverage scoring runs with `physical_available: false`.",
      "Two production call sites are easy to mistake for tests and vice versa. `lib.rs:474-479` (`generate_reading_source_from_path`) is real non-test code — a CLI fixture generator sitting ~1,000 lines above the `#[cfg(test)]` boundary at 1,480 — while `pdf_geometry.rs:3,156-3,162` looks like production but is past that file's `#[cfg(test)]` boundary at 2,883. Grepping for callers without checking the boundaries gives the wrong answer in both directions."
    ]
  }
]