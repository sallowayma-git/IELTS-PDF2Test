# 完整性批判与实施顺序建议（critic agent）

## 真实后端链路测试配方

WHERE IT GOES. Create F:/workspace/PDF2Test/src-tauri/src/product_chain.rs and add `#[cfg(test)] mod product_chain;` to lib.rs next to the existing `#[cfg(test)] mod cross_repo_contract_fixture;` (lib.rs:51-52); ielts_grammar/mod.rs:23-24 is the same precedent. Do NOT use src-tauri/tests/: there is no such directory, and an integration test links the rlib and sees only `pub` items — every module in the chain is a private `mod` (lib.rs:43-86, only `pub mod schema` is public) and every `_core` is `pub(crate)`.

NO Cargo.toml CHANGE. Adding `tauri = { features = ["test"] }` or a [dev-dependencies] block buys nothing: `app_root` is `app.path().app_data_dir()` (lib.rs:357-359), which resolves via dirs 6.0 known-folders to the developer's real %APPDATA%\com.ielts.author.studio. A mock AppHandle would pollute live app data. Also do not add tempfile — it is only a transitive dep; every test rolls its own temp dir.

ONE PREREQUISITE REFACTOR (small). `create_import_job_core` (job_commands.rs:77) and `import_source_file_core` (job_commands.rs:158) are the only two chain members that take `AppHandle`, and they take it solely to call `app_root`. Split each into a root-taking inner fn plus a 2-line shim: `pub(crate) fn create_import_job_at_root(root: &Path, input: CreateJobInput) -> CommandResult<ImportJob>` (body from `ensure_app_dirs(&root)?;` onward) and `pub(crate) fn import_source_file_at_root(root: &Path, job_id: &str, file_path: &str, role: &str) -> CommandResult<SourceFile>` (body from `let dir = job_dir(&root, &job_id);` onward). Make them `fn`, not `async fn` — neither body awaits. This also closes a real hole: job_commands.rs has no `#[cfg(test)]` block, so these two have never been executed by any test.

REAL SQLITE IS FREE. `save_job` (job_store.rs:33) writes job.json then calls `library_commands::upsert_reading_job`, which opens `<root>/authoring_hub.db` via `db::open_connection` and runs SCHEMA_SQL. job_store.rs:92 proves it works against a bare temp root with no `ensure_app_dirs`. But ASSERT THE ROW EXPLICITLY — `save_job` swallows every DB error to stderr (job_store.rs:37-42), so a broken DB write still returns Ok. Use `db::open_connection(&root)` and confirm a `library_items` row with `id == job.job_id` (that identity is set at library_commands.rs:50 and :187).

TEST A — create → import → pipeline → get → patch. Achievable today; this is the missing coverage.
1. `let root = env::temp_dir().join(format!("product-chain-{}", Uuid::new_v4().simple()));` then `ensure_app_dirs(&root)`.
2. `create_import_job_at_root(&root, CreateJobInput { title: Some(..), category: Some("P1".into()), frequency: Some("medium".into()), tags: Some(vec![]), llm_profile_id: None })`.
3. `import_source_file_at_root(&root, &job.job_id, <fixture path>, "MainQuestion")`. THE FIXTURE MUST BE A PDF: auto_pipeline.rs:1487 filters `source.file_type == "pdf"` before materializing document-ir-v2.shadow.json, so a .txt/.md/.docx source yields PHYSICAL_SHADOW_MISSING and can never be Ready. Use fixtures/golden/synthetic/pdf/pdf-two-column.pdf or fixtures/parser/complex-reading.pdf (both present). pdfium resolves under `cargo test` via the CARGO_MANIFEST_DIR probe (pdf_geometry.rs:82-89) to src-tauri/lib/pdfium-windows/pdfium.dll, which is present — verified.
4. `run_auto_pipeline_core_with_gateway(&root, &job.job_id, Some(AutoPipelineInput { execution_mode: Some("localOnly".into()), target: Some("editableDraft".into()), confidence_threshold: None, profile_id: None, allow_overwrite: None }), |_r,_j,cmd,_i,_k| panic!("product chain must not call the gateway: {cmd}"))`. PASS execution_mode EXPLICITLY: with `input: None`, `local_only` is false (auto_pipeline.rs:1394), the group-repair loop runs, fails without a profile, and records `llm.failures` in pipeline-report.json — which later blocks publish through the marker scan. Model: `auto_pipeline_local_only_skips_all_pdf_cloud_calls` (lib.rs:9910) asserts gateway_calls == 0.
5. Assert BOTH `jobs/<id>/authoring-ir-v2.shadow.json` and `jobs/<id>/document-ir-v2.shadow.json` exist, and on failure print the contents of `authoring-ir-v2.shadow.error.json` / `document-ir-v2.shadow.error.json`. Both writes swallow their error and return Ok (auto_pipeline.rs:300-312 and :1502-1518), so the pipeline reports success with no V2 artifact at all — this assertion is the whole point of the test and nothing in the repo makes it today.
6. `get_authoring_v2_core(&root, &job.job_id)` — assert `source == "shadow"` and `revision == 0`. This function currently has zero test callers anywhere in the crate.
7. `apply_authoring_v2_patches_core(&root, json!({"jobId": job.job_id, "baseRevision": 0, "patches": [{"op":"replaceText","nodeId": <first text node id>, "from":0,"to":0,"text":"chain-edit"}]}))` — assert `revision == 1` and that `jobs/<id>/authoring/revisions/1.json` and `authoring/current-revision.json` exist. Copy the patch shape from real_pdf_acceptance.rs:1627-1652, the only working example of this call pair.
8. `let _ = fs::remove_dir_all(root);`

WHY A AND B CANNOT BE ONE TEST (the blocker). After step 7, `mark_user_audit` (authoring_v2_commands.rs:839-846) has written `audit.humanVerified: false`, and the publish gate requires `true` (:255-261). No patch op can reach the audit object (the 19-op dispatch at :853-873 has no audit op; `is_safe_node_attribute` at :1894 does not list humanVerified) and no registered Tauri command sets it (full list at lib.rs:1417-1477). Independently, `export_authoring_v2_core` calls `refresh_quality_report` at :474 and then rejects anything but `ready` at :481-486, and `ready` needs `issues.is_empty()` literally (quality.rs:186-198) with no filtering of resolved issues — which a really-recognized PDF will not satisfy. So the continuous chain is blocked until recommendedOrder step 2 lands; until then, Test A ends at step 8 and Test B is seeded.

TEST B — export → publish. Achievable today, seeded, no recognition.
1. Temp root; `fs::create_dir_all(root.join("jobs").join(job_id))`.
2. Authoring: `serde_json::from_str(include_str!("../../fixtures/golden/synthetic/ielts/early-approaches-authoring-v2.json"))`. Set `authoring["jobId"] = json!(job_id)` — the publish gate compares it and errors `job_id_mismatch` otherwise. Leave everything else as shipped: `audit.humanVerified` is already `true`, `sourceDocumentId` is `"early-approaches-document"`, `assets` is `[]` (so no asset materialization), `answerKey` kinds are `option` (not `unresolved`), and its single quality issue is PHYSICAL_SHADOW_MISSING (a warning that a matching shadow clears). Write it to `jobs/<job_id>/authoring-ir-v2.shadow.json`.
3. Physical shadow: copy the `valid_physical_shadow` helper verbatim from ielts_grammar/quality.rs:3689-3742 (it is a typed-valid DocumentIRV2 whose single region's childLineIds are every authoring node id, which is what makes coverage complete), then change THREE fields: `jobId` → job_id, `documentId` → `"early-approaches-document"`, and — the non-obvious one — `sourceFiles[0].sourceFileId` from `"source-pdf-1"` to `"early-approaches-pdf"`. `refresh_quality_report` filters the shadow through `physical_shadow_matches_authoring` (authoring_v2_commands.rs:757-780), which requires schemaVersion DocumentIRV2 + matching jobId + `shadow.documentId == authoring.sourceDocumentId` + at least one `shadow.sourceFiles[].sourceFileId` present in `authoring.exam.sourceFiles[].sourceFileId`; the fixture declares `"early-approaches-pdf"`. The quality unit test never exercises that filter because it calls `evaluate_quality` directly, so copying the helper unchanged silently yields `physicalShadow: "missing"` → review_required. SourceFileRecordV2 is deny_unknown_fields with all six fields required (schema/common.rs:125-133). Write to `jobs/<job_id>/document-ir-v2.shadow.json`.
4. Create NO `document-ir.json` and NO `source-review.json`. With both absent, `source_review_status_for_job` synthesizes required=false → resolved=true and the four source-review checks pass (authoring_v2_commands.rs:263-285). Create NO `authoring-ir.json` and NO `pipeline-report.json`: the marker scan reads exactly those two files (:295) and nothing else, so their absence is what keeps `ai_fallback` / `partial_failures` empty. (Verified the fixture's own `audit.notes` text does not trip the four string literals.)
5. `export_authoring_v2_core(&root, json!({"jobId": job_id, "exportDir": <ABSOLUTE path under root>}))`. Absolute is enforced (:495-499). It calls `ensure_job_artifact_layout` itself. Take `outputDir` from the result and assert manifest-v2.json / authoring-ir-v2.json / reading-source-v2.json exist and that the receipt has `reviewRequired == false`.
6. `publish_nas_package_v2_core(&root, json!({"libraryRoot": <ABSOLUTE nas dir>, "sourcePath": output_dir.join("reading-source-v2.json")}))`. Assert `result["status"] == "committed"`, `nas/manifest.js`, `nas/early-approaches.js`, `nas/.publish-control/export.lock`. `normalize_nas_library_root` strips a trailing `publish/` component, so assert on the normalized parent, not the path you passed. Model: `publish_normalizes_publish_child_to_one_transaction_root` (nas_package_v2.rs:1540-1567).
7. Why this holds: `validate_v2_export_binding` compares the persisted authoring against the exported one with `quality` REMOVED (nas_package_v2.rs:155-186), precisely because export refreshes quality in memory without persisting it — so seeding the shadow and letting export recompute quality is exactly what the binding expects. It then recompiles and re-runs the full readiness gate against the live job dir and demands byte-exact proof equality, which is why steps 3-4 must be right.

BASELINE CAVEAT. Do not expect a green `cargo test` and do not try to establish one first. `phase4_pdf_fixtures()` (lib.rs:1588) asserts `status == "available"` from fixtures/golden/manifest.json — which declares all 8 private-real PDFs available — then hashes each file at lib.rs:1663; fixtures/golden/private-real/ contains only .gitignore and README.md, so `registered_phase4_pdf_corpus_reaches_frozen_v1_parse_split_and_authoring` (lib.rs:9202, not #[ignore]d) panics, as do the 12 tests in ielts_grammar/real_pdf_acceptance.rs. Run the new tests by exact name (`cargo test -p ielts-author-studio product_chain`). Also do not call `env::set_var` anywhere: only three lib.rs tests guard env mutation behind `env_test_lock()` (lib.rs:1858), which is why the CLI's headless chain driver (lib.rs:502-596, which sets EPIC8_ALLOW_PLAINTEXT_SECRET_FALLBACK and needs live LLM credentials) cannot be lifted into a test even though it is otherwise the closest existing thing to this recipe.

## 仍缺的事实

- **Who or what is allowed to set V2 `audit.humanVerified = true`, and is it derived or explicitly confirmed? No map, and no plan section, answers this — yet it is a hard publish blocker.**
  - 为什么阻塞：`mark_user_audit` writes `humanVerified: false` on EVERY successful patch save and the shadow builder hardcodes false, while the publish gate demands true. No patch op can reach the `audit` object (the 19-op table has no audit op; `is_safe_node_attribute` does not list it) and no registered Tauri command sets it. So publish is unreachable, not merely strict, and every Phase-4/5/7 acceptance metric that says 'Ready' or 'published' is currently unprovable. Note the V1 side DERIVES it (all questions `verified` + zero hard defects) — V2 has no per-question verified flag at all, and `TaskGroupV2.review_state` is written once as "unreviewed" and never read.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:839-846 (mark_user_audit), :255-261 (the gate), :853-873 (the 19-op table), :1894-1910 (is_safe_node_attribute); F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:369; F:/workspace/PDF2Test/src-tauri/src/authoring_review.rs:606-630 (the V1 derivation to copy or reject); F:/workspace/PDF2Test/src-tauri/src/lib.rs:1417-1477 (full command list — nothing sets it)
- **After a user resolves or ignores every issue, is the document Ready? Two contradictory notions of 'resolved' live inside one function chain and neither the plan nor any map picks one.**
  - 为什么阻塞：`evaluate_quality` sets `state = "ready"` only when `issues.is_empty()` LITERALLY — it does not filter issues whose `details.resolution` is resolved/ignored — while `validate_authoring_v2_publish_readiness` DOES honour those resolutions for its own blocker list. `resolveIssue` + `preserve_issue_resolutions` write the resolution but nothing removes the issue. Result: `refresh_quality_report` on export recomputes state to review_required and export dies at `quality_state=review_required` before the blocker list is ever consulted. Every Phase 4 acceptance metric is conditioned on 'Ready in the golden corpus', so P4 cannot be measured until this is decided.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:186-198 (state derivation); F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:233-254 (resolution-aware blocker list), :474-486 (refresh then hard state check), :780-810 (preserve_issue_resolutions)
- **Does `probe_v1_compatibility` remain a BLOCKING publish gate after V1 is retired? Plan §20.2 wants the V1 compiler deleted; the code makes its failure a hardFailure.**
  - 为什么阻塞：`append_compiler_probe_issue` pushes V1_COMPATIBILITY_COMPILER_FAILED with severity blocking → hardFailures → `state = "blocked"` → export blocked. So `reading_source::ReadingExamSourceV1` and `validator::validate_reading_source_contract` cannot be deleted in P8-T03 without an explicit decision, and any P4 geometry-first recognizer must still produce something that compiles down to V1. No map or plan section resolves this.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:169-180 (the two probe issues), :896-948 (append_compiler_probe_issue), :952-988 (probe_v1_compatibility); F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:481-486
- **What is the exam identity and re-publish policy? No map established where `exam.examId` comes from.**
  - 为什么阻塞：`exam_value` builds it as `format!("{}-{}", category.to_lowercase(), &job_id[len-8..])` — i.e. `p1-<last 8 chars of the job id>`, where the last 8 chars are a random uuid fragment from `make_job`. So importing the same paper twice yields two different exam ids, and the NAS publisher keys the manifest entry, `<examId>.js` and `resources/<examId>/` on it. A re-publish therefore ADDS a second student-visible exam instead of replacing one, and the manifest CAS cannot detect it. This must be settled before P7-T03 (what does 'overwrite' mean in a batch?) and before any NAS-side orphan GC in P7-T05.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:1740-1753 (exam_value); F:/workspace/PDF2Test/src-tauri/src/job_store.rs:9-11 (job id minting); F:/workspace/PDF2Test/src-tauri/src/nas_package_v2.rs:428-487 (manifest entry keyed by safe_exam_id)
- **Where do V2 answer-key values come from once recognition starts from DocumentIRV2 instead of V1 authoring?**
  - 为什么阻塞：`answer_key_from_v1` reading `v1_authoring.answerKey` is the SOLE producer of V2 answerKey values, and the publish gate rejects any slot whose kind is `unresolved`. Plan 17.7's target signature `recognize_local(document: &DocumentIRV2)` has no answers input, so a literal implementation yields all-unresolved answers and blocks every job. The separate answer-source path (`parse_dynamic_answer_text` → `parse_answer_source_candidates`) lives inside the file P4 wants to freeze. Decide: optional answers argument, or a separate answer-source pass promoted out of authoring_pipeline.rs first.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/answer_key.rs:3; F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/mod.rs:133, :352-354; F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:211-224; F:/workspace/PDF2Test/src-tauri/src/authoring_pipeline.rs:2734; F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:61
- **Which issue vocabulary is authoritative for the user, and what is the code→message mapping? Verified: the converged workspace never reads the backend quality report at all.**
  - 为什么阻塞：`grep -rn quality src/features/editor src/exam-canvas` returns ZERO hits. The workspace derives its own 6-code list in `actionableIssues.ts` from the current DS, while the backend has 53 codes in `issue_codes.rs` plus a `state` that gates publish. A user can clear every issue the UI shows and still be blocked with no on-screen cause, and `publishClient.ts` then explains it as 'missing stems/options/answers' regardless. P7-T02's typed PublishCheckResultV1 cannot be specified without picking one vocabulary. Renaming backend codes is a DATA migration, not a rename: `preserve_issue_resolutions` keys on `issueId`, which is persisted inside saved revisions.
  - 去哪看：F:/workspace/PDF2Test/src/features/editor/actionableIssues.ts:1-27; F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/issue_codes.rs:7-53; F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:780-810; F:/workspace/PDF2Test/src/api/publishClient.ts:29-42
- **Can the 8-PDF private corpus be restored on the build machine, and if not, what is the interim acceptance gate? Verified: `cargo test` is RED before any change is made.**
  - 为什么阻塞：`phase4_pdf_fixtures()` asserts `status == "available"` against fixtures/golden/manifest.json — which declares all 8 private-real PDFs as available — and then hashes each file. fixtures/golden/private-real/ contains only .gitignore and README.md. `registered_phase4_pdf_corpus_reaches_frozen_v1_parse_split_and_authoring` (lib.rs:9202) is NOT #[ignore]d, so it panics. Until this is fixed there is no pass/fail signal for ANY phase, and the P4 acceptance metrics (option-label recall, statement completeness, matching structure) have no corpus to be measured on. Cheapest fix: make availability a skip-with-message instead of an assert, and gate the metrics behind an env var.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/lib.rs:1588-1678 (phase4_pdf_fixtures), :9201-9204; F:/workspace/PDF2Test/fixtures/golden/manifest.json (statuses); F:/workspace/PDF2Test/fixtures/golden/private-real/ (only .gitignore + README.md); F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/real_pdf_acceptance.rs:1945, :2016
- **Is DOCX in scope for geometry-first recognition, or does it stay semantic-only? Plan §17.5 assumes 'the same DocumentIRV2'.**
  - 为什么阻塞：The auto-import path filters `source.file_type == "pdf"` before materializing the physical shadow, so an auto-imported DOCX has NO DocumentIRV2 at all — physicalShadow 'missing', sourceCoverage 0.0, never Ready. And the default DOCX geometry (render assist off unless EPIC8_DOCX_RENDER_ASSIST is set) advances x by `font_size * 0.52` per ASCII char with no wrapping, so every P4-T03 geometry score (left-margin position, font-size consistency, hanging indent, vertical gap) is meaningless on it. Decide: mirror the parse_document dispatch here, make render assist mandatory for DOCX, or declare DOCX out of scope for P4.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1487-1490; F:/workspace/PDF2Test/src-tauri/src/authoring_commands.rs:186-190; F:/workspace/PDF2Test/src-tauri/src/docx_facts_shadow.rs:302-326, :748-1145, :1784-1797; F:/workspace/PDF2Test/src-tauri/src/docx_ingest/render_fallback.rs:103-112
- **What replaces `source-review.json` as the 'the source is fully explained' publish gate? Plan §13.3 removes it from the normal line but names no replacement.**
  - 为什么阻塞：The gate today requires a SourceReviewV1 that is present, non-stale, resolved and issue-free — but it is computed entirely from DocumentIRV1 shape, and once cleanup deletes document-ir.json `source_review_status` returns the saved file verbatim, freezing `stale: true` forever. With BOTH document-ir.json and source-review.json absent it synthesizes required=false → resolved=true and silently passes. The V2 coverage ledger (SIGNIFICANT_REGION_UNASSIGNED) is the natural replacement, but nothing decides whether it takes over or whether both run.
  - 去哪看：F:/workspace/PDF2Test/src-tauri/src/authoring_v2_commands.rs:263-285; F:/workspace/PDF2Test/src-tauri/src/source_review.rs:118-162; F:/workspace/PDF2Test/src-tauri/src/cleanup.rs:124-139, :176-198; F:/workspace/PDF2Test/src-tauri/src/ielts_grammar/quality.rs:141-161
- **After P6-T02, who owns the concurrency numbers and does `panic = "abort"` stay?**
  - 为什么阻塞：Two independent unknowns that both land on P6-T02. (1) `localConcurrency`/`cloudConcurrency` exist ONLY in browser localStorage with no Rust command, config file or struct field; the plan's Rust semaphore would be a second, competing limiter unless ownership moves. (2) `run_cloud_conversion_worker` wraps its body in `catch_unwind` and documents that a panic 'must degrade into an Err outcome (local draft keeps working)', but `[profile.release] panic = "abort"` makes that impossible in a shipped build — so P6's acceptance criterion 'one path failing must not cancel the other' is unverifiable in release without either dropping panic=abort or making the worker structurally panic-free and relying on the dropped mpsc sender.
  - 去哪看：F:/workspace/PDF2Test/src/features/settings/appSettings.ts:5-30; F:/workspace/PDF2Test/src-tauri/src/lib.rs:1417-1477 (no app-settings command); F:/workspace/PDF2Test/src-tauri/src/auto_pipeline.rs:1238, :1862-1866; F:/workspace/PDF2Test/src-tauri/Cargo.toml ([profile.release] panic = "abort")

## 矛盾

- **Does pipeline-report.json survive to publish time, i.e. can a failed cloud call permanently block publish?**
  - A：Map 4 (authoring V2 / publish readiness): 'minimize_process_artifacts_after_authoring deletes pipeline-report.json right after it is written (auto_pipeline.rs:2185-2186), so vector (b) only survives when developer retention is on'.
  - B：Map 5 (LLM gateway): 'A failed cloud call blocks V2 publish' — cloud_outline_report_from_output writes the raw error into `failure`, which lands at quality.cloudComparison.failure in pipeline-report.json and is picked up by collect_publish_gate_markers.
  - 如何裁决：Map 5 is right for the SHIPPED flow; map 4's mitigation does not apply. I verified `run_cloud_review_core_with_gateway` re-writes pipeline-report.json at auto_pipeline.rs:2665 and never calls minimize (the only minimize call sites are authoring_commands.rs:458, auto_pipeline.rs:2186, export_pack.rs:147/254, export_nas_library.rs:1118). useImportFiles.ts:154-159 runs runCloudReview after runAutoPipeline for every PDF whenever a cloud profile resolves, so in the shipped product pipeline-report.json IS on disk at publish time. Its `quality.cloudComparison` sits at a path containing both 'cloud' and 'quality', so a non-empty `failure` matches the ungated `failure` rule and yields `authoring_v2_export_blocked:partial_failures=...`. Treat the marker scan as live on the product path, not as a diagnostics-only artefact.
- **Which export gate fires first — quality state or humanVerified? The fix order depends on the answer.**
  - A：Map 4: 'V2 publish is currently unreachable... mark_user_audit forces humanVerified to false on every save' — presented as THE blocker, with 'I could not confirm which assertion fires first'.
  - B：Map 12: 'The quality gate, not the AppHandle, is the real cost driver... an existing test asserts the error is exactly authoring_v2_export_blocked:quality_state=review_required'.
  - 如何裁决：Both blockers are real and independent, but the ORDER is settled in code: export_authoring_v2_core calls refresh_quality_report at :474, checks `quality_state != "ready"` at :481-486, and only then calls validate_authoring_v2_publish_readiness at :488 — where the humanVerified check is at :255-261. So quality_state fires first and fixing humanVerified alone changes nothing observable. Fix both in one change, quality-state semantics first.
- **Whether the only end-to-end edit→export harness in the repo can pass, and why not.**
  - A：Map 4: 'Its .expect(...) at line 2035 cannot pass with the current gates: mark_user_audit sets humanVerified=false on both saves, and refresh_quality_report recomputes state to review_required. I could not confirm which assertion fires first because the corpus is absent.'
  - B：Map 6 and map 11 both list `phase5_real_pdf_edit_and_v2_export_round_trip` as the live acceptance gate for the convergence, without flagging that it is already broken.
  - 如何裁决：There are THREE independent blockers, and one of them no map found: `run_phase5_real_pdf_edit_export` writes the phase4-derived authoring (whose `jobId` is `phase4-real-<fixture>`, real_pdf_acceptance.rs:186) into a NEW job dir named `phase5-real-<fixture>-<millis>` (:1593) and never rewrites `authoring["jobId"]`. So even with a clean quality report and humanVerified true, validate_authoring_v2_publish_readiness fails at `authoring_v2_export_blocked:job_id_mismatch` (authoring_v2_commands.rs:181-183). Fix all three (missing corpus, jobId, humanVerified/quality) in the same change, or the harness cannot be the P4/P5 gate the plan says it is.
- **Is the DocumentIRV1 geometry that local recognition falls back to fabricated or real, for PDF?**
  - A：Map 6 (ielts_grammar): 'For PDF, the V1 geometry that recognition uses is fabricated. parser.rs:310-330 sets bbox = [72, y0, 520, min(y0+36,794)]... so rebind_lines_to_physical's bbox-intersection fallback is matching synthetic rectangles against real ones.'
  - B：Map 11 (V1 pipeline/parser): 'Fake bbox is already off the good path: document_block_with_bbox emits real coordinates... the fabricated envelope now only appears in parse_pdf_with_text_layer, the degraded fallback used when pdfium is unavailable.'
  - 如何裁决：Map 11 is right. `parse_pdf_with_rust_text_extractor` tries `pdf_geometry::parse_pdf_with_pdfium` first and only falls back to `parse_pdf_with_text_layer` on failure, appending a `GEOMETRY_PIPELINE_UNAVAILABLE` warning. pdfium.dll is present at src-tauri/lib/pdfium-windows/pdfium.dll and resolves under cargo test via the CARGO_MANIFEST_DIR probe, so the fabricated-bbox path is the exception. Consequence for P4 sequencing: the V1 bboxes are real, so the bbox-intersection rebind is sound and 'delete tier 1 of group_lines' is a genuinely cheap win — but V1 pdfium blocks still carry NO font size or font name (collect_chars_with_origin takes only unicode_char + origin_x + origin_y), which is the real typography gap.
- **How many things the DocumentIRV2 physical shadow actually feeds into local recognition.**
  - A：Map 6: 'physical_shadow is Option<&Value> and is passed to only two things: semantic_lines_from_v2_shadow (flat line list) and evaluate_quality (coverage ledger).'
  - B：Map 7 (DocumentIRV2 facts): the diagram page-crop asset is merged into `document.assets` and `page.assetIds`, and a recognizer must join `document.assets[]` rather than walk regions.
  - 如何裁决：Three, not two. `build_authoring_v2_shadow` also reads `physical_shadow["assets"]` (ielts_grammar/mod.rs:113-117) and that array is BOTH passed into build_passage and emitted verbatim as the V2 `assets` field (:362). This is the only source of V2 assets, so a job with no physical shadow (any non-PDF auto-import) gets `assets: []`, and any figure/diagram/image node then trips ASSET_REFERENCE_MISSING as a hardFailure. Any P4 rewrite must keep an asset channel from the physical layer, and the DOCX gap in `missing` becomes a hard blocker rather than a quality degradation.
- **Whether the chain test map 12 proposes can actually run to a committed publish.**
  - A：Map 12 gives a 7-stage recipe (create → import → pipeline → get → patch → export → publish) and correctly identifies export_authoring_v2 as the mandatory 7th step.
  - B：Map 12 also states the quality gate is the real cost driver and that only the golden fixture + valid_physical_shadow reaches Ready.
  - 如何裁决：The two halves of map 12 are inconsistent as a recipe: stage 5 (apply_authoring_v2_patches) makes stages 6-7 unreachable, because mark_user_audit sets humanVerified=false and no later step can set it back. The chain must be split into two tests — see realTauriTestRecipe. Map 12's stage 1-5 half is correct and is the genuinely missing coverage (job_commands.rs has no #[cfg(test)] block at all, and get_authoring_v2_core has zero callers outside lib.rs:1011).
- **The size of the SQLite schema blob, used by two maps as a cost argument.**
  - A：Map 1 (db.rs): '8 CREATE TABLE IF NOT EXISTS + 10 CREATE INDEX IF NOT EXISTS statements'.
  - B：Map 3 (library repository): 'the ENTIRE SCHEMA_SQL batch of 11 CREATE TABLE + 9 CREATE INDEX statements' — repeated twice, once as 'one save_job executes the batch about a dozen times'.
  - 如何裁决：Map 1 is right: I counted 8 CREATE TABLE and 10 CREATE INDEX in db.rs:77-195, and the tables are library_meta, exams, library_items, library_item_revisions, ingest_jobs, source_assets, publish_records, job_artifact_index. Map 3's figure is wrong. The qualitative point (schema re-runs on every connection open; two transient connections per save_job) stands either way.
- **Whether to extend the existing dead `ingest_jobs` table or create `processing_jobs_v2`.**
  - A：Map 2: 'an unused ingest_jobs table already exists with almost exactly the ProcessingJob shape the plan wants... Deciding between extending this table and adding processing_jobs_v2 should happen before any migration is written, or the schema will carry two dead job tables.'
  - B：Map 1 and map 3 both plan for a new `processing_jobs_v2` and treat ingest_jobs purely as dead schema to note.
  - 如何裁决：Not a factual conflict but a decision that must be made once, before P6-T01, and it is nearly free either way because SCHEMA_SQL is append-only CREATE-IF-NOT-EXISTS. Deciding factor the maps missed: ingest_jobs' `status` CHECK is `IN ('created','importing','parsing','review_required','authoring','ready_to_publish','published','archived','failed')` and SQLite cannot relax a CHECK via ALTER TABLE — and there is no ALTER TABLE anywhere in the repo. Since a queue needs lease/worker_id/attempt_count/stage columns added later, and the plan's LibraryItemStatusV2 vocabulary (Processing/ActionRequired/Ready/Publishing/Published/Failed/Archived) does not match that CHECK, prefer a fresh `processing_jobs_v2` and drop ingest_jobs in P8-T04.

## 建议实施顺序

| 步骤 | 计划阶段 | 规模 | 依赖 | 理由 |
|---|---|---|---|---|
| Restore a runnable test baseline. Make `phase4_pdf_fixtures()` skip-with-message when the private corpus is absent instead of asserting `status == "available"` then hashing the file, and gate the acceptance metrics behind an env var. Do the same for the two non-ignored real_pdf_acceptance tests. | P0-T02 (prerequisite, not in the plan's task list) | small | nothing | `cargo test` panics today before any of your changes: fixtures/golden/manifest.json declares all 8 private-real PDFs available and fixtures/golden/private-real/ holds only .gitignore + README.md, so lib.rs:9202 fails in the helper at lib.rs:1663. Every phase below is measured by these tests. Doing anything else first means you cannot tell your regressions from the pre-existing red. |
| Fix the publish-readiness contract as one change: make `evaluate_quality`'s state computation resolution-aware and severity-aware; decide and implement the V2 humanVerified contract (derive it, or add an explicit confirm op that mark_user_audit does not clobber); delete `collect_publish_gate_markers` and its two historical-file reads; and version the proof shape (AuthoringV2PublishProofV1 → V2) rather than editing it in place. | P7-T02 | medium | step 1 | This is the plan's Phase 7 but it is a correctness unblock, not UX polish, so it must come first. Publish is currently unreachable through the real commands for two independent reasons that are mutually exclusive with each other (quality.state requires literally zero issues; humanVerified is forced false on every save). Consequently: every Phase 4 acceptance metric ('Ready in the golden corpus, empty prompt = 0') is vacuous; the only end-to-end harness cannot pass; and the shipped flow's own cloud review re-writes pipeline-report.json so a provider outage permanently blocks a good draft. Version the proof because nas_package_v2.rs:200-204 compares it byte-for-byte against receipts already staged on a NAS. |
| Land the real backend chain test in Rust as two tests (see realTauriTestRecipe), plus the small job_commands.rs refactor it needs. This replaces the plan's three Node/CDP scripts. | P0-T02 | medium | steps 1-2 | The plan's chosen mechanism (scripts/e2e/tauri-*.mjs) does not exist and is far more expensive than an in-crate test; the one script that does exist says in its own header that it is a devFallbackBackend test and not a real Tauri/SQLite/filesystem chain. This step also closes the two worst coverage holes: job_commands.rs has no #[cfg(test)] block at all, and get_authoring_v2_core has no test caller. Do it after step 2 so the publish leg is actually reachable; the create→patch leg is achievable today and should be written first. |
| Widen `SemanticLine` and start consuming reading order and region membership. Add baseline, column-relative indent, hanging indent, inline gaps, span ids + span bbox/style, and owning region id/kind/columnIndex; persist `RegionBuild.line_to_region` out of `enrich_page`; iterate `page.readingOrder` instead of `page.lines` array order. | P4-T01/T02 prerequisite (unscheduled in the plan) | medium | step 3 | Purely additive — every existing consumer keeps compiling — and it is the hard prerequisite nobody scheduled. `semantic_lines_from_v2_shadow` reduces the whole physical layer to {id, text, one anchor, page, order, bbox}, and `build_lines` sorts lines page-globally by (min y, min x), which interleaves columns before any grammar rule runs. A reading-order DAG with four relation types is already computed and then completely ignored. Without this, P4-T03's number/stem/option geometry scoring has nothing to score on, and `line_to_region` is thrown away after apply_reading_order. Two traps to fix while here: `indentationPt` is absolute bbox.x (not column-relative) and `lineHeightPt` is glyph-box height (not inter-baseline leading), so both are unusable as written. |
| Decide and implement the V2 answer source: either keep an explicit optional answers argument on the new entry point, or lift `parse_dynamic_answer_text` / `parse_answer_source_candidates` out of authoring_pipeline.rs into a standalone answer-source pass before freezing that file. | P4-T02 prerequisite (unscheduled) | medium | step 4 | Must precede the recognizer rewrite, not follow it. `answer_key_from_v1` is the only producer of V2 answerKey values and the publish gate rejects `unresolved` slots, so a literal `recognize_local(&DocumentIRV2)` produces a document that can never publish. Doing this first also settles what 'freeze authoring_pipeline.rs' means, because the answer parser is inside the file plan §17.6 wants frozen. |
| Formalize physical ingest (P4-T01), scoped to the fixes that actually block recognition: multiple tables per page (drop the `if tables.is_empty()` gate and the 'any vector path kills borderless detection' guard), page-unique table and cell ids, stop overwriting region.confidence with the page gutter confidence, cross-page repeated header/footer detection, collapse the 3-5x per-glyph anchor duplication. Explicitly DEFER plan §17.3's pdfium/pdf-extract unification. | P4-T01 | large | step 4 | Table ids are literally `p-table-0001` on every page and `p-table-1-r0-c0` in every cell, and the coverage ledger's BTreeMap silently de-duplicates them — so multi-page tables are invisible rather than wrong, and any global id→node index you build in P4-T02 will be incorrect. The double PDF scan is real but is the largest and least urgent piece: pdf-extract has the font size, pdfium has the page-crop rendering, and neither alone can produce a font-aware DocumentIRV2. Adding fields here is riskier than it looks: every producer builds untyped Value validated once at the end against `deny_unknown_fields` structs, and the failure is written to `.shadow.error.json` and swallowed, so a typo silently yields zero physical facts. Add the contracts/document-ir-v2.schema.json gate in the same change — nothing references that file today. |
| Add `recognize_local(&DocumentIRV2)` BESIDE `build_authoring_v2_shadow`, keeping the latter as a legacy adapter. Replace the `for candidate in split["questionGroupCandidates"]` loop with instruction-zone discovery over DocumentIRV2, and move classification after boundary recovery. | P4-T02 / P4-T03 | very-large | steps 4-6 | The single largest piece of new work in the convergence and the one the plan most understates. Verified: the group loop over V1 split candidates is the ONLY group-discovery path, `group_lines`' primary substrate is `sectionEvidence[].textPreview` capped at 120 chars, and `rebind_lines_to_physical` rewrites only the anchor and bbox — never the text. Keeping the old fn as an adapter lets the ~31 colocated tests plus the 6 production call sites migrate individually instead of in one commit. Cheap independent win to take first: delete tier 1 of `group_lines` so full text comes from `v1_lines` instead of the truncated preview. Also note classification currently feeds detection (task_type gates detect_option_bank's matching semantics, the completion bank and recover_completion_structure), so P4-T03's ordering rule requires untangling that back-edge. |
| Hard closures and the per-task unassigned ledger (P4-T04/T06): add require_unique_option_labels, require_exact_fixed_response_set, per-task source coverage at 0.92, and a per-question-range unassigned rule. Reconcile the issue-code names with the plan's vocabulary in the same change. | P4-T04 / P4-T06 | large | step 7 | Most of the plan's checks already exist under different names, so this is reconciliation more than construction — but three are genuinely absent: duplicate option labels vanish into a BTreeSet, per-group `sourceCoverage` is a disguised boolean (1.0 if the group has any anchors else 0.0), and there is no per-task coverage at all (only document-wide). Do the code renames HERE and only here: the strings cross the IPC boundary and `preserve_issue_resolutions` keys on `issueId` inside persisted revisions, so renaming later is a data migration. |
| Add the v2 tables and a real migration mechanism in one commit: append `library_items_v2`, `processing_jobs_v2`, `actionable_issues_v1`, `library_item_recovery_v1`, `editor_journal_v1` to SCHEMA_SQL as CREATE TABLE IF NOT EXISTS, plus a `run_migrations(conn)` gated on a new library_meta key called from open_connection right after ensure_schema. Add `PRAGMA foreign_keys=ON` to the `mem_conn` test helper. | plan 11.2-M1 + M2 (Phase 2 leftover) | medium | step 1 | Runs fully in parallel with P4 — shares no code. Adding tables is nearly free because ensure_schema re-runs the blob on every connection open, but the plan's own DDL is not copy-pasteable (plan lines 385/398/428/435 use bare CREATE TABLE, which throws on the second ensure_schema, and actionable_issues_v1 has no DDL anywhere). Introduce the version baseline NOW, in the same commit as the first v2 table: there is no PRAGMA user_version, no migrations table, and zero ALTER TABLE statements in the entire repo, so adding a column to a table that has already shipped has no supported path. The mem_conn pragma matters immediately: foreign_keys is ON in production and OFF in every db.rs test, so `processing_jobs_v2.library_item_id REFERENCES library_items_v2(id)` would be unenforced in tests and enforced on users' machines. |
| Give the library row `currentStep` and a `needsReview` count, then drop `listJobs()` from libraryStore and flip the read driver from `exams` to `library_items_v2`. Fix the two asymmetric delete paths and add a purge in the same change. | plan 11.2-M3/M4 + 17.12 | large | step 9 | Ordering is forced by the shipped frontend, not by the plan: libraryStore merges three read sources and prefers the job's title/updatedAt/stage, so deleting the save_job→DB hook first changes nothing visible while dropping listJobs() is what makes repository correctness load-bearing. And the DB literally cannot answer what the page asks — LibraryExamSummary has no currentStep and status_from_reading drops issue_counts.needs_review. Flipping the driver is a behaviour change, not a refactor: `exams` is the DRIVING table of both generated queries, and source_hash/issue_errors/issue_warnings have no column on library_items. Fix deletes here because delete_job hard-deletes only the exams row (leaving library_items with deleted_at NULL — invisible in both the list and the trash) while delete_library_exam soft-deletes only library_items and leaves the files forever, so nothing on disk is ever reclaimed today. |
| Durable processing queue plus the event channel (P6-T01). Move ImportJob state into processing_jobs_v2 behind the existing `update_job(root, id, |job| {...})` signature, add lease/attempt/stage columns and a claim UPDATE, add startup recovery, and add `tauri::Emitter` for processing://item-updated. | P6-T01 | large | step 10 | Both cores are already pure `(root, job_id, input) -> CommandResult<Value>` with no AppHandle, so a worker pool can call them unchanged — the queue is additive to the pipeline. Two things the plan does not mention: there is NO event mechanism at all (lib.rs:90 imports only `AppHandle, Manager`; grep for emit matches comments only), and `update_job` is an unlocked read-modify-write over a non-atomic `fs::write`, so two concurrent writers silently lose one mutation today — a Rust worker pool makes that worse before it makes it better. Keeping the closure signature lets all 26 write sites compile while the body moves to SQL. |
| Real cloud concurrency (P6-T02): replace the `F: FnMut` + `Mutex<F>` gateway with a Send+Sync transport trait holding one pooled reqwest client, split the single timeout budget into per-attempt plus total, replace the error-string sniffing with a typed error, and settle panic=abort. | P6-T02 | large | step 11 | Sequence after the queue because the queue is what makes concurrency observable. Note the plan's premise is wrong about where the serialization is: the shipped UI never spawns the Mutex worker (executionMode 'localOnly' forces profile_id None), so the actual serializer is run_cloud_review, which is plainly sequential with no Mutex and no thread. Also fix the double PDF render here — `extract_pdf_images_for_vision` never reads its own output file, so every scanned PDF is rendered twice on the live path — and remember pdfium serializes globally through a static Mutex regardless, so rendering will queue even after the gateway mutex is gone. |
| Cloud skill bundle, typed CloudRecognitionCandidateV1, and per-group salvage (P5-T01..T05). | P5-T01 / T02 / T03 / T04 / T05 | large | steps 2, 12 | Must come after step 2, not before: a salvaged candidate with rejected groups, or an outline call that merely failed, currently blocks publish through the same marker scan, so building salvage on top of the old gate ships an unpublishable feature. Mostly assembly rather than new modelling — TaskTypeV2, QuestionNumberExpressionV2, ContentNodeV2, OptionBankV2 and ResponseGroupV2 already exist as typed deny_unknown_fields structs. Three hard-fail-on-good-data bugs to fix while here: answerKey is mandatory so a paper with no printed key cannot be recognised at all; a single unparseable key like '8-9' rejects the whole outline; and layoutHint is checked for presence only, so an unnormalized value flows into a raw string comparison and produces a spurious mismatch. Add max_tokens and read finish_reason — truncation is currently indistinguishable from malformed JSON. |
| Reconciliation engine and workspace live update (P6-T03/T04). Replace exact questionRange-tuple alignment with overlap-based alignment before building the proofreader trigger. | P6-T03 / P6-T04 | large | steps 8, 13 | Genuinely depends on both P4's layout graph and P5's typed candidates, so it is correctly last among the recognition phases. The current alignment key is exact `[start,end]` tuple equality against local groups, so any split difference degrades to 'cloud_group_missing_locally' instead of a structural diff — that must be fixed first or the diff cannot drive a proofread call. Preserve two hard-won domain rules when rewriting: local inline-notes evidence beating a cloud 'list' layout, and the completion-family folding of summary/sentence/note kinds. |
| Batch NAS publisher (P7-T03): hoist lock + recovery + manifest baseline + CAS to run once per batch, loop asset-copy/wrapper/probe per item into one staging root, merge all items into a single manifest rewrite, and extend the journal and state.json from four scalar bools to a per-item array (keeping the V1 read path so old journals still recover). | P7-T03 | medium | step 2 | Independent of P4/P5 and can run any time after step 2, so schedule it whenever there is spare capacity. Today's 'batch' is a serial for-loop: N transactions, N manifest rewrites, N journals, no cross-item rollback. The single-item shape is in the TYPES, not just the loop. Two rules are load-bearing and easy to break: the manifest is copy-verified into the backup while everything else is renamed (so a student loader never sees a missing manifest.js), and the rollback closure deliberately refuses to delete visible files when a backup is incomplete, leaving ';rollback=incomplete' as the one outcome a human must be told about. Also gate the live `fault` fault-injection field out of the shipped command, and stop leaving authoring-ir-v2.json — the full answer key — permanently on the NAS beside manifest.js. |
| Cleanup and settings convergence (P7-T05, P7-T04). Make `write_authoring_project` stop hard-reading authoring-ir.json, turn the deletion allowlist into a retain-set computed from the DB row + referenced assets + the source policy, stop propagating cleanup errors with `?`, add startup and close hooks, move the app settings into diagnostics.rs, and narrow SUPPORTED_LLM_PROVIDERS. | P7-T05 / P7-T04 | medium | step 10 | `write_authoring_project` does `read_json(authoring-ir.json)?` — a hard error — and is called unconditionally by both cleanup entry points, which are called by build_authoring_ir, run_auto_pipeline and every export. So the moment the canonical DS stops writing authoring-ir.json, cleanup and therefore the whole pipeline fails. That makes this a prerequisite of P8-T03, not a follow-up. Also: the V2 publisher calls neither cleanup function and never touches job status, so retiring V1 export silently removes the success-side trigger with no compile error; there is no startup or close hook at all (lib.rs registers no RunEvent/on_window_event); and age-based deletion has nothing to key on because job.json has no terminal-state timestamp. The keepSourceFiles toggle is already a no-op — nothing ever deletes uploads/ or sources/. |
| Delete the V1 write paths, dual writes and retired frontend, then split the oversized files (P8-T01..T04). Split quality.rs before mod.rs; extract the generic DocumentIRV1 accessors out of authoring_pipeline.rs first. | P8-T01 / T02 / T03 / T04 | large | steps 8, 14, 16 | Correctly last. Three cost estimates in the plan are inverted: quality.rs (4468 lines) is a bigger split job than ielts_grammar/mod.rs (3030), lib.rs is 87% test module so splitting it is mostly moving tests, and `dynamic_document_blocks` and friends are consumed by source_review, llm_commands, auto_pipeline and parser — extracting those accessors into their own module is the single move that makes the rest of authoring_pipeline.rs isolatable. Also settle the V1 compatibility probe (see missing #3) before deleting reading_source.rs, and delete the four dead Phase-1 tables plus the five dead JobArtifactPaths directories here rather than 'migrating' them. |